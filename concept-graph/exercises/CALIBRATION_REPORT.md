# Vocab Calibration Report — Seed Pilot (4 exercises)

**Date:** 2026-05-18
**Pass:** seed-pilot-1 through seed-pilot-4
**Tagged:** `0_2_4` Conv2d, `0_2_1` Linear, `0_2_5` BatchNorm2d, `0_2_17` minimal 1D conv (part 1)
**Vocab size:** 36 atoms in `vocab/atoms.json` (+ 4 proposed = 40 effective)

---

## Headline stats

| Metric | Target | Actual | Verdict |
|---|---|---|---|
| Atoms per exercise (mean) | 4-7 | **6.5** | ✅ inside band |
| Atoms per exercise (range) | n/a | 5-9 | wide but plausible — `0_2_5` BatchNorm2d is genuinely denser |
| False positives per exercise | n/a (lower better) | 1.75 mean | ⚠️ seed over-tags exercises whose *topic* is mentioned in intro markdown but skill isn't required |
| Proposed new atoms per exercise | n/a (decaying ideal) | 1.0 | acceptable; would expect this to decay to ~0.3 by exercise 10 |
| Recurring atom fraction (≥2 of 4) | n/a | 7/17 = 41% | healthy; would converge higher with more tagging |

---

## Proposed new atoms (validator flagged)

| id | label | exercises seen in | confidence |
|---|---|---|---|
| `kaiming-uniform-init` | uniform Kaiming init with ±1/√fan_in | `0_2_1`, `0_2_4` (**recurring**) | high — definitely add |
| `register-buffer` | non-learnable state via `register_buffer` | `0_2_5` | high — distinct from `nn-parameter-wrap`, will recur in attention masks, position encodings, more batchnorm variants |
| `conv-kernel-shape` | weight tensor layout `(out_ch, in_ch, kH, kW)` | `0_2_4` | medium — may merge into a broader "PyTorch tensor-shape conventions" atom later |
| `functional-module-wrap` | `Module` is a parameter container; `forward` calls `F.*` op | `0_2_4` | medium — recurs in `MaxPool2d` (0_2_23) and `AvgPool2d` (0_2_6); useful pattern |

**Recommendation:** accept all 4 into the vocab.

---

## False positives (atoms seed predicted that didn't actually apply)

| Exercise | False positives | Pattern |
|---|---|---|
| `0_2_4` Conv2d | `module-composition`, `conv-channel-sum`, `conv-padding-zero`, `conv-windowing-2d` | Seed assigned conv-mechanics atoms wherever the *topic* appeared; this exercise just wraps `F.conv2d` — windowing/summing/padding are hidden inside PyTorch |
| `0_2_1` Linear | `matmul-2d` | Contraction is expressed via `einsum`, not `@`; `einsum-contraction` subsumes it |
| `0_2_5` BatchNorm2d | (none) | Seed nailed this one perfectly |
| `0_2_17` minimal 1D conv pt 1 | `nn-module-subclass` | It's a plain function, not a Module |

**Pattern:** **the seed's "expected exercises" column was generated by skimming overview markdown, not by reading what each exercise actually requires.** Expect ~1-2 false positives per exercise until we re-derive the expected-exercises column from actual tagged data.

---

## False negatives (atoms that should have been in seed but weren't predicted)

| Exercise | Missed atom | Why missed |
|---|---|---|
| All 4 | (the 4 newly-proposed atoms) | not in seed vocab at all |
| `0_2_5` | `tensor-reshape-view` | seed only listed 2,4,9 for this atom; reshape-via-einops in BatchNorm forward wasn't anticipated |

**Pattern:** ~1 false negative per exercise. Net add to vocab is ~1 atom/exercise so far. **Likely vocab steady-state is 40-50 atoms for part2_cnns** (vs 36 currently, vs my pre-tagging guess of 25-60). Inside the target band.

---

## Atom-usage frequencies in the 4 tagged exercises

| atom | count | exercises |
|---|---|---|
| `nn-module-subclass` | 3 | 0_2_1, 0_2_4, 0_2_5 |
| `nn-parameter-wrap` | 3 | 0_2_1, 0_2_4, 0_2_5 |
| `module-extra-repr` | 3 | 0_2_1, 0_2_4, 0_2_5 |
| `kaiming-uniform-init` | 2 | 0_2_1, 0_2_4 |
| `einsum-contraction` | 2 | 0_2_1, 0_2_17 |
| `broadcasting-rules` | 2 | 0_2_1, 0_2_5 |
| (singletons in this sample, 11 atoms) | 1 each | varies |

**Spine of part2_cnns:** `nn-module-subclass` + `nn-parameter-wrap` + `module-extra-repr` co-occur in every Module exercise. These three define the "implement a PyTorch Module" composite skill. **All three are DD-N (no drill coverage).** This is the headline cost of the gap question — the most reused atoms in part2 are the ones Delta Drills doesn't drill.

---

## DD coverage in actual taggings

| DD code | Atom-tag count (out of 26) | % |
|---|---|---|
| DD-Y (covered by drills) | 4 | 15% |
| DD-? (partial) | 3 | 12% |
| DD-N (no drills) | 19 | 73% |

**73% of atom-tags so far point to atoms Delta Drills doesn't drill.** Even more skewed than the vocab-level 64% DD-N. The gap question isn't theoretical — it's what 3 out of every 4 atom hits in a real exercise look like.

---

## Implications for next steps

1. **Accept all 4 proposed atoms.** Update `vocab/atoms.json` from 36 → 40.
2. **Re-derive "expected exercises" column.** The seed's column is unreliable (~30-40% false positives). Replace with: actual exercise-to-atom mapping aggregated from `exercises/*.json` files. Becomes accurate by construction.
3. **The gap question is urgent.** 73% of real atom-tags are DD-N. Whatever the answer (extend drill format / route to docs / mark non-drillable), it determines what 3 of 4 atoms even do downstream.
4. **Granularity is approximately right.** Mean 6.5 atoms/exercise is at the high end of target band but reasonable for these exercises (BatchNorm is genuinely complex). No urgent split/merge calls.
5. **Stop tagging at n=4 for now.** Diminishing returns until either (a) Callum responds with terminology corrections, or (b) the gap question is decided. Both inform downstream tagging.

---

## What to send Callum

If you want to send him the calibration report alongside the seed vocab, this is the bottom line: **the vocab works structurally, but it's missing kaiming-init and register-buffer, and the seed's exercise-mapping column was over-generous.** Ask him to focus on naming + missing atoms; the structural feedback we'll get from continued tagging.
