{
  "exercise_id": "0_3_11",
  "title": "complete DistResNetTrainer",
  "source_notebook": "arena-book-colab/ARENA_5.0/chapter0_fundamentals/part3_optimization/0_3_11_exercise-complete-distresnettrainer.ipynb",
  "arena_chapter": 0,
  "arena_part": 3,
  "arena_index": 11,
  "difficulty": 5,
  "importance": 3,
  "tagged_by": "claude-opus-4-7",
  "tagged_at": "2026-05-18",
  "tagging_pass": "part3-pilot",
  "atoms": [
    {"id": "dataclass-training-args", "role": "core", "evidence": "@dataclass class DistResNetTrainingArgs(WandbResNetFinetuningArgs): world_size: int = 1; wandb_project: str | None = 'day3-resnet-dist-training'"},
    {"id": "trainer-class-skeleton", "role": "core", "evidence": "DistResNetTrainer defines __init__, pre_training_setup, training_step, evaluate, train — same 4-method lifecycle (markdown says 'We recommend not using inheritance for this' so this is a sibling rather than subclass)"},
    {"id": "cross-entropy-classification-loss", "role": "core", "evidence": "loss = F.cross_entropy(logits, labels) inside training_step"},
    {"id": "argmax-accuracy-eval", "role": "core", "evidence": "total_correct += (logits.argmax(dim=1) == labels).sum().item() inside evaluate"},
    {"id": "tqdm-postfix-metrics", "role": "core", "evidence": "pbar = tqdm(self.train_loader, desc='Training', disable=self.rank != 0); pbar.set_postfix(loss=..., ex_seen=..., accuracy=...)"},
    {"id": "loss-item-scalar-extract", "role": "core", "evidence": "wandb.log({'loss': loss.item(), ...}, step=self.examples_seen) — .item() to extract Python float"},
    {"id": "dataloader-pin-memory-workers", "role": "core", "evidence": "dataloader_shared_kwargs = dict(batch_size=self.args.batch_size, num_workers=2, pin_memory=True) — the ResNet-on-CIFAR throughput pattern"},
    {"id": "dataloader-batching", "role": "core", "evidence": "self.train_loader = DataLoader(self.trainset, sampler=self.train_sampler, **dataloader_shared_kwargs); self.test_loader = DataLoader(...)"},
    {"id": "training-step-cycle", "role": "core", "evidence": "logits = self.model(imgs); loss = F.cross_entropy(logits, labels); loss.backward(); ... self.optimizer.step(); self.optimizer.zero_grad()"},
    {"id": "validation-no-grad", "role": "core", "evidence": "@t.inference_mode() decorator on evaluate; total_correct/total_samples accumulator"},
    {"id": "train-eval-mode-branch", "role": "core", "evidence": "self.model.train() in train(); self.model.eval() at top of evaluate()"},
    {"id": "tensor-to-device", "role": "core", "evidence": "imgs, labels = imgs.to(self.device), labels.to(self.device) — in both training_step and evaluate"},
    {"id": "backward-on-scalar-loss", "role": "core", "evidence": "loss.backward() in training_step"},
    {"id": "wandb-init-run", "role": "core", "evidence": "if self.rank == 0: wandb.init(project=self.args.wandb_project, name=self.args.wandb_name, config=self.args)"},
    {"id": "wandb-log-step", "role": "core", "evidence": "wandb.log({'loss': loss.item(), 'fwd_time': (t1-t0), 'bwd_time': (t2-t1), 'dist_time': (t3-t2)}, step=self.examples_seen)"},
    {"id": "wandb-finish", "role": "core", "evidence": "if self.rank == 0: wandb.finish() at end of train()"},
    {"id": "examples-seen-step-axis", "role": "core", "evidence": "self.examples_seen += imgs.shape[0] * self.args.world_size — markdown: 'We recommend you increment examples_seen by the total number of examples across processes ... convenient for them to have a consistent x-axis'"},
    {"id": "param-grad-access", "role": "core", "evidence": "for param in self.model.parameters(): all_reduce(param.grad, ...) — iterate parameters and access grad"},
    {"id": "rank-world-size-args", "role": "core", "evidence": "def dist_train_resnet_from_scratch(rank, world_size): / DistResNetTrainer.__init__(self, args, rank): — rank threaded through"},
    {"id": "broadcast-source-fanout", "role": "core", "evidence": "for param in self.model.parameters(): broadcast(param.data, self.rank, self.args.world_size, src=0) — sync initial weights from rank 0"},
    {"id": "all-reduce-compose", "role": "core", "evidence": "all_reduce(param.grad, self.rank, self.args.world_size, op='mean') — uses the all_reduce implemented in 0_3_10 (which composes reduce + broadcast)"},
    {"id": "reduce-op-mean-divide", "role": "core", "evidence": "all_reduce(param.grad, ..., op='mean') for grad-sync, all_reduce(tensor, ..., op='sum') for eval; mean is sum-then-divide-by-world-size inside all_reduce"},
    {"id": "init-process-group-nccl", "role": "core", "evidence": "dist.init_process_group(backend='nccl', rank=rank, world_size=world_size) ... dist.destroy_process_group() in dist_train_resnet_from_scratch"},
    {"id": "mp-spawn-workers", "role": "core", "evidence": "mp.spawn(dist_train_resnet_from_scratch, args=(world_size,), nprocs=world_size, join=True)"},
    {"id": "per-rank-cuda-device", "role": "core", "evidence": "self.device = t.device(f'cuda:{rank}'); self.model = get_untrained_resnet(...).to(self.device)"},
    {"id": "broadcast-initial-weights", "role": "core", "evidence": "if self.args.world_size > 1: for param in self.model.parameters(): broadcast(param.data, self.rank, self.args.world_size, src=0) — sync all params from rank 0 after independent construction"},
    {"id": "all-reduce-grad-sync", "role": "core", "evidence": "if self.args.world_size > 1: for param in self.model.parameters(): all_reduce(param.grad, self.rank, self.args.world_size, op='mean') — after loss.backward and before optimizer.step"},
    {"id": "distributed-sampler-shard", "role": "core", "evidence": "self.train_sampler = DistributedSampler(self.trainset, num_replicas=self.args.world_size, rank=self.rank); ... self.train_sampler.set_epoch(epoch) — both creation and set_epoch reshuffle"},
    {"id": "rank0-only-side-effects", "role": "core", "evidence": "if self.rank == 0: wandb.init(...); if self.rank == 0: wandb.log(...); if self.rank == 0: wandb.finish(); t.save(...) — every wandb call and save guarded"},
    {"id": "all-reduce-eval-metrics", "role": "core", "evidence": "tensor = t.tensor([total_correct, total_samples], device=self.device); all_reduce(tensor, self.rank, self.args.world_size, op='sum'); total_correct, total_samples = tensor.tolist()"},
    {"id": "time-stage-instrumentation", "role": "core", "evidence": "t0 = time.time(); ... t1 = time.time(); ... t2 = time.time(); ... t3 = time.time(); wandb.log({'fwd_time': (t1-t0), 'bwd_time': (t2-t1), 'dist_time': (t3-t2)}, ...); also wandb.log({'epoch_duration': time.time() - t0})"},
    {"id": "model-save-state-dict", "role": "core", "evidence": "if self.rank == 0: ...; t.save(self.model.state_dict(), f'resnet_{self.rank}.pth') at end of train()"}
  ],
  "atoms_in_seed_but_not_actually_present": [
    {"id": "trainer-subclass-extend", "reason": "Seed predicted this from the WandbResNetFinetuner pattern, but markdown explicitly says 'We recommend not using inheritance for this' and the solution builds DistResNetTrainer as a sibling class (not extending WandbResNetFinetuner). No super().pre_training_setup() calls."}
  ],
  "notes": "32 atoms — above seed prediction of 25. The capstone exercise: composes the full training-loop spine (8 atoms) + full wandb cluster (4 atoms) + every distributed primitive (5) + every DDP-integration atom (7) + both NEW instrumentation atoms (time-stage + model-save) + dataloader-pin-memory-workers + dataclass-training-args + trainer-class-skeleton. trainer-subclass-extend is a seed FP because the solution explicitly avoids inheritance here. This is the densest exercise in chapter 0 (prior record was 0_1_9 at 14)."
}
