# Deploy Worktree

This directory is for production deploys to Vercel.

- Branch: deploy
- Vercel production branch: deploy
- Public URL: https://delta-drills.vercel.app

To pull in the latest local changes from main, run:

```
./scripts/sync-deploy.sh
```
