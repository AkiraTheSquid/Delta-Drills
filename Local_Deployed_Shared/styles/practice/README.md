# styles/practice

## Purpose
- Stylesheets for the Practice tab (the adaptive-queue question page), split out of the former monolithic `practice.css` (972 LOC) into per-concern files.

## Owns
- All CSS for the practice page shell, session setup/status, question display, feedback/rating UI, code editor/output, and the tab's smaller extras.

## Does NOT own
- The bar *primitives* in `../../practice/bars.css`. Nothing on the practice page draws them any more — the ARENA unlock interstitial and Targeted Practice build their own bars out of those classes, which is why the file is still linked. Their own stylesheets too: `../../practice/arena-unlock.css`, `arena-unlock-timer.css`, `../../targeted-practice/targeted-practice.css`.
- Global utilities — `.hidden`, buttons, variables live in `../components.css`, `../base.css`, `../variables.css`.

## Key Files
- `layout.css`: page container, left/right split panels, responsive stacking under 900px.
- `timer.css`: the idle CARD and the (hidden) in-session status row, plus the `session-idle` page-state rules. 🔴 The pre-session setup panel is gone — the learner does not set the clock any more (2026-08-23), so `.session-setup-title/-note/-fields`, `.session-field(-label)`, `.session-time-estimate`, `.session-resume-title`, `.session-resume-actions` and `.timer-input` were all deleted with the inputs they styled. What is left of the panel is `.session-setup` (the box) and `.session-summary`. ⚠️ `.session-resume-panel` and `.session-start-btn` are CLIPPED, not `display: none`: they still carry timer.js's handlers and the readiness screen's one button forwards clicks to them, and a display-none button is out of the accessibility tree — which matters because the resume path writes its refusal as text into `#session-resume-summary`. `.session-end-btn` went with `#session-end-btn`.
- `readiness.css`: **the readiness dial** — the whole of the Practice tab's idle screen (`.session-readiness`, markup in `index.html`, behaviour in `../../practice/session-idle.js`). One conic-gradient ring with a hard stop at `--dd-ready-pct`, masked to an annulus by a `::after` disc that carries `--surface` (a translucent hole over a conic gradient shows the gradient through it and reads as a second, dimmer ring). No SVG and no canvas: the fill is one custom property, so painting it re-lays-out nothing. 🔴 `.readiness-dial--unknown` flattens the ring when the registry could not be read — an unreadable number must not render as a full or an empty ring, either of which reads as a measurement. `.readiness-detail` reserves its line height because it is written after a fetch and would otherwise shift the button under the reader's cursor. 🔴 `.readiness-dial--sm` is the SAME dial on the placement results card (`../../practice/placement-results.js` mints it), showing the same figure from the same reader — the two screens looking like one instrument is half of why they stopped disagreeing. It changes `--dd-ready-ring` and the box only; anything else added here must be added to the modifier too or the two drift.
- `question.css`: the question's **heading CARD**, its text, imported-helpers pills + detail, target-image visual, meta chips, prose/code-block split. 🔴 `.question-number` is a 20px `<h2>` naming the CONCEPT under test ("Reshape, ravel, and element order"), not the uppercase 14px muted counter it used to be — "Question 21" was a number that only goes up. The two audit chips that shared its row (`#question-id-chip`, `#practice-graph-jump`) moved into the notch's three-dot menu, and `#placement-timer` followed them onto the notch **tab** later the same day — `.question-number-row` is the heading and nothing else now. 🔴 **`.question-number-row` IS THE RECTANGLE** (2026-08-23): a `flex-direction: column` box with a border, a radius and a centred `<h2>`, holding `#stage-ladder` directly under the title. It was a baseline ROW; the ladder was a full-width strip above both panels, which put the concept's name on the screen twice — once in the strip's pop-up and once in this heading, in two type sizes. Default `align-items: stretch` is load-bearing: it is what gives the track the card's full width, which is why the heading is centred with `text-align` and not with flex.
- `feedback.css`: submit/skip/don't-know row, result badge, felt-difficulty rating buttons, problem quality flags, missed-fact row, failed-tests block.
- `editor.css`: code editor, Run button, output area + output visual, solution section, AI explanation.
- `misc.css`: hint/answer aids, practice-mode intro, self-report row, placement entry button, torch Colab notice, mode-demotion notice. (The `.topbar-auth*` block was deleted 2026-08-23 with the markup; the `[hidden]`-vs-class-`display` lesson it left behind is kept in the file.)
- `notch-menu.css`: **the session controls, on the seam between the panels** (`#practice-notch`, behaviour in `../../practice/notch-menu.js`). A tab hanging from `.practice-container`'s top edge, carrying four things left to right: `.practice-notch-stop` (a square that PAUSES), the **countdown**, `#placement-timer` (the placement test's own countdown, styled in `diagnostic.css`), and the three dots that open the menu. 🔴 **The two clocks take turns, never share.** The placement runs OUTSIDE a session, so `_sessionOpen()` is false for the whole test and `.practice-notch-clock` would sit next to a running probe greyed at its idle 02:00 — two numbers, one of them stopped. `notch-menu.js::_placementOnClock` hides the session clock for as long as `#placement-timer` is not `.hidden`, reading the ELEMENT rather than `PlacementTimer.isRunning()` (the two disagree exactly when it matters: `pauseForGrading` and `_expire` both clear the interval before the chip goes). 🔴 It anchors to the CONTAINER, not the split — the split is `display: none` between sessions and the notch has to outlive one (2026-08-23). There is no End session anywhere: a block has no length. `.practice-notch-tab` owns the frame so the clock and the dots read as one control; `.practice-notch-btn` is now just the glyph. 🔴 `.practice-notch-clock` is a MIRROR of `#session-countdown`, written by `notch-menu.js` — `timer.css` hides the whole status row while `timer.js` keeps counting and auto-submitting, so this is the only place the deadline is visible, and a clock of its own here would be the one that disagrees with the submit. Tabular figures (the tab is centred on the seam; jiggling glyph widths move the whole thing), `--review` tints it `--ok` and `--low` tints it `--danger` for the last 30 seconds of ANSWER time only. `.practice-container` carries the `position: relative` it anchors to (`layout.css`); `.practice-split` keeps its own for the panels' overlays. ⚠️ The clock does NOT disappear between sessions any more — `--idle` greys it and `notch-menu.js` writes the allowance the next question gets (02:00, read from `PracticeSession.idleClockText()`, never computed here). `.practice-notch-stop:disabled` is DIMMED rather than hidden for the same reason the padding is symmetric: the tab is centred on the seam, so anything that vanishes and comes back moves the whole notch. `.practice-notch-sr` is the clipped span carrying the phase word for screen readers — clipped, not `display: none`, which would take it out of the accessibility tree along with the pixels. The menu also carries the two AUDIT controls now (`#question-id-chip`, `#practice-graph-jump`, moved out of the question row 2026-08-23 with their ids intact so `../../practice/ui.js` and `../../practice/graph-jump.js` still drive them). They keep `.question-id-chip` so `graph-jump.js`'s `.is-untagged` marking still lands — which means the CHIP BOX comes with them unless it is overridden, hence `.practice-notch-item.question-id-chip`, and hence a restatement of the untagged marking, because `.is-untagged` only says `border-style: dashed` and a flat menu row has no border to dash. 🔴 No `data-dd-info` in this menu: `infotips.js` mints the ⓘ as a SIBLING, and in a column-flex menu that is its own row — each item grew a stray "i" underneath it. That copy moved into the buttons' `title` and both registry entries were deleted rather than orphaned.
- `stage-ladder.css`: **the one progress readout on the practice page** (`#stage-ladder`, INSIDE the question's heading card in the left panel; behaviour in `../../practice/stage-ladder.js`). A column: the concept name, the rung you are on named in words (plus `.stage-ladder-flag`, the INTEGRATED chip — a property of the problem, never a rung), then the meter — Lesson · Faded · Worked example · Solo — and a text caption under it. 🔴 **The meter is ONE bar cut into four, not four bars in a row.** A single rounded `.stage-ladder-bar` carries a single `.stage-ladder-fill` across the whole ladder, and the rung boundaries are `.stage-ladder-seam` chevrons — sideways Vs pointing right, drawn in the panel's own `--surface-2` so they read as the bar being cut rather than as ticks laid over it. The rung names are INSIDE the track, and they are drawn **twice**: `.stage-ladder-track--base` is coloured for the empty track, `.stage-ladder-track--on` is coloured for the accent and clipped to `inset(0 calc(100% - var(--dd-ladder-pct)) 0 0)`, so every pixel of every glyph — and of the rung's ⓘ — is painted for the ground behind THAT pixel and the contrast flips exactly at the fill's edge, mid-word. 🔴 The two layers must stay pixel-identical: the clipped copy carries an inert `<span class="dd-info">` twin of each rung's button so both rows measure the same, which is why the dots are hand-written in the script instead of injected by `infotips.js`. 🔴 **THE READING IS A LINE OF TEXT UNDER THE TRACK, and there is no strip any more** — 2026-08-23, Seth: "the bar goes on the left as well right below the title of the concept ... it is part of a rectangle that holds both the bar and the concept right above it". The ladder is a block inside `.question-number-row` (see `question.css`, which owns the rectangle) and the `<h2>` in that same card names the concept, so `.stage-ladder-reading` states the percentage and carries the INTEGRATED chip, the lesson eyebrow and the withdrawn-scaffold note — nothing else. 🪦 `.stage-ladder-callout` is DELETED with `--dd-ladder-gap`, `--dd-callout-arrow-x`, the `padding-top` reserve above the bar and `stage-ladder.js`'s resize listener: it was an absolutely-positioned pop-up whose BOX was clamped by measurement to stay inside the strip and whose ARROW was aimed separately, in the box's own coordinates, because the moment that clamp bit the box's centre stopped being the position described. All five were one mechanism and they were removed together — putting any floating shape back here needs all five, and it can only float over the question now. 🔴 The bar's height still lives in ONE place, `--dd-ladder-bar-h` on `.stage-ladder-meter` (26px, 20px on the Colab rail, 24px on a phone). 🔴 **The ladder — not the card — is `display: none` in basic mode** (`body.dd-basic-mode .stage-ladder`, class written by `app.js::applyModeVisibility`): Seth's call, 2026-08-23. The heading card and its centred title stay in both modes; only the bar rides with Advanced. ⚠️ The empty track is tinted 0.13 and not 0.07: the seams are cut in the panel colour, so a fainter track hid every division the fill had not yet reached. ⚠️ The rung names now have to fit HALF a window rather than a whole one — `.stage-seg-label` ellipsises, and the Colab rail's overrides are worth reading before you widen anything. There is no difficulty clause: the ladder states one quantity. Replaces `concept-topbar.css` and `difficulty-bar.css`, both deleted.
- `notebook-view.css`: **the Notebooks tab**, which is not the practice page at all — it lives here because it is a practice-surface stylesheet and shares the tokens. Styles the notebook list, the sticky toolbar (Back / title / jump-to / Restart session), the banner the view uses to say the session restarted or that the learner is signed out, and the cell: a `[n]` execution counter, an editable source block, its output, and the run/failed/stale borders. Solutions and hints are `<details>` — closed is the default and the styling must not make an open one look like an ordinary cell. Scoped under `.nbv-*`; nothing here is shared with `editor.css`, because a notebook cell and the practice editor look alike and are not the same thing.
- `basic-mode.css`: **what the practice question is when Advanced mode is off**, which is the default. Nothing but `body.dd-basic-mode` overrides — the same class `app.js::applyModeVisibility` writes from the Account tab's checkbox and that `stage-ladder.css` already gated the ladder on. Seth, 2026-08-23: the tab "shows way too much information ... for now it just needs to show the title of the problem area, the question, and the examples if it's a faded or worked example problem", with "the feedback and all the other extra information for Advanced mode from the settings". Survives: `.question-number-row`, `#question-text` (and the `.ladder-example` `ladder.js` appends INSIDE it), `#question-visual`, `#torch-colab-notice`, `#practice-submit-area`, `.result-row`, `#next-problem-btn`. Hidden: imported helpers, the hint aids, the override row, the felt-difficulty rating, the failed-tests block, the missed-fact and problem-flag rows, the concept-understanding bar, the solution, the AI explanation and the tutor. 🔴 **Must stay LAST in `index.html`'s `<link>` order** — every rule in it overrides something declared in the sheets above, so a link moved earlier loses the ties and the screen quietly comes back. `watch.py` asserts the position against the whole of `CSS_FILES`, and asserts the keep-list is never hidden. 🔴 It hides the three `.feedback-btn`s **individually, never `.feedback-buttons`**: `#next-problem-btn` is their sibling in that row and carries none of their classes, so hiding the row takes the only way off a graded question with it. 🔴 It also hides `.dd-info[data-dd-info="feedback-rating"]` — `infotips.js` mints the dot as a SIBLING of its subject, so hiding `#feedback-prompt` alone leaves a bare "i" under the result badge (same trap the placement timer hit). ⚠️ Display-only by design: every element keeps its id and is still written to, so the AI explanation still loads and ticking Advanced reveals content that is already there. The ONE behavioural consequence — the hidden rating is the only backend mutation that commits an attempt to mastery — is handled in `../../practice/basic-mode.js`. 🔴 **Not on the Colab edition** — every selector is prefixed `html:not(.dd-colab-edition)`, and `practice/basic-mode.js::active()` states the same predicate. That deploy already cut this rail down differently and on purpose, and the two things it deliberately KEEPS are the two this sheet would have taken: the reference solution (its whole review step — *"after pressing submit… it should display the solution to the problem at the bottom"*) and the felt-difficulty rating with its help line, which `colab-edition.css` restyles into a stacked column because *"nothing advances until one is pressed"*. `body.dd-basic-mode` is written there too, and these rules out-specify `colab-edition.css`'s, which only sets border and padding on `.solution-section` — so unguarded, Basic mode silently deleted the solution that edition exists to show. ⚠️ The guard is the whole edition, not `:not(.dd-no-notebook)`, so Basic mode also stays off for the ~75 Colab questions with no published notebook; one predicate both halves can state identically was judged worth more than that corner.
- `colab-edition.css`: the Colab edition's tutor rail, and its review step (a verdict opens the solution; everything else in the feedback panel is hidden there). Everything is scoped under `html.dd-colab-edition` (set by `practice/colab_mode.js` on the `delta-drills-colab` deploy) and is inert on the normal app. Strips the prompt, the editor and the worked example — they are in the notebook beside the panel — and re-stacks the stage ladder for panel widths (its four sections wrap to a 2-column grid, which is what keeps "Worked example" from truncating at 300px). `html.dd-no-notebook` (set by `ui.js`) turns all of it back off for the ~75 questions with no published notebook, which would otherwise get an empty rail.

## Data & External Dependencies
- CSS custom properties from `../variables.css` (`--border`, `--surface`, `--muted`, `--accent`, …).
- Markup lives in `../../index.html` (`#page-practice`); class toggling driven by `../../practice/*.js`.

## How It Works (Flow)
1. `index.html` links every file in this folder (after `../components.css`, so same-specificity practice rules win).
2. `#page-practice.session-idle` (set in markup, toggled by `practice/timer.js`) shows the setup panel and hides `.practice-split` **and `.stage-ladder`**; starting a session flips it.

## Invariants & Constraints
- Load order: these files must come AFTER `styles/components.css` — several rules rely on overriding it by order.
- Any rule that sets `display` on an element JS toggles with `.hidden` must re-assert `.selector.hidden { display: none; }` when its selector outweighs the global `.hidden` class (see `#practice-submit-area.hidden` in `feedback.css`).
- Keep selectors class-based; ID selectors beat `.hidden` and have caused real hide-failures.

## Extension Points
- New practice-tab UI: add to the file matching its concern (or a new file + `<link>` in `index.html` with a `?v=` param). Bump the file's `?v=` on every edit — stale-cache trap.

## Known Issues, Recurring Bugs, and Pain Points (and How to Prevent Them)

- **Page-state rules that name only `.practice-split`** — `RESOLVED`
  - When it happens: a new element is added to `.practice-container` as a SIBLING of `.practice-split` rather than a child of it.
  - Symptom: the setup screen shows a strip of leftover question state (concept name, rung, difficulty) for a question that is not on screen.
  - Root cause: `#page-practice.session-idle .practice-split { display: none }` hides the split's subtree only. The readout is deliberately lifted out of the split so it can span both panels, which also lifts it out of that rule.
  - Prevention/fix: any new direct child of `.practice-container` that belongs to the question view needs its own `#page-practice.session-idle` rule. `watch.py` asserts the ladder's.
  - Status: RESOLVED (2026-07-30 for `.concept-topbar`; recurred immediately — the difficulty bar was added beside it as a second such child with no rule of its own, and shipped a 0-100 track on the setup screen until 2026-08-19. One element now, one rule.)

- **ID selector defeats `.hidden`** — `RESOLVED`
  - When it happens: a rule like `#practice-submit-area { display: flex }` styles an element JS hides via `classList.add("hidden")`.
  - Symptom: element never disappears.
  - Root cause: ID specificity (1,0,0) beats the global `.hidden` class (0,1,0).
  - Prevention/fix: re-assert `#the-id.hidden { display: none; }` next to the rule, or use a class selector.
  - Status: RESOLVED (2026-07-12) for `#practice-submit-area`; the rule stands for new code.

## Recent Changes
- 2026-08-24 (the timer moved into the topbar, and the placement moved onto the Learner Home): **`notch-menu.css`** (`?v=7`) and **`diagnostic.css`** (`?v=10`). Seth: "put the timer in the top middle bar rather than as the notch", and "the diagnostic and practice should be combined into one tab". `#practice-notch` was `position: absolute; left: 50%; translateX(-50%)` with a hanging-notch shape (`border-radius: 0 0 10px 10px`, `border-top: 0`) — it is an ordinary flex child of the new `.topbar-mid` cell now, with a `999px` pill radius and real padding, so it sits beside the tab strip in advanced mode and alone in basic mode without being positioned by hand. Its menu keeps its behaviour and re-anchors to `top: calc(100% + 10px); z-index: 60`. 🔴 **`diagnostic.css` gained the specificity guard the merge made necessary.** With `#diagnostic-workspace-host` on the SAME page as the idle screen, `#page-practice.session-idle .practice-split { display: none }` (`timer.css`, `(1,2,0)`) outranks `.diagnostic-workspace-host .practice-split { display: flex }` (`(0,2,0)`) — a placement probe rendered as a blank idle screen. Settled with two-ID selectors (`#page-practice #diagnostic-workspace-host .practice-split`, `(2,1,0)`) rather than a JS class dance, so it holds whatever order the stylesheets are linked in; asserted in `../../watch.py::check_front_door`. Every `#page-diagnostic` selector was repointed at `#page-practice`, which is what carries `.diagnostic-running` now. New `.learner-areas` block: the area bars are a section on the idle surface (`.is-empty { display: none }` is the only thing that hides them), not content locked inside the completion-gated results card.
- 2026-08-23: **The progress ladder came off the top of the page and into the question's heading card.** Seth, after seeing the stripped screen: "it doesn't have the progress bar for the advanced version of the app ... the bar goes on the left as well right below the title of the concept, and the title of the concept is center aligned for the left panel ... it is part of a rectangle that holds both the bar and the concept right above it ... it also gets rid of `class="stage-ladder-callout"`". `question.css` turns `.question-number-row` into that rectangle (column, border, radius, centred `<h2>`); `stage-ladder.css` drops the strip's own background, border and padding and becomes a block inside it; `colab-edition.css` retunes the CARD instead of the strip. 🔴 **The pop-up reading and everything holding it up are gone**: `.stage-ladder-callout`, its two `::before`/`::after` arrow triangles, `--dd-ladder-gap`, `--dd-callout-arrow-x`, the `padding-top` reserve on `.stage-ladder-meter` and the resize listener in `stage-ladder.js` that re-clamped the box on every drag. What replaces all of it is `.stage-ladder-reading`, a flex line of 11.5px text under the track. Nothing in the ladder is script-positioned or script-measured any more. 🔴 **The concept is named ONCE.** `#stage-ladder-kc` and the `<h2>` are both written from `ladder_kc_title`, so in the card they were the same words a centimetre apart; `stage-ladder.js` now hides the button whenever its label would repeat the heading — by READING the heading, not by a flag from the caller, which keeps the lesson screen (heading = "Lesson") naming its concept. The jump is not lost: "See in knowledge graph" in the notch menu is the same trip, gated on the same tag, in both modes. ⚠️ `#page-practice` is still a flex column and `timer.css`'s `#page-practice.session-idle .stage-ladder` still exists — both are now belt-and-braces (the ladder is inside `.practice-split`, which the idle screen already hides), kept because the next thing to want a strip above the panels will want exactly that shape. Measured on a served copy of the patched tree at 1680px: card 770.5px, heading centre error 0.00px, bar 8px under the heading at the card's full inner width, reading line under the bar, notch clear of it; basic mode leaves a 50px card with the ladder `display: none`. `styles/practice/watch.py` swapped the two callout assertions for the pair that replaced them (`.stage-ladder-reading` present, `.stage-ladder-callout`/`--dd-callout-arrow-x` absent from the RULES — the file's own history comments still name them — plus the card's column/radius and the centred heading).
- 2026-08-23: **One dial, two screens: the placement results card now wears the Practice tab's readiness ring, and the placement page lost its walls of text.** Seth: the placement's "how ready you are for ARENA" figure "is different from practice thing before you enter the practice ... it needs to be the same", plus "get rid of the information overload of text ... and the archaic less modern design". 🔴 The two figures were computed by two different files from two different quantities — `placement-results.js` averaged `(theta - 20) / 80` over the NINE areas, `session-idle.js` counted the 63 concepts at `r >= 0.95` — and `diagnostic.py` seeds a finished placement at `SEED_MASTERY_CAP = 0.92`, UNDER that threshold, so the card read ~45% and the very next screen read 0%. Both call `../../practice/readiness.js` now. `readiness.css` gained `.readiness-dial--sm` (the same ring, 108px, for the results card — only the two size knobs change) and `diagnostic.css` gained `.placement-chip`, `.placement-overall-caption` and `.placement-overall-detail` while `.placement-overall-figure` was deleted with the block it styled. 🔴 `.placement-overall` deliberately has NO background of its own: the dial's centre hole is drawn in `--surface` (see `readiness.css` in Key Files), so a tinted panel behind it would show the hole as a second, dimmer disc. 🔴 Two rules here are scoped through `.diagnostic-results` rather than marked important — the card ships `.diagnostic-results p { color: var(--muted); margin: 0 }` at (0,1,1), which beats a bare class and would render the caption muted and flush. The page itself is narrower (880px), the h1 carries it, the status line is a dot + one clause, and the length quote is a chip row rather than two sentences. `watch.py` here follows the class list.
- 2026-08-23: **The practice question is down to its concept, its text and its worked example — the rest rides with Advanced mode.** New `basic-mode.css` (see Key Files), linked LAST of the practice sheets. Nothing was deleted and no markup moved: this is one sheet of `body.dd-basic-mode` overrides on the toggle that already existed, so the Advanced checkbox on the Account tab now reaches the question as well as the nav and the ladder. 🔴 The one rule that is not cosmetic is hiding `.feedback-btn`: `POST /api/practice/feedback` is the only backend mutation that moves a subtopic baseline, and `#next-problem-btn` is revealed inside that button's own click handler — hide the row with nothing standing in for it and the student model silently stops moving while the only way forward is the 02:00 review clock timing out. `../../practice/basic-mode.js` settles it by clicking the real default button; see that folder's README for what the neutral default costs. `watch.py` here gained the cascade-position check, the keep-list check and the orphaned-ⓘ check; `../../practice/watch_basic_mode.py` holds the behavioural half. 🔴 **Self-review caught one real defect** before this shipped: unguarded, these rules deleted the Colab edition's reference solution and its rating row — see the Colab clause in Key Files. `/critic` could NOT run to catch it (codex over its usage limit until 08-27 11:40, the same wall @Z, @Q, @N and @P hit).
- 2026-08-23: **The placement clock moved onto the notch tab, and the three dots are centred by layout instead of by nudge.** Seth: "for the placement test, it has the problem that the timer is on the left next to the title thing instead of being on the tab", and "for the three dots, it needs to be more center aligned". `#placement-timer` left `.question-number-row` for `.practice-notch-tab`; `diagnostic.css` dropped its pill (border + 999px radius, which drew a bordered capsule inside a bordered 15px tab). It then briefly kept an `--info` tint to mark it as the placement's clock — and that tint was itself removed within the hour, because Seth's follow-up was that the placement should be *"indistinguishable user experience wise from the practice ... in terms of the top bar for the timer and all that, exactly the same."* 🔴 `#placement-timer` now carries `.practice-notch-clock` **in the markup** and `.placement-timer` declares nothing but `.hidden`; the class survives only as the anchor (the id JS writes, the hook `notch-menu.js` reads, what `watch.py` asserts). Verified computed-style equal on all of colour, size, weight, letter-spacing, line-height, padding and `font-variant-numeric`. Its low state is `.practice-notch-clock--low`. Do not give `.placement-timer` visual properties again — that is how the two drift back apart. 🔴 The two clocks TAKE TURNS — see `notch-menu.css` in Key Files. `.practice-notch-btn` became an `inline-flex` box that centres its one child: it was a 13px button with `padding: 1px 6px 3px` **and** a `translateY(-2px)` on the glyph, three offsets each correcting the last, and the -2px was correcting a problem `&#8943;` (U+22EF, MIDLINE ellipsis — already on the math axis) does not have. 🔴 The obvious follow-up correction — a `margin-right: -1px` to cancel `letter-spacing`'s trailing pixel — was tried and **measured worse**: the trailing spacing is inside the span's box (the flex centring already accounts for it) and the margin is outside it, so shaving the outer width shifts a centred child half a pixel right. Glyph-centre offsets from the button's centre at 1680px: old geometry (0, −3), with the margin (0.5, 0), with neither (0, 0). The ⓘ went with the move: an `infotips.js` dot is a SIBLING, and in a 15px row that is an icon wedged between the clock and the dots — key and anchor were deleted together (`watch.py` asserts both lists stay in step).
- 2026-08-23: **The Practice tab's idle screen is a readiness dial, and the notch outlives the session.** New `readiness.css`; `timer.css` lost the setup panel's eight rule groups; `notch-menu.css` gained the square (`.practice-notch-stop`) and the idle clock tint; `layout.css` moved `position: relative` from `.practice-split` to `.practice-container` so `#practice-notch` anchors to a box that is on screen between blocks. Measured after the move: `notchTop` 44 = `splitTop` 44 while a session is open, so nothing shifted. See `../../practice/README.md` for the session-model half.
- 2026-08-23: **The topbar's tab strip is exactly centred.** `../layout.css` turned `.topbar` into a `1fr auto 1fr` grid — `justify-content: space-between` centres the middle child only when the outer two are the same width, which they never are. Strip centre = bar centre, 0.00px error, measured signed out and with all ten tabs showing. `../infotips.css` lost its whole topbar variant (`.tab.has-info`, `.dd-info.tab-info` and both overrides) with the ten ⓘ dots; deleting those is also what straightened the active underline, which had been spanning the label while the eye read label+dot as the tab.
- 2026-08-23: **The chrome came off the practice screen: no brand line, no email, tabs on the right, no ladder, no counter — and the black bar under the page is gone.** Seth's call, after a screenshot. 🔴 **The black band was a footer that does not exist.** `layout.css` here had `#page-practice { height: calc(100vh - 56px - 40px) }`; the 40px reserved room for `.footer`, which `../layout.css` still styles (fixed, bottom, 40px) and which **no `<footer>` in `index.html` has ever filled** — measured on prod and locally, 0 `.footer, footer` elements and 40px of bare `--bg` under the page. Now `calc(100vh - var(--dd-topbar-h))`, re-measured at 0. ⚠️ Two things left alone deliberately: the topbar's height is still SUBTRACTED rather than measured, so a solo `/practice` (`html.dd-solo`, no topbar) takes off a bar that is not there; and `#guest-banner` is 116px and has never been in this arithmetic, so a GUEST's practice page overflows the viewport — pre-existing, and the dead 40px was masking part of it. `../layout.css` gained `--dd-topbar-h` (44px) as the single source for the bar's height, which `../nav-drawer.css`'s drawer header now reads too. `stage-ladder.css` gained `body.dd-basic-mode .stage-ladder { display: none }` and the callout became a detached pop-up (see Key Files); `question.css`'s `.question-number` became a concept heading; `notch-menu.css` re-shaped the two audit controls that moved into the menu. `watch.py` here swapped the two stage-ladder tab assertions for `--dd-ladder-gap` + `--dd-callout-arrow-x` + the basic-mode rule, and added the two `.practice-notch-item.question-id-chip` guards.
- 2026-08-23: **The placement CTA became a button again, the results card got a body, and three standing notices were deleted.** 🔴 `#placement-start-btn` carried `.primary` AND `.placement-start-btn`: `.primary` (components.css) sets `width: 100%` with 12px/28px padding, `.placement-start-btn` here overrode the padding and NOT the width, so the CTA rendered as a card-wide 4px-tall strip — a two-file collision where neither rule looks wrong on its own. Both placement buttons dropped `.primary`; `.placement-start-btn` is a real 14px/10px-20px CTA now with `width: max-content` so it cannot inherit a stretch from a future parent, and `.placement-next-btn` is its neutral twin. `diagnostic.css` gained `.placement-cta` (an inline-flex wrapper that keeps the infotip ⓘ beside the button — `infotips.js` forces `place="after"` on a `<button>` anchor and inserts into its `parentNode`, so unwrapped the dot was a flex item on its own line), the `.placement-progress` bar for a test in progress, and the whole `.placement-overall` / `.placement-area` results block written by `practice/placement-results.js`. 🔴 `.placement-area--unprobed` is load-bearing, not decoration: the backend returns a theta for every area including ones the test never probed, and dimming those is what stops a propagated prior from reading as a measurement. **Deleted with their markup:** `.cold-start-badge` + `.cold-start-label` + `.cold-start-note` (question.css), `.practice-mode-intro` (misc.css), `.practice-mode-notice` (feedback.css), plus their entries in lessons.css, how-it-works.css and diagnostic.css's hosted-workspace rule. `watch.py` here swapped `.cold-start-badge` for `.question-number-row` in question.css's required list, dropped `.practice-mode-notice`, and now guards every class the results renderer mints.
- 2026-08-23: **The reading stands on the bar, and the notch grew a clock.** `.stage-ladder-callout` was a bordered box below the bar with a CSS triangle aimed at the fill edge; it is a tab on top of the bar now — `bottom: var(--dd-ladder-bar-h)`, `border-radius: 8px 8px 0 0`, `border-bottom: 0` — and `.stage-ladder-callout-arrow` is deleted with its two pseudo-element triangles. The meter's spare room moved from `padding-bottom` to `padding-top` (`justify-content: flex-end`), so the bar sits at the BOTTOM of the strip. 🔴 That move is what let `stage-ladder.js` drop `_pushNotchAside`: the reading no longer overhangs the split, so it can no longer collide with `#practice-notch`, and `--dd-notch-dx` is gone from `notch-menu.css` — if the reading is ever moved back under the bar, the collision comes back and the notch is WIDER now. `notch-menu.css` gained `.practice-notch-tab` (the frame both halves share) and `.practice-notch-clock` + its `--review` / `--low` tints; `.practice-notch-btn` lost its background and border to it. `watch.py` here asserts the callout's `bottom:` anchor, the absence of the arrow, and that `.practice-notch-clock` exists. The ⓘ that explained the ladder is gone from the reading (its copy is deleted from `infotips-registry.js`) — each rung's own name carries that copy inside the track, so the dot asked "what is this bar?" twice.
- 2026-08-23: **The rung names moved inside the bar, the reading moved under it, and the session controls moved to the seam.** `stage-ladder.css` was rebuilt around two stacked `.stage-ladder-track` layers (see Key Files) — the base one coloured with `--muted`/`--white`/`--muted-dim`, the clipped one with `--on-accent` — which is the same clipped-label trick `../xp.css` uses on the level pill. The bar grew to 30px to hold a name and a 13px ⓘ; `.stage-ladder-row`, `.stage-ladder-foot` and `.stage-ladder-caret` are deleted. 🔴 The ⓘs are `display: none` under 720px: a 13px target beside a name truncated to six characters is neither readable nor hittable, and the copy is still reachable from the concept's own ⓘ. New `notch-menu.css` (+ `.practice-split { position: relative }` in `layout.css`) draws the three-dot tab that now carries Pause & exit / End session; `timer.css`'s TEMPORARY `.session-status-row { display: none }` stays, and with it the warning that **the countdown still runs and still auto-submits**. `watch.py` here lists `notch-menu.css`.
- 2026-08-22: **The ladder is the top strip, and it is the only chrome up there.** `#stage-ladder` moved OUT of `.practice-container` to become a direct child of `#page-practice`, so it sits between the topbar and everything on the tab — the setup panel included — edge to edge. 🔴 The viewport arithmetic moved with it: `#page-practice` is the flex column at `calc(100vh - 56px - 40px)` and `.practice-container` is now `flex: 1 1 auto; min-height: 0`, because leaving the height on the container would push the split down by exactly the strip's height and cut the bottom off both panels. 🔴 **`#page-practice.hidden` had to be re-asserted in the same file** — an id (1,0,0) setting `display` beats `.hidden` (0,1,0) in components.css, and without it the practice tab renders under every other page. Same trap as the `#practice-submit-area` note above. The strip is two rows now, not four: one context line (concept · rung · what the rung asks) and the meter. The difficulty caption's own row is gone — `.stage-ladder-foot` is absolutely positioned at the right of the row the percentage rides in and says one clause, `47.4 mastery`. ⚠️ `timer.css` hides `.session-status-row` outright (counter, phase, countdown, Pause & exit, End session) at Seth's request while those controls move to a three-dot menu on the panel notch; **the markup and `timer.js` are untouched, so the countdown still runs and still auto-submits with nothing on screen counting down.** That rule is marked TEMPORARY in the file and goes when the menu ships.
- 2026-08-22: **The four rung bars became one cut bar.** `stage-ladder.css` drew four bordered `.stage-seg-bar`s side by side, one per rung; four meters read as four quantities when the learner is asking one. There is now a single 26px `.stage-ladder-bar` (rounded both ends, `overflow: hidden` so the fill's square leading edge is the only visible seam), one `.stage-ladder-fill`, `.stage-ladder-seam` chevrons injected by the script at each boundary, `.stage-ladder-callout` + `.stage-ladder-caret` above the fill edge, and the `.stage-seg-label`s moved underneath. 🔴 The narrow-rail override in `colab-edition.css` and the ≤720px rule here both STOPPED wrapping the labels to a 2×2 grid — that was right while each rung owned its own bar, and against one continuous track it would leave every label pointing at the wrong quarter. `watch.py` here now asserts `.stage-ladder-fill` (singular) and `.stage-ladder-seam`.
- 2026-08-20: **The ladder lost its fifth section and gained a chip.** `integrated` is a property of the problem on screen, not a rung — it rides on a `solo` record and flips from question to question, so it cannot be a section of a track that fills left to right (Solo read as finished and Integrated as current, and both snapped back on the next problem). `.stage-ladder-flag` is the chip that says it instead; the track is four sections and the narrow-rail override says so. See `../../practice/README.md` for the model this was read against.
- 2026-08-19: **Four readouts became one, and `difficulty-bar.css` + `concept-topbar.css` are deleted.** Entering practice through the knowledge graph put three bars on screen at once — a difficulty track, an accuracy track and a BKT competency track — in one visual language, for three quantities that are not comparable. `stage-ladder.css` is what replaced them (see Key Files): the ladder's sections, the rung named above the track, and the caption that difficulty is now a clause of. `misc.css` loses the `.competency-bar-*` block (that bar is headless — its mastery reading is a clause of the same caption), `timer.css`'s idle rule names `.stage-ladder`, and `colab-edition.css` drops the `.concept-topbar` / `.stage-dot` / `#ewma-accuracy` / `.difficulty-bar` overrides for a `.stage-ladder` block. `watch.py` here asserts the ladder's idle rule, that the track and its sections still exist, and that **no** stylesheet in this folder brings any of the four old selectors back. The 2026-08-18, 08-04 and 08-03 entries below describe rules that no longer exist and are kept as history.
- 2026-08-19: Added `notebook-view.css` for the new Notebooks tab (see Key Files). Linked from `index.html` alongside the other practice stylesheets; `practice/watch_notebook.py` asserts both the link and the file, since a missing stylesheet here fails as an unstyled 656-cell wall of text rather than as an error.
- 2026-08-18: **One difficulty bar, and it is `difficulty-bar.css`.** The page had two — `concept-topbar.css`'s `.concept-topbar-diff-bar` (96px, in the strip) and `bars.css`'s `.target-difficulty` card down in `.question-meta-row` — showing one quantity in two visual languages. The diff and estimate blocks are deleted from `concept-topbar.css` (the strip is now the concept name plus the rung dots), and `colab-edition.css` loses the overrides that sized them plus the `.question-meta-row` rule that placed the card; all it keeps for the rail is the bar's padding. The 2026-08-04 and 2026-08-03 entries below describe those deleted rules and are kept as history, not as current behaviour.
- 2026-08-07: **`misc.css` — `.topbar-auth[hidden] { display: none }`.** `app.js#updateAuthIndicators` hides the signed-in indicator with the `hidden` *attribute*, but `.topbar-auth { display: flex }` is a class rule and beats the UA's `[hidden] { display: none }` — so the green "signed in" dot had been showing to signed-out guests, which is the exact confusion the indicator was added to remove. Link tag `?v=4`. Found when the new ⓘ next to it turned up in a guest session; the same fix is why `styles/layout.css` had to pin `.tabs` with `margin-right: auto` (with the indicator genuinely gone, `space-between` slid the whole tab strip to the right edge).
- 2026-08-04: **The felt-difficulty buttons stack on the Colab edition, and `#feedback-help` is no longer hidden there.** Three buttons on one row is a 1600px page's layout; in a 290px side panel each gets ~85px and "Way too easy" wraps to three lines inside a box sized for one. `colab-edition.css` sets `.feedback-buttons { flex-direction: column }` with `flex: 0 0 auto` on the buttons — verified legible with no truncation at 290/340/410px. `#feedback-help` comes back (smaller type) because the rating now moves where the next problem is pitched and that consequence is written down nowhere else; three buttons with no stated effect read as a survey. `.next-problem-btn` shares the container but never shares a line — `showNextProblemButton` hides the three.
- 2026-08-04: **The scaffold rail staggers its labels, and the difficulty bar shows the move the last answer made.** `colab-edition.css` takes `.stage-dot-label` out of flow (absolutely positioned, `nth-child(even)` below the dot and the rest above) so four labels can overlap in width without colliding and the dots sit closer together; first/last anchor to their edge instead of centring, so nothing runs off the panel. Verified at 290/340/410/520px. `concept-topbar.css` gains `.concept-topbar-diff-delta` (green `is-gain` / red `is-loss`, animating width) and `.concept-topbar-diff-from`, a hairline at the value the answer started from — the fill stops at the LOWER of the two values so the delta span owns the contested stretch, which is what lets one rule serve both directions: on a gain it grows outwards, on a loss it collapses back. `colab-edition.css` widens that track to 10px and turns the fill blue, so the strip's two bars stop reading as the same quantity.
- 2026-08-03: **`.question-meta-row` is back on the Colab edition, and the 2026-07-31 entry below is now wrong about it.** It was hidden there because a verdict recorded an attempt and learned nothing from it — there was no old → new to draw, so the bar sat on whatever the previous question had left on it. `recordLocalEval` now returns the step it caused (offline: sampled either side of the engine call; backend: from `/submit-local-eval`, which had never been finalizing its attempts at all), so the rail draws difficulty climbing inside the current stage — green when an answer earned it, red when one cost it. `#ewma-accuracy` stays hidden on purpose, and that is not the same call: the concept strip three rows up already carries a correct/total and an interval for the concept, and a second percentage computed a different way reads as the panel disagreeing with itself in a 400px column. `concept-topbar.css` also stopped dropping every scaffold-rung label below 700px — every side panel is below 700px, so on that deploy the ladder had always rendered as four unlabelled circles; the narrow breakpoint now keeps the active rung's label and `colab-edition.css` restores all four.
- 2026-07-31: The Colab edition's review step. `colab-edition.css?v=3` stopped hiding `.solution-section` — it lives inside `#practice-feedback-area`, which stays hidden until a verdict, so answering now opens the reference solution under the buttons that recorded it. The rest of that panel is hidden there instead (`#feedback-help`, `#ewma-accuracy`, `.question-meta-row`, `#ai-explanation-section`, `#override-row`): all of it describes a grade the tutor did not compute on this deploy, stacked above the answer the learner just asked for.
  Also fixed three more live instances of the invariant below, found by auditing every `.hidden` element's computed display on a running page: `.override-row` and `.missed-fact-row` (`feedback.css?v=2`) and `.output-visual-canvas` (`editor.css?v=2`) all outranked the global `.hidden` by load order. These were **not** Colab-only — on the normal app every correct answer showed "Is this a mistake? I got it right", and "I missed one specific thing" showed after correct answers too. `question.css` had carried the matching `.question-visual-canvas.hidden` pair since it was written, which is why the same bug in `editor.css` looked deliberate.
- 2026-07-31: Added `colab-edition.css` (see Key Files). Also fixed a live instance of the invariant above: `layout.css`'s `.practice-right` outweighed `.hidden` by load order, so the panel ignored `classList.add("hidden")` and the editor stayed on screen through torch routing — `.practice-left.hidden, .practice-right.hidden` now re-assert it. Never visible on the normal deploy because no bank question carries a notebook path; found the moment the Colab edition started routing.
- 2026-07-30: `timer.css` hides `.concept-topbar` in the `session-idle` state. The topbar is a sibling of `.practice-split`, so the existing idle rule never reached it and the setup screen displayed the paused session's concept strip. `watch.py` now asserts the new rule.
- 2026-07-12: Folder created — `practice.css` split into layout/timer/question/feedback/editor/misc. `timer.css` rewritten from the old timed-mode toggle to the rigid session setup/status UI. Added `#practice-submit-area.hidden` specificity fix.
