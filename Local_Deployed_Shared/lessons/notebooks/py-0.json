{"id":"py-0","title":"Python you need first","topic":"Python","subtopic_key":"Python: Getting started","segments":{},"cells":[{"t":"md","id":"dd-lesson-py-0","role":"prose","src":"<!-- dd:dd-lesson-py-0 -->\n\n# Python you need first\n\n*Python · `py-0`*\n\nWork through this with the **Delta Drills** side panel open. It picks what you practise, sends you to the cell, and records how it went — you do not need to read this notebook in order.\n"},{"t":"code","id":"dd-setup","role":"setup","src":"# === Delta Drills ===\n# Which lesson this notebook is, for the side panel. Nothing to run.\nDD_LESSON_ID = \"py-0\"\n"},{"t":"code","id":"dd-checker","role":"checker","src":"#@title 🔧 Delta Drills checker — run me first { display-mode: \"form\" }\n# Delta Drills — problem checker. Generated; see scripts/colab_grader.py.\n#\n# `dd_check(<problem id>)` runs your `solve` against the same cases the tutor\n# grades with, and tells you which ones failed. It reads `solve` out of the\n# notebook, so define it (run your cell) before you check.\nimport base64\nimport json\nimport sys\nimport zlib\n\nimport numpy as np\n\n# Filled in by the generated cell that follows this source: {qid: {fn, cases}}.\n_DD_TESTS = {}\n\n# Where the ARENA digits fixture is fetched from, also filled in by that cell.\n_DD_FIXTURE_URL = \"\"\n_DD_FIXTURE_PATH = \"/delta_numbers.npy\"\n\n_DD_RTOL = 1e-5\n_DD_ATOL = 1e-6\n\n\ndef _dd_install_fixtures():\n    \"\"\"Make `np.load('/delta_numbers.npy')` work here the way it does in the app.\n\n    24 of the einops drills are written against the ARENA digits image, and the\n    bank refers to it by an absolute path the backend rewrites at grade time\n    (`code_runner.CODE_PREAMBLE`). Nothing rewrote it in a notebook, so those\n    problems could not run at all in Colab — not the checker, not the starter\n    code the learner was sent there to fill in. Downloaded on first use, so the\n    six notebooks that never touch it never pay for it.\n    \"\"\"\n    import os\n    import urllib.request\n\n    original = np.load\n    if getattr(original, \"_dd_patched\", False):\n        return\n\n    def _load(file, *args, **kwargs):\n        if str(file) == _DD_FIXTURE_PATH and not os.path.exists(_DD_FIXTURE_PATH):\n            if not _DD_FIXTURE_URL:\n                raise FileNotFoundError(\n                    \"This drill needs the ARENA digits fixture and no source was \"\n                    \"compiled into this notebook — regenerate it.\"\n                )\n            urllib.request.urlretrieve(_DD_FIXTURE_URL, _DD_FIXTURE_PATH)\n        return original(file, *args, **kwargs)\n\n    _load._dd_patched = True\n    np.load = _load\n\n\ndef _dd_load(blob):\n    \"\"\"The test payload, deflated and base64'd.\n\n    Not encryption and not pretending to be — it is one `zlib.decompress` away.\n    It is compressed because the payload for a 84-problem notebook is ~80 KB of\n    JSON, and out of sight because an expanded grader cell would otherwise sit\n    in the notebook spelling out the expected answer to every problem below it.\n    \"\"\"\n    return json.loads(zlib.decompress(base64.b64decode(blob)).decode(\"utf-8\"))\n\n\ndef _dd_preflight_torch():\n    \"\"\"Import torch once, here, where a failure can still be explained.\n\n    Every drill cell opens with `import torch as t`, so the learner meets a\n    broken torch install as a traceback through torch's own internals — the one\n    reported was `AttributeError: partially initialized module 'torch' has no\n    attribute 'fx'` from `torch/_export/utils.py`, raised while evaluating a\n    function's annotations. That message names neither the cause nor the cure,\n    and it is not even the real error: it is what a LATER import sees after an\n    earlier one died partway and left the half-built module in `sys.modules`.\n    Python does unwind a failed import normally, but a torch that was swapped\n    on disk under a running kernel (a `pip install` mid-session) or shadowed by\n    a stray `torch.py` gets far enough in to be cached before it falls over.\n\n    So: purge the wreckage and retry ONCE, which is the whole fix whenever the\n    first failure was transient, and report what actually broke when it is not.\n    Importing torch in this cell rather than lazily is safe now in a way the\n    `_dd_tensor` comment below still guards against for the per-comparison\n    path — the bank is 448/448 torch and every notebook imports it a few cells\n    down, so there is no numpy-only notebook left to charge for it.\n\n    Never raises: a checker that refuses to load over this would take the\n    lesson down with the runtime.\n    \"\"\"\n\n    def _purge():\n        # Submodules too, and that is the whole point. Python drops only the\n        # module that raised, so `torch` goes and a `torch._export` imported\n        # seconds earlier STAYS — and the next `import torch` re-runs\n        # `torch/__init__.py` straight back into that stale submodule, which\n        # reaches for a `torch.fx` the half-built parent has not bound yet.\n        # Leaving one behind reproduces the bug instead of clearing it.\n        for name in [n for n in sys.modules if n == \"torch\" or n.startswith(\"torch.\")]:\n            del sys.modules[name]\n\n    def _usable(mod):\n        # `import torch` does NOT re-execute a module already in sys.modules,\n        # so a corpse left by a failed import is imported \"successfully\" and\n        # the error surfaces later, from the learner's own cell. Judge the\n        # object, not the statement: a torch that finished has both of these.\n        return hasattr(mod, \"fx\") and hasattr(mod, \"__version__\")\n\n    cached = sys.modules.get(\"torch\")\n    if cached is not None and not _usable(cached):\n        _purge()\n\n    for attempt in (1, 2):\n        try:\n            import torch\n            if not _usable(torch):\n                raise ImportError(\n                    \"torch imported but is only partially initialised \"\n                    \"(no .fx) — an earlier import in this session died partway\"\n                )\n            return True\n        except Exception as exc:\n            if attempt == 1:\n                _purge()\n                continue\n            print(\n                \"⚠️  This runtime cannot import PyTorch, so no drill in this \"\n                \"notebook will run.\\n\"\n                \"    %s: %s\\n\"\n                \"    Fix: Runtime ▸ Disconnect and delete runtime, then reopen \"\n                \"this notebook and run\\n\"\n                \"    this cell first. If it comes back, check for a file named \"\n                \"torch.py in /content,\\n\"\n                \"    and re-run any pip install BEFORE anything imports torch.\"\n                % (type(exc).__name__, exc)\n            )\n    return False\n\n\ndef _dd_tensor(value):\n    # torch only if something already imported it. numpy-only notebooks must\n    # not pay a torch import to compare two lists of ints.\n    torch = sys.modules.get(\"torch\")\n    return torch is not None and isinstance(value, torch.Tensor)\n\n\ndef _dd_close(a, b):\n    \"\"\"Tolerance compare, but ONLY when a float or complex is involved.\n\n    torch defaults to float32 where numpy defaults to float64 and honest\n    answers differ in reduction order, so exact equality fails correct work.\n    Integer and boolean results stay exact — an index answer (argmax, nonzero,\n    searchsorted) must never be fudged by a tolerance. Returns None to mean\n    \"not a float comparison, use exact equality\".\n    \"\"\"\n    try:\n        floaty = any(\n            np.issubdtype(x.dtype, np.floating) or np.issubdtype(x.dtype, np.complexfloating)\n            for x in (a, b)\n        )\n        if not floaty:\n            return None\n        if a.shape != b.shape:\n            return False\n        return bool(np.allclose(a, b, rtol=_DD_RTOL, atol=_DD_ATOL, equal_nan=True))\n    except Exception:\n        return None\n\n\ndef _dd_array_equal(a, b):\n    close = _dd_close(a, b)\n    if close is not None:\n        return close\n    return bool(np.array_equal(a, b))\n\n\ndef _dd_equal(a, b):\n    if _dd_tensor(a) or _dd_tensor(b):\n        try:\n            a2 = a.detach().cpu().numpy() if _dd_tensor(a) else np.asarray(a)\n            b2 = b.detach().cpu().numpy() if _dd_tensor(b) else np.asarray(b)\n            return _dd_array_equal(a2, b2)\n        except Exception:\n            # dtypes numpy cannot hold (bfloat16, conj views): equal tensors\n            # must not grade as unequal — ask torch itself.\n            torch = sys.modules.get(\"torch\")\n            if torch is not None and isinstance(a, torch.Tensor) and isinstance(b, torch.Tensor):\n                try:\n                    return bool(torch.equal(a.detach().cpu().resolve_conj(),\n                                            b.detach().cpu().resolve_conj()))\n                except Exception:\n                    return False\n            return False\n    if isinstance(a, np.ndarray) or isinstance(b, np.ndarray):\n        return _dd_array_equal(np.asarray(a), np.asarray(b))\n    if isinstance(a, (list, tuple)) and isinstance(b, (list, tuple)):\n        if len(a) != len(b):\n            return False\n        return all(_dd_equal(x, y) for x, y in zip(a, b))\n    close = _dd_close(np.asarray(a), np.asarray(b))\n    if close is not None:\n        return close\n    return bool(a == b)\n\n\ndef _dd_seed():\n    # The same seed before the actual-side and the expected-side setup runs, for\n    # BOTH rngs: setup executes twice, so an unseeded torch.rand in a fixture\n    # would hand the two sides different data and fail an honest answer.\n    np.random.seed(0)\n    torch = sys.modules.get(\"torch\")\n    if torch is not None:\n        torch.manual_seed(0)\n\n\ndef _dd_show(value, limit=320):\n    try:\n        text = repr(value)\n    except Exception as exc:\n        text = \"<unrepresentable: %s>\" % type(exc).__name__\n    text = \" \".join(text.split()) if len(text) > limit else text\n    if len(text) > limit:\n        text = text[: limit - 1] + \"…\"\n    return text\n\n\ndef dd_check(question_id, verbose=True):\n    \"\"\"Grade the `solve` you just defined against this problem's cases.\n\n    Returns True when every case passes. Prints which ones did not, with the\n    fixture, what was expected and what came back — a failing grade should be\n    evidence you can act on, not a verdict.\n    \"\"\"\n    qid = str(question_id)\n    entry = _DD_TESTS.get(qid)\n    if entry is None:\n        print(\"No checker for problem %s in this notebook.\" % qid)\n        return False\n\n    # The learner's namespace, not this function's: `solve` lives in the cell\n    # they ran, and in Colab that is the caller's globals.\n    try:\n        env = sys._getframe(1).f_globals\n    except Exception:\n        env = globals()\n\n    fn_name = entry.get(\"fn\") or \"solve\"\n    if fn_name not in env:\n        print(\"❌ `%s` is not defined yet — run your solution cell first.\" % fn_name)\n        return False\n\n    cases = entry.get(\"cases\") or []\n    failures = []\n    for i, case in enumerate(cases, 1):\n        # A fresh copy per case: fixtures are exec'd, and exec'ing them into the\n        # notebook's own globals would quietly overwrite whatever the learner\n        # named `x` two cells ago.\n        ns = dict(env)\n        try:\n            if case.get(\"setup_code\"):\n                _dd_seed()\n                exec(case[\"setup_code\"], ns)\n            actual = eval(case[\"call\"], ns)\n            expected_setup = case.get(\"expected_setup_code\") or case.get(\"setup_code\")\n            if expected_setup:\n                _dd_seed()\n                exec(expected_setup, ns)\n            expected = eval(case[\"expected_expr\"], ns)\n            if not _dd_equal(actual, expected):\n                failures.append((i, case, _dd_show(expected), _dd_show(actual), \"\"))\n        except Exception as exc:\n            failures.append((i, case, \"\", \"\", \"%s: %s\" % (type(exc).__name__, exc)))\n\n    total = len(cases)\n    if not failures:\n        print(\"✅ Problem %s — %d/%d cases passed.\" % (qid, total, total))\n        return True\n\n    print(\"❌ Problem %s — %d of %d cases failed.\" % (qid, len(failures), total))\n    if verbose:\n        for i, case, expected, actual, error in failures:\n            print(\"\\n  case %d\" % i)\n            if case.get(\"setup_code\"):\n                for line in case[\"setup_code\"].strip().splitlines():\n                    print(\"    given     %s\" % line)\n            print(\"    called    %s\" % _dd_show_source(case.get(\"call\", \"\")))\n            if error:\n                print(\"    raised    %s\" % error)\n            else:\n                print(\"    expected  %s\" % expected)\n                print(\"    you got   %s\" % actual)\n    return False\n\n\ndef _dd_show_source(text, limit=160):\n    text = \" \".join(str(text).split())\n    return text if len(text) <= limit else text[: limit - 1] + \"…\"\n\n_DD_FIXTURE_URL = \"https://raw.githubusercontent.com/AkiraTheSquid/arena-book-colab/main/ARENA_5.0/ch-1-foundations/numbers.npy\"\n_dd_preflight_torch()\n_dd_install_fixtures()\n_DD_TESTS = _dd_load(\n    \"eNrdXd1v2zgS/1d8eZENWAJJiZJ1b922h1vg9un24QDBKJLU3Q2axkWTHrpX9H8/funDiYee8VBeYB82DZbKT9RwvmdIfr/S\"\n    \"9ebq74vvVx8ezD9Xj/v7/+6u1our2+vH3aP5P933q8fd09fP727373f2CT94fz88vVTrRbmy/3v37fPu9mn3/p355Ysd11c/\"\n    \"1ovTAFKsF3l1HKLGQRgEcRxA4ACa9aI5DiCrqx9bg2Eo1TIpJdcLdfwllohyhZtqdp2tF9lNBiCZETN8na3QlGsBpNYRFYWS\"\n    \"m0/TAIw2q2u/zRGxkVx2K9eLCqSjXSwUyxlygywj8ZQDvxrJ+qow1PFrf5T7CzHQjc180NcipypYH5prlngaOh3/+6bQPYWU\"\n    \"mE2RuSG31kxt5IZoYgWwhh/DQ0nHaRAZh2EjXIUYhFVJJkmz3+8APfX49AU3cWDGdw9PTNb5cL+/RmJ4zX0cxvzl/W4gmGIS\"\n    \"DDCDy8ppvc2KJatU7oOMVW7Ewf1XoU0WbK6uvb0KP92DPTFLrqk4/s6WRUSkxoIcGlmj5fU4gCrUqPIqtnzu/gBXxoz9zayK\"\n    \"kVTsMoNQRJz9RxBp/5GI9dt+8dsehHOjI2Igq2aSVcOOnjEjEiuACnaqcgqOgA2bxaGZEAhI2NFyNB66EQk8PchcGdIorPZR\"\n    \"sXnrQlhKFgRatm1Eva7Q8QZk2O1QNVKRa4IFK7ACvUbk3+c1S4tKhN8nNnxphf2UfnS9MB4HheEiSmwYXy+cE4JnPVDDujHK\"\n    \"HK3gZ9+yiFowoz1gIDQ3BIGUmXnJeuH8qBXTp1s6etLAImtvjAINS2C+MAR04q+ZTRBJlGPeQJzpMzU9EWUCIp4K+9CktAKT\"\n    \"/WF//A+kZxgOwocmKmwuBCkMNIpHR1M1frR3hOpZyWtoW5NokIC3QtisYSRNcivsDxnzLeppPN3wUxQ1xFmPXz9lhBARpmZA\"\n    \"EgRLUqaBkjFqBiyn+3t6snPXsOCbcFvWl423Y056a5ZWrpheU5/mUVbKC1GrCSnnMu7Ogfq4+/yUrZLZ5aX3yM6ChdbKuVDn\"\n    \"AMLOSHD0noMGx76aJ7uRGV8jY848c25Uxl2nzPhPSJBfv3zdASA3+/19NlCNG5pnlQJmq5ExSSYAgA3y73MNAJRIACmgKUgT\"\n    \"DAWBLtm5W8MeEKkKLq0EGiGX4DTyEo0ioYkoMdZYSjWTSJaLu6fdp8eMpaEyQUKBsrdZrkg4sJIwIwNSIGA5W73AaQijTK1K\"\n    \"YWr6f1zfP1ooqyi5mc0eizAt2F6ET/TKd7DMJddQqKJh6RvjLwCyg3W5C8BpzZEIuoCrj4FIbLvQZGALgF0XJJ803gU/huJY\"\n    \"BQljVW+kXEeYUAb6+WE+gX7cSK8sWudZsrKNygZMClrsCutG+6kAKDm2pcSitEUL5i8D4ZrZHJJlZcIQP8wzts61FWiYvMki\"\n    \"yRc/zHVUlmbIYPkHelJyozmQABTRg5wNgszlko8hxImPCf5wzdd7cB2isfKYNUkYUBRELpSgXOS2/GN+OET/HA6zFNDqLs3Q\"\n    \"emF+OEz/XE/ihq8a4QQYvogl4VyFQie/QN/D5sVyLE4FKVfXpzBmZ8tZKzPeXfKRI7uAEhw59w+lhLJINzvHeg/7EzMMiIHC\"\n    \"FT/Iq+EyVe0yezjZMjwB2jH7ZdWKH/A5BibwaSYKBWIJlwYbk7MyRWrBfKdCrR/XmzyHYY32hbO+Z0wvt04q1Nl3nF/ruUx8\"\n    \"kDcSOZpkshsx1+dMLK9QMwtuU8vWATaXWsYK4DqjsFgyR1Rm4d2Qrckqgi9qwZSAHQBn+Ed3tBJ83QpymK8skpjCBFcRNEVE\"\n    \"i6lrV/wgKYKoyvbNgv3sAnHZ2cnTquwMDRnptx0lj+pz4KZJobctNGvEPJ/RXLETSZAj63JpxOwc6NCeAWZifp0OrQHd22dg\"\n    \"PkDYiFmr5F0Y3hKcMUQnQnfwEAE8XorvwjABUIZNEK7NBCDB+EgADqSX8zXRSHxcdaqJZhjGEjiPdHvk2DLwgQ5AuCZBS1Qz\"\n    \"FYJ72XFl0IxfBHZCSMWDk+8h7U4FFNivDdTVaXQF3BrbOX1hpKSzD223eC4OkhuR7S5Itv93SxcQ8+PW/ngPSnrXP2pf0z9M\"\n    \"eVWvKuzXg4vdBY3Sk6nQ/SvCMnHjlEFpR9rAkR/UbSOdX1gMvYW1HBpkWJp422EgYTMzCQezibc0ICmXK3ybjjWcII41qisC\"\n    \"SV2HiNhGG0jE6oA1uVlyn3WEBMMpDbPK93eP6OroEkQjI+k1iKXJYAdaB8Q9eGr6jkDvNgUfD/qYWxjopIWKyAWh1mGXp2PP\"\n    \"aCKHXeUaSOsopiernrH92a8g2dUI/nBS0Fib5VmA0U04LyEDsedtkz5DE8cdjWGYEPIcyDqknQ8eokcokVa/6SMHEQo36O4m\"\n    \"8d8gXxELjLaFQZM4zbRN4acEr5AfTkFOYLwAFuhdXs7tmKwMIXsTdUI6IpYNiGPcbiF90ExBncSF/g/Bd7x40r7w5Z8/S1nr\"\n    \"iwrFMiwXTdFa+YjxgDwD8Shb29939vcPMItbOVRnvPAwXovQ5xA8rBJXlLzMBzlJ42esvPMj+c6Plzb2pLQTMY3xeKp504Re\"\n    \"n/dyYX63f1Cu0mT1uoGNQuRPAkflI11cZYXhIC9JfZXdJwE28pmXtPYV4aED8LBI3PwM9OYgXKHtlLCpLJIAGyB9JkxyU1Zn\"\n    \"TjGSUxsQ++TamB/QF0ixlAlSA7Q9PPFES9pcy9TvaTQ33RKaj93GGnhFDUWXfkfGwHZ9vzLyy/qO6Uj97thLXLM2KfHt9Egz\"\n    \"0yv6r3CbuGUhziJXWDluQifGumGeaLaLWGrLcp6yaDQREQYi1GGWBib3CBuomyp9EzsD6GjRFU9vA+5Sp8nL2TaaWCafs03E\"\n    \"RRJ1YAFab/gq3/aH2+NMIM6SIo1+NcNIoAZAaLAyAnuzYiBcMzvhylSEu7kU4cZK2BbcL+B1QK3Skc+8DtRq/omkGm3MaJGI\"\n    \"kiI/ZGNqY0Cdq9xCgPX0NIaanXsJ7+RyxpZ1sGM6Fg9xBpwRrwfScYNDW12Q8MpTCgywC0CpLTT8mUwkBExzTSPsWidJK421\"\n    \"c9a2t84Yyi3r0MzuRW2qi+RBzRNYWMuXFs1xpk1fRL522FBc1xehbnk56iIS4OUcGWu3UoGozZzVXH9WLi1pONbBIJM05mdo\"\n    \"wC6MjkbZREBXHW9WsVO2mpdpTfaB16c9j5JgYplMjPEizjFY64U+abPaC2RwVkxzE072TpN08XRGN/J1vpO6ghnlYL+U3mwS\"\n    \"a9jYwVFoK/YcEj44m6K6Y+lmTZna8yKQm6CKs67ezM260ukHlYzxpp7QKpX3aZPlNUE8RKRVaUyTeuXAPv3zGeN5L6WOlZc1\"\n    \"uizWbiMUaSnlPGfub+DCHbH1t/PpgL7DJ0LsMfPLPiH0tD0jx42RcFHPEB/fnCETsIFbbvy5dIG86iJu79B5R9gTHPGBfZZO\"\n    \"U4TikN5bdLvcNS1DMTawRanRP+BYc9xS3JQXWQ1KoGsBddBPYMsAIepFFe0peAehSR9NI8Jp9gllSM5XtLN4Jh8U1wqEHT+d\"\n    \"bcSFJ+iOcRuO5UFzRShoKxcuwZP99nxHUcM+5SxarPDFBlrS3x0Nuooc8We73ggtRqNhBLuXem+HOFERqaQspW8qPNzx0qQs\"\n    \"S0ROfncPEQtDkn/C7wuNLdOc9TtJEG/htpTNNEHMPkqjCyrFilVQKhGHEG9HrSHdRg7FxQOFnJKfoAtMUk3RaW0L6zSf+8Xa\"\n    \"6bjneTtuU2zVvDFPyWu0EOyOR0kIY0IjU7xwpFv2DSSFrKRuQaVgxwmnDQvoShD0cfpCwCfsSixMLgtVViCOHe4lnr031gb2\"\n    \"Ei4JSXUZrtPMNF04dtp6GdAaDmfZsTe1nqBZuHyC2c9EUYv2pJy8jWcP8pa0Z0oVVdRdM+OD2eH34PviGlQWGxQjjxe7Ld43\"\n    \"kDAxO38ivMSi9WY/Fmc/8yEGwpapCNs7j0dfXvo78rAfNGJG9j6T1yw6Qyqt42jDUwOdqwQaoYUZuMF7fJqpSN1p8VvWKZF+\"\n    \"725VCIQq1fMSDn0QMmxA0AgRBSQLTdOdGCuUpD8XjtC/3t0/3T28+/D14fbpbv/wbv/l3afd0+/79xkxHXCBVyBTkcnep3nv\"\n    \"CAuYaAO2gmNaWdQNvhTWRkNaiijBlSOaNIFhqxzkQLfVzKWimnTJTJdHlLC7vsQfyZZEHfgbTNRwdSiStMJ9UQM3Ni6VdVDE\"\n    \"pE9Xt3pmH43oREAkMQMU2rq7rSXoN/bUzRN7a7fTxqlBIWzmJfH0pKLJo0kaF6fQ46M8A9Fvf0cuZ+Cf+ARVSPiGR3vKt5el\"\n    \"fP92nrHpfFXjwnSenBID72090hh3e9grGCjfilQZHPdLrbjlmD4FUwip2bWn+HnplElJq5ALqRBHxbdyXp/apido+xF8oTNi\"\n    \"Eyl7n2UinFxFPrF6XoNgJ2db+ESoCt/T4B/lopy4YY5wU1v0GsB83OXTlhftNsEmbQ0qWMFVgtimyu1QxZSVh2sN2mpmi0XZ\"\n    \"C5mkt8+xU+ywpZzkOZ9oSw/ll+sxHGM7uGVR6TrSKWltgXmmZu/adaeYCIrL7wsCsQNgZeEcYfMY+wzO/l6CZnLXdDvTRYD1\"\n    \"n3lVfI4tboCn+leTMJbrgsJHD9T4m7N5dILvQ8+lIJzkA9C7Hm4Cq4WY5xBM7DQ1nJnQl2BJFSGTFBM6ydm4ytKAcDtsxOGg\"\n    \"XA0LHryaEzouomd0lXawHHVXLbimtoVqy4LHKti/B5ODlayULHWtyqYUrUbW5+tC6VPZ6VqwL2uAb63858/Ymy9/ufu2e794\"\n    \"bd4KYf3y83/evlm8fvXvtxnvprkM+/fXN7dSQYf5Z69+em1HBzLWs5FxaelIOCEWhCHcH5y92d0/gadIZ2/e/uvXVxk+Asmu\"\n    \"F/CZzq8WP2WTg95qwb7buSj53dc5jOKK8NjlAG/co0Rw8BHvQ+r3x/8BZjpoxg==\"\n)\nprint(\"Delta Drills checker ready — 108 problems. Run dd_check(<problem number>) under any of them.\")\n"},{"t":"md","id":"dd-kp-python-values-and-names","role":"prose","src":"<!-- dd:dd-kp-python-values-and-names -->\n\n## Values and names — what = really does\n\n`python.values-and-names`\n"},{"t":"md","id":"python-values-and-names-segment-concept","role":"prose","src":"Everything in this course is built out of two moves: producing a **value**, and\ngiving that value a **name** so you can talk about it again later.\n\nA value is a thing — the number `5`, the text `\"hi\"`, a list of numbers. An\n**expression** is anything that produces one: `2 + 3` is an expression whose\nvalue is `5`.\n\n`print(...)` shows you a value. It is how a program says something out loud, and\nalmost every cell below ends with one.\n"},{"t":"code","id":"python-values-and-names-segment-concept-code","role":"code","src":"print(2 + 3)\nprint(\"two\" + \"three\")\n"},{"t":"md","id":"python-values-and-names-segment-concept-1","role":"prose","src":"\n\nThe `=` sign is **not** a claim that two things are equal. It is an instruction:\n*work out the value on the right, then attach the name on the left to it.* The\nname is a label, and the value is what the label is stuck to.\n"},{"t":"code","id":"python-values-and-names-segment-concept-code-1","role":"code","src":"total = 2 + 3\nprint(total)\n"},{"t":"md","id":"python-values-and-names-segment-concept-2","role":"prose","src":"\n\nRead `total = 2 + 3` as \"let `total` be 5 from now on\". Once a name exists you\ncan use it anywhere the value would work, including in building the next one:\n"},{"t":"code","id":"python-values-and-names-segment-concept-code-2","role":"code","src":"price = 4\nquantity = 3\nsubtotal = price * quantity\nprint(subtotal)\n"},{"t":"md","id":"python-values-and-names-segment-concept-3","role":"prose","src":"\n\nBecause the right-hand side is worked out **first**, a name can be built out of\nits own old value. This trips people up until they read it in the right order:\n"},{"t":"code","id":"python-values-and-names-segment-concept-code-3","role":"code","src":"n = 10\nn = n + 3\nprint(n)\n"},{"t":"md","id":"python-values-and-names-segment-concept-4","role":"prose","src":"\n\nThe second line does not say \"n equals n plus 3\", which would be nonsense. It\nsays: take what `n` is now (10), add 3 (13), then re-attach the name `n` to\nthat. The old value is simply let go.\n\nA name holds one value at a time, but you can hand several back together by\nputting them in **parentheses**, separated by commas. That is a *tuple*, and it\nis how a piece of code gives more than one answer at once.\n"},{"t":"code","id":"python-values-and-names-segment-concept-code-4","role":"code","src":"pair = (1, 2)\nprint(pair)\n"},{"t":"md","id":"python-values-and-names-segment-concept-5","role":"prose","src":"\n\nEvery value has a **kind** — a whole number, a decimal, a piece of text, a\ntuple. `type(...)` reports it, and the answer is about the value, not about the\nname stuck to it.\n"},{"t":"code","id":"python-values-and-names-segment-concept-code-5","role":"code","src":"count = 5\nlabel = \"five\"\nprint(type(count))\nprint(type(label))\n"},{"t":"md","id":"python-values-and-names-segment-concept-6","role":"prose","src":"\n\nRe-attaching a name to a different value can change the kind as well as the\nvalue, because the kind was never the name's to begin with.\n"},{"t":"code","id":"python-values-and-names-segment-concept-code-6","role":"code","src":"n = 5\nprint(type(n))\nn = (5, 5)\nprint(type(n))\n"},{"t":"md","id":"python-values-and-names-segment-watch","role":"prose","src":"> **Watch out.** - **`=` is not a comparison** — `x = 5` sets a name; `x == 5` asks a question and\n  produces `True` or `False`. Almost every early error message about assignment\n  is one of these two written where the other belonged.\n- **A quoted name is text, not the value** — `total` is the name; `\"total\"` is a\n  five-letter string that has nothing to do with it.\n- **Re-assigning does not change the old value** — it moves the label. Anything\n  else already holding that value keeps it.\n"},{"t":"md","id":"dd-q568-worked","role":"prose","src":"Two values, named, then combined and handed back as a pair.\n","q":568},{"t":"code","id":"dd-q568-worked-code","role":"code","src":"a = 7\nb = 2\n\ntotal = a + b\ndifference = a - b\n\nprint(\"total is\", total)\nprint(\"difference is\", difference)\nprint(\"both together:\", (total, difference))\n","q":568},{"t":"md","id":"dd-q568-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `a` and `b` are names for the two inputs, so the arithmetic below reads as\n   words rather than as bare numbers.\n2. `total` and `difference` name the two results. Naming a result is what lets\n   the next line use it without recomputing it.\n3. The final `(total, difference)` is a tuple: one value that carries both\n   answers, which is exactly what a function returns when it has two things to\n   say.\n","q":568},{"t":"md","id":"dd-q568","role":"problem","src":"<!-- dd:dd-q568 -->\n\n### Problem 568 · faded — your turn\n\nGive the sum a name, then hand the name back.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n5\n```\n","q":568},{"t":"code","id":"dd-q568-code","role":"code","src":"def solve(a, b):\n    \"\"\"Store the sum under a name, then return the name.\"\"\"\n    _____ = a + b\n    return total\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (2, 3)\nprint(solve(*example))\n","q":568},{"t":"code","id":"dd-q568-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(568)\n","q":568},{"t":"code","id":"dd-q568-solution","role":"solution","src":"#@title 💡 Solution — Problem 568\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(a, b):\n    \"\"\"Store the sum under a name, then return the name.\"\"\"\n    total = a + b\n    return total\n\n\nexample = (2, 3)\nprint(solve(*example))\n","q":568},{"t":"md","id":"dd-q569-worked","role":"prose","src":"Two values, named, then combined and handed back as a pair.\n","q":569},{"t":"code","id":"dd-q569-worked-code","role":"code","src":"a = 7\nb = 2\n\ntotal = a + b\ndifference = a - b\n\nprint(\"total is\", total)\nprint(\"difference is\", difference)\nprint(\"both together:\", (total, difference))\n","q":569},{"t":"md","id":"dd-q569-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `a` and `b` are names for the two inputs, so the arithmetic below reads as\n   words rather than as bare numbers.\n2. `total` and `difference` name the two results. Naming a result is what lets\n   the next line use it without recomputing it.\n3. The final `(total, difference)` is a tuple: one value that carries both\n   answers, which is exactly what a function returns when it has two things to\n   say.\n","q":569},{"t":"md","id":"dd-q569","role":"problem","src":"<!-- dd:dd-q569 -->\n\n### Problem 569 · faded — your turn\n\nSwap two values by naming each one first.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(2, 1)\n```\n","q":569},{"t":"code","id":"dd-q569-code","role":"code","src":"def solve(x, y):\n    \"\"\"Swap, via two names.\"\"\"\n    first = _____\n    second = x\n    return (first, second)\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (1, 2)\nprint(solve(*example))\n","q":569},{"t":"code","id":"dd-q569-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(569)\n","q":569},{"t":"code","id":"dd-q569-solution","role":"solution","src":"#@title 💡 Solution — Problem 569\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(x, y):\n    \"\"\"Swap, via two names.\"\"\"\n    first = y\n    second = x\n    return (first, second)\n\n\nexample = (1, 2)\nprint(solve(*example))\n","q":569},{"t":"md","id":"dd-q718-worked","role":"prose","src":"Two values, named, then combined and handed back as a pair.\n","q":718},{"t":"code","id":"dd-q718-worked-code","role":"code","src":"a = 7\nb = 2\n\ntotal = a + b\ndifference = a - b\n\nprint(\"total is\", total)\nprint(\"difference is\", difference)\nprint(\"both together:\", (total, difference))\n","q":718},{"t":"md","id":"dd-q718-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `a` and `b` are names for the two inputs, so the arithmetic below reads as\n   words rather than as bare numbers.\n2. `total` and `difference` name the two results. Naming a result is what lets\n   the next line use it without recomputing it.\n3. The final `(total, difference)` is a tuple: one value that carries both\n   answers, which is exactly what a function returns when it has two things to\n   say.\n","q":718},{"t":"md","id":"dd-q718","role":"problem","src":"<!-- dd:dd-q718 -->\n\n### Problem 718 · faded — your turn\n\nName the first result, then build the second out of that name.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n14\n```\n","q":718},{"t":"code","id":"dd-q718-code","role":"code","src":"def solve(price, quantity, fee):\n    \"\"\"Name the first result, then read that name to build the second.\"\"\"\n    subtotal = price * quantity\n    total = _____ + fee\n    return total\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (3, 4, 2)\nprint(solve(*example))\n","q":718},{"t":"code","id":"dd-q718-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(718)\n","q":718},{"t":"code","id":"dd-q718-solution","role":"solution","src":"#@title 💡 Solution — Problem 718\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(price, quantity, fee):\n    \"\"\"Name the first result, then read that name to build the second.\"\"\"\n    subtotal = price * quantity\n    total = subtotal + fee\n    return total\n\n\nexample = (3, 4, 2)\nprint(solve(*example))\n","q":718},{"t":"md","id":"dd-q719-worked","role":"prose","src":"Two values, named, then combined and handed back as a pair.\n","q":719},{"t":"code","id":"dd-q719-worked-code","role":"code","src":"a = 7\nb = 2\n\ntotal = a + b\ndifference = a - b\n\nprint(\"total is\", total)\nprint(\"difference is\", difference)\nprint(\"both together:\", (total, difference))\n","q":719},{"t":"md","id":"dd-q719-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `a` and `b` are names for the two inputs, so the arithmetic below reads as\n   words rather than as bare numbers.\n2. `total` and `difference` name the two results. Naming a result is what lets\n   the next line use it without recomputing it.\n3. The final `(total, difference)` is a tuple: one value that carries both\n   answers, which is exactly what a function returns when it has two things to\n   say.\n","q":719},{"t":"md","id":"dd-q719","role":"problem","src":"<!-- dd:dd-q719 -->\n\n### Problem 719 · faded — your turn\n\nBuild the new value out of the name's own old value.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n15\n```\n","q":719},{"t":"code","id":"dd-q719-code","role":"code","src":"def solve(n):\n    \"\"\"The right-hand side runs first, reading the OLD value.\"\"\"\n    n = _____ + 5\n    return n\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (10,)\nprint(solve(*example))\n","q":719},{"t":"code","id":"dd-q719-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(719)\n","q":719},{"t":"code","id":"dd-q719-solution","role":"solution","src":"#@title 💡 Solution — Problem 719\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(n):\n    \"\"\"The right-hand side runs first, reading the OLD value.\"\"\"\n    n = n + 5\n    return n\n\n\nexample = (10,)\nprint(solve(*example))\n","q":719},{"t":"md","id":"dd-q720-worked","role":"prose","src":"Two values, named, then combined and handed back as a pair.\n","q":720},{"t":"code","id":"dd-q720-worked-code","role":"code","src":"a = 7\nb = 2\n\ntotal = a + b\ndifference = a - b\n\nprint(\"total is\", total)\nprint(\"difference is\", difference)\nprint(\"both together:\", (total, difference))\n","q":720},{"t":"md","id":"dd-q720-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `a` and `b` are names for the two inputs, so the arithmetic below reads as\n   words rather than as bare numbers.\n2. `total` and `difference` name the two results. Naming a result is what lets\n   the next line use it without recomputing it.\n3. The final `(total, difference)` is a tuple: one value that carries both\n   answers, which is exactly what a function returns when it has two things to\n   say.\n","q":720},{"t":"md","id":"dd-q720","role":"problem","src":"<!-- dd:dd-q720 -->\n\n### Problem 720 · faded — your turn\n\nPut both inputs and their sum into one tuple.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(2, 3, 5)\n```\n","q":720},{"t":"code","id":"dd-q720-code","role":"code","src":"def solve(first, second):\n    \"\"\"Three values, one tuple.\"\"\"\n    s = first + second\n    p = (_____, _____, s)\n    return p\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (2, 3)\nprint(solve(*example))\n","q":720},{"t":"code","id":"dd-q720-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(720)\n","q":720},{"t":"code","id":"dd-q720-solution","role":"solution","src":"#@title 💡 Solution — Problem 720\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(first, second):\n    \"\"\"Three values, one tuple.\"\"\"\n    s = first + second\n    p = (first, second, s)\n    return p\n\n\nexample = (2, 3)\nprint(solve(*example))\n","q":720},{"t":"md","id":"dd-q721-worked","role":"prose","src":"Two values, named, then combined and handed back as a pair.\n","q":721},{"t":"code","id":"dd-q721-worked-code","role":"code","src":"a = 7\nb = 2\n\ntotal = a + b\ndifference = a - b\n\nprint(\"total is\", total)\nprint(\"difference is\", difference)\nprint(\"both together:\", (total, difference))\n","q":721},{"t":"md","id":"dd-q721-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `a` and `b` are names for the two inputs, so the arithmetic below reads as\n   words rather than as bare numbers.\n2. `total` and `difference` name the two results. Naming a result is what lets\n   the next line use it without recomputing it.\n3. The final `(total, difference)` is a tuple: one value that carries both\n   answers, which is exactly what a function returns when it has two things to\n   say.\n","q":721},{"t":"md","id":"dd-q721","role":"problem","src":"<!-- dd:dd-q721 -->\n\n### Problem 721 · faded — your turn\n\nAsk what KIND of value arrived, and hand the answer back.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n<class 'str'>\n```\n","q":721},{"t":"code","id":"dd-q721-code","role":"code","src":"def solve(value):\n    \"\"\"type(...) reports the kind of the VALUE.\"\"\"\n    kind = _____(value)\n    return kind\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ('hi',)\nprint(solve(*example))\n","q":721},{"t":"code","id":"dd-q721-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(721)\n","q":721},{"t":"code","id":"dd-q721-solution","role":"solution","src":"#@title 💡 Solution — Problem 721\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(value):\n    \"\"\"type(...) reports the kind of the VALUE.\"\"\"\n    kind = type(value)\n    return kind\n\n\nexample = ('hi',)\nprint(solve(*example))\n","q":721},{"t":"md","id":"dd-q722-worked","role":"prose","src":"Two values, named, then combined and handed back as a pair.\n","q":722},{"t":"code","id":"dd-q722-worked-code","role":"code","src":"a = 7\nb = 2\n\ntotal = a + b\ndifference = a - b\n\nprint(\"total is\", total)\nprint(\"difference is\", difference)\nprint(\"both together:\", (total, difference))\n","q":722},{"t":"md","id":"dd-q722-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `a` and `b` are names for the two inputs, so the arithmetic below reads as\n   words rather than as bare numbers.\n2. `total` and `difference` name the two results. Naming a result is what lets\n   the next line use it without recomputing it.\n3. The final `(total, difference)` is a tuple: one value that carries both\n   answers, which is exactly what a function returns when it has two things to\n   say.\n","q":722},{"t":"md","id":"dd-q722","role":"problem","src":"<!-- dd:dd-q722 -->\n\n### Problem 722 · faded — your turn\n\nStick a second label on the same value, then read both.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(4, 4, 8)\n```\n","q":722},{"t":"code","id":"dd-q722-code","role":"code","src":"def solve(base):\n    \"\"\"Two labels stuck to one value.\"\"\"\n    left = base\n    right = _____\n    return (left, right, left + right)\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (4,)\nprint(solve(*example))\n","q":722},{"t":"code","id":"dd-q722-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(722)\n","q":722},{"t":"code","id":"dd-q722-solution","role":"solution","src":"#@title 💡 Solution — Problem 722\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(base):\n    \"\"\"Two labels stuck to one value.\"\"\"\n    left = base\n    right = base\n    return (left, right, left + right)\n\n\nexample = (4,)\nprint(solve(*example))\n","q":722},{"t":"md","id":"dd-q723-worked","role":"prose","src":"Two values, named, then combined and handed back as a pair.\n","q":723},{"t":"code","id":"dd-q723-worked-code","role":"code","src":"a = 7\nb = 2\n\ntotal = a + b\ndifference = a - b\n\nprint(\"total is\", total)\nprint(\"difference is\", difference)\nprint(\"both together:\", (total, difference))\n","q":723},{"t":"md","id":"dd-q723-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `a` and `b` are names for the two inputs, so the arithmetic below reads as\n   words rather than as bare numbers.\n2. `total` and `difference` name the two results. Naming a result is what lets\n   the next line use it without recomputing it.\n3. The final `(total, difference)` is a tuple: one value that carries both\n   answers, which is exactly what a function returns when it has two things to\n   say.\n","q":723},{"t":"md","id":"dd-q723","role":"problem","src":"<!-- dd:dd-q723 -->\n\n### Problem 723 · faded — your turn\n\nRe-attach the name to its own square.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n9\n```\n","q":723},{"t":"code","id":"dd-q723-code","role":"code","src":"def solve(a):\n    \"\"\"Re-attach the name to its own square.\"\"\"\n    x = a\n    x = _____ * _____\n    return x\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (3,)\nprint(solve(*example))\n","q":723},{"t":"code","id":"dd-q723-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(723)\n","q":723},{"t":"code","id":"dd-q723-solution","role":"solution","src":"#@title 💡 Solution — Problem 723\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(a):\n    \"\"\"Re-attach the name to its own square.\"\"\"\n    x = a\n    x = x * x\n    return x\n\n\nexample = (3,)\nprint(solve(*example))\n","q":723},{"t":"md","id":"dd-q724-worked","role":"prose","src":"Two values, named, then combined and handed back as a pair.\n","q":724},{"t":"code","id":"dd-q724-worked-code","role":"code","src":"a = 7\nb = 2\n\ntotal = a + b\ndifference = a - b\n\nprint(\"total is\", total)\nprint(\"difference is\", difference)\nprint(\"both together:\", (total, difference))\n","q":724},{"t":"md","id":"dd-q724-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `a` and `b` are names for the two inputs, so the arithmetic below reads as\n   words rather than as bare numbers.\n2. `total` and `difference` name the two results. Naming a result is what lets\n   the next line use it without recomputing it.\n3. The final `(total, difference)` is a tuple: one value that carries both\n   answers, which is exactly what a function returns when it has two things to\n   say.\n","q":724},{"t":"md","id":"dd-q724","role":"problem","src":"<!-- dd:dd-q724 -->\n\n### Problem 724 · faded — your turn\n\nBuild a new string, name it, and report its kind.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n('hey!', <class 'str'>)\n```\n","q":724},{"t":"code","id":"dd-q724-code","role":"code","src":"def solve(word):\n    \"\"\"A built string is a value like any other: name it, ask its kind.\"\"\"\n    greeting = word + _____\n    kind = type(greeting)\n    return (greeting, kind)\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ('hey',)\nprint(solve(*example))\n","q":724},{"t":"code","id":"dd-q724-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(724)\n","q":724},{"t":"code","id":"dd-q724-solution","role":"solution","src":"#@title 💡 Solution — Problem 724\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(word):\n    \"\"\"A built string is a value like any other: name it, ask its kind.\"\"\"\n    greeting = word + \"!\"\n    kind = type(greeting)\n    return (greeting, kind)\n\n\nexample = ('hey',)\nprint(solve(*example))\n","q":724},{"t":"md","id":"dd-q725-worked","role":"prose","src":"Two values, named, then combined and handed back as a pair.\n","q":725},{"t":"code","id":"dd-q725-worked-code","role":"code","src":"a = 7\nb = 2\n\ntotal = a + b\ndifference = a - b\n\nprint(\"total is\", total)\nprint(\"difference is\", difference)\nprint(\"both together:\", (total, difference))\n","q":725},{"t":"md","id":"dd-q725-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `a` and `b` are names for the two inputs, so the arithmetic below reads as\n   words rather than as bare numbers.\n2. `total` and `difference` name the two results. Naming a result is what lets\n   the next line use it without recomputing it.\n3. The final `(total, difference)` is a tuple: one value that carries both\n   answers, which is exactly what a function returns when it has two things to\n   say.\n","q":725},{"t":"md","id":"dd-q725","role":"problem","src":"<!-- dd:dd-q725 -->\n\n### Problem 725 · faded — your turn\n\nTwo named results, handed back as one pair.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(3, 10)\n```\n","q":725},{"t":"code","id":"dd-q725-code","role":"code","src":"def solve(a, b):\n    \"\"\"Two named results, packed as one pair.\"\"\"\n    diff = a - b\n    prod = _____ * _____\n    both = (diff, prod)\n    return both\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (5, 2)\nprint(solve(*example))\n","q":725},{"t":"code","id":"dd-q725-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(725)\n","q":725},{"t":"code","id":"dd-q725-solution","role":"solution","src":"#@title 💡 Solution — Problem 725\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(a, b):\n    \"\"\"Two named results, packed as one pair.\"\"\"\n    diff = a - b\n    prod = a * b\n    both = (diff, prod)\n    return both\n\n\nexample = (5, 2)\nprint(solve(*example))\n","q":725},{"t":"md","id":"dd-q570","role":"problem","src":"<!-- dd:dd-q570 -->\n\n### Problem 570 · independent\n\nWrite a function solve(price, qty) that returns a tuple (total, doubled): `total` is price times qty, and `doubled` is that same total added to itself. Compute `total` ONCE, store it under a name, and build `doubled` out of that name rather than multiplying again.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(12, 24)\n```\n","q":570},{"t":"code","id":"dd-q570-code","role":"code","src":"def solve(price, qty):\n    \"\"\"One computation, named once, reused.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (3, 4)\nprint(solve(*example))\n","q":570},{"t":"code","id":"dd-q570-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(570)\n","q":570},{"t":"code","id":"dd-q570-solution","role":"solution","src":"#@title 💡 Solution — Problem 570\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(price, qty):\n    \"\"\"One computation, named once, reused.\"\"\"\n    total = price * qty\n    doubled = total + total\n    return (total, doubled)\n\n\nexample = (3, 4)\nprint(solve(*example))\n","q":570},{"t":"md","id":"dd-q571","role":"problem","src":"<!-- dd:dd-q571 -->\n\n### Problem 571 · independent\n\nWrite a function solve(n) that adds 3 to n, then adds 3 again, RE-USING the same name `n` both times, and returns the final value. A name can be reassigned; the right-hand side is worked out first, using the OLD value.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n6\n```\n","q":571},{"t":"code","id":"dd-q571-code","role":"code","src":"def solve(n):\n    \"\"\"Rebind the same name twice.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (0,)\nprint(solve(*example))\n","q":571},{"t":"code","id":"dd-q571-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(571)\n","q":571},{"t":"code","id":"dd-q571-solution","role":"solution","src":"#@title 💡 Solution — Problem 571\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(n):\n    \"\"\"Rebind the same name twice.\"\"\"\n    n = n + 3\n    n = n + 3\n    return n\n\n\nexample = (0,)\nprint(solve(*example))\n","q":571},{"t":"md","id":"dd-q708","role":"problem","src":"<!-- dd:dd-q708 -->\n\n### Problem 708 · independent\n\nWrite a function solve(number, word) that gives each argument a name of its own — `amount` for the number, `label` for the word — and returns the tuple (amount, label, kind), where `kind` is what KIND of value `amount` is. Use `type` to get it. Naming a value does not change what kind of value it is.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(5, 'hi', <class 'int'>)\n```\n","q":708},{"t":"code","id":"dd-q708-code","role":"code","src":"def solve(number, word):\n    \"\"\"A name does not change the kind of the value behind it.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (5, 'hi')\nprint(solve(*example))\n","q":708},{"t":"code","id":"dd-q708-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(708)\n","q":708},{"t":"code","id":"dd-q708-solution","role":"solution","src":"#@title 💡 Solution — Problem 708\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(number, word):\n    \"\"\"A name does not change the kind of the value behind it.\"\"\"\n    amount = number\n    label = word\n    kind = type(amount)\n    return (amount, label, kind)\n\n\nexample = (5, 'hi')\nprint(solve(*example))\n","q":708},{"t":"md","id":"dd-q709","role":"problem","src":"<!-- dd:dd-q709 -->\n\n### Problem 709 · independent\n\nWrite a function solve(n) that stores n under the name `value`, records type(value) as `before`, then re-attaches that SAME name to the pair (value, value), records type(value) again as `after`, and returns (before, after). Re-attaching a name can change the kind of thing it holds, not just the amount.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(<class 'int'>, <class 'tuple'>)\n```\n","q":709},{"t":"code","id":"dd-q709-code","role":"code","src":"def solve(n):\n    \"\"\"Re-attaching a name can change the KIND, not just the value.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (5,)\nprint(solve(*example))\n","q":709},{"t":"code","id":"dd-q709-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(709)\n","q":709},{"t":"code","id":"dd-q709-solution","role":"solution","src":"#@title 💡 Solution — Problem 709\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(n):\n    \"\"\"Re-attaching a name can change the KIND, not just the value.\"\"\"\n    value = n\n    before = type(value)\n    value = (value, value)\n    after = type(value)\n    return (before, after)\n\n\nexample = (5,)\nprint(solve(*example))\n","q":709},{"t":"md","id":"dd-q710","role":"problem","src":"<!-- dd:dd-q710 -->\n\n### Problem 710 · independent\n\nWrite a function solve(start, replacement) that names `start` as `original`, gives the SAME value a second name `copy`, then re-attaches `original` to `replacement`, and returns (original, copy). Re-assigning moves one label; it does not reach through to anything else holding that value.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(2, 1)\n```\n","q":710},{"t":"code","id":"dd-q710-code","role":"code","src":"def solve(start, replacement):\n    \"\"\"Two names on one value; moving one label leaves the other alone.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (1, 2)\nprint(solve(*example))\n","q":710},{"t":"code","id":"dd-q710-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(710)\n","q":710},{"t":"code","id":"dd-q710-solution","role":"solution","src":"#@title 💡 Solution — Problem 710\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(start, replacement):\n    \"\"\"Two names on one value; moving one label leaves the other alone.\"\"\"\n    original = start\n    copy = original\n    original = replacement\n    return (original, copy)\n\n\nexample = (1, 2)\nprint(solve(*example))\n","q":710},{"t":"md","id":"dd-q711","role":"problem","src":"<!-- dd:dd-q711 -->\n\n### Problem 711 · independent\n\nWrite a function solve(a, b, c) that rotates the three values one place to the left and returns them as a tuple: the answer for solve(1, 2, 3) is (2, 3, 1). Keep the first value under a temporary name before you overwrite it, or it is gone by the time you need it.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(2, 3, 1)\n```\n","q":711},{"t":"code","id":"dd-q711-code","role":"code","src":"def solve(a, b, c):\n    \"\"\"A temporary name is what keeps the value about to be overwritten.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (1, 2, 3)\nprint(solve(*example))\n","q":711},{"t":"code","id":"dd-q711-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(711)\n","q":711},{"t":"code","id":"dd-q711-solution","role":"solution","src":"#@title 💡 Solution — Problem 711\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(a, b, c):\n    \"\"\"A temporary name is what keeps the value about to be overwritten.\"\"\"\n    keep = a\n    a = b\n    b = c\n    c = keep\n    return (a, b, c)\n\n\nexample = (1, 2, 3)\nprint(solve(*example))\n","q":711},{"t":"md","id":"dd-q726","role":"problem","src":"<!-- dd:dd-q726 -->\n\n### Problem 726 · independent\n\nWrite a function solve(a, b, c) that names a + b `total`, builds `grand` by adding c to that name, and returns the tuple (total, grand). Compute the first sum once and read it, not recompute it.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(3, 6)\n```\n","q":726},{"t":"code","id":"dd-q726-code","role":"code","src":"def solve(a, b, c):\n    \"\"\"A running total: each step reads the name before it.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (1, 2, 3)\nprint(solve(*example))\n","q":726},{"t":"code","id":"dd-q726-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(726)\n","q":726},{"t":"code","id":"dd-q726-solution","role":"solution","src":"#@title 💡 Solution — Problem 726\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(a, b, c):\n    \"\"\"A running total: each step reads the name before it.\"\"\"\n    total = a + b\n    grand = total + c\n    return (total, grand)\n\n\nexample = (1, 2, 3)\nprint(solve(*example))\n","q":726},{"t":"md","id":"dd-q727","role":"problem","src":"<!-- dd:dd-q727 -->\n\n### Problem 727 · independent\n\nWrite a function solve(a, b) that names the text \"sum\" `label`, names a + b `value`, and returns the tuple (label, value). A quoted word is a value of its own — the five letters, not a name.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n('sum', 8)\n```\n","q":727},{"t":"code","id":"dd-q727-code","role":"code","src":"def solve(a, b):\n    \"\"\"A string value and a number value, named and paired.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (2, 6)\nprint(solve(*example))\n","q":727},{"t":"code","id":"dd-q727-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(727)\n","q":727},{"t":"code","id":"dd-q727-solution","role":"solution","src":"#@title 💡 Solution — Problem 727\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(a, b):\n    \"\"\"A string value and a number value, named and paired.\"\"\"\n    label = \"sum\"\n    value = a + b\n    return (label, value)\n\n\nexample = (2, 6)\nprint(solve(*example))\n","q":727},{"t":"md","id":"dd-q728","role":"problem","src":"<!-- dd:dd-q728 -->\n\n### Problem 728 · independent\n\nWrite a function solve(x) that names x * x `x2`, names x2 * x2 `x4`, and returns the tuple (x, x2, x4). Each power is built by reading the name of the one before it — x is never multiplied four times.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(2, 4, 16)\n```\n","q":728},{"t":"code","id":"dd-q728-code","role":"code","src":"def solve(x):\n    \"\"\"Powers by reuse: each name reads the previous one.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (2,)\nprint(solve(*example))\n","q":728},{"t":"code","id":"dd-q728-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(728)\n","q":728},{"t":"code","id":"dd-q728-solution","role":"solution","src":"#@title 💡 Solution — Problem 728\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(x):\n    \"\"\"Powers by reuse: each name reads the previous one.\"\"\"\n    x2 = x * x\n    x4 = x2 * x2\n    return (x, x2, x4)\n\n\nexample = (2,)\nprint(solve(*example))\n","q":728},{"t":"md","id":"dd-q729","role":"problem","src":"<!-- dd:dd-q729 -->\n\n### Problem 729 · independent\n\nWrite a function solve(n) that names n `v`, re-attaches `v` to the tuple (v, \"kept\"), and returns the tuple (v, kind) where kind is the type of what v holds NOW. Re-attaching a name can change the kind of thing it holds.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n((5, 'kept'), <class 'tuple'>)\n```\n","q":729},{"t":"code","id":"dd-q729-code","role":"code","src":"def solve(n):\n    \"\"\"After re-attaching, the name holds a tuple — whatever it held before.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (5,)\nprint(solve(*example))\n","q":729},{"t":"code","id":"dd-q729-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(729)\n","q":729},{"t":"code","id":"dd-q729-solution","role":"solution","src":"#@title 💡 Solution — Problem 729\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(n):\n    \"\"\"After re-attaching, the name holds a tuple — whatever it held before.\"\"\"\n    v = n\n    v = (v, \"kept\")\n    kind = type(v)\n    return (v, kind)\n\n\nexample = (5,)\nprint(solve(*example))\n","q":729},{"t":"md","id":"python-values-and-names-misconceptions","role":"prose","src":"#### Common mistakes\n\n- **\"`x = x + 1` is a contradiction.\"** — It is an instruction, not a claim. The\n  right-hand side is worked out with the OLD value, and the name is then\n  re-attached to the result.\n- **\"Printing a value and returning it are the same thing.\"** — `print` shows\n  something to a human; `return` hands a value back to whatever called the\n  function. A drill that prints instead of returning gives the grader nothing.\n- **\"A name IS the value.\"** — A name is a label. Two names can be stuck to the\n  same value, and re-labelling one leaves the other where it was.\n"},{"t":"md","id":"dd-kp-python-types-and-conversion","role":"prose","src":"<!-- dd:dd-kp-python-types-and-conversion -->\n\n## The everyday types, and converting between them\n\n`python.types-and-conversion`\n"},{"t":"md","id":"python-types-and-conversion-segment-concept","role":"prose","src":"Every value has a **type**, and the type decides what the value can do. Four of\nthem carry almost all of the early work:\n\n| Type | What it holds | Written as |\n|---|---|---|\n| `int` | a whole number | `5`, `-2`, `0` |\n| `float` | a number with a fractional part | `2.5`, `-0.75`, `4.0` |\n| `str` | text | `\"hi\"`, `'42'` |\n| `bool` | a yes/no answer | `True`, `False` |\n\n`type(value)` hands back the type itself. Its `__name__` is the readable word,\nwhich is usually what you actually want to look at:\n"},{"t":"code","id":"python-types-and-conversion-segment-concept-code","role":"code","src":"print(type(5).__name__)\nprint(type(2.5).__name__)\nprint(type(\"42\").__name__)\nprint(type(True).__name__)\n"},{"t":"md","id":"python-types-and-conversion-segment-concept-1","role":"prose","src":"\n\nNotice that `42` and `\"42\"` are **not** the same value. One is a number; the\nother is two characters that happen to look like one. The difference shows up\nthe moment you use `+`, because `+` does a different job for each type:\n"},{"t":"code","id":"python-types-and-conversion-segment-concept-code-1","role":"code","src":"print(4 + 2)\nprint(\"4\" + \"2\")\n"},{"t":"md","id":"python-types-and-conversion-segment-concept-2","role":"prose","src":"\n\nThat is not a quirk — it is the type deciding the meaning. Numbers add; text\njoins end to end.\n\nAsking whether they are **equal** gives the same answer for the same reason.\n`==` compares values, and a value made of characters is never equal to a value\nmade of digits — no matter how alike they look on the page:\n"},{"t":"code","id":"python-types-and-conversion-segment-concept-code-2","role":"code","src":"print(\"42\" == 42)\nprint(42 == 42)\n"},{"t":"md","id":"python-types-and-conversion-segment-concept-3","role":"prose","src":"\n\nComparing does not convert. If you want the question answered about the\n*number*, you have to do the converting yourself first:\n"},{"t":"code","id":"python-types-and-conversion-segment-concept-code-3","role":"code","src":"print(int(\"42\") == 42)\n"},{"t":"md","id":"python-types-and-conversion-segment-concept-4","role":"prose","src":"\n\nTo move between the two you **convert**, by calling the type's own name as a\nfunction. Each conversion produces a NEW value and leaves the original alone:\n"},{"t":"code","id":"python-types-and-conversion-segment-concept-code-4","role":"code","src":"text = \"42\"\nnumber = int(text)\n\nprint(number + 8)\nprint(text)\n"},{"t":"md","id":"python-types-and-conversion-segment-concept-5","role":"prose","src":"\n\n`float(...)` and `str(...)` work the same way, and going number → text →\nnumber again gets you back where you started:\n"},{"t":"code","id":"python-types-and-conversion-segment-concept-code-5","role":"code","src":"print(float(\"2.5\"))\nprint(str(7) + \"!\")\nprint(int(str(7)))\n"},{"t":"md","id":"python-types-and-conversion-segment-concept-6","role":"prose","src":"\n\nTwo conversions that look similar and are not: `int(...)` throws the fractional\npart **away**, while `round(...)` goes to the nearest whole number.\n"},{"t":"code","id":"python-types-and-conversion-segment-concept-code-6","role":"code","src":"print(int(3.9))\nprint(round(3.9))\nprint(int(-2.7), round(-2.7))\n"},{"t":"md","id":"python-types-and-conversion-segment-concept-7","role":"prose","src":"\n\n`bool(...)` is a conversion too, and the one that surprises people. It asks a\nsingle question of a value — *is there anything here?* — and almost everything\nanswers yes. Zero answers no, and so does empty text:\n"},{"t":"code","id":"python-types-and-conversion-segment-concept-code-7","role":"code","src":"print(bool(7), bool(0))\nprint(bool(\"hi\"), bool(\"\"))\nprint(bool(0.0), bool(-3))\n"},{"t":"md","id":"python-types-and-conversion-segment-concept-8","role":"prose","src":"\n\nThe trap is the text `\"0\"`. It is one character long, so there **is** something\nthere and it converts to `True`. The zero only shows up once the digits have\nbeen read as a number:\n"},{"t":"code","id":"python-types-and-conversion-segment-concept-code-8","role":"code","src":"print(bool(\"0\"))\nprint(bool(int(\"0\")))\n"},{"t":"md","id":"python-types-and-conversion-segment-watch","role":"prose","src":"> **Watch out.** - **`int()` truncates, it does not round** — `int(3.9)` is `3`. If you wanted\n  `4`, `round` is the call you meant.\n- **`\"5\"` is not `5`** — a string of digits stays text until something converts\n  it, and comparing the two gives `False`.\n- **`bool` is its own type** — `True` behaves like `1` in arithmetic, but\n  `type(True).__name__` is `\"bool\"`, not `\"int\"`.\n"},{"t":"md","id":"dd-q574-worked","role":"prose","src":"One string, read three different ways, with the type named at each step.\n","q":574},{"t":"code","id":"dd-q574-worked-code","role":"code","src":"text = \"12\"\n\nas_int = int(text)\nas_float = float(text)\nback_to_text = str(as_int)\n\nprint(as_int, type(as_int).__name__)\nprint(as_float, type(as_float).__name__)\nprint(back_to_text, type(back_to_text).__name__)\nprint(\"the original is untouched:\", text, type(text).__name__)\n","q":574},{"t":"md","id":"dd-q574-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `int(text)` reads the digits as a whole number. The string itself is not\n   changed — conversion produces a new value.\n2. `float(text)` reads the same digits as a number with a fractional part. `12`\n   and `12.0` are equal in value and different in type.\n3. `str(as_int)` goes back the other way, which is how a number gets glued into\n   a message.\n","q":574},{"t":"md","id":"dd-q574","role":"problem","src":"<!-- dd:dd-q574 -->\n\n### Problem 574 · faded — your turn\n\nReport the name of a value's type.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n'int'\n```\n","q":574},{"t":"code","id":"dd-q574-code","role":"code","src":"def solve(value):\n    \"\"\"The name of the value's type.\"\"\"\n    return type(value)._____\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (3,)\nprint(solve(*example))\n","q":574},{"t":"code","id":"dd-q574-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(574)\n","q":574},{"t":"code","id":"dd-q574-solution","role":"solution","src":"#@title 💡 Solution — Problem 574\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(value):\n    \"\"\"The name of the value's type.\"\"\"\n    return type(value).__name__\n\n\nexample = (3,)\nprint(solve(*example))\n","q":574},{"t":"md","id":"dd-q575-worked","role":"prose","src":"One string, read three different ways, with the type named at each step.\n","q":575},{"t":"code","id":"dd-q575-worked-code","role":"code","src":"text = \"12\"\n\nas_int = int(text)\nas_float = float(text)\nback_to_text = str(as_int)\n\nprint(as_int, type(as_int).__name__)\nprint(as_float, type(as_float).__name__)\nprint(back_to_text, type(back_to_text).__name__)\nprint(\"the original is untouched:\", text, type(text).__name__)\n","q":575},{"t":"md","id":"dd-q575-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `int(text)` reads the digits as a whole number. The string itself is not\n   changed — conversion produces a new value.\n2. `float(text)` reads the same digits as a number with a fractional part. `12`\n   and `12.0` are equal in value and different in type.\n3. `str(as_int)` goes back the other way, which is how a number gets glued into\n   a message.\n","q":575},{"t":"md","id":"dd-q575","role":"problem","src":"<!-- dd:dd-q575 -->\n\n### Problem 575 · faded — your turn\n\nText in, arithmetic out — the digits have to become a number first.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n50\n```\n","q":575},{"t":"code","id":"dd-q575-code","role":"code","src":"def solve(digits):\n    \"\"\"Text in, number out.\"\"\"\n    return _____(digits) + 8\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ('42',)\nprint(solve(*example))\n","q":575},{"t":"code","id":"dd-q575-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(575)\n","q":575},{"t":"code","id":"dd-q575-solution","role":"solution","src":"#@title 💡 Solution — Problem 575\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(digits):\n    \"\"\"Text in, number out.\"\"\"\n    return int(digits) + 8\n\n\nexample = ('42',)\nprint(solve(*example))\n","q":575},{"t":"md","id":"dd-q731-worked","role":"prose","src":"One string, read three different ways, with the type named at each step.\n","q":731},{"t":"code","id":"dd-q731-worked-code","role":"code","src":"text = \"12\"\n\nas_int = int(text)\nas_float = float(text)\nback_to_text = str(as_int)\n\nprint(as_int, type(as_int).__name__)\nprint(as_float, type(as_float).__name__)\nprint(back_to_text, type(back_to_text).__name__)\nprint(\"the original is untouched:\", text, type(text).__name__)\n","q":731},{"t":"md","id":"dd-q731-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `int(text)` reads the digits as a whole number. The string itself is not\n   changed — conversion produces a new value.\n2. `float(text)` reads the same digits as a number with a fractional part. `12`\n   and `12.0` are equal in value and different in type.\n3. `str(as_int)` goes back the other way, which is how a number gets glued into\n   a message.\n","q":731},{"t":"md","id":"dd-q731","role":"problem","src":"<!-- dd:dd-q731 -->\n\n### Problem 731 · faded — your turn\n\nConvert first — then * means arithmetic, not repetition.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n5.0\n```\n","q":731},{"t":"code","id":"dd-q731-code","role":"code","src":"def solve(text):\n    \"\"\"Convert first — then * means arithmetic.\"\"\"\n    return _____(text) * 2\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ('2.5',)\nprint(solve(*example))\n","q":731},{"t":"code","id":"dd-q731-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(731)\n","q":731},{"t":"code","id":"dd-q731-solution","role":"solution","src":"#@title 💡 Solution — Problem 731\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(text):\n    \"\"\"Convert first — then * means arithmetic.\"\"\"\n    return float(text) * 2\n\n\nexample = ('2.5',)\nprint(solve(*example))\n","q":731},{"t":"md","id":"dd-q732-worked","role":"prose","src":"One string, read three different ways, with the type named at each step.\n","q":732},{"t":"code","id":"dd-q732-worked-code","role":"code","src":"text = \"12\"\n\nas_int = int(text)\nas_float = float(text)\nback_to_text = str(as_int)\n\nprint(as_int, type(as_int).__name__)\nprint(as_float, type(as_float).__name__)\nprint(back_to_text, type(back_to_text).__name__)\nprint(\"the original is untouched:\", text, type(text).__name__)\n","q":732},{"t":"md","id":"dd-q732-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `int(text)` reads the digits as a whole number. The string itself is not\n   changed — conversion produces a new value.\n2. `float(text)` reads the same digits as a number with a fractional part. `12`\n   and `12.0` are equal in value and different in type.\n3. `str(as_int)` goes back the other way, which is how a number gets glued into\n   a message.\n","q":732},{"t":"md","id":"dd-q732","role":"problem","src":"<!-- dd:dd-q732 -->\n\n### Problem 732 · faded — your turn\n\nA number cannot be glued onto text until it IS text.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n'3 items'\n```\n","q":732},{"t":"code","id":"dd-q732-code","role":"code","src":"def solve(count):\n    \"\"\"str() makes a number gluable.\"\"\"\n    label = _____(count)\n    return label + \" items\"\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (3,)\nprint(solve(*example))\n","q":732},{"t":"code","id":"dd-q732-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(732)\n","q":732},{"t":"code","id":"dd-q732-solution","role":"solution","src":"#@title 💡 Solution — Problem 732\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(count):\n    \"\"\"str() makes a number gluable.\"\"\"\n    label = str(count)\n    return label + \" items\"\n\n\nexample = (3,)\nprint(solve(*example))\n","q":732},{"t":"md","id":"dd-q733-worked","role":"prose","src":"One string, read three different ways, with the type named at each step.\n","q":733},{"t":"code","id":"dd-q733-worked-code","role":"code","src":"text = \"12\"\n\nas_int = int(text)\nas_float = float(text)\nback_to_text = str(as_int)\n\nprint(as_int, type(as_int).__name__)\nprint(as_float, type(as_float).__name__)\nprint(back_to_text, type(back_to_text).__name__)\nprint(\"the original is untouched:\", text, type(text).__name__)\n","q":733},{"t":"md","id":"dd-q733-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `int(text)` reads the digits as a whole number. The string itself is not\n   changed — conversion produces a new value.\n2. `float(text)` reads the same digits as a number with a fractional part. `12`\n   and `12.0` are equal in value and different in type.\n3. `str(as_int)` goes back the other way, which is how a number gets glued into\n   a message.\n","q":733},{"t":"md","id":"dd-q733","role":"problem","src":"<!-- dd:dd-q733 -->\n\n### Problem 733 · faded — your turn\n\nIts truth, and the name of its kind.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(True, 'str')\n```\n","q":733},{"t":"code","id":"dd-q733-code","role":"code","src":"def solve(value):\n    \"\"\"Its truth, and the name of its kind.\"\"\"\n    return (_____(value), type(value).__name__)\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ('hi',)\nprint(solve(*example))\n","q":733},{"t":"code","id":"dd-q733-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(733)\n","q":733},{"t":"code","id":"dd-q733-solution","role":"solution","src":"#@title 💡 Solution — Problem 733\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(value):\n    \"\"\"Its truth, and the name of its kind.\"\"\"\n    return (bool(value), type(value).__name__)\n\n\nexample = ('hi',)\nprint(solve(*example))\n","q":733},{"t":"md","id":"dd-q734-worked","role":"prose","src":"One string, read three different ways, with the type named at each step.\n","q":734},{"t":"code","id":"dd-q734-worked-code","role":"code","src":"text = \"12\"\n\nas_int = int(text)\nas_float = float(text)\nback_to_text = str(as_int)\n\nprint(as_int, type(as_int).__name__)\nprint(as_float, type(as_float).__name__)\nprint(back_to_text, type(back_to_text).__name__)\nprint(\"the original is untouched:\", text, type(text).__name__)\n","q":734},{"t":"md","id":"dd-q734-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `int(text)` reads the digits as a whole number. The string itself is not\n   changed — conversion produces a new value.\n2. `float(text)` reads the same digits as a number with a fractional part. `12`\n   and `12.0` are equal in value and different in type.\n3. `str(as_int)` goes back the other way, which is how a number gets glued into\n   a message.\n","q":734},{"t":"md","id":"dd-q734","role":"problem","src":"<!-- dd:dd-q734 -->\n\n### Problem 734 · faded — your turn\n\nTo the nearest whole number — not truncated.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n3\n```\n","q":734},{"t":"code","id":"dd-q734-code","role":"code","src":"def solve(x):\n    \"\"\"round() goes to the nearest whole number.\"\"\"\n    return _____(x)\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (2.7,)\nprint(solve(*example))\n","q":734},{"t":"code","id":"dd-q734-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(734)\n","q":734},{"t":"code","id":"dd-q734-solution","role":"solution","src":"#@title 💡 Solution — Problem 734\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(x):\n    \"\"\"round() goes to the nearest whole number.\"\"\"\n    return round(x)\n\n\nexample = (2.7,)\nprint(solve(*example))\n","q":734},{"t":"md","id":"dd-q735-worked","role":"prose","src":"One string, read three different ways, with the type named at each step.\n","q":735},{"t":"code","id":"dd-q735-worked-code","role":"code","src":"text = \"12\"\n\nas_int = int(text)\nas_float = float(text)\nback_to_text = str(as_int)\n\nprint(as_int, type(as_int).__name__)\nprint(as_float, type(as_float).__name__)\nprint(back_to_text, type(back_to_text).__name__)\nprint(\"the original is untouched:\", text, type(text).__name__)\n","q":735},{"t":"md","id":"dd-q735-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `int(text)` reads the digits as a whole number. The string itself is not\n   changed — conversion produces a new value.\n2. `float(text)` reads the same digits as a number with a fractional part. `12`\n   and `12.0` are equal in value and different in type.\n3. `str(as_int)` goes back the other way, which is how a number gets glued into\n   a message.\n","q":735},{"t":"md","id":"dd-q735","role":"problem","src":"<!-- dd:dd-q735 -->\n\n### Problem 735 · faded — your turn\n\nPut both values on the same side of the type line, then compare.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\nTrue\n```\n","q":735},{"t":"code","id":"dd-q735-code","role":"code","src":"def solve(text, number):\n    \"\"\"Convert to the SAME type, then compare.\"\"\"\n    return text == _____(number)\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ('7', 7)\nprint(solve(*example))\n","q":735},{"t":"code","id":"dd-q735-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(735)\n","q":735},{"t":"code","id":"dd-q735-solution","role":"solution","src":"#@title 💡 Solution — Problem 735\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(text, number):\n    \"\"\"Convert to the SAME type, then compare.\"\"\"\n    return text == str(number)\n\n\nexample = ('7', 7)\nprint(solve(*example))\n","q":735},{"t":"md","id":"dd-q736-worked","role":"prose","src":"One string, read three different ways, with the type named at each step.\n","q":736},{"t":"code","id":"dd-q736-worked-code","role":"code","src":"text = \"12\"\n\nas_int = int(text)\nas_float = float(text)\nback_to_text = str(as_int)\n\nprint(as_int, type(as_int).__name__)\nprint(as_float, type(as_float).__name__)\nprint(back_to_text, type(back_to_text).__name__)\nprint(\"the original is untouched:\", text, type(text).__name__)\n","q":736},{"t":"md","id":"dd-q736-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `int(text)` reads the digits as a whole number. The string itself is not\n   changed — conversion produces a new value.\n2. `float(text)` reads the same digits as a number with a fractional part. `12`\n   and `12.0` are equal in value and different in type.\n3. `str(as_int)` goes back the other way, which is how a number gets glued into\n   a message.\n","q":736},{"t":"md","id":"dd-q736","role":"problem","src":"<!-- dd:dd-q736 -->\n\n### Problem 736 · faded — your turn\n\nTruncate both — drop the fraction, do not round — then multiply.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n6\n```\n","q":736},{"t":"code","id":"dd-q736-code","role":"code","src":"def solve(x, y):\n    \"\"\"int() drops the fraction; it does not round.\"\"\"\n    return _____(x) * _____(y)\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (3.9, 2.2)\nprint(solve(*example))\n","q":736},{"t":"code","id":"dd-q736-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(736)\n","q":736},{"t":"code","id":"dd-q736-solution","role":"solution","src":"#@title 💡 Solution — Problem 736\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(x, y):\n    \"\"\"int() drops the fraction; it does not round.\"\"\"\n    return int(x) * int(y)\n\n\nexample = (3.9, 2.2)\nprint(solve(*example))\n","q":736},{"t":"md","id":"dd-q737-worked","role":"prose","src":"One string, read three different ways, with the type named at each step.\n","q":737},{"t":"code","id":"dd-q737-worked-code","role":"code","src":"text = \"12\"\n\nas_int = int(text)\nas_float = float(text)\nback_to_text = str(as_int)\n\nprint(as_int, type(as_int).__name__)\nprint(as_float, type(as_float).__name__)\nprint(back_to_text, type(back_to_text).__name__)\nprint(\"the original is untouched:\", text, type(text).__name__)\n","q":737},{"t":"md","id":"dd-q737-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `int(text)` reads the digits as a whole number. The string itself is not\n   changed — conversion produces a new value.\n2. `float(text)` reads the same digits as a number with a fractional part. `12`\n   and `12.0` are equal in value and different in type.\n3. `str(as_int)` goes back the other way, which is how a number gets glued into\n   a message.\n","q":737},{"t":"md","id":"dd-q737","role":"problem","src":"<!-- dd:dd-q737 -->\n\n### Problem 737 · faded — your turn\n\nText to number to text: the kind changes twice, the digits never do.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(42, '42')\n```\n","q":737},{"t":"code","id":"dd-q737-code","role":"code","src":"def solve(digits):\n    \"\"\"Text to number to text: the kind changes, the digits do not.\"\"\"\n    n = _____(digits)\n    back = _____(n)\n    return (n, back)\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ('42',)\nprint(solve(*example))\n","q":737},{"t":"code","id":"dd-q737-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(737)\n","q":737},{"t":"code","id":"dd-q737-solution","role":"solution","src":"#@title 💡 Solution — Problem 737\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(digits):\n    \"\"\"Text to number to text: the kind changes, the digits do not.\"\"\"\n    n = int(digits)\n    back = str(n)\n    return (n, back)\n\n\nexample = ('42',)\nprint(solve(*example))\n","q":737},{"t":"md","id":"dd-q738-worked","role":"prose","src":"One string, read three different ways, with the type named at each step.\n","q":738},{"t":"code","id":"dd-q738-worked-code","role":"code","src":"text = \"12\"\n\nas_int = int(text)\nas_float = float(text)\nback_to_text = str(as_int)\n\nprint(as_int, type(as_int).__name__)\nprint(as_float, type(as_float).__name__)\nprint(back_to_text, type(back_to_text).__name__)\nprint(\"the original is untouched:\", text, type(text).__name__)\n","q":738},{"t":"md","id":"dd-q738-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `int(text)` reads the digits as a whole number. The string itself is not\n   changed — conversion produces a new value.\n2. `float(text)` reads the same digits as a number with a fractional part. `12`\n   and `12.0` are equal in value and different in type.\n3. `str(as_int)` goes back the other way, which is how a number gets glued into\n   a message.\n","q":738},{"t":"md","id":"dd-q738","role":"problem","src":"<!-- dd:dd-q738 -->\n\n### Problem 738 · faded — your turn\n\nThe truth of the NUMBER, not of the text holding it.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\nFalse\n```\n","q":738},{"t":"code","id":"dd-q738-code","role":"code","src":"def solve(text):\n    \"\"\"bool of the NUMBER, not of the text holding it.\"\"\"\n    return bool(_____(text))\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ('0',)\nprint(solve(*example))\n","q":738},{"t":"code","id":"dd-q738-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(738)\n","q":738},{"t":"code","id":"dd-q738-solution","role":"solution","src":"#@title 💡 Solution — Problem 738\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(text):\n    \"\"\"bool of the NUMBER, not of the text holding it.\"\"\"\n    return bool(int(text))\n\n\nexample = ('0',)\nprint(solve(*example))\n","q":738},{"t":"md","id":"dd-q576","role":"problem","src":"<!-- dd:dd-q576 -->\n\n### Problem 576 · independent\n\nWrite a function solve(text) that takes a string of digits and returns the 3-tuple (as_int, as_float, back_to_text): the value as an int, the same value as a float, and str() of the int. Converting is a one-way trip each time — nothing changes the original string.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(7, 7.0, '7')\n```\n","q":576},{"t":"code","id":"dd-q576-code","role":"code","src":"def solve(text):\n    \"\"\"One string, three ways of reading it.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ('7',)\nprint(solve(*example))\n","q":576},{"t":"code","id":"dd-q576-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(576)\n","q":576},{"t":"code","id":"dd-q576-solution","role":"solution","src":"#@title 💡 Solution — Problem 576\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(text):\n    \"\"\"One string, three ways of reading it.\"\"\"\n    as_int = int(text)\n    as_float = float(text)\n    return (as_int, as_float, str(as_int))\n\n\nexample = ('7',)\nprint(solve(*example))\n","q":576},{"t":"md","id":"dd-q577","role":"problem","src":"<!-- dd:dd-q577 -->\n\n### Problem 577 · independent\n\nWrite a function solve(x) that takes a float and returns the tuple (truncated, rounded): int(x), which throws the fraction AWAY, and round(x), which goes to the nearest whole number. Show that the two disagree.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(3, 4)\n```\n","q":577},{"t":"code","id":"dd-q577-code","role":"code","src":"def solve(x):\n    \"\"\"int() truncates; round() goes to the nearest.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (3.9,)\nprint(solve(*example))\n","q":577},{"t":"code","id":"dd-q577-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(577)\n","q":577},{"t":"code","id":"dd-q577-solution","role":"solution","src":"#@title 💡 Solution — Problem 577\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(x):\n    \"\"\"int() truncates; round() goes to the nearest.\"\"\"\n    return (int(x), round(x))\n\n\nexample = (3.9,)\nprint(solve(*example))\n","q":577},{"t":"md","id":"dd-q713","role":"problem","src":"<!-- dd:dd-q713 -->\n\n### Problem 713 · independent\n\nWrite a function solve(number, text) that returns the tuple (bool(number), bool(text)) — that is, whether each argument counts as a yes or a no. Zero and empty text are the only nos here; every other number and every non-empty piece of text is a yes.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(True, True)\n```\n","q":713},{"t":"code","id":"dd-q713-code","role":"code","src":"def solve(number, text):\n    \"\"\"Zero and empty text are the nos; everything else is a yes.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (5, 'hi')\nprint(solve(*example))\n","q":713},{"t":"code","id":"dd-q713-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(713)\n","q":713},{"t":"code","id":"dd-q713-solution","role":"solution","src":"#@title 💡 Solution — Problem 713\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(number, text):\n    \"\"\"Zero and empty text are the nos; everything else is a yes.\"\"\"\n    return (bool(number), bool(text))\n\n\nexample = (5, 'hi')\nprint(solve(*example))\n","q":713},{"t":"md","id":"dd-q714","role":"problem","src":"<!-- dd:dd-q714 -->\n\n### Problem 714 · independent\n\nWrite a function solve(text) that reads a piece of text such as '2.6' as a number with a fractional part, and returns the tuple (exact, nearest) where `exact` is that number and `nearest` is it rounded to the nearest whole number. The text has to become a number before it can be rounded at all.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(2.6, 3)\n```\n","q":714},{"t":"code","id":"dd-q714-code","role":"code","src":"def solve(text):\n    \"\"\"Text cannot be rounded; the number it converts to can.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ('2.6',)\nprint(solve(*example))\n","q":714},{"t":"code","id":"dd-q714-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(714)\n","q":714},{"t":"code","id":"dd-q714-solution","role":"solution","src":"#@title 💡 Solution — Problem 714\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(text):\n    \"\"\"Text cannot be rounded; the number it converts to can.\"\"\"\n    exact = float(text)\n    nearest = round(exact)\n    return (exact, nearest)\n\n\nexample = ('2.6',)\nprint(solve(*example))\n","q":714},{"t":"md","id":"dd-q715","role":"problem","src":"<!-- dd:dd-q715 -->\n\n### Problem 715 · independent\n\nWrite a function solve(text, number) that returns the tuple (before, after), where `before` is whether the text is equal to the number as they were handed to you, and `after` is whether they are equal once the text has been converted to a whole number. Text that looks like a number is still text until something converts it.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(False, True)\n```\n","q":715},{"t":"code","id":"dd-q715-code","role":"code","src":"def solve(text, number):\n    \"\"\"A digit string is not the number it looks like — until it is converted.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ('42', 42)\nprint(solve(*example))\n","q":715},{"t":"code","id":"dd-q715-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(715)\n","q":715},{"t":"code","id":"dd-q715-solution","role":"solution","src":"#@title 💡 Solution — Problem 715\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(text, number):\n    \"\"\"A digit string is not the number it looks like — until it is converted.\"\"\"\n    before = text == number\n    after = int(text) == number\n    return (before, after)\n\n\nexample = ('42', 42)\nprint(solve(*example))\n","q":715},{"t":"md","id":"dd-q716","role":"problem","src":"<!-- dd:dd-q716 -->\n\n### Problem 716 · independent\n\nWrite a function solve(digits) that takes a piece of text holding digits and returns the tuple (as_text, as_number), where `as_text` is whether the text itself counts as a yes and `as_number` is whether the WHOLE NUMBER it converts to counts as a yes. The two answers disagree for '0': the text is not empty, and the number it holds is zero.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(True, False)\n```\n","q":716},{"t":"code","id":"dd-q716-code","role":"code","src":"def solve(digits):\n    \"\"\"Ask before converting and you are asking about the characters.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ('0',)\nprint(solve(*example))\n","q":716},{"t":"code","id":"dd-q716-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(716)\n","q":716},{"t":"code","id":"dd-q716-solution","role":"solution","src":"#@title 💡 Solution — Problem 716\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(digits):\n    \"\"\"Ask before converting and you are asking about the characters.\"\"\"\n    as_text = bool(digits)\n    as_number = bool(int(digits))\n    return (as_text, as_number)\n\n\nexample = ('0',)\nprint(solve(*example))\n","q":716},{"t":"md","id":"dd-q739","role":"problem","src":"<!-- dd:dd-q739 -->\n\n### Problem 739 · independent\n\nWrite a function solve(a, b) that converts both digit strings to ints, names their sum `total`, and returns the tuple (total, total_text) where total_text is the sum converted back to text. In that order.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(5, '5')\n```\n","q":739},{"t":"code","id":"dd-q739-code","role":"code","src":"def solve(a, b):\n    \"\"\"Convert, add as numbers, and carry the answer back as text too.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ('2', '3')\nprint(solve(*example))\n","q":739},{"t":"code","id":"dd-q739-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(739)\n","q":739},{"t":"code","id":"dd-q739-solution","role":"solution","src":"#@title 💡 Solution — Problem 739\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(a, b):\n    \"\"\"Convert, add as numbers, and carry the answer back as text too.\"\"\"\n    total = int(a) + int(b)\n    return (total, str(total))\n\n\nexample = ('2', '3')\nprint(solve(*example))\n","q":739},{"t":"md","id":"dd-q740","role":"problem","src":"<!-- dd:dd-q740 -->\n\n### Problem 740 · independent\n\nWrite a function solve(text) that converts text to a float named `f` and returns the tuple (int(f), round(f), whole) — the truncation, the nearest whole number, and whether f == round(f), in that order.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(2, 3, False)\n```\n","q":740},{"t":"code","id":"dd-q740-code","role":"code","src":"def solve(text):\n    \"\"\"Truncated, rounded, and whether there was anything to drop.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ('2.7',)\nprint(solve(*example))\n","q":740},{"t":"code","id":"dd-q740-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(740)\n","q":740},{"t":"code","id":"dd-q740-solution","role":"solution","src":"#@title 💡 Solution — Problem 740\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(text):\n    \"\"\"Truncated, rounded, and whether there was anything to drop.\"\"\"\n    f = float(text)\n    return (int(f), round(f), f == round(f))\n\n\nexample = ('2.7',)\nprint(solve(*example))\n","q":740},{"t":"md","id":"dd-q741","role":"problem","src":"<!-- dd:dd-q741 -->\n\n### Problem 741 · independent\n\nWrite a function solve(a, b) that returns the tuple (bool(a), bool(b), a == b): each value's truth, then whether the values themselves are equal. Two values can share a truth without being equal.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(False, False, False)\n```\n","q":741},{"t":"code","id":"dd-q741-code","role":"code","src":"def solve(a, b):\n    \"\"\"Same truth is not the same value.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ('', 0)\nprint(solve(*example))\n","q":741},{"t":"code","id":"dd-q741-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(741)\n","q":741},{"t":"code","id":"dd-q741-solution","role":"solution","src":"#@title 💡 Solution — Problem 741\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(a, b):\n    \"\"\"Same truth is not the same value.\"\"\"\n    return (bool(a), bool(b), a == b)\n\n\nexample = ('', 0)\nprint(solve(*example))\n","q":741},{"t":"md","id":"dd-q742","role":"problem","src":"<!-- dd:dd-q742 -->\n\n### Problem 742 · independent\n\nWrite a function solve(x) that returns the tuple of type NAMES (as text) of int(x) and str(x), in that order — what kind each conversion produced, via type(...).__name__.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n('int', 'str')\n```\n","q":742},{"t":"code","id":"dd-q742-code","role":"code","src":"def solve(x):\n    \"\"\"Each conversion has a kind of its own.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (2.9,)\nprint(solve(*example))\n","q":742},{"t":"code","id":"dd-q742-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(742)\n","q":742},{"t":"code","id":"dd-q742-solution","role":"solution","src":"#@title 💡 Solution — Problem 742\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(x):\n    \"\"\"Each conversion has a kind of its own.\"\"\"\n    return (type(int(x)).__name__, type(str(x)).__name__)\n\n\nexample = (2.9,)\nprint(solve(*example))\n","q":742},{"t":"md","id":"python-types-and-conversion-misconceptions","role":"prose","src":"#### Common mistakes\n\n- **\"Converting changes the value.\"** — It produces a new one. `int(text)` hands\n  back a number and leaves `text` exactly as it was.\n- **\"int() rounds.\"** — It truncates toward zero: `int(3.9)` is `3` and\n  `int(-2.7)` is `-2`.\n- **\"If it looks like a number it is one.\"** — `\"42\"` looks like a number to a\n  human and is text to Python. Only a conversion makes it arithmetic-ready.\n"},{"t":"md","id":"dd-kp-python-lists-and-tuples","role":"prose","src":"<!-- dd:dd-kp-python-lists-and-tuples -->\n\n## Lists and tuples — holding more than one value\n\n`python.lists-and-tuples`\n"},{"t":"md","id":"python-lists-and-tuples-segment-concept","role":"prose","src":"A name holds one value — but that value can itself be a **container** holding\nmany.\n\nA **list** is written with square brackets, and holds its items in order:\n"},{"t":"code","id":"python-lists-and-tuples-segment-concept-code","role":"code","src":"scores = [10, 20, 30]\nprint(scores)\nprint(len(scores))\n"},{"t":"md","id":"python-lists-and-tuples-segment-concept-1","role":"prose","src":"\n\n`len(...)` asks the container how many items it holds. Asking is better than\nremembering: the answer stays right when the list changes.\n\nA list can hold values of different types, and its items can themselves be\nlists. A list of lists is how a table (rows of equal length) is written before\nany library gets involved:\n"},{"t":"code","id":"python-lists-and-tuples-segment-concept-code-1","role":"code","src":"mixed = [1, \"two\", 3.0]\nrows = [[1, 2, 3], [4, 5, 6]]\n\nprint(mixed)\nprint(rows)\nprint(len(rows), \"rows of\", len(rows[0]))\n"},{"t":"md","id":"python-lists-and-tuples-segment-concept-2","role":"prose","src":"\n\nA **tuple** is written with parentheses instead. It holds items in order in\nexactly the same way — the difference is that a tuple cannot be changed after it\nis built:\n"},{"t":"code","id":"python-lists-and-tuples-segment-concept-code-2","role":"code","src":"point = (3, 4)\nprint(point, len(point))\n"},{"t":"md","id":"python-lists-and-tuples-segment-concept-3","role":"prose","src":"\n\nThat is why a tuple is the usual way to hand back several answers at once: it is\na fixed group of values, not a collection you are still working on. Use a list\nwhen the contents will grow or change, a tuple when they are one finished\nanswer.\n\nThe two convert into each other, and converting always produces a NEW container:\n"},{"t":"code","id":"python-lists-and-tuples-segment-concept-code-3","role":"code","src":"values = [1, 2, 3]\nas_tuple = tuple(values)\nback = list(as_tuple)\n\nprint(as_tuple)\nprint(back)\nprint(\"same length:\", len(values) == len(back))\n"},{"t":"md","id":"python-lists-and-tuples-segment-concept-4","role":"prose","src":"\n\nA one-item tuple needs a trailing comma — `(5)` is just the number 5 with\nbrackets around it, while `(5,)` is a tuple holding it:\n"},{"t":"code","id":"python-lists-and-tuples-segment-concept-code-4","role":"code","src":"print(type((5)).__name__)\nprint(type((5,)).__name__)\n"},{"t":"md","id":"python-lists-and-tuples-segment-watch","role":"prose","src":"> **Watch out.** - **Brackets decide the type** — `[a, b]` is a list, `(a, b)` is a tuple. They\n  hold the same values and are still different types.\n- **`len` counts the OUTER level only** — `len([[1, 2, 3]])` is `1`: one inner\n  list. What is inside it is a second question.\n- **`(5)` is not a tuple** — a one-item tuple is `(5,)`.\n"},{"t":"md","id":"dd-q580-worked","role":"prose","src":"A table as a list of lists, measured at both levels, then frozen into a tuple.\n","q":580},{"t":"code","id":"dd-q580-worked-code","role":"code","src":"rows = [[1, 2, 3], [4, 5, 6]]\n\nn_rows = len(rows)\nn_cols = len(rows[0])\n\nprint(\"rows:\", n_rows)\nprint(\"columns:\", n_cols)\n\nshape = (n_rows, n_cols)\nprint(\"as one finished answer:\", shape, type(shape).__name__)\n","q":580},{"t":"md","id":"dd-q580-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `len(rows)` counts the inner lists — the rows of the table.\n2. `len(rows[0])` reaches into the first row and counts what IT holds. Two\n   levels, two separate measurements.\n3. The pair is packed into a tuple because it is one finished answer with two\n   parts, which is exactly the job a tuple is for.\n","q":580},{"t":"md","id":"dd-q580","role":"problem","src":"<!-- dd:dd-q580 -->\n\n### Problem 580 · faded — your turn\n\nBuild the list, then ask it how long it is.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n([1, 2, 3], 3)\n```\n","q":580},{"t":"code","id":"dd-q580-code","role":"code","src":"def solve(a, b, c):\n    \"\"\"Build the list, then measure it.\"\"\"\n    items = [a, b, c]\n    return (items, _____(items))\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (1, 2, 3)\nprint(solve(*example))\n","q":580},{"t":"code","id":"dd-q580-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(580)\n","q":580},{"t":"code","id":"dd-q580-solution","role":"solution","src":"#@title 💡 Solution — Problem 580\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(a, b, c):\n    \"\"\"Build the list, then measure it.\"\"\"\n    items = [a, b, c]\n    return (items, len(items))\n\n\nexample = (1, 2, 3)\nprint(solve(*example))\n","q":580},{"t":"md","id":"dd-q581-worked","role":"prose","src":"A table as a list of lists, measured at both levels, then frozen into a tuple.\n","q":581},{"t":"code","id":"dd-q581-worked-code","role":"code","src":"rows = [[1, 2, 3], [4, 5, 6]]\n\nn_rows = len(rows)\nn_cols = len(rows[0])\n\nprint(\"rows:\", n_rows)\nprint(\"columns:\", n_cols)\n\nshape = (n_rows, n_cols)\nprint(\"as one finished answer:\", shape, type(shape).__name__)\n","q":581},{"t":"md","id":"dd-q581-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `len(rows)` counts the inner lists — the rows of the table.\n2. `len(rows[0])` reaches into the first row and counts what IT holds. Two\n   levels, two separate measurements.\n3. The pair is packed into a tuple because it is one finished answer with two\n   parts, which is exactly the job a tuple is for.\n","q":581},{"t":"md","id":"dd-q581","role":"problem","src":"<!-- dd:dd-q581 -->\n\n### Problem 581 · faded — your turn\n\nTwo values in a tuple — parentheses, not brackets.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(1, 2)\n```\n","q":581},{"t":"code","id":"dd-q581-code","role":"code","src":"def solve(a, b):\n    \"\"\"A tuple of the two values.\"\"\"\n    return _____a, b_____\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (1, 2)\nprint(solve(*example))\n","q":581},{"t":"code","id":"dd-q581-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(581)\n","q":581},{"t":"code","id":"dd-q581-solution","role":"solution","src":"#@title 💡 Solution — Problem 581\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(a, b):\n    \"\"\"A tuple of the two values.\"\"\"\n    return (a, b)\n\n\nexample = (1, 2)\nprint(solve(*example))\n","q":581},{"t":"md","id":"dd-q744-worked","role":"prose","src":"A table as a list of lists, measured at both levels, then frozen into a tuple.\n","q":744},{"t":"code","id":"dd-q744-worked-code","role":"code","src":"rows = [[1, 2, 3], [4, 5, 6]]\n\nn_rows = len(rows)\nn_cols = len(rows[0])\n\nprint(\"rows:\", n_rows)\nprint(\"columns:\", n_cols)\n\nshape = (n_rows, n_cols)\nprint(\"as one finished answer:\", shape, type(shape).__name__)\n","q":744},{"t":"md","id":"dd-q744-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `len(rows)` counts the inner lists — the rows of the table.\n2. `len(rows[0])` reaches into the first row and counts what IT holds. Two\n   levels, two separate measurements.\n3. The pair is packed into a tuple because it is one finished answer with two\n   parts, which is exactly the job a tuple is for.\n","q":744},{"t":"md","id":"dd-q744","role":"problem","src":"<!-- dd:dd-q744 -->\n\n### Problem 744 · faded — your turn\n\nThe trailing comma is what makes the tuple.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n('int', 'tuple')\n```\n","q":744},{"t":"code","id":"dd-q744-code","role":"code","src":"def solve(x):\n    \"\"\"(x) is just x; (x,) is a tuple holding it.\"\"\"\n    return (type((x)).__name__, type(_____).__name__)\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (5,)\nprint(solve(*example))\n","q":744},{"t":"code","id":"dd-q744-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(744)\n","q":744},{"t":"code","id":"dd-q744-solution","role":"solution","src":"#@title 💡 Solution — Problem 744\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(x):\n    \"\"\"(x) is just x; (x,) is a tuple holding it.\"\"\"\n    return (type((x)).__name__, type((x,)).__name__)\n\n\nexample = (5,)\nprint(solve(*example))\n","q":744},{"t":"md","id":"dd-q745-worked","role":"prose","src":"A table as a list of lists, measured at both levels, then frozen into a tuple.\n","q":745},{"t":"code","id":"dd-q745-worked-code","role":"code","src":"rows = [[1, 2, 3], [4, 5, 6]]\n\nn_rows = len(rows)\nn_cols = len(rows[0])\n\nprint(\"rows:\", n_rows)\nprint(\"columns:\", n_cols)\n\nshape = (n_rows, n_cols)\nprint(\"as one finished answer:\", shape, type(shape).__name__)\n","q":745},{"t":"md","id":"dd-q745-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `len(rows)` counts the inner lists — the rows of the table.\n2. `len(rows[0])` reaches into the first row and counts what IT holds. Two\n   levels, two separate measurements.\n3. The pair is packed into a tuple because it is one finished answer with two\n   parts, which is exactly the job a tuple is for.\n","q":745},{"t":"md","id":"dd-q745","role":"problem","src":"<!-- dd:dd-q745 -->\n\n### Problem 745 · faded — your turn\n\nA table is a list whose items are lists.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n([[1, 2], [3, 4]], 2)\n```\n","q":745},{"t":"code","id":"dd-q745-code","role":"code","src":"def solve(a, b, c, d):\n    \"\"\"A table is a list whose items are lists.\"\"\"\n    table = [_____, _____]\n    return (table, len(table))\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (1, 2, 3, 4)\nprint(solve(*example))\n","q":745},{"t":"code","id":"dd-q745-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(745)\n","q":745},{"t":"code","id":"dd-q745-solution","role":"solution","src":"#@title 💡 Solution — Problem 745\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(a, b, c, d):\n    \"\"\"A table is a list whose items are lists.\"\"\"\n    table = [[a, b], [c, d]]\n    return (table, len(table))\n\n\nexample = (1, 2, 3, 4)\nprint(solve(*example))\n","q":745},{"t":"md","id":"dd-q746-worked","role":"prose","src":"A table as a list of lists, measured at both levels, then frozen into a tuple.\n","q":746},{"t":"code","id":"dd-q746-worked-code","role":"code","src":"rows = [[1, 2, 3], [4, 5, 6]]\n\nn_rows = len(rows)\nn_cols = len(rows[0])\n\nprint(\"rows:\", n_rows)\nprint(\"columns:\", n_cols)\n\nshape = (n_rows, n_cols)\nprint(\"as one finished answer:\", shape, type(shape).__name__)\n","q":746},{"t":"md","id":"dd-q746-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `len(rows)` counts the inner lists — the rows of the table.\n2. `len(rows[0])` reaches into the first row and counts what IT holds. Two\n   levels, two separate measurements.\n3. The pair is packed into a tuple because it is one finished answer with two\n   parts, which is exactly the job a tuple is for.\n","q":746},{"t":"md","id":"dd-q746","role":"problem","src":"<!-- dd:dd-q746 -->\n\n### Problem 746 · faded — your turn\n\nWrap the whole list as one item, then measure both containers.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(3, 1)\n```\n","q":746},{"t":"code","id":"dd-q746-code","role":"code","src":"def solve(items):\n    \"\"\"A list holding one thing — which happens to be a list.\"\"\"\n    outer = _____\n    return (len(items), len(outer))\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([1, 2, 3],)\nprint(solve(*example))\n","q":746},{"t":"code","id":"dd-q746-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(746)\n","q":746},{"t":"code","id":"dd-q746-solution","role":"solution","src":"#@title 💡 Solution — Problem 746\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(items):\n    \"\"\"A list holding one thing — which happens to be a list.\"\"\"\n    outer = [items]\n    return (len(items), len(outer))\n\n\nexample = ([1, 2, 3],)\nprint(solve(*example))\n","q":746},{"t":"md","id":"dd-q747-worked","role":"prose","src":"A table as a list of lists, measured at both levels, then frozen into a tuple.\n","q":747},{"t":"code","id":"dd-q747-worked-code","role":"code","src":"rows = [[1, 2, 3], [4, 5, 6]]\n\nn_rows = len(rows)\nn_cols = len(rows[0])\n\nprint(\"rows:\", n_rows)\nprint(\"columns:\", n_cols)\n\nshape = (n_rows, n_cols)\nprint(\"as one finished answer:\", shape, type(shape).__name__)\n","q":747},{"t":"md","id":"dd-q747-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `len(rows)` counts the inner lists — the rows of the table.\n2. `len(rows[0])` reaches into the first row and counts what IT holds. Two\n   levels, two separate measurements.\n3. The pair is packed into a tuple because it is one finished answer with two\n   parts, which is exactly the job a tuple is for.\n","q":747},{"t":"md","id":"dd-q747","role":"problem","src":"<!-- dd:dd-q747 -->\n\n### Problem 747 · faded — your turn\n\nRehouse the items in a tuple; the count comes along unchanged.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n((1, 2, 3), 3)\n```\n","q":747},{"t":"code","id":"dd-q747-code","role":"code","src":"def solve(values):\n    \"\"\"tuple() rehouses the items; the count comes along unchanged.\"\"\"\n    t = _____(values)\n    return (t, len(t))\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([1, 2, 3],)\nprint(solve(*example))\n","q":747},{"t":"code","id":"dd-q747-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(747)\n","q":747},{"t":"code","id":"dd-q747-solution","role":"solution","src":"#@title 💡 Solution — Problem 747\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(values):\n    \"\"\"tuple() rehouses the items; the count comes along unchanged.\"\"\"\n    t = tuple(values)\n    return (t, len(t))\n\n\nexample = ([1, 2, 3],)\nprint(solve(*example))\n","q":747},{"t":"md","id":"dd-q748-worked","role":"prose","src":"A table as a list of lists, measured at both levels, then frozen into a tuple.\n","q":748},{"t":"code","id":"dd-q748-worked-code","role":"code","src":"rows = [[1, 2, 3], [4, 5, 6]]\n\nn_rows = len(rows)\nn_cols = len(rows[0])\n\nprint(\"rows:\", n_rows)\nprint(\"columns:\", n_cols)\n\nshape = (n_rows, n_cols)\nprint(\"as one finished answer:\", shape, type(shape).__name__)\n","q":748},{"t":"md","id":"dd-q748-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `len(rows)` counts the inner lists — the rows of the table.\n2. `len(rows[0])` reaches into the first row and counts what IT holds. Two\n   levels, two separate measurements.\n3. The pair is packed into a tuple because it is one finished answer with two\n   parts, which is exactly the job a tuple is for.\n","q":748},{"t":"md","id":"dd-q748","role":"problem","src":"<!-- dd:dd-q748 -->\n\n### Problem 748 · faded — your turn\n\nThaw a tuple into a list, and prove it with the type name.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n([3, 4], 'list')\n```\n","q":748},{"t":"code","id":"dd-q748-code","role":"code","src":"def solve(pair):\n    \"\"\"list() thaws a tuple into a list you could change.\"\"\"\n    lst = _____(pair)\n    return (lst, type(lst).__name__)\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ((3, 4),)\nprint(solve(*example))\n","q":748},{"t":"code","id":"dd-q748-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(748)\n","q":748},{"t":"code","id":"dd-q748-solution","role":"solution","src":"#@title 💡 Solution — Problem 748\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(pair):\n    \"\"\"list() thaws a tuple into a list you could change.\"\"\"\n    lst = list(pair)\n    return (lst, type(lst).__name__)\n\n\nexample = ((3, 4),)\nprint(solve(*example))\n","q":748},{"t":"md","id":"dd-q749-worked","role":"prose","src":"A table as a list of lists, measured at both levels, then frozen into a tuple.\n","q":749},{"t":"code","id":"dd-q749-worked-code","role":"code","src":"rows = [[1, 2, 3], [4, 5, 6]]\n\nn_rows = len(rows)\nn_cols = len(rows[0])\n\nprint(\"rows:\", n_rows)\nprint(\"columns:\", n_cols)\n\nshape = (n_rows, n_cols)\nprint(\"as one finished answer:\", shape, type(shape).__name__)\n","q":749},{"t":"md","id":"dd-q749-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `len(rows)` counts the inner lists — the rows of the table.\n2. `len(rows[0])` reaches into the first row and counts what IT holds. Two\n   levels, two separate measurements.\n3. The pair is packed into a tuple because it is one finished answer with two\n   parts, which is exactly the job a tuple is for.\n","q":749},{"t":"md","id":"dd-q749","role":"problem","src":"<!-- dd:dd-q749 -->\n\n### Problem 749 · faded — your turn\n\nSame size is a question for len, answered twice.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\nTrue\n```\n","q":749},{"t":"code","id":"dd-q749-code","role":"code","src":"def solve(a, b):\n    \"\"\"Same size is a question for len, answered twice.\"\"\"\n    return _____(a) == _____(b)\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([1, 2], [3, 4])\nprint(solve(*example))\n","q":749},{"t":"code","id":"dd-q749-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(749)\n","q":749},{"t":"code","id":"dd-q749-solution","role":"solution","src":"#@title 💡 Solution — Problem 749\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(a, b):\n    \"\"\"Same size is a question for len, answered twice.\"\"\"\n    return len(a) == len(b)\n\n\nexample = ([1, 2], [3, 4])\nprint(solve(*example))\n","q":749},{"t":"md","id":"dd-q750-worked","role":"prose","src":"A table as a list of lists, measured at both levels, then frozen into a tuple.\n","q":750},{"t":"code","id":"dd-q750-worked-code","role":"code","src":"rows = [[1, 2, 3], [4, 5, 6]]\n\nn_rows = len(rows)\nn_cols = len(rows[0])\n\nprint(\"rows:\", n_rows)\nprint(\"columns:\", n_cols)\n\nshape = (n_rows, n_cols)\nprint(\"as one finished answer:\", shape, type(shape).__name__)\n","q":750},{"t":"md","id":"dd-q750-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `len(rows)` counts the inner lists — the rows of the table.\n2. `len(rows[0])` reaches into the first row and counts what IT holds. Two\n   levels, two separate measurements.\n3. The pair is packed into a tuple because it is one finished answer with two\n   parts, which is exactly the job a tuple is for.\n","q":750},{"t":"md","id":"dd-q750","role":"problem","src":"<!-- dd:dd-q750 -->\n\n### Problem 750 · faded — your turn\n\nSame values, different brackets, different type.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n('list', 'tuple')\n```\n","q":750},{"t":"code","id":"dd-q750-code","role":"code","src":"def solve(a, b):\n    \"\"\"Same values, different brackets, different type.\"\"\"\n    return (type(_____).__name__, type(_____).__name__)\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (1, 2)\nprint(solve(*example))\n","q":750},{"t":"code","id":"dd-q750-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(750)\n","q":750},{"t":"code","id":"dd-q750-solution","role":"solution","src":"#@title 💡 Solution — Problem 750\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(a, b):\n    \"\"\"Same values, different brackets, different type.\"\"\"\n    return (type([a, b]).__name__, type((a, b)).__name__)\n\n\nexample = (1, 2)\nprint(solve(*example))\n","q":750},{"t":"md","id":"dd-q751-worked","role":"prose","src":"A table as a list of lists, measured at both levels, then frozen into a tuple.\n","q":751},{"t":"code","id":"dd-q751-worked-code","role":"code","src":"rows = [[1, 2, 3], [4, 5, 6]]\n\nn_rows = len(rows)\nn_cols = len(rows[0])\n\nprint(\"rows:\", n_rows)\nprint(\"columns:\", n_cols)\n\nshape = (n_rows, n_cols)\nprint(\"as one finished answer:\", shape, type(shape).__name__)\n","q":751},{"t":"md","id":"dd-q751-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `len(rows)` counts the inner lists — the rows of the table.\n2. `len(rows[0])` reaches into the first row and counts what IT holds. Two\n   levels, two separate measurements.\n3. The pair is packed into a tuple because it is one finished answer with two\n   parts, which is exactly the job a tuple is for.\n","q":751},{"t":"md","id":"dd-q751","role":"problem","src":"<!-- dd:dd-q751 -->\n\n### Problem 751 · faded — your turn\n\nBuild as a list, hand back as a tuple.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n((1, 2, 3), 3)\n```\n","q":751},{"t":"code","id":"dd-q751-code","role":"code","src":"def solve(a, b, c):\n    \"\"\"Build as a list, hand back as a tuple.\"\"\"\n    built = [a, b, c]\n    frozen = _____(built)\n    return (frozen, len(frozen))\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (1, 2, 3)\nprint(solve(*example))\n","q":751},{"t":"code","id":"dd-q751-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(751)\n","q":751},{"t":"code","id":"dd-q751-solution","role":"solution","src":"#@title 💡 Solution — Problem 751\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(a, b, c):\n    \"\"\"Build as a list, hand back as a tuple.\"\"\"\n    built = [a, b, c]\n    frozen = tuple(built)\n    return (frozen, len(frozen))\n\n\nexample = (1, 2, 3)\nprint(solve(*example))\n","q":751},{"t":"md","id":"dd-q582","role":"problem","src":"<!-- dd:dd-q582 -->\n\n### Problem 582 · independent\n\nWrite a function solve(rows) that takes a list whose items are themselves lists (all the same length) and returns the tuple (n_rows, n_cols): how many inner lists there are, and how many items the FIRST inner list holds.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(2, 3)\n```\n","q":582},{"t":"code","id":"dd-q582-code","role":"code","src":"def solve(rows):\n    \"\"\"Outer length, then inner length.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([[1, 2, 3], [4, 5, 6]],)\nprint(solve(*example))\n","q":582},{"t":"code","id":"dd-q582-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(582)\n","q":582},{"t":"code","id":"dd-q582-solution","role":"solution","src":"#@title 💡 Solution — Problem 582\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(rows):\n    \"\"\"Outer length, then inner length.\"\"\"\n    return (len(rows), len(rows[0]))\n\n\nexample = ([[1, 2, 3], [4, 5, 6]],)\nprint(solve(*example))\n","q":582},{"t":"md","id":"dd-q583","role":"problem","src":"<!-- dd:dd-q583 -->\n\n### Problem 583 · independent\n\nWrite a function solve(items) that takes a list and returns the tuple (as_tuple, back_to_list, same_length): the list converted to a tuple, that tuple converted back to a list, and whether the two lengths agree.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n((1, 2, 3), [1, 2, 3], True)\n```\n","q":583},{"t":"code","id":"dd-q583-code","role":"code","src":"def solve(items):\n    \"\"\"A round trip between the two sequence types.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([1, 2, 3],)\nprint(solve(*example))\n","q":583},{"t":"code","id":"dd-q583-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(583)\n","q":583},{"t":"code","id":"dd-q583-solution","role":"solution","src":"#@title 💡 Solution — Problem 583\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(items):\n    \"\"\"A round trip between the two sequence types.\"\"\"\n    as_tuple = tuple(items)\n    back = list(as_tuple)\n    return (as_tuple, back, len(items) == len(back))\n\n\nexample = ([1, 2, 3],)\nprint(solve(*example))\n","q":583},{"t":"md","id":"dd-q752","role":"problem","src":"<!-- dd:dd-q752 -->\n\n### Problem 752 · independent\n\nWrite a function solve(rows) that measures a table: name the pair (len(rows), len(rows[0])) `shape` — outer count first, then the length of the first row — and return the tuple (shape, kind_name) where kind_name is shape's type name.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n((2, 3), 'tuple')\n```\n","q":752},{"t":"code","id":"dd-q752-code","role":"code","src":"def solve(rows):\n    \"\"\"Two levels, two measurements, one finished answer.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([[1, 2, 3], [4, 5, 6]],)\nprint(solve(*example))\n","q":752},{"t":"code","id":"dd-q752-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(752)\n","q":752},{"t":"code","id":"dd-q752-solution","role":"solution","src":"#@title 💡 Solution — Problem 752\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(rows):\n    \"\"\"Two levels, two measurements, one finished answer.\"\"\"\n    shape = (len(rows), len(rows[0]))\n    return (shape, type(shape).__name__)\n\n\nexample = ([[1, 2, 3], [4, 5, 6]],)\nprint(solve(*example))\n","q":752},{"t":"md","id":"dd-q753","role":"problem","src":"<!-- dd:dd-q753 -->\n\n### Problem 753 · independent\n\nWrite a function solve(pair, items) that converts the tuple `pair` to a list and returns whether that converted list equals `items`, using ==.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\nTrue\n```\n","q":753},{"t":"code","id":"dd-q753-code","role":"code","src":"def solve(pair, items):\n    \"\"\"Convert first — then equality compares item by item.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ((1, 2), [1, 2])\nprint(solve(*example))\n","q":753},{"t":"code","id":"dd-q753-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(753)\n","q":753},{"t":"code","id":"dd-q753-solution","role":"solution","src":"#@title 💡 Solution — Problem 753\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(pair, items):\n    \"\"\"Convert first — then equality compares item by item.\"\"\"\n    return list(pair) == items\n\n\nexample = ((1, 2), [1, 2])\nprint(solve(*example))\n","q":753},{"t":"md","id":"dd-q754","role":"problem","src":"<!-- dd:dd-q754 -->\n\n### Problem 754 · independent\n\nWrite a function solve(a, b, c) that builds `short` as the one-item list [a], `long` as [a, b, c], nests them as `table = [short, long]`, and returns the tuple (table, (len(short), len(long))).\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n([[1], [1, 2, 3]], (1, 3))\n```\n","q":754},{"t":"code","id":"dd-q754-code","role":"code","src":"def solve(a, b, c):\n    \"\"\"Rows of different lengths are still one item each.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (1, 2, 3)\nprint(solve(*example))\n","q":754},{"t":"code","id":"dd-q754-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(754)\n","q":754},{"t":"code","id":"dd-q754-solution","role":"solution","src":"#@title 💡 Solution — Problem 754\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(a, b, c):\n    \"\"\"Rows of different lengths are still one item each.\"\"\"\n    short = [a]\n    long = [a, b, c]\n    table = [short, long]\n    return (table, (len(short), len(long)))\n\n\nexample = (1, 2, 3)\nprint(solve(*example))\n","q":754},{"t":"md","id":"dd-q755","role":"problem","src":"<!-- dd:dd-q755 -->\n\n### Problem 755 · independent\n\nWrite a function solve(n) that names (n,) `single` and (n) `plain`, and returns the tuple of three answers: single's type name, plain's type name, and len(single).\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n('tuple', 'int', 1)\n```\n","q":755},{"t":"code","id":"dd-q755-code","role":"code","src":"def solve(n):\n    \"\"\"One comma is the whole difference.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (7,)\nprint(solve(*example))\n","q":755},{"t":"code","id":"dd-q755-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(755)\n","q":755},{"t":"code","id":"dd-q755-solution","role":"solution","src":"#@title 💡 Solution — Problem 755\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(n):\n    \"\"\"One comma is the whole difference.\"\"\"\n    single = (n,)\n    plain = (n)\n    return (type(single).__name__, type(plain).__name__, len(single))\n\n\nexample = (7,)\nprint(solve(*example))\n","q":755},{"t":"md","id":"dd-q756","role":"problem","src":"<!-- dd:dd-q756 -->\n\n### Problem 756 · independent\n\nWrite a function solve(items) that returns the tuple of three counts: len(items), len(tuple(items)), len(list(items)) — the original, and both conversions. Converting rehouses the items; it never changes how many there are.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(3, 3, 3)\n```\n","q":756},{"t":"code","id":"dd-q756-code","role":"code","src":"def solve(items):\n    \"\"\"Three containers, one count.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([1, 2, 3],)\nprint(solve(*example))\n","q":756},{"t":"code","id":"dd-q756-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(756)\n","q":756},{"t":"code","id":"dd-q756-solution","role":"solution","src":"#@title 💡 Solution — Problem 756\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(items):\n    \"\"\"Three containers, one count.\"\"\"\n    return (len(items), len(tuple(items)), len(list(items)))\n\n\nexample = ([1, 2, 3],)\nprint(solve(*example))\n","q":756},{"t":"md","id":"dd-q757","role":"problem","src":"<!-- dd:dd-q757 -->\n\n### Problem 757 · independent\n\nWrite a function solve(flag, text, number) that builds the list [flag, text, number] named `mixed`, and returns the tuple (len(mixed), kinds) where kinds is the tuple of the three items' type names, in the same order.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(3, ('bool', 'str', 'float'))\n```\n","q":757},{"t":"code","id":"dd-q757-code","role":"code","src":"def solve(flag, text, number):\n    \"\"\"A container does not convert its contents.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (True, 'hi', 2.5)\nprint(solve(*example))\n","q":757},{"t":"code","id":"dd-q757-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(757)\n","q":757},{"t":"code","id":"dd-q757-solution","role":"solution","src":"#@title 💡 Solution — Problem 757\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(flag, text, number):\n    \"\"\"A container does not convert its contents.\"\"\"\n    mixed = [flag, text, number]\n    kinds = (type(flag).__name__, type(text).__name__, type(number).__name__)\n    return (len(mixed), kinds)\n\n\nexample = (True, 'hi', 2.5)\nprint(solve(*example))\n","q":757},{"t":"md","id":"dd-q758","role":"problem","src":"<!-- dd:dd-q758 -->\n\n### Problem 758 · independent\n\nWrite a function solve(items) that returns the tuple (len(items), bool(items), len([items])): the count, whether the list counts as a yes, and the length of a new list holding items as its only element.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(0, False, 1)\n```\n","q":758},{"t":"code","id":"dd-q758-code","role":"code","src":"def solve(items):\n    \"\"\"An empty list is a no; a list HOLDING it is not empty.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([],)\nprint(solve(*example))\n","q":758},{"t":"code","id":"dd-q758-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(758)\n","q":758},{"t":"code","id":"dd-q758-solution","role":"solution","src":"#@title 💡 Solution — Problem 758\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(items):\n    \"\"\"An empty list is a no; a list HOLDING it is not empty.\"\"\"\n    return (len(items), bool(items), len([items]))\n\n\nexample = ([],)\nprint(solve(*example))\n","q":758},{"t":"md","id":"dd-q759","role":"problem","src":"<!-- dd:dd-q759 -->\n\n### Problem 759 · independent\n\nWrite a function solve(lst, t) that returns the tuple (list(t) == lst, tuple(lst) == t, lst == t): the two conversions compared, then the raw list-versus-tuple comparison. The container is part of the value.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(True, True, False)\n```\n","q":759},{"t":"code","id":"dd-q759-code","role":"code","src":"def solve(lst, t):\n    \"\"\"Equal contents still need equal containers.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([1, 2], (1, 2))\nprint(solve(*example))\n","q":759},{"t":"code","id":"dd-q759-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(759)\n","q":759},{"t":"code","id":"dd-q759-solution","role":"solution","src":"#@title 💡 Solution — Problem 759\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(lst, t):\n    \"\"\"Equal contents still need equal containers.\"\"\"\n    return (list(t) == lst, tuple(lst) == t, lst == t)\n\n\nexample = ([1, 2], (1, 2))\nprint(solve(*example))\n","q":759},{"t":"md","id":"python-lists-and-tuples-misconceptions","role":"prose","src":"#### Common mistakes\n\n- **\"A tuple is just a list that is written differently.\"** — It is a different\n  type, and it cannot be changed once built. A grader comparing a list against a\n  tuple reports them as unequal.\n- **\"`len` gives the total number of values.\"** — It gives the number of items at\n  the top level. For a list of lists that is the number of rows.\n- **\"Converting a list to a tuple changes the list.\"** — It builds a new\n  container. The original list is still there, unchanged.\n"},{"t":"md","id":"dd-kp-python-indexing","role":"prose","src":"<!-- dd:dd-kp-python-indexing -->\n\n## Indexing — pulling one item out, counting from zero\n\n`python.indexing`\n"},{"t":"md","id":"python-indexing-segment-concept","role":"prose","src":"Square brackets after a container mean **\"give me the item at this position\"**.\nPositions are counted from **zero**, so the first item is at 0 and the last is at\n`len(...) - 1`:\n"},{"t":"code","id":"python-indexing-segment-concept-code","role":"code","src":"letters = [\"a\", \"b\", \"c\"]\n\nprint(letters[0])\nprint(letters[1])\nprint(letters[2])\nprint(\"length is\", len(letters), \"so the last position is\", len(letters) - 1)\n"},{"t":"md","id":"python-indexing-segment-concept-1","role":"prose","src":"\n\nCounting from zero is the single most common source of early off-by-one errors,\nand it is worth saying out loud: `letters[1]` is the SECOND item.\n\nBecause \"the last one\" is needed constantly, negative positions count backwards\nfrom the end. `-1` is the last item, `-2` the one before it:\n"},{"t":"code","id":"python-indexing-segment-concept-code-1","role":"code","src":"letters = [\"a\", \"b\", \"c\", \"d\"]\n\nprint(letters[-1])\nprint(letters[-2])\n"},{"t":"md","id":"python-indexing-segment-concept-2","role":"prose","src":"\n\nNegative indexing is not just shorter than `letters[len(letters) - 1]` — it is\nsafer, because there is no length to get wrong.\n\nReaching into nested data is **one index per level**, read left to right. The\nfirst bracket picks the row; the second picks inside that row:\n"},{"t":"code","id":"python-indexing-segment-concept-code-2","role":"code","src":"rows = [[1, 2, 3], [4, 5, 6]]\n\nprint(rows[0])\nprint(rows[0][2])\nprint(rows[1][0])\n"},{"t":"md","id":"python-indexing-segment-concept-3","role":"prose","src":"\n\n`rows[0]` is itself a list, so `rows[0][2]` is \"the third item of the first\nrow\". The two indices are not interchangeable: `rows[2][0]` asks for a third row\nthat does not exist.\n\nThe two styles combine, which is how you name a corner of a table without\nmeasuring anything:\n"},{"t":"code","id":"python-indexing-segment-concept-code-3","role":"code","src":"rows = [[1, 2, 3], [4, 5, 6]]\n\nprint(\"top-left    :\", rows[0][0])\nprint(\"top-right   :\", rows[0][-1])\nprint(\"bottom-right:\", rows[-1][-1])\n"},{"t":"md","id":"python-indexing-segment-concept-4","role":"prose","src":"\n\nAsking for a position that does not exist stops the program with an\n`IndexError` rather than guessing:\n"},{"t":"code","id":"python-indexing-segment-concept-code-4","role":"code","src":"letters = [\"a\", \"b\"]\ntry:\n    print(letters[5])\nexcept IndexError as exc:\n    print(\"IndexError:\", exc)\n"},{"t":"md","id":"python-indexing-segment-watch","role":"prose","src":"> **Watch out.** - **The first item is at 0** — so the last valid position is one LESS than the\n  length. `items[len(items)]` is always one past the end.\n- **`-1` is the last item, not \"one before the start\"** — the negative side\n  starts at `-1`, there is no `-0`.\n- **The first index is the row** — `rows[i][j]` picks row `i`, then item `j`\n  inside it. Swapping them reads a different value, or fails.\n"},{"t":"md","id":"dd-q586-worked","role":"prose","src":"One row of a table, pulled out and then read from both ends.\n","q":586},{"t":"code","id":"dd-q586-worked-code","role":"code","src":"rows = [[10, 20, 30], [40, 50]]\n\nrow = rows[1]\n\nprint(\"the row itself :\", row)\nprint(\"how many items :\", len(row))\nprint(\"its first item :\", row[0])\nprint(\"its last item  :\", row[-1])\nprint(\"not the same as the last ROW:\", rows[-1])\n","q":586},{"t":"md","id":"dd-q586-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `rows[1]` names the second row, so the reads below are all one level down and\n   do not need a second bracket every time.\n2. `row[0]` and `row[-1]` are the two ends of THAT row.\n3. The last line is the trap in one line: `rows[-1]` is the last row of the\n   whole table, which is a different depth from `row[-1]`.\n","q":586},{"t":"md","id":"dd-q586","role":"problem","src":"<!-- dd:dd-q586 -->\n\n### Problem 586 · faded — your turn\n\nThe first item lives at position zero.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n10\n```\n","q":586},{"t":"code","id":"dd-q586-code","role":"code","src":"def solve(items):\n    \"\"\"First item — position zero.\"\"\"\n    return items[_____]\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([10, 20, 30],)\nprint(solve(*example))\n","q":586},{"t":"code","id":"dd-q586-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(586)\n","q":586},{"t":"code","id":"dd-q586-solution","role":"solution","src":"#@title 💡 Solution — Problem 586\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(items):\n    \"\"\"First item — position zero.\"\"\"\n    return items[0]\n\n\nexample = ([10, 20, 30],)\nprint(solve(*example))\n","q":586},{"t":"md","id":"dd-q587-worked","role":"prose","src":"One row of a table, pulled out and then read from both ends.\n","q":587},{"t":"code","id":"dd-q587-worked-code","role":"code","src":"rows = [[10, 20, 30], [40, 50]]\n\nrow = rows[1]\n\nprint(\"the row itself :\", row)\nprint(\"how many items :\", len(row))\nprint(\"its first item :\", row[0])\nprint(\"its last item  :\", row[-1])\nprint(\"not the same as the last ROW:\", rows[-1])\n","q":587},{"t":"md","id":"dd-q587-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `rows[1]` names the second row, so the reads below are all one level down and\n   do not need a second bracket every time.\n2. `row[0]` and `row[-1]` are the two ends of THAT row.\n3. The last line is the trap in one line: `rows[-1]` is the last row of the\n   whole table, which is a different depth from `row[-1]`.\n","q":587},{"t":"md","id":"dd-q587","role":"problem","src":"<!-- dd:dd-q587 -->\n\n### Problem 587 · faded — your turn\n\nThe last item, counted back from the end rather than measured.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n30\n```\n","q":587},{"t":"code","id":"dd-q587-code","role":"code","src":"def solve(items):\n    \"\"\"Last item — count back from the end.\"\"\"\n    return items[_____]\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([10, 20, 30],)\nprint(solve(*example))\n","q":587},{"t":"code","id":"dd-q587-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(587)\n","q":587},{"t":"code","id":"dd-q587-solution","role":"solution","src":"#@title 💡 Solution — Problem 587\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(items):\n    \"\"\"Last item — count back from the end.\"\"\"\n    return items[-1]\n\n\nexample = ([10, 20, 30],)\nprint(solve(*example))\n","q":587},{"t":"md","id":"dd-q762-worked","role":"prose","src":"One row of a table, pulled out and then read from both ends.\n","q":762},{"t":"code","id":"dd-q762-worked-code","role":"code","src":"rows = [[10, 20, 30], [40, 50]]\n\nrow = rows[1]\n\nprint(\"the row itself :\", row)\nprint(\"how many items :\", len(row))\nprint(\"its first item :\", row[0])\nprint(\"its last item  :\", row[-1])\nprint(\"not the same as the last ROW:\", rows[-1])\n","q":762},{"t":"md","id":"dd-q762-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `rows[1]` names the second row, so the reads below are all one level down and\n   do not need a second bracket every time.\n2. `row[0]` and `row[-1]` are the two ends of THAT row.\n3. The last line is the trap in one line: `rows[-1]` is the last row of the\n   whole table, which is a different depth from `row[-1]`.\n","q":762},{"t":"md","id":"dd-q762","role":"problem","src":"<!-- dd:dd-q762 -->\n\n### Problem 762 · faded — your turn\n\nCounting starts at zero — the second item is at position 1.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(20, 30)\n```\n","q":762},{"t":"code","id":"dd-q762-code","role":"code","src":"def solve(items):\n    \"\"\"items[1] is the SECOND item.\"\"\"\n    return (items[_____], items[_____])\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([10, 20, 30, 40],)\nprint(solve(*example))\n","q":762},{"t":"code","id":"dd-q762-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(762)\n","q":762},{"t":"code","id":"dd-q762-solution","role":"solution","src":"#@title 💡 Solution — Problem 762\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(items):\n    \"\"\"items[1] is the SECOND item.\"\"\"\n    return (items[1], items[2])\n\n\nexample = ([10, 20, 30, 40],)\nprint(solve(*example))\n","q":762},{"t":"md","id":"dd-q763-worked","role":"prose","src":"One row of a table, pulled out and then read from both ends.\n","q":763},{"t":"code","id":"dd-q763-worked-code","role":"code","src":"rows = [[10, 20, 30], [40, 50]]\n\nrow = rows[1]\n\nprint(\"the row itself :\", row)\nprint(\"how many items :\", len(row))\nprint(\"its first item :\", row[0])\nprint(\"its last item  :\", row[-1])\nprint(\"not the same as the last ROW:\", rows[-1])\n","q":763},{"t":"md","id":"dd-q763-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `rows[1]` names the second row, so the reads below are all one level down and\n   do not need a second bracket every time.\n2. `row[0]` and `row[-1]` are the two ends of THAT row.\n3. The last line is the trap in one line: `rows[-1]` is the last row of the\n   whole table, which is a different depth from `row[-1]`.\n","q":763},{"t":"md","id":"dd-q763","role":"problem","src":"<!-- dd:dd-q763 -->\n\n### Problem 763 · faded — your turn\n\nThe last valid position is one LESS than the length.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n7\n```\n","q":763},{"t":"code","id":"dd-q763-code","role":"code","src":"def solve(items):\n    \"\"\"The last position is len minus one.\"\"\"\n    last = items[len(items) - _____]\n    return last\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([5, 6, 7],)\nprint(solve(*example))\n","q":763},{"t":"code","id":"dd-q763-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(763)\n","q":763},{"t":"code","id":"dd-q763-solution","role":"solution","src":"#@title 💡 Solution — Problem 763\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(items):\n    \"\"\"The last position is len minus one.\"\"\"\n    last = items[len(items) - 1]\n    return last\n\n\nexample = ([5, 6, 7],)\nprint(solve(*example))\n","q":763},{"t":"md","id":"dd-q764-worked","role":"prose","src":"One row of a table, pulled out and then read from both ends.\n","q":764},{"t":"code","id":"dd-q764-worked-code","role":"code","src":"rows = [[10, 20, 30], [40, 50]]\n\nrow = rows[1]\n\nprint(\"the row itself :\", row)\nprint(\"how many items :\", len(row))\nprint(\"its first item :\", row[0])\nprint(\"its last item  :\", row[-1])\nprint(\"not the same as the last ROW:\", rows[-1])\n","q":764},{"t":"md","id":"dd-q764-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `rows[1]` names the second row, so the reads below are all one level down and\n   do not need a second bracket every time.\n2. `row[0]` and `row[-1]` are the two ends of THAT row.\n3. The last line is the trap in one line: `rows[-1]` is the last row of the\n   whole table, which is a different depth from `row[-1]`.\n","q":764},{"t":"md","id":"dd-q764","role":"problem","src":"<!-- dd:dd-q764 -->\n\n### Problem 764 · faded — your turn\n\nRead one end from zero, the other from minus one, and compare.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\nTrue\n```\n","q":764},{"t":"code","id":"dd-q764-code","role":"code","src":"def solve(items):\n    \"\"\"Do the two ends match?\"\"\"\n    return items[_____] == items[_____]\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([3, 1, 3],)\nprint(solve(*example))\n","q":764},{"t":"code","id":"dd-q764-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(764)\n","q":764},{"t":"code","id":"dd-q764-solution","role":"solution","src":"#@title 💡 Solution — Problem 764\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(items):\n    \"\"\"Do the two ends match?\"\"\"\n    return items[0] == items[-1]\n\n\nexample = ([3, 1, 3],)\nprint(solve(*example))\n","q":764},{"t":"md","id":"dd-q765-worked","role":"prose","src":"One row of a table, pulled out and then read from both ends.\n","q":765},{"t":"code","id":"dd-q765-worked-code","role":"code","src":"rows = [[10, 20, 30], [40, 50]]\n\nrow = rows[1]\n\nprint(\"the row itself :\", row)\nprint(\"how many items :\", len(row))\nprint(\"its first item :\", row[0])\nprint(\"its last item  :\", row[-1])\nprint(\"not the same as the last ROW:\", rows[-1])\n","q":765},{"t":"md","id":"dd-q765-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `rows[1]` names the second row, so the reads below are all one level down and\n   do not need a second bracket every time.\n2. `row[0]` and `row[-1]` are the two ends of THAT row.\n3. The last line is the trap in one line: `rows[-1]` is the last row of the\n   whole table, which is a different depth from `row[-1]`.\n","q":765},{"t":"md","id":"dd-q765","role":"problem","src":"<!-- dd:dd-q765 -->\n\n### Problem 765 · faded — your turn\n\nThe first bracket picks the row; the second reads inside it.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n2\n```\n","q":765},{"t":"code","id":"dd-q765-code","role":"code","src":"def solve(rows):\n    \"\"\"Pick the row, then read inside it.\"\"\"\n    row = rows[_____]\n    return row[_____]\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([[1, 2], [3, 4]],)\nprint(solve(*example))\n","q":765},{"t":"code","id":"dd-q765-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(765)\n","q":765},{"t":"code","id":"dd-q765-solution","role":"solution","src":"#@title 💡 Solution — Problem 765\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(rows):\n    \"\"\"Pick the row, then read inside it.\"\"\"\n    row = rows[0]\n    return row[-1]\n\n\nexample = ([[1, 2], [3, 4]],)\nprint(solve(*example))\n","q":765},{"t":"md","id":"dd-q766-worked","role":"prose","src":"One row of a table, pulled out and then read from both ends.\n","q":766},{"t":"code","id":"dd-q766-worked-code","role":"code","src":"rows = [[10, 20, 30], [40, 50]]\n\nrow = rows[1]\n\nprint(\"the row itself :\", row)\nprint(\"how many items :\", len(row))\nprint(\"its first item :\", row[0])\nprint(\"its last item  :\", row[-1])\nprint(\"not the same as the last ROW:\", rows[-1])\n","q":766},{"t":"md","id":"dd-q766-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `rows[1]` names the second row, so the reads below are all one level down and\n   do not need a second bracket every time.\n2. `row[0]` and `row[-1]` are the two ends of THAT row.\n3. The last line is the trap in one line: `rows[-1]` is the last row of the\n   whole table, which is a different depth from `row[-1]`.\n","q":766},{"t":"md","id":"dd-q766","role":"problem","src":"<!-- dd:dd-q766 -->\n\n### Problem 766 · faded — your turn\n\nBottom-left: the last row first, then its first item.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n3\n```\n","q":766},{"t":"code","id":"dd-q766-code","role":"code","src":"def solve(rows):\n    \"\"\"Last row first, then its first item.\"\"\"\n    return rows[_____][_____]\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([[1, 2], [3, 4]],)\nprint(solve(*example))\n","q":766},{"t":"code","id":"dd-q766-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(766)\n","q":766},{"t":"code","id":"dd-q766-solution","role":"solution","src":"#@title 💡 Solution — Problem 766\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(rows):\n    \"\"\"Last row first, then its first item.\"\"\"\n    return rows[-1][0]\n\n\nexample = ([[1, 2], [3, 4]],)\nprint(solve(*example))\n","q":766},{"t":"md","id":"dd-q767-worked","role":"prose","src":"One row of a table, pulled out and then read from both ends.\n","q":767},{"t":"code","id":"dd-q767-worked-code","role":"code","src":"rows = [[10, 20, 30], [40, 50]]\n\nrow = rows[1]\n\nprint(\"the row itself :\", row)\nprint(\"how many items :\", len(row))\nprint(\"its first item :\", row[0])\nprint(\"its last item  :\", row[-1])\nprint(\"not the same as the last ROW:\", rows[-1])\n","q":767},{"t":"md","id":"dd-q767-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `rows[1]` names the second row, so the reads below are all one level down and\n   do not need a second bracket every time.\n2. `row[0]` and `row[-1]` are the two ends of THAT row.\n3. The last line is the trap in one line: `rows[-1]` is the last row of the\n   whole table, which is a different depth from `row[-1]`.\n","q":767},{"t":"md","id":"dd-q767","role":"problem","src":"<!-- dd:dd-q767 -->\n\n### Problem 767 · faded — your turn\n\nTuples index exactly like lists — and stay tuples.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(3, 4, 'tuple')\n```\n","q":767},{"t":"code","id":"dd-q767-code","role":"code","src":"def solve(point):\n    \"\"\"Tuples index exactly like lists.\"\"\"\n    return (point[_____], point[_____], type(point).__name__)\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ((3, 4),)\nprint(solve(*example))\n","q":767},{"t":"code","id":"dd-q767-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(767)\n","q":767},{"t":"code","id":"dd-q767-solution","role":"solution","src":"#@title 💡 Solution — Problem 767\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(point):\n    \"\"\"Tuples index exactly like lists.\"\"\"\n    return (point[0], point[1], type(point).__name__)\n\n\nexample = ((3, 4),)\nprint(solve(*example))\n","q":767},{"t":"md","id":"dd-q768-worked","role":"prose","src":"One row of a table, pulled out and then read from both ends.\n","q":768},{"t":"code","id":"dd-q768-worked-code","role":"code","src":"rows = [[10, 20, 30], [40, 50]]\n\nrow = rows[1]\n\nprint(\"the row itself :\", row)\nprint(\"how many items :\", len(row))\nprint(\"its first item :\", row[0])\nprint(\"its last item  :\", row[-1])\nprint(\"not the same as the last ROW:\", rows[-1])\n","q":768},{"t":"md","id":"dd-q768-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `rows[1]` names the second row, so the reads below are all one level down and\n   do not need a second bracket every time.\n2. `row[0]` and `row[-1]` are the two ends of THAT row.\n3. The last line is the trap in one line: `rows[-1]` is the last row of the\n   whole table, which is a different depth from `row[-1]`.\n","q":768},{"t":"md","id":"dd-q768","role":"problem","src":"<!-- dd:dd-q768 -->\n\n### Problem 768 · faded — your turn\n\nOne step further back than len minus one.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n3\n```\n","q":768},{"t":"code","id":"dd-q768-code","role":"code","src":"def solve(items):\n    \"\"\"One step further back than len minus one.\"\"\"\n    mid = len(items) - _____\n    return items[mid]\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([1, 2, 3, 4],)\nprint(solve(*example))\n","q":768},{"t":"code","id":"dd-q768-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(768)\n","q":768},{"t":"code","id":"dd-q768-solution","role":"solution","src":"#@title 💡 Solution — Problem 768\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(items):\n    \"\"\"One step further back than len minus one.\"\"\"\n    mid = len(items) - 2\n    return items[mid]\n\n\nexample = ([1, 2, 3, 4],)\nprint(solve(*example))\n","q":768},{"t":"md","id":"dd-q769-worked","role":"prose","src":"One row of a table, pulled out and then read from both ends.\n","q":769},{"t":"code","id":"dd-q769-worked-code","role":"code","src":"rows = [[10, 20, 30], [40, 50]]\n\nrow = rows[1]\n\nprint(\"the row itself :\", row)\nprint(\"how many items :\", len(row))\nprint(\"its first item :\", row[0])\nprint(\"its last item  :\", row[-1])\nprint(\"not the same as the last ROW:\", rows[-1])\n","q":769},{"t":"md","id":"dd-q769-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `rows[1]` names the second row, so the reads below are all one level down and\n   do not need a second bracket every time.\n2. `row[0]` and `row[-1]` are the two ends of THAT row.\n3. The last line is the trap in one line: `rows[-1]` is the last row of the\n   whole table, which is a different depth from `row[-1]`.\n","q":769},{"t":"md","id":"dd-q769","role":"problem","src":"<!-- dd:dd-q769 -->\n\n### Problem 769 · faded — your turn\n\n-1 and len-1 are two roads to the same item.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(3, 3)\n```\n","q":769},{"t":"code","id":"dd-q769-code","role":"code","src":"def solve(items):\n    \"\"\"-1 and len-1 name the same position.\"\"\"\n    return (items[_____], items[len(items) - 1])\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([1, 2, 3],)\nprint(solve(*example))\n","q":769},{"t":"code","id":"dd-q769-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(769)\n","q":769},{"t":"code","id":"dd-q769-solution","role":"solution","src":"#@title 💡 Solution — Problem 769\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(items):\n    \"\"\"-1 and len-1 name the same position.\"\"\"\n    return (items[-1], items[len(items) - 1])\n\n\nexample = ([1, 2, 3],)\nprint(solve(*example))\n","q":769},{"t":"md","id":"dd-q588","role":"problem","src":"<!-- dd:dd-q588 -->\n\n### Problem 588 · independent\n\nWrite a function solve(rows, i, j) that takes a list of inner lists and two positions, and returns the single value at row i, column j. Reading a nested structure is one index per level, left to right.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n2\n```\n","q":588},{"t":"code","id":"dd-q588-code","role":"code","src":"def solve(rows, i, j):\n    \"\"\"One index per level: pick the row, then the item.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([[1, 2], [3, 4]], 0, 1)\nprint(solve(*example))\n","q":588},{"t":"code","id":"dd-q588-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(588)\n","q":588},{"t":"code","id":"dd-q588-solution","role":"solution","src":"#@title 💡 Solution — Problem 588\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(rows, i, j):\n    \"\"\"One index per level: pick the row, then the item.\"\"\"\n    return rows[i][j]\n\n\nexample = ([[1, 2], [3, 4]], 0, 1)\nprint(solve(*example))\n","q":588},{"t":"md","id":"dd-q589","role":"problem","src":"<!-- dd:dd-q589 -->\n\n### Problem 589 · independent\n\nWrite a function solve(items) that returns the tuple (first, last, second_from_end) for a list of at least two items, using a negative position for the last two.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(1, 3, 2)\n```\n","q":589},{"t":"code","id":"dd-q589-code","role":"code","src":"def solve(items):\n    \"\"\"Front from zero, back from minus one.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([1, 2, 3],)\nprint(solve(*example))\n","q":589},{"t":"code","id":"dd-q589-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(589)\n","q":589},{"t":"code","id":"dd-q589-solution","role":"solution","src":"#@title 💡 Solution — Problem 589\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(items):\n    \"\"\"Front from zero, back from minus one.\"\"\"\n    return (items[0], items[-1], items[-2])\n\n\nexample = ([1, 2, 3],)\nprint(solve(*example))\n","q":589},{"t":"md","id":"dd-q770","role":"problem","src":"<!-- dd:dd-q770 -->\n\n### Problem 770 · independent\n\nWrite a function solve(rows) that returns the tuple (rows[0][0], rows[-1][0]) — the first item of the first row, and the first item of the last row. The FIRST bracket decides which row.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(1, 5)\n```\n","q":770},{"t":"code","id":"dd-q770-code","role":"code","src":"def solve(rows):\n    \"\"\"Same column, opposite rows.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([[1, 2], [3, 4], [5, 6]],)\nprint(solve(*example))\n","q":770},{"t":"code","id":"dd-q770-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(770)\n","q":770},{"t":"code","id":"dd-q770-solution","role":"solution","src":"#@title 💡 Solution — Problem 770\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(rows):\n    \"\"\"Same column, opposite rows.\"\"\"\n    return (rows[0][0], rows[-1][0])\n\n\nexample = ([[1, 2], [3, 4], [5, 6]],)\nprint(solve(*example))\n","q":770},{"t":"md","id":"dd-q771","role":"problem","src":"<!-- dd:dd-q771 -->\n\n### Problem 771 · independent\n\nWrite a function solve(items) that returns the tuple (items[1], items[-2]) — the inner neighbours of the two ends: one step in from the front, one step in from the back.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(2, 3)\n```\n","q":771},{"t":"code","id":"dd-q771-code","role":"code","src":"def solve(items):\n    \"\"\"One step in from each end.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([1, 2, 3, 4],)\nprint(solve(*example))\n","q":771},{"t":"code","id":"dd-q771-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(771)\n","q":771},{"t":"code","id":"dd-q771-solution","role":"solution","src":"#@title 💡 Solution — Problem 771\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(items):\n    \"\"\"One step in from each end.\"\"\"\n    return (items[1], items[-2])\n\n\nexample = ([1, 2, 3, 4],)\nprint(solve(*example))\n","q":771},{"t":"md","id":"dd-q772","role":"problem","src":"<!-- dd:dd-q772 -->\n\n### Problem 772 · independent\n\nWrite a function solve(rows) that names the LAST row `last_row` (pick it with -1), and returns the tuple (last_row, last_row[0], last_row[-1]) — the row itself, then its two ends.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n([3, 4], 3, 4)\n```\n","q":772},{"t":"code","id":"dd-q772-code","role":"code","src":"def solve(rows):\n    \"\"\"One pick, then both ends of what was picked.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([[1, 2], [3, 4]],)\nprint(solve(*example))\n","q":772},{"t":"code","id":"dd-q772-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(772)\n","q":772},{"t":"code","id":"dd-q772-solution","role":"solution","src":"#@title 💡 Solution — Problem 772\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(rows):\n    \"\"\"One pick, then both ends of what was picked.\"\"\"\n    last_row = rows[-1]\n    return (last_row, last_row[0], last_row[-1])\n\n\nexample = ([[1, 2], [3, 4]],)\nprint(solve(*example))\n","q":772},{"t":"md","id":"dd-q773","role":"problem","src":"<!-- dd:dd-q773 -->\n\n### Problem 773 · independent\n\nWrite a function solve(rows) that returns whether rows[0][1] equals rows[1][0], using ==. The two brackets are not interchangeable — these are different cells.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\nFalse\n```\n","q":773},{"t":"code","id":"dd-q773-code","role":"code","src":"def solve(rows):\n    \"\"\"Swapping the brackets reads a different cell.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([[1, 2], [3, 4]],)\nprint(solve(*example))\n","q":773},{"t":"code","id":"dd-q773-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(773)\n","q":773},{"t":"code","id":"dd-q773-solution","role":"solution","src":"#@title 💡 Solution — Problem 773\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(rows):\n    \"\"\"Swapping the brackets reads a different cell.\"\"\"\n    return rows[0][1] == rows[1][0]\n\n\nexample = ([[1, 2], [3, 4]],)\nprint(solve(*example))\n","q":773},{"t":"md","id":"dd-q774","role":"problem","src":"<!-- dd:dd-q774 -->\n\n### Problem 774 · independent\n\nWrite a function solve(rows) that names the TOP-RIGHT corner `corner` — the first row's last item — and returns the tuple (corner, kind_name) with corner's type name.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(2, 'int')\n```\n","q":774},{"t":"code","id":"dd-q774-code","role":"code","src":"def solve(rows):\n    \"\"\"Row zero, last item — and what kind of value lives there.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([[1, 2], [3, 4]],)\nprint(solve(*example))\n","q":774},{"t":"code","id":"dd-q774-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(774)\n","q":774},{"t":"code","id":"dd-q774-solution","role":"solution","src":"#@title 💡 Solution — Problem 774\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(rows):\n    \"\"\"Row zero, last item — and what kind of value lives there.\"\"\"\n    corner = rows[0][-1]\n    return (corner, type(corner).__name__)\n\n\nexample = ([[1, 2], [3, 4]],)\nprint(solve(*example))\n","q":774},{"t":"md","id":"dd-q775","role":"problem","src":"<!-- dd:dd-q775 -->\n\n### Problem 775 · independent\n\nWrite a function solve(pair) that builds `flipped` as the tuple (pair[-1], pair[0]) and returns the tuple (flipped, same) where same is whether flipped[0] equals pair[0] — true only when the two ends match.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n((2, 1), False)\n```\n","q":775},{"t":"code","id":"dd-q775-code","role":"code","src":"def solve(pair):\n    \"\"\"Flip by indexing, then index what you built.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ((1, 2),)\nprint(solve(*example))\n","q":775},{"t":"code","id":"dd-q775-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(775)\n","q":775},{"t":"code","id":"dd-q775-solution","role":"solution","src":"#@title 💡 Solution — Problem 775\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(pair):\n    \"\"\"Flip by indexing, then index what you built.\"\"\"\n    flipped = (pair[-1], pair[0])\n    return (flipped, flipped[0] == pair[0])\n\n\nexample = ((1, 2),)\nprint(solve(*example))\n","q":775},{"t":"md","id":"dd-q776","role":"problem","src":"<!-- dd:dd-q776 -->\n\n### Problem 776 · independent\n\nWrite a function solve(items, n) that returns the tuple (items[n], items[n - 1]) — the item at position n and its LEFT neighbour. Work the position out inside the brackets.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(30, 20)\n```\n","q":776},{"t":"code","id":"dd-q776-code","role":"code","src":"def solve(items, n):\n    \"\"\"A position and the one before it.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([10, 20, 30], 2)\nprint(solve(*example))\n","q":776},{"t":"code","id":"dd-q776-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(776)\n","q":776},{"t":"code","id":"dd-q776-solution","role":"solution","src":"#@title 💡 Solution — Problem 776\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(items, n):\n    \"\"\"A position and the one before it.\"\"\"\n    return (items[n], items[n - 1])\n\n\nexample = ([10, 20, 30], 2)\nprint(solve(*example))\n","q":776},{"t":"md","id":"dd-q777","role":"problem","src":"<!-- dd:dd-q777 -->\n\n### Problem 777 · independent\n\nWrite a function solve(cube) that returns the tuple (cube[0][0][0], cube[-1][-1][-1]) — the very first value and the very last value of a three-level nest. One index per level, three levels deep.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(1, 4)\n```\n","q":777},{"t":"code","id":"dd-q777-code","role":"code","src":"def solve(cube):\n    \"\"\"Three levels: three brackets each way.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([[[1, 2]], [[3, 4]]],)\nprint(solve(*example))\n","q":777},{"t":"code","id":"dd-q777-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(777)\n","q":777},{"t":"code","id":"dd-q777-solution","role":"solution","src":"#@title 💡 Solution — Problem 777\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(cube):\n    \"\"\"Three levels: three brackets each way.\"\"\"\n    return (cube[0][0][0], cube[-1][-1][-1])\n\n\nexample = ([[[1, 2]], [[3, 4]]],)\nprint(solve(*example))\n","q":777},{"t":"md","id":"python-indexing-misconceptions","role":"prose","src":"#### Common mistakes\n\n- **\"`items[1]` is the first item.\"** — It is the second. Counting starts at 0.\n- **\"To get the last item I need the length.\"** — `items[-1]` does it with no\n  measurement, and cannot go one past the end.\n- **\"`rows[i][j]` and `rows[j][i]` are the same.\"** — Only on a square table, and\n  only by coincidence. The first index always picks the row.\n"},{"t":"md","id":"dd-kp-python-calling-functions","role":"prose","src":"<!-- dd:dd-kp-python-calling-functions -->\n\n## Calling a function — arguments in, one value out\n\n`python.calling-functions`\n"},{"t":"md","id":"python-calling-functions-segment-concept","role":"prose","src":"A **function** is a piece of work that has been given a name. **Calling** it is\nwriting that name followed by parentheses — and the parentheses are what make\nthe work happen:\n"},{"t":"code","id":"python-calling-functions-segment-concept-code","role":"code","src":"items = [10, 20, 30]\n\nprint(len(items))\nprint(len)\n"},{"t":"md","id":"python-calling-functions-segment-concept-1","role":"prose","src":"\n\nThe second line prints something like `<built-in function len>`. Without\nparentheses you are talking ABOUT the function; with them you are asking it to\nrun. Almost every \"why did I get `<function ...>` back?\" is this.\n\nValues handed to a call are its **arguments**, separated by commas. Order\nmatters, because each position means something different to the function:\n"},{"t":"code","id":"python-calling-functions-segment-concept-code-1","role":"code","src":"print(round(3.14159, 2))\nprint(round(3.14159, 4))\n"},{"t":"md","id":"python-calling-functions-segment-concept-2","role":"prose","src":"\n\n`round` takes the number first and how many decimal places second. Swapping them\nasks a different question — and usually an impossible one.\n\nEvery call produces **one value**, which you can name, print, or feed straight\ninto another call:\n"},{"t":"code","id":"python-calling-functions-segment-concept-code-2","role":"code","src":"numbers = [4, 1, 7]\n\nprint(sum(numbers))\nprint(max(numbers))\nprint(min(numbers))\nprint(sum(numbers) / len(numbers))\n"},{"t":"md","id":"python-calling-functions-segment-concept-3","role":"prose","src":"\n\nThat last line is a call inside an expression built from two other calls: each\none is worked out first, and the results are then divided.\n\nSome arguments are settings rather than data, and those are passed **by name**\nso the call still reads as English. A named argument is written\n`name=value` and can be left out entirely, in which case the function uses its\nown default:\n"},{"t":"code","id":"python-calling-functions-segment-concept-code-3","role":"code","src":"items = [3, 1, 2]\n\nprint(sorted(items))\nprint(sorted(items, reverse=True))\n"},{"t":"md","id":"python-calling-functions-segment-concept-4","role":"prose","src":"\n\n`sorted` returns a NEW list and leaves the original alone — which you can check,\nand which is why the name `sorted` is worth trusting:\n"},{"t":"code","id":"python-calling-functions-segment-concept-code-4","role":"code","src":"items = [3, 1, 2]\nordered = sorted(items)\n\nprint(ordered)\nprint(items)\n"},{"t":"md","id":"python-calling-functions-segment-watch","role":"prose","src":"> **Watch out.** - **No parentheses, no work** — `len` is the function, `len(items)` is the\n  number.\n- **Position is meaning** — `round(x, 2)` and `round(2, x)` are different\n  questions.\n- **A keyword argument must be named exactly** — `reverse=True` changes the\n  direction; `True` on its own in that slot means something else entirely.\n"},{"t":"md","id":"dd-q592-worked","role":"prose","src":"Three calls over one list, then a fourth built from two of them.\n","q":592},{"t":"code","id":"dd-q592-worked-code","role":"code","src":"readings = [4, 9, 2, 9]\n\ncount = len(readings)\ntotal = sum(readings)\nbiggest = max(readings)\n\nprint(\"count  :\", count)\nprint(\"total  :\", total)\nprint(\"biggest:\", biggest)\nprint(\"mean   :\", round(total / count, 2))\n","q":592},{"t":"md","id":"dd-q592-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `len`, `sum` and `max` each take the same list and each hand back one value.\n   Naming those values is what lets the last line read as a formula.\n2. The mean is `total / count` — two names, no recomputation.\n3. `round(..., 2)` wraps that expression: the division happens first, and the\n   rounded result is what gets printed.\n","q":592},{"t":"md","id":"dd-q592","role":"problem","src":"<!-- dd:dd-q592 -->\n\n### Problem 592 · faded — your turn\n\nAsk the built-in how many items the list holds.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n3\n```\n","q":592},{"t":"code","id":"dd-q592-code","role":"code","src":"def solve(items):\n    \"\"\"Call len on the list.\"\"\"\n    return _____(items)\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([1, 2, 3],)\nprint(solve(*example))\n","q":592},{"t":"code","id":"dd-q592-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(592)\n","q":592},{"t":"code","id":"dd-q592-solution","role":"solution","src":"#@title 💡 Solution — Problem 592\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(items):\n    \"\"\"Call len on the list.\"\"\"\n    return len(items)\n\n\nexample = ([1, 2, 3],)\nprint(solve(*example))\n","q":592},{"t":"md","id":"dd-q593-worked","role":"prose","src":"Three calls over one list, then a fourth built from two of them.\n","q":593},{"t":"code","id":"dd-q593-worked-code","role":"code","src":"readings = [4, 9, 2, 9]\n\ncount = len(readings)\ntotal = sum(readings)\nbiggest = max(readings)\n\nprint(\"count  :\", count)\nprint(\"total  :\", total)\nprint(\"biggest:\", biggest)\nprint(\"mean   :\", round(total / count, 2))\n","q":593},{"t":"md","id":"dd-q593-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `len`, `sum` and `max` each take the same list and each hand back one value.\n   Naming those values is what lets the last line read as a formula.\n2. The mean is `total / count` — two names, no recomputation.\n3. `round(..., 2)` wraps that expression: the division happens first, and the\n   rounded result is what gets printed.\n","q":593},{"t":"md","id":"dd-q593","role":"problem","src":"<!-- dd:dd-q593 -->\n\n### Problem 593 · faded — your turn\n\nTwo arguments, in the order the function expects them.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n3.14\n```\n","q":593},{"t":"code","id":"dd-q593-code","role":"code","src":"def solve(x, places):\n    \"\"\"round takes the value first, then how many places.\"\"\"\n    return round(x, _____)\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (3.14159, 2)\nprint(solve(*example))\n","q":593},{"t":"code","id":"dd-q593-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(593)\n","q":593},{"t":"code","id":"dd-q593-solution","role":"solution","src":"#@title 💡 Solution — Problem 593\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(x, places):\n    \"\"\"round takes the value first, then how many places.\"\"\"\n    return round(x, places)\n\n\nexample = (3.14159, 2)\nprint(solve(*example))\n","q":593},{"t":"md","id":"dd-q780-worked","role":"prose","src":"Three calls over one list, then a fourth built from two of them.\n","q":780},{"t":"code","id":"dd-q780-worked-code","role":"code","src":"readings = [4, 9, 2, 9]\n\ncount = len(readings)\ntotal = sum(readings)\nbiggest = max(readings)\n\nprint(\"count  :\", count)\nprint(\"total  :\", total)\nprint(\"biggest:\", biggest)\nprint(\"mean   :\", round(total / count, 2))\n","q":780},{"t":"md","id":"dd-q780-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `len`, `sum` and `max` each take the same list and each hand back one value.\n   Naming those values is what lets the last line read as a formula.\n2. The mean is `total / count` — two names, no recomputation.\n3. `round(..., 2)` wraps that expression: the division happens first, and the\n   rounded result is what gets printed.\n","q":780},{"t":"md","id":"dd-q780","role":"problem","src":"<!-- dd:dd-q780 -->\n\n### Problem 780 · faded — your turn\n\nThe parentheses are what make the work happen.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n12\n```\n","q":780},{"t":"code","id":"dd-q780-code","role":"code","src":"def solve(numbers):\n    \"\"\"One call, one value back.\"\"\"\n    return _____(numbers)\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([4, 1, 7],)\nprint(solve(*example))\n","q":780},{"t":"code","id":"dd-q780-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(780)\n","q":780},{"t":"code","id":"dd-q780-solution","role":"solution","src":"#@title 💡 Solution — Problem 780\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(numbers):\n    \"\"\"One call, one value back.\"\"\"\n    return sum(numbers)\n\n\nexample = ([4, 1, 7],)\nprint(solve(*example))\n","q":780},{"t":"md","id":"dd-q781-worked","role":"prose","src":"Three calls over one list, then a fourth built from two of them.\n","q":781},{"t":"code","id":"dd-q781-worked-code","role":"code","src":"readings = [4, 9, 2, 9]\n\ncount = len(readings)\ntotal = sum(readings)\nbiggest = max(readings)\n\nprint(\"count  :\", count)\nprint(\"total  :\", total)\nprint(\"biggest:\", biggest)\nprint(\"mean   :\", round(total / count, 2))\n","q":781},{"t":"md","id":"dd-q781-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `len`, `sum` and `max` each take the same list and each hand back one value.\n   Naming those values is what lets the last line read as a formula.\n2. The mean is `total / count` — two names, no recomputation.\n3. `round(..., 2)` wraps that expression: the division happens first, and the\n   rounded result is what gets printed.\n","q":781},{"t":"md","id":"dd-q781","role":"problem","src":"<!-- dd:dd-q781 -->\n\n### Problem 781 · faded — your turn\n\nThe two extremes, biggest first.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(7, 1)\n```\n","q":781},{"t":"code","id":"dd-q781-code","role":"code","src":"def solve(numbers):\n    \"\"\"The two extremes, biggest first.\"\"\"\n    return (_____(numbers), _____(numbers))\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([4, 1, 7],)\nprint(solve(*example))\n","q":781},{"t":"code","id":"dd-q781-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(781)\n","q":781},{"t":"code","id":"dd-q781-solution","role":"solution","src":"#@title 💡 Solution — Problem 781\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(numbers):\n    \"\"\"The two extremes, biggest first.\"\"\"\n    return (max(numbers), min(numbers))\n\n\nexample = ([4, 1, 7],)\nprint(solve(*example))\n","q":781},{"t":"md","id":"dd-q782-worked","role":"prose","src":"Three calls over one list, then a fourth built from two of them.\n","q":782},{"t":"code","id":"dd-q782-worked-code","role":"code","src":"readings = [4, 9, 2, 9]\n\ncount = len(readings)\ntotal = sum(readings)\nbiggest = max(readings)\n\nprint(\"count  :\", count)\nprint(\"total  :\", total)\nprint(\"biggest:\", biggest)\nprint(\"mean   :\", round(total / count, 2))\n","q":782},{"t":"md","id":"dd-q782-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `len`, `sum` and `max` each take the same list and each hand back one value.\n   Naming those values is what lets the last line read as a formula.\n2. The mean is `total / count` — two names, no recomputation.\n3. `round(..., 2)` wraps that expression: the division happens first, and the\n   rounded result is what gets printed.\n","q":782},{"t":"md","id":"dd-q782","role":"problem","src":"<!-- dd:dd-q782 -->\n\n### Problem 782 · faded — your turn\n\nA new, ordered list comes back from one call.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n[1, 2, 3]\n```\n","q":782},{"t":"code","id":"dd-q782-code","role":"code","src":"def solve(items):\n    \"\"\"sorted returns a new, ordered list.\"\"\"\n    return _____(items)\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([3, 1, 2],)\nprint(solve(*example))\n","q":782},{"t":"code","id":"dd-q782-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(782)\n","q":782},{"t":"code","id":"dd-q782-solution","role":"solution","src":"#@title 💡 Solution — Problem 782\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(items):\n    \"\"\"sorted returns a new, ordered list.\"\"\"\n    return sorted(items)\n\n\nexample = ([3, 1, 2],)\nprint(solve(*example))\n","q":782},{"t":"md","id":"dd-q783-worked","role":"prose","src":"Three calls over one list, then a fourth built from two of them.\n","q":783},{"t":"code","id":"dd-q783-worked-code","role":"code","src":"readings = [4, 9, 2, 9]\n\ncount = len(readings)\ntotal = sum(readings)\nbiggest = max(readings)\n\nprint(\"count  :\", count)\nprint(\"total  :\", total)\nprint(\"biggest:\", biggest)\nprint(\"mean   :\", round(total / count, 2))\n","q":783},{"t":"md","id":"dd-q783-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `len`, `sum` and `max` each take the same list and each hand back one value.\n   Naming those values is what lets the last line read as a formula.\n2. The mean is `total / count` — two names, no recomputation.\n3. `round(..., 2)` wraps that expression: the division happens first, and the\n   rounded result is what gets printed.\n","q":783},{"t":"md","id":"dd-q783","role":"problem","src":"<!-- dd:dd-q783 -->\n\n### Problem 783 · faded — your turn\n\nThe direction travels as a named argument.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n[3, 2, 1]\n```\n","q":783},{"t":"code","id":"dd-q783-code","role":"code","src":"def solve(items, flag):\n    \"\"\"The direction travels as a named argument.\"\"\"\n    return sorted(items, _____=flag)\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([3, 1, 2], True)\nprint(solve(*example))\n","q":783},{"t":"code","id":"dd-q783-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(783)\n","q":783},{"t":"code","id":"dd-q783-solution","role":"solution","src":"#@title 💡 Solution — Problem 783\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(items, flag):\n    \"\"\"The direction travels as a named argument.\"\"\"\n    return sorted(items, reverse=flag)\n\n\nexample = ([3, 1, 2], True)\nprint(solve(*example))\n","q":783},{"t":"md","id":"dd-q784-worked","role":"prose","src":"Three calls over one list, then a fourth built from two of them.\n","q":784},{"t":"code","id":"dd-q784-worked-code","role":"code","src":"readings = [4, 9, 2, 9]\n\ncount = len(readings)\ntotal = sum(readings)\nbiggest = max(readings)\n\nprint(\"count  :\", count)\nprint(\"total  :\", total)\nprint(\"biggest:\", biggest)\nprint(\"mean   :\", round(total / count, 2))\n","q":784},{"t":"md","id":"dd-q784-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `len`, `sum` and `max` each take the same list and each hand back one value.\n   Naming those values is what lets the last line read as a formula.\n2. The mean is `total / count` — two names, no recomputation.\n3. `round(..., 2)` wraps that expression: the division happens first, and the\n   rounded result is what gets printed.\n","q":784},{"t":"md","id":"dd-q784","role":"problem","src":"<!-- dd:dd-q784 -->\n\n### Problem 784 · faded — your turn\n\nThe distance between the two extremes.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n7\n```\n","q":784},{"t":"code","id":"dd-q784-code","role":"code","src":"def solve(items):\n    \"\"\"The distance between the two extremes.\"\"\"\n    return _____(items) - _____(items)\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([4, 9, 2],)\nprint(solve(*example))\n","q":784},{"t":"code","id":"dd-q784-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(784)\n","q":784},{"t":"code","id":"dd-q784-solution","role":"solution","src":"#@title 💡 Solution — Problem 784\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(items):\n    \"\"\"The distance between the two extremes.\"\"\"\n    return max(items) - min(items)\n\n\nexample = ([4, 9, 2],)\nprint(solve(*example))\n","q":784},{"t":"md","id":"dd-q785-worked","role":"prose","src":"Three calls over one list, then a fourth built from two of them.\n","q":785},{"t":"code","id":"dd-q785-worked-code","role":"code","src":"readings = [4, 9, 2, 9]\n\ncount = len(readings)\ntotal = sum(readings)\nbiggest = max(readings)\n\nprint(\"count  :\", count)\nprint(\"total  :\", total)\nprint(\"biggest:\", biggest)\nprint(\"mean   :\", round(total / count, 2))\n","q":785},{"t":"md","id":"dd-q785-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `len`, `sum` and `max` each take the same list and each hand back one value.\n   Naming those values is what lets the last line read as a formula.\n2. The mean is `total / count` — two names, no recomputation.\n3. `round(..., 2)` wraps that expression: the division happens first, and the\n   rounded result is what gets printed.\n","q":785},{"t":"md","id":"dd-q785","role":"problem","src":"<!-- dd:dd-q785 -->\n\n### Problem 785 · faded — your turn\n\nThe total over the count — each call runs first, then the division.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n5.0\n```\n","q":785},{"t":"code","id":"dd-q785-code","role":"code","src":"def solve(numbers):\n    \"\"\"sum over len — each call runs first, then the division.\"\"\"\n    return sum(numbers) / _____(numbers)\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([4, 9, 2],)\nprint(solve(*example))\n","q":785},{"t":"code","id":"dd-q785-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(785)\n","q":785},{"t":"code","id":"dd-q785-solution","role":"solution","src":"#@title 💡 Solution — Problem 785\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(numbers):\n    \"\"\"sum over len — each call runs first, then the division.\"\"\"\n    return sum(numbers) / len(numbers)\n\n\nexample = ([4, 9, 2],)\nprint(solve(*example))\n","q":785},{"t":"md","id":"dd-q786-worked","role":"prose","src":"Three calls over one list, then a fourth built from two of them.\n","q":786},{"t":"code","id":"dd-q786-worked-code","role":"code","src":"readings = [4, 9, 2, 9]\n\ncount = len(readings)\ntotal = sum(readings)\nbiggest = max(readings)\n\nprint(\"count  :\", count)\nprint(\"total  :\", total)\nprint(\"biggest:\", biggest)\nprint(\"mean   :\", round(total / count, 2))\n","q":786},{"t":"md","id":"dd-q786-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `len`, `sum` and `max` each take the same list and each hand back one value.\n   Naming those values is what lets the last line read as a formula.\n2. The mean is `total / count` — two names, no recomputation.\n3. `round(..., 2)` wraps that expression: the division happens first, and the\n   rounded result is what gets printed.\n","q":786},{"t":"md","id":"dd-q786","role":"problem","src":"<!-- dd:dd-q786 -->\n\n### Problem 786 · faded — your turn\n\nThe function is a value; its answer is another.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n('builtin_function_or_method', 'int')\n```\n","q":786},{"t":"code","id":"dd-q786-code","role":"code","src":"def solve(items):\n    \"\"\"The function is a value; its answer is another.\"\"\"\n    return (type(_____).__name__, type(_____).__name__)\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([1, 2],)\nprint(solve(*example))\n","q":786},{"t":"code","id":"dd-q786-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(786)\n","q":786},{"t":"code","id":"dd-q786-solution","role":"solution","src":"#@title 💡 Solution — Problem 786\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(items):\n    \"\"\"The function is a value; its answer is another.\"\"\"\n    return (type(len).__name__, type(len(items)).__name__)\n\n\nexample = ([1, 2],)\nprint(solve(*example))\n","q":786},{"t":"md","id":"dd-q787-worked","role":"prose","src":"Three calls over one list, then a fourth built from two of them.\n","q":787},{"t":"code","id":"dd-q787-worked-code","role":"code","src":"readings = [4, 9, 2, 9]\n\ncount = len(readings)\ntotal = sum(readings)\nbiggest = max(readings)\n\nprint(\"count  :\", count)\nprint(\"total  :\", total)\nprint(\"biggest:\", biggest)\nprint(\"mean   :\", round(total / count, 2))\n","q":787},{"t":"md","id":"dd-q787-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `len`, `sum` and `max` each take the same list and each hand back one value.\n   Naming those values is what lets the last line read as a formula.\n2. The mean is `total / count` — two names, no recomputation.\n3. `round(..., 2)` wraps that expression: the division happens first, and the\n   rounded result is what gets printed.\n","q":787},{"t":"md","id":"dd-q787","role":"problem","src":"<!-- dd:dd-q787 -->\n\n### Problem 787 · faded — your turn\n\nThree calls, one line: the mean, trimmed.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n1.67\n```\n","q":787},{"t":"code","id":"dd-q787-code","role":"code","src":"def solve(numbers, places):\n    \"\"\"Three calls, one line: the mean, trimmed.\"\"\"\n    return _____(sum(numbers) / len(numbers), places)\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([1, 2, 2], 2)\nprint(solve(*example))\n","q":787},{"t":"code","id":"dd-q787-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(787)\n","q":787},{"t":"code","id":"dd-q787-solution","role":"solution","src":"#@title 💡 Solution — Problem 787\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(numbers, places):\n    \"\"\"Three calls, one line: the mean, trimmed.\"\"\"\n    return round(sum(numbers) / len(numbers), places)\n\n\nexample = ([1, 2, 2], 2)\nprint(solve(*example))\n","q":787},{"t":"md","id":"dd-q594","role":"problem","src":"<!-- dd:dd-q594 -->\n\n### Problem 594 · independent\n\nWrite a function solve(numbers) that returns the tuple (total, biggest, smallest) by calling the built-ins sum, max and min on the same list. Each call gives back ONE value.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(6, 3, 1)\n```\n","q":594},{"t":"code","id":"dd-q594-code","role":"code","src":"def solve(numbers):\n    \"\"\"Three built-in calls over one list.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([1, 2, 3],)\nprint(solve(*example))\n","q":594},{"t":"code","id":"dd-q594-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(594)\n","q":594},{"t":"code","id":"dd-q594-solution","role":"solution","src":"#@title 💡 Solution — Problem 594\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(numbers):\n    \"\"\"Three built-in calls over one list.\"\"\"\n    return (sum(numbers), max(numbers), min(numbers))\n\n\nexample = ([1, 2, 3],)\nprint(solve(*example))\n","q":594},{"t":"md","id":"dd-q595","role":"problem","src":"<!-- dd:dd-q595 -->\n\n### Problem 595 · independent\n\nWrite a function solve(items) that returns the list sorted from LARGEST to smallest, by calling sorted with the keyword argument reverse=True. A keyword argument names the setting it is changing.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n[3, 2, 1]\n```\n","q":595},{"t":"code","id":"dd-q595-code","role":"code","src":"def solve(items):\n    \"\"\"sorted, with the direction named.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([3, 1, 2],)\nprint(solve(*example))\n","q":595},{"t":"code","id":"dd-q595-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(595)\n","q":595},{"t":"code","id":"dd-q595-solution","role":"solution","src":"#@title 💡 Solution — Problem 595\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(items):\n    \"\"\"sorted, with the direction named.\"\"\"\n    return sorted(items, reverse=True)\n\n\nexample = ([3, 1, 2],)\nprint(solve(*example))\n","q":595},{"t":"md","id":"dd-q788","role":"problem","src":"<!-- dd:dd-q788 -->\n\n### Problem 788 · independent\n\nWrite a function solve(items) that names sorted(items) `ordered` and returns the tuple (ordered, items) — the new list AND the original, which sorted must have left alone.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n([1, 2, 3], [3, 1, 2])\n```\n","q":788},{"t":"code","id":"dd-q788-code","role":"code","src":"def solve(items):\n    \"\"\"A new list comes back; the original is untouched.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([3, 1, 2],)\nprint(solve(*example))\n","q":788},{"t":"code","id":"dd-q788-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(788)\n","q":788},{"t":"code","id":"dd-q788-solution","role":"solution","src":"#@title 💡 Solution — Problem 788\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(items):\n    \"\"\"A new list comes back; the original is untouched.\"\"\"\n    ordered = sorted(items)\n    return (ordered, items)\n\n\nexample = ([3, 1, 2],)\nprint(solve(*example))\n","q":788},{"t":"md","id":"dd-q789","role":"problem","src":"<!-- dd:dd-q789 -->\n\n### Problem 789 · independent\n\nWrite a function solve(items) that returns the tuple (sorted(items), sorted(items, reverse=True)) — both directions of the same list.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n([1, 2, 3], [3, 2, 1])\n```\n","q":789},{"t":"code","id":"dd-q789-code","role":"code","src":"def solve(items):\n    \"\"\"Ascending and descending, from two calls.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([3, 1, 2],)\nprint(solve(*example))\n","q":789},{"t":"code","id":"dd-q789-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(789)\n","q":789},{"t":"code","id":"dd-q789-solution","role":"solution","src":"#@title 💡 Solution — Problem 789\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(items):\n    \"\"\"Ascending and descending, from two calls.\"\"\"\n    return (sorted(items), sorted(items, reverse=True))\n\n\nexample = ([3, 1, 2],)\nprint(solve(*example))\n","q":789},{"t":"md","id":"dd-q790","role":"problem","src":"<!-- dd:dd-q790 -->\n\n### Problem 790 · independent\n\nWrite a function solve(x, y) that returns whether the two numbers agree once both are rounded to 2 decimal places — round each, then compare with ==.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\nTrue\n```\n","q":790},{"t":"code","id":"dd-q790-code","role":"code","src":"def solve(x, y):\n    \"\"\"Equal after trimming is a different question from equal.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (3.14159, 3.14162)\nprint(solve(*example))\n","q":790},{"t":"code","id":"dd-q790-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(790)\n","q":790},{"t":"code","id":"dd-q790-solution","role":"solution","src":"#@title 💡 Solution — Problem 790\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(x, y):\n    \"\"\"Equal after trimming is a different question from equal.\"\"\"\n    return round(x, 2) == round(y, 2)\n\n\nexample = (3.14159, 3.14162)\nprint(solve(*example))\n","q":790},{"t":"md","id":"dd-q791","role":"problem","src":"<!-- dd:dd-q791 -->\n\n### Problem 791 · independent\n\nWrite a function solve(items) that names the span max(items) - min(items) `span` and returns the tuple (span, flat) where flat is whether span == 0.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(7, False)\n```\n","q":791},{"t":"code","id":"dd-q791-code","role":"code","src":"def solve(items):\n    \"\"\"A zero span means every value is the same.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([4, 9, 2],)\nprint(solve(*example))\n","q":791},{"t":"code","id":"dd-q791-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(791)\n","q":791},{"t":"code","id":"dd-q791-solution","role":"solution","src":"#@title 💡 Solution — Problem 791\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(items):\n    \"\"\"A zero span means every value is the same.\"\"\"\n    span = max(items) - min(items)\n    return (span, span == 0)\n\n\nexample = ([4, 9, 2],)\nprint(solve(*example))\n","q":791},{"t":"md","id":"dd-q792","role":"problem","src":"<!-- dd:dd-q792 -->\n\n### Problem 792 · independent\n\nWrite a function solve(a, b) that returns the two values as the ordered tuple (smaller, larger) — min and max called on the two arguments directly, no list needed.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(4, 9)\n```\n","q":792},{"t":"code","id":"dd-q792-code","role":"code","src":"def solve(a, b):\n    \"\"\"min and max take bare arguments too.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (9, 4)\nprint(solve(*example))\n","q":792},{"t":"code","id":"dd-q792-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(792)\n","q":792},{"t":"code","id":"dd-q792-solution","role":"solution","src":"#@title 💡 Solution — Problem 792\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(a, b):\n    \"\"\"min and max take bare arguments too.\"\"\"\n    first = min(a, b)\n    second = max(a, b)\n    return (first, second)\n\n\nexample = (9, 4)\nprint(solve(*example))\n","q":792},{"t":"md","id":"dd-q793","role":"problem","src":"<!-- dd:dd-q793 -->\n\n### Problem 793 · independent\n\nWrite a function solve(rows) that returns the total of the FIRST row plus the total of the LAST row — sum called on rows[0] and on rows[-1], added together.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n14\n```\n","q":793},{"t":"code","id":"dd-q793-code","role":"code","src":"def solve(rows):\n    \"\"\"Two sums, two picks, one addition.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([[1, 2], [3, 4], [5, 6]],)\nprint(solve(*example))\n","q":793},{"t":"code","id":"dd-q793-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(793)\n","q":793},{"t":"code","id":"dd-q793-solution","role":"solution","src":"#@title 💡 Solution — Problem 793\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(rows):\n    \"\"\"Two sums, two picks, one addition.\"\"\"\n    return sum(rows[0]) + sum(rows[-1])\n\n\nexample = ([[1, 2], [3, 4], [5, 6]],)\nprint(solve(*example))\n","q":793},{"t":"md","id":"dd-q794","role":"problem","src":"<!-- dd:dd-q794 -->\n\n### Problem 794 · independent\n\nWrite a function solve(items) that returns the tuple (sorted(items)[0], min(items)) — the smallest value found two ways: the front of the sorted list, and min directly.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(1, 1)\n```\n","q":794},{"t":"code","id":"dd-q794-code","role":"code","src":"def solve(items):\n    \"\"\"Two roads to the smallest value.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ([3, 1, 2],)\nprint(solve(*example))\n","q":794},{"t":"code","id":"dd-q794-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(794)\n","q":794},{"t":"code","id":"dd-q794-solution","role":"solution","src":"#@title 💡 Solution — Problem 794\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(items):\n    \"\"\"Two roads to the smallest value.\"\"\"\n    return (sorted(items)[0], min(items))\n\n\nexample = ([3, 1, 2],)\nprint(solve(*example))\n","q":794},{"t":"md","id":"dd-q795","role":"problem","src":"<!-- dd:dd-q795 -->\n\n### Problem 795 · independent\n\nWrite a function solve(x) that returns the tuple (round(x), round(x, 0), round(x, 2)) — no places, zero places, two places. The second argument changes the KIND of the answer, not only its precision.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(3, 3.0, 3.46)\n```\n","q":795},{"t":"code","id":"dd-q795-code","role":"code","src":"def solve(x):\n    \"\"\"round(x) is an int; round(x, 0) is still a float.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (3.456,)\nprint(solve(*example))\n","q":795},{"t":"code","id":"dd-q795-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(795)\n","q":795},{"t":"code","id":"dd-q795-solution","role":"solution","src":"#@title 💡 Solution — Problem 795\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(x):\n    \"\"\"round(x) is an int; round(x, 0) is still a float.\"\"\"\n    return (round(x), round(x, 0), round(x, 2))\n\n\nexample = (3.456,)\nprint(solve(*example))\n","q":795},{"t":"md","id":"python-calling-functions-misconceptions","role":"prose","src":"#### Common mistakes\n\n- **\"`len` gives the length.\"** — `len(items)` gives the length. `len` on its own\n  is the function object, and returning it is the most common way a drill fails\n  with no error message.\n- **\"Arguments can go in any order.\"** — Only keyword arguments can. Positional\n  arguments mean whatever their position means.\n- **\"`sorted` sorts the list.\"** — It returns a new one. The original is\n  unchanged, which is exactly why you have to keep the result.\n"},{"t":"md","id":"dd-kp-python-defining-functions","role":"prose","src":"<!-- dd:dd-kp-python-defining-functions -->\n\n## Writing your own function — def and return\n\n`python.defining-functions`\n"},{"t":"md","id":"python-defining-functions-segment-concept","role":"prose","src":"Every drill in this course asks for the same shape: **write a function called\n`solve`**. This page is that shape.\n\n`def` names a new function and lists the **parameters** it expects. The indented\nlines below it are its **body** — the work — and `return` is how it hands a\nvalue back to whoever called it:\n"},{"t":"code","id":"python-defining-functions-segment-concept-code","role":"code","src":"def double(n):\n    \"\"\"Twice n.\"\"\"\n    return n * 2\n\n\nprint(double(5))\nprint(double(2.5))\n"},{"t":"md","id":"python-defining-functions-segment-concept-1","role":"prose","src":"\n\nThree things to notice. The parameter `n` is a name that does not exist until\nthe function is called — it takes whatever value the caller passes. The\ntriple-quoted line is a **docstring**, a sentence saying what the function is\nfor. And the body is indented; the indentation is what says which lines belong\nto the function.\n\n`return` both hands the value back and **ends the call immediately**. Anything\nafter it never runs:\n"},{"t":"code","id":"python-defining-functions-segment-concept-code-1","role":"code","src":"def first_only(a, b):\n    \"\"\"The first argument, and proof that the second line is dead.\"\"\"\n    return a\n    return b\n\n\nprint(first_only(\"kept\", \"never reached\"))\n"},{"t":"md","id":"python-defining-functions-segment-concept-2","role":"prose","src":"\n\nA function with no `return` hands back `None`. This is the quietest bug in early\nPython, because nothing goes wrong — you simply get nothing:\n"},{"t":"code","id":"python-defining-functions-segment-concept-code-2","role":"code","src":"def forgot(n):\n    \"\"\"Computes, and then throws it away.\"\"\"\n    n * 3\n\n\nprint(forgot(5))\n"},{"t":"md","id":"python-defining-functions-segment-concept-3","role":"prose","src":"\n\nTo give more than one answer, return a tuple. One `return`, several values:\n"},{"t":"code","id":"python-defining-functions-segment-concept-code-3","role":"code","src":"def stats(a, b):\n    \"\"\"Sum and product, together.\"\"\"\n    return (a + b, a * b)\n\n\nprint(stats(2, 3))\n"},{"t":"md","id":"python-defining-functions-segment-concept-4","role":"prose","src":"\n\nA parameter can carry a **default**, used only when the caller says nothing.\nThat is what makes one function serve two calls:\n"},{"t":"code","id":"python-defining-functions-segment-concept-code-4","role":"code","src":"def scale(x, times=2):\n    \"\"\"Multiply x, doubling unless told otherwise.\"\"\"\n    return x * times\n\n\nprint(scale(5))\nprint(scale(5, 10))\n"},{"t":"md","id":"python-defining-functions-segment-concept-5","role":"prose","src":"\n\nFunctions can also be defined INSIDE other functions, which is how a repeated\nstep gets a name without leaking out into the rest of the program:\n"},{"t":"code","id":"python-defining-functions-segment-concept-code-5","role":"code","src":"def ends_doubled(values):\n    \"\"\"Double the first and last items, via one local helper.\"\"\"\n    def double(n):\n        \"\"\"Twice n.\"\"\"\n        return n * 2\n\n    return (double(values[0]), double(values[-1]))\n\n\nprint(ends_doubled([1, 2, 7]))\n"},{"t":"md","id":"python-defining-functions-segment-watch","role":"prose","src":"> **Watch out.** - **No `return` means `None`** — the work happens and the answer is discarded.\n  A grader sees `None` and reports a failure with nothing obviously wrong.\n- **`return` stops the function** — a second `return` on the next line is dead\n  code, not a second answer. Return a tuple instead.\n- **A default belongs in the `def` line** — `def solve(x, times=2)`. Writing\n  `times = 2` in the body ignores whatever the caller passed.\n"},{"t":"md","id":"dd-q598-worked","role":"prose","src":"One function, two parameters, a default, and a tuple of two answers.\n","q":598},{"t":"code","id":"dd-q598-worked-code","role":"code","src":"def summarize(values, places=2):\n    \"\"\"Return (count, mean) with the mean rounded to `places` places.\"\"\"\n    count = len(values)\n    mean = round(sum(values) / count, places)\n    return (count, mean)\n\n\nprint(summarize([1, 2, 4]))\nprint(summarize([1, 2, 4], 0))\n","q":598},{"t":"md","id":"dd-q598-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. The `def` line is the contract: two parameters, the second optional.\n2. `count` and `mean` are named inside the body. Those names exist only while\n   the call is running.\n3. One `return` hands back both answers as a tuple — and the two printed lines\n   show the default being used, then overridden.\n","q":598},{"t":"md","id":"dd-q598","role":"problem","src":"<!-- dd:dd-q598 -->\n\n### Problem 598 · faded — your turn\n\nThe work is done; `return` is what hands it back.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n6\n```\n","q":598},{"t":"code","id":"dd-q598-code","role":"code","src":"def solve(x):\n    \"\"\"Hand back three times x.\"\"\"\n    _____ x * 3\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (2,)\nprint(solve(*example))\n","q":598},{"t":"code","id":"dd-q598-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(598)\n","q":598},{"t":"code","id":"dd-q598-solution","role":"solution","src":"#@title 💡 Solution — Problem 598\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(x):\n    \"\"\"Hand back three times x.\"\"\"\n    return x * 3\n\n\nexample = (2,)\nprint(solve(*example))\n","q":598},{"t":"md","id":"dd-q599-worked","role":"prose","src":"One function, two parameters, a default, and a tuple of two answers.\n","q":599},{"t":"code","id":"dd-q599-worked-code","role":"code","src":"def summarize(values, places=2):\n    \"\"\"Return (count, mean) with the mean rounded to `places` places.\"\"\"\n    count = len(values)\n    mean = round(sum(values) / count, places)\n    return (count, mean)\n\n\nprint(summarize([1, 2, 4]))\nprint(summarize([1, 2, 4], 0))\n","q":599},{"t":"md","id":"dd-q599-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. The `def` line is the contract: two parameters, the second optional.\n2. `count` and `mean` are named inside the body. Those names exist only while\n   the call is running.\n3. One `return` hands back both answers as a tuple — and the two printed lines\n   show the default being used, then overridden.\n","q":599},{"t":"md","id":"dd-q599","role":"problem","src":"<!-- dd:dd-q599 -->\n\n### Problem 599 · faded — your turn\n\nTwo parameters, and both of them have to appear in the body.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n6\n```\n","q":599},{"t":"code","id":"dd-q599-code","role":"code","src":"def solve(a, b):\n    \"\"\"Two parameters, one returned value.\"\"\"\n    return a * _____\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (2, 3)\nprint(solve(*example))\n","q":599},{"t":"code","id":"dd-q599-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(599)\n","q":599},{"t":"code","id":"dd-q599-solution","role":"solution","src":"#@title 💡 Solution — Problem 599\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(a, b):\n    \"\"\"Two parameters, one returned value.\"\"\"\n    return a * b\n\n\nexample = (2, 3)\nprint(solve(*example))\n","q":599},{"t":"md","id":"dd-q600","role":"problem","src":"<!-- dd:dd-q600 -->\n\n### Problem 600 · independent\n\nWrite a function solve(x, times=2) whose second parameter has a DEFAULT of 2, and which returns x multiplied by `times`. Callers that pass one argument must get double; callers that pass two must get their own multiplier.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n10\n```\n","q":600},{"t":"code","id":"dd-q600-code","role":"code","src":"def solve(x, times=2):\n    \"\"\"A default is used only when the caller says nothing.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (5,)\nprint(solve(*example))\n","q":600},{"t":"code","id":"dd-q600-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(600)\n","q":600},{"t":"code","id":"dd-q600-solution","role":"solution","src":"#@title 💡 Solution — Problem 600\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(x, times=2):\n    \"\"\"A default is used only when the caller says nothing.\"\"\"\n    return x * times\n\n\nexample = (5,)\nprint(solve(*example))\n","q":600},{"t":"md","id":"dd-q601","role":"problem","src":"<!-- dd:dd-q601 -->\n\n### Problem 601 · independent\n\nWrite a function solve(a, b) that returns TWO values as a tuple: their sum and their product. One `return` can hand back a tuple, which is how a function gives more than one answer.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(5, 6)\n```\n","q":601},{"t":"code","id":"dd-q601-code","role":"code","src":"def solve(a, b):\n    \"\"\"Two answers, one return.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (2, 3)\nprint(solve(*example))\n","q":601},{"t":"code","id":"dd-q601-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(601)\n","q":601},{"t":"code","id":"dd-q601-solution","role":"solution","src":"#@title 💡 Solution — Problem 601\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(a, b):\n    \"\"\"Two answers, one return.\"\"\"\n    return (a + b, a * b)\n\n\nexample = (2, 3)\nprint(solve(*example))\n","q":601},{"t":"md","id":"python-defining-functions-misconceptions","role":"prose","src":"#### Common mistakes\n\n- **\"The function ran, so it returned something.\"** — Only `return` returns.\n  Without it every call hands back `None`.\n- **\"Two returns give two answers.\"** — The first one ends the call. A tuple is\n  how one return carries several values.\n- **\"Parameters are variables I set.\"** — They are named by the `def` line and\n  filled in by the caller, fresh on every call.\n"},{"t":"md","id":"dd-kp-python-dots-and-imports","role":"prose","src":"<!-- dd:dd-kp-python-dots-and-imports -->\n\n## Dots — importing a library, attributes, and methods\n\n`python.dots-and-imports`\n"},{"t":"md","id":"python-dots-and-imports-segment-concept","role":"prose","src":"Most of the code you will write reaches into something else with a **dot**. Three\ndifferent things use that same dot, and telling them apart is what this page is\nfor.\n\n**A module.** `import` brings in a library; the dot then reaches inside it for\nthe things it holds:\n"},{"t":"code","id":"python-dots-and-imports-segment-concept-code","role":"code","src":"import math\n\nprint(math.sqrt(9))\nprint(math.floor(2.7), math.ceil(2.7))\n"},{"t":"md","id":"python-dots-and-imports-segment-concept-1","role":"prose","src":"\n\n`import math as m` gives the same module a shorter name. This is not cosmetic —\nit is the convention the rest of this course runs on: the tensor library you\nmeet in the next lesson is always brought in under a two-letter alias, and every\nline of it after that is written through the dot.\n"},{"t":"code","id":"python-dots-and-imports-segment-concept-code-1","role":"code","src":"import math as m\n\nprint(m.sqrt(16))\nprint(m.floor(-1.2))\n"},{"t":"md","id":"python-dots-and-imports-segment-concept-2","role":"prose","src":"\n\n**An attribute** is a value that belongs to something. It is read with a dot and\n**no parentheses**, because there is no work to do — the value is already there:\n"},{"t":"code","id":"python-dots-and-imports-segment-concept-code-2","role":"code","src":"import math\n\nprint(math.pi)\n"},{"t":"md","id":"python-dots-and-imports-segment-concept-3","role":"prose","src":"\n\n**A method** is a function that belongs to something. It is reached with a dot\nand, being a function, still has to be **called**:\n"},{"t":"code","id":"python-dots-and-imports-segment-concept-code-3","role":"code","src":"text = \"hello\"\n\nprint(text.upper())\nprint(text)\n"},{"t":"md","id":"python-dots-and-imports-segment-concept-4","role":"prose","src":"\n\nNotice `text` is unchanged: `upper()` returned a NEW string. Notice too that the\ncall is `text.upper()` — the string it works on is the thing before the dot, so\nnothing goes in the parentheses.\n\nThat is the distinction to hold on to, because it decides whether parentheses\nbelong:\n\n| Written | Means |\n|---|---|\n| `math.pi` | an attribute — a value, read as-is |\n| `math.sqrt` | the function itself, not run |\n| `math.sqrt(9)` | the call — the number 3.0 |\n| `text.upper` | the method itself, not run |\n| `text.upper()` | the call — the capitalised text |\n\nNot everything is a method. `len` is a plain built-in function that takes the\nvalue as an argument, and strings have no `.len()` at all:\n"},{"t":"code","id":"python-dots-and-imports-segment-concept-code-4","role":"code","src":"text = \"hello\"\n\nprint(text.upper())\nprint(len(text))\n"},{"t":"md","id":"python-dots-and-imports-segment-concept-5","role":"prose","src":"\n\nSome methods change the thing they belong to and return `None`. `list.append` is\nthe one you meet first, and the `None` is what surprises people:\n"},{"t":"code","id":"python-dots-and-imports-segment-concept-code-5","role":"code","src":"items = [1, 2]\nresult = items.append(3)\n\nprint(items)\nprint(\"append returned:\", result)\n"},{"t":"md","id":"python-dots-and-imports-segment-concept-6","role":"prose","src":"\n\nSo append on its own line, then hand back the LIST — never the result of the\nappend.\n"},{"t":"md","id":"python-dots-and-imports-segment-watch","role":"prose","src":"> **Watch out.** - **Attribute or call?** — `math.pi` has no parentheses because it is a value.\n  `math.sqrt(9)` has them because it is work.\n- **A method with no parentheses is not run** — returning `text.upper` hands back\n  a method object, and nothing complains.\n- **`.append()` returns `None`** — it changes the list in place. Returning what\n  it gave you returns nothing.\n"},{"t":"md","id":"dd-q604-worked","role":"prose","src":"The three kinds of dot in one place, on the same two values.\n","q":604},{"t":"code","id":"dd-q604-worked-code","role":"code","src":"import math\n\ntext = \"delta\"\nx = 6.25\n\nshouted = text.upper()\nlength = len(text)\nroot = math.sqrt(x)\n\nprint(\"method on the value  :\", shouted)\nprint(\"built-in on the value:\", length)\nprint(\"function in a module :\", root)\nprint(\"attribute, no call   :\", math.pi)\n","q":604},{"t":"md","id":"dd-q604-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `text.upper()` is a method: it belongs to the string, and the parentheses run\n   it.\n2. `len(text)` is a built-in function: the string goes INSIDE the parentheses.\n   Same job shape, opposite arrangement.\n3. `math.sqrt(x)` reaches into the module, then calls what it found.\n4. `math.pi` is read with no parentheses at all, because it is already a value.\n","q":604},{"t":"md","id":"dd-q604","role":"problem","src":"<!-- dd:dd-q604 -->\n\n### Problem 604 · faded — your turn\n\nReach into the module with a dot, then call what you found.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n3.0\n```\n","q":604},{"t":"code","id":"dd-q604-code","role":"code","src":"import math\n\n\ndef solve(x):\n    \"\"\"Reach into the module with a dot, then call.\"\"\"\n    return math._____(x)\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (9,)\nprint(solve(*example))\n","q":604},{"t":"code","id":"dd-q604-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(604)\n","q":604},{"t":"code","id":"dd-q604-solution","role":"solution","src":"#@title 💡 Solution — Problem 604\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\nimport math\n\n\ndef solve(x):\n    \"\"\"Reach into the module with a dot, then call.\"\"\"\n    return math.sqrt(x)\n\n\nexample = (9,)\nprint(solve(*example))\n","q":604},{"t":"md","id":"dd-q605-worked","role":"prose","src":"The three kinds of dot in one place, on the same two values.\n","q":605},{"t":"code","id":"dd-q605-worked-code","role":"code","src":"import math\n\ntext = \"delta\"\nx = 6.25\n\nshouted = text.upper()\nlength = len(text)\nroot = math.sqrt(x)\n\nprint(\"method on the value  :\", shouted)\nprint(\"built-in on the value:\", length)\nprint(\"function in a module :\", root)\nprint(\"attribute, no call   :\", math.pi)\n","q":605},{"t":"md","id":"dd-q605-worked-1","role":"prose","src":"\n\nWhy each step:\n\n1. `text.upper()` is a method: it belongs to the string, and the parentheses run\n   it.\n2. `len(text)` is a built-in function: the string goes INSIDE the parentheses.\n   Same job shape, opposite arrangement.\n3. `math.sqrt(x)` reaches into the module, then calls what it found.\n4. `math.pi` is read with no parentheses at all, because it is already a value.\n","q":605},{"t":"md","id":"dd-q605","role":"problem","src":"<!-- dd:dd-q605 -->\n\n### Problem 605 · faded — your turn\n\nA method belongs to the value — and still has to be called.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n'HI'\n```\n","q":605},{"t":"code","id":"dd-q605-code","role":"code","src":"def solve(text):\n    \"\"\"A method belongs to the value, and still has to be called.\"\"\"\n    return text.upper_____\n\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ('hi',)\nprint(solve(*example))\n","q":605},{"t":"code","id":"dd-q605-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(605)\n","q":605},{"t":"code","id":"dd-q605-solution","role":"solution","src":"#@title 💡 Solution — Problem 605\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(text):\n    \"\"\"A method belongs to the value, and still has to be called.\"\"\"\n    return text.upper()\n\n\nexample = ('hi',)\nprint(solve(*example))\n","q":605},{"t":"md","id":"dd-q606","role":"problem","src":"<!-- dd:dd-q606 -->\n\n### Problem 606 · independent\n\nWrite a function solve(text) that returns the tuple (shouted, size): the string in capitals, and how long it is. One of those is a METHOD on the string, the other is a built-in function that takes the string as an argument.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n('HI', 2)\n```\n","q":606},{"t":"code","id":"dd-q606-code","role":"code","src":"def solve(text):\n    \"\"\"A method on the value, and a function taking the value.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = ('hi',)\nprint(solve(*example))\n","q":606},{"t":"code","id":"dd-q606-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(606)\n","q":606},{"t":"code","id":"dd-q606-solution","role":"solution","src":"#@title 💡 Solution — Problem 606\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\ndef solve(text):\n    \"\"\"A method on the value, and a function taking the value.\"\"\"\n    return (text.upper(), len(text))\n\n\nexample = ('hi',)\nprint(solve(*example))\n","q":606},{"t":"md","id":"dd-q607","role":"problem","src":"<!-- dd:dd-q607 -->\n\n### Problem 607 · independent\n\nWrite a function solve(x) that imports `math` under the shorter name `m` and returns the tuple (floor_value, ceil_value): x rounded down, and x rounded up. `import math as m` gives the same module a shorter name.\n\n**Expected output** — run the cell below once `solve` is right and it should print this.\n\n```text\n(2, 3)\n```\n","q":607},{"t":"code","id":"dd-q607-code","role":"code","src":"import math as m\n\n\n\ndef solve(x):\n    \"\"\"The same module, under a shorter name.\"\"\"\n    return None\n\n# Example run — the grader calls solve() with several different inputs,\n# including edge cases. Your function must work for all of them.\nexample = (2.3,)\nprint(solve(*example))\n","q":607},{"t":"code","id":"dd-q607-check","role":"check","src":"# Did it work? Run this. (NameError → run the checker cell at\n# the top of the notebook first: Runtime ▸ Run before.)\ndd_check(607)\n","q":607},{"t":"code","id":"dd-q607-solution","role":"solution","src":"#@title 💡 Solution — Problem 607\n# Running this rebinds `solve` to the reference answer. Re-run your\n# own cell before dd_check() again, or you are checking this one.\nimport math as m\n\n\ndef solve(x):\n    \"\"\"The same module, under a shorter name.\"\"\"\n    return (m.floor(x), m.ceil(x))\n\n\nexample = (2.3,)\nprint(solve(*example))\n","q":607},{"t":"md","id":"python-dots-and-imports-misconceptions","role":"prose","src":"#### Common mistakes\n\n- **\"Everything after a dot needs parentheses.\"** — Attributes do not.\n  `math.pi()` is an error; `math.pi` is a number.\n- **\"`text.len()` should work.\"** — Length is a built-in function, not a string\n  method. The arrangement is `len(text)`.\n- **\"`items.append(x)` gives me the longer list.\"** — It gives `None` and changes\n  `items` in place.\n"}]}
