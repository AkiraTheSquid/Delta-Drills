# numpy

## Purpose
- The array-programming knowledge-point lessons (42 KPs across registry lessons np-1..np-4): first-encounter teaching for every array KC in the drill course.
- **Fully converted to the PyTorch dialect.** Every page here teaches `import torch as t`, matching ARENA 0.0, which the KC graph mirrors: np-1 ("Arrays from the ground up", 12 KPs), np-2 ("Indexing and selection", 11 KPs), np-3 ("Vectorization and broadcasting", 11 KPs) and np-4 ("Applied patterns", 8 KPs). The folder name is historical — there is no NumPy teaching left in it, and the drills these pages fade into are torch as well.

## Owns
- One `kp-<slug>.md` per NumPy KC — concept prose, worked examples, faded/guided/independent exercise assignments, misconceptions.

## Does NOT own
- Format rules and registry (`../AUTHORING.md`, `../kc_registry.json`), tooling (`This-Directory-Only/scripts/`), einsum/einops content (sibling folders).

## Key Files
- `kp-ndarray-model.md`, `kp-constructors.md`, `kp-ranges.md`, `kp-dtype-astype.md`: np-1 openers — the pilot of the strict segment rhythm (one concept → one worked example → learner solves one) awaiting Seth's learner review.
- `kp-diag-triangles.md`: reference example of a 4-segment split (extract diag / trace / build diag / triangles).
- Remaining `kp-*.md`: mix of restructured (segments) and legacy single-segment files still awaiting the segment split. All of them are torch; what is outstanding is structure, not dialect.
- `kp-structured-dtypes.md`: **deleted, not converted.** If you are looking for np-4's ninth KP, it was retired on 2026-07-28 — see the invariant below.

## Data & External Dependencies
- Exercise ids reference `../../questions_structured.json`; segment structure is validated/compiled by `scripts/validate_lessons.py` / `compile_lessons.py`.

## How It Works (Flow)
1. Edit a KP following `../AUTHORING.md` (repeatable `## Concept:` segments).
2. Validate (`--coverage`) then compile; the app's lesson gate pages through segments.

## Invariants & Constraints
- **One dialect per page, and it must match the drills it fades into.** A page teaching `np.repeat` while its exercise grades `t.repeat_interleave` still passes every test and simply confuses the learner, so `watch.py` compares each KP's fence imports against the `answer_code` of every question id in its frontmatter. Convert a lesson and its bank questions in the same pass.
- **A KC the target dialect cannot express is RETIRED, not half-converted.** `numpy.structured-dtypes` taught record dtypes, `datetime64` and `genfromtxt`; a tensor holds one dtype for every element, so there is nothing in torch for those ideas to become. Inventing a torch-flavoured stand-in would have taught a NumPy idea in a dialect that cannot hold it, and leaving the page in NumPy would have re-broken the one-dialect rule above. So the page was `git rm`'d, its 6 drills (3, 55, 83, 96, 125, 157) were retired through `This-Directory-Only/chatgpt/function_mode_deleted_ids.json`, its rows were dropped from `../qmatrix_tags.json` and `../kc_registry.json`, and `../watch.py`'s KC floor moved 64 → 63 with the reason recorded beside the constant. Do the same four things together if another KC ever has to go: a registry entry with no page, or tags pointing at ids the bank no longer holds, fails `watch.py`.
- One concept per segment; exactly ONE worked example per segment; every segment has a faded exercise whose solution passes bank tests.
- Concept prose teaches the GENERAL procedure before the example (never example-only).
- Fences execute top-to-bottom in a shared namespace per file — later segments may rely on earlier definitions, so don't reorder segments without re-validating.

## Extension Points
- Splitting a legacy KP: reuse its guided/independent bank ids as per-segment faded exercises (dump contracts with the scratchpad `dumpq.py`/`avail.py` helpers or read the bank directly); update frontmatter lists to match.

## Known Issues, Recurring Bugs, and Pain Points (and How to Prevent Them)

- **Faded starter written against assumed contract** — `ACTIVE`
  - When it happens: promoting a guided/independent qid to faded without reading its test_cases.
  - Symptom: validator FAILs the solution (e.g. q64's tuple order is (allclose, array_equal), not the "obvious" order).
  - Root cause: question text/tests differ from what the prose suggests.
  - Prevention/fix: always dump the bank question's test_cases first; let the validator be the gate.

- **Exact float equality in a torch fence** — `ACTIVE`
  - When it happens: writing `assert x.tolist() == [0.1, 0.4, 0.9]` in a converted KP.
  - Symptom: `validate_lessons.py` reports `fence failed: AssertionError` with no further detail; the code is correct.
  - Root cause: torch's default float is **float32**, so `0.4` round-trips as `0.4000000059604645`. NumPy's float64 hid this.
  - Prevention/fix: use dyadic values in worked examples (0.25, 0.5, 0.75, 3.5, 9.5 — exact in binary), or assert with a tolerance. The grader itself is tolerant; only the lesson fences use bare `==`.

- **A local variable named `t` shadows the torch alias** — `ACTIVE`
  - When it happens: converting a page whose maths uses `t` as a name — an angle, a time, a threshold, a transform matrix.
  - Symptom: the fence dies with something unrelated-looking like `AttributeError: 'Tensor' object has no attribute 'sqrt'` on the NEXT line, because `t` is no longer the module.
  - Root cause: `import torch as t` is the ARENA convention, so the alias occupies a very ordinary one-letter name.
  - Prevention/fix: rename the variable before translating, not after (`theta`, `m`, `cutoff`). The drill generator does this through its `SHADOW_RENAMES` table for the bank side; the page has to be renamed by hand and the starter's blank renamed with it, or the faded exercise stops matching its solution.

## Recent Changes
- 2026-08-28: **`numpy.ndarray-model` split — the memory story moved out of the course's first page into two new concepts above it.** Seth hit q535 (`a.T` / `.contiguous()` / `.data_ptr()`) at the FADED rung of the very first concept and reported that none of it had been explained: "explain 'same memory block' and 'packed'. Don't explain jargon with more jargon". He was right twice over — the page used transposition with no context at all, and the only node that taught memory sharing (`numpy.memory-model`) sits in np-4, the LAST numpy lesson. The first thing a learner was drilled on was a concept the graph put near the end. Now: `kp-transpose-axes.md` (`numpy.transpose-axes`, prereq `numpy.ndarray-model`) teaches `a.T` as rows-become-columns with no memory talk, and `kp-views-and-copies.md` (`numpy.views-and-copies`, prereq `numpy.transpose-axes`) explains the block as *one long strip of numbers* and a *note saying how to read it* before ever using the words "block", "contiguous" or "packed". Eight questions moved off `kp-ndarray-model.md` onto them (523, 535, 536, 552, 553, 558, 561, 565 — q535 is now INTEGRATED, the composite it always was) and nine new ones were authored to fill the lower rungs (610–618). `numpy.slicing-views` and `numpy.memory-model` now depend on `numpy.views-and-copies`. q535's near-miss note was also factually wrong — it claimed a packed copy's pointer "is never the original's", which its own `[[9]]` and `[[1, 2]]` cases disprove. Two supporting fixes: `lesson_lib.blank_new_syntax` no longer blanks inside the function DOCSTRING (it had turned q535's prompt into "does a._____ share a's block?" — the learner could not read what was being asked), and because the new pages declare 1 and 3 `new_syntax` symbols instead of 15, their faded starters keep `t.tensor(...)` visible instead of blanking a call the same page drilled ten items earlier.
- 2026-07-30: The eight numpy KPs that had no guided rung got one (q523–q530, authored — the pool was exhausted). One page changed in substance: `kp-geometry-transforms` now SHOWS the polar→cartesian direction (`r * t.cos(theta)`, `r * t.sin(theta)`) in a runnable cell, having previously only described cartesian→polar; its guided item practises the inverse, and a guided item must not need a function the page never demonstrates. `kp-topk-selection`'s `new_syntax` was the free-text `topk`, which matched no symbol and declared nothing — it is now `torch.topk#dim, torch.topk#largest` (the page's own contribution; plain `torch.topk` belongs to `kp-sorting`), and `Tensor.flip` moved from `kp-memory-model` to `kp-argsort-ranking`, the first page that actually shows the method spelling.
- 2026-07-28: np-4's remaining 8 KPs converted to the PyTorch dialect alongside their 45 bank drills, which completes the migration — every page in this folder is now torch, and the bank is 448/448 torch. The ninth KP, `numpy.structured-dtypes`, was RETIRED instead (see the invariant above): tensors are homogeneous, so record dtypes, `datetime64` and `genfromtxt` have no torch form to convert into. q65 went the same way: the entry below says it "stays NumPy", and as of this pass it does not — a drill about `ndarray.flags.writeable` has no torch form either (read-only tensors do not exist), so it was retired alongside the structured-dtype six rather than left as the bank's last NumPy question. As in the earlier passes, several np-4 pages changed in SUBSTANCE: `kp-memory-model` now proves sharing with `x.untyped_storage().data_ptr()` because torch has no `.base` (and it must be the STORAGE pointer — plain `data_ptr()` folds in the view's offset, so `z[1:]` and `z` compare unequal despite sharing every byte), and it warns that torch overloads `view` to mean both "reinterpret dtype" and "reshape"; `kp-window-stencil` replaces `np.add.reduceat` with `split` + `sum`, since torch has no `reduceat` at all; `kp-scatter-gather` replaces the `np.add.at`/`np.maximum.at` family with one `scatter_reduce_`/`index_put_(..., accumulate=True)` method taking a `reduce=` argument, and flags that `repeat`/`repeat_interleave` trade meanings against NumPy; `kp-set-combinatorics` loses `np.r_`, `intersect1d` and `unique(return_index=)` and composes `t.cat` / `unique` + `isin` instead; `kp-random-sampling` swaps the numpy `Generator` for `t.Generator().manual_seed(...)`, `percentile` for `quantile` on a **0–1 scale taking a tensor**, and `choice` for `randperm` slicing, and has to cast a bool mask with `.to(t.int64)` before `argmax` because torch refuses `argmax` on bools; `kp-pairwise-metrics` notes that `t.corrcoef` always reads ROWS as the variables; `kp-geometry-transforms` renames its polar angle from `t` to `theta` for the alias clash above and spells `np.c_` out as `t.cat([..., t.ones(n, 1)], dim=1)`.
- 2026-07-28: np-2 and np-3's 22 KPs converted to the PyTorch dialect alongside their 120 bank questions. Several pages changed in SUBSTANCE, not just spelling, because the torch behaviour is the opposite of what they taught: `kp-centering` (torch `std` divides by n−1, NumPy by n — the population version now needs `correction=0`), `kp-inplace-out` (torch marks in-place with a trailing underscore, and `x.sort()` is NOT in place — there is no `sort_`), `kp-nonzero-argwhere` (torch's default is coordinate rows; the NumPy tuple layout needs `as_tuple=True`), `kp-topk-selection` (no partition/argpartition — `t.topk` returns values AND indices, already sorted), `kp-where-select` and `kp-rescaling` (no ufunc `where=` — masked assignment instead), `kp-sliding-windows` (`Tensor.unfold`, no stride_tricks or `convolve`), `kp-pad-borders` (`t.nn.functional.pad` takes LAST-dimension-first pairs), `kp-index-grids` (no `ogrid`), `kp-cumulative-diff` (`cummax`/`cummin` instead of `ufunc.accumulate`; `cumsum` requires `dim`) and `kp-unique` (no `intersect1d`/`union1d`/`setdiff1d` — compose `unique` with `isin`). q65 was dropped from `kp-inplace-out`'s independent list: `ndarray.flags.writeable` has no torch equivalent, so that one drill stays NumPy.
- 2026-07-27: np-1's 12 KPs converted to the PyTorch dialect alongside their 49 bank questions. Several KPs gained torch-specific content the NumPy version had no reason to teach: `t.flip` (negative slice steps are rejected), `.repeat` vs `repeat_interleave` (the names swap meaning against NumPy), `t.sort` returning a (values, indices) pair, `meshgrid`'s mandatory `indexing=`, and float32-vs-float64 defaults. `watch.py` filled with the lesson↔drill dialect check.
- 2026-07-19: ~20 KPs restructured into single-concept segments; np-1 openers trimmed to one-worked-example rhythm (pilot for Seth's review).
- 2026-07-15: Initial 33 KPs authored (Pass 1).
