# lessons

## Purpose
- First-encounter teaching content for Delta Drills easy topics (Numpy/Einsum/Einops): the knowledge-point (KP) lessons shown by the in-app lesson gate before a learner's first question on a new concept.

## Owns
- KP markdown sources (`numpy/`, `einsum/`, `einops/`), the KC registry (`kc_registry.json`), question→KC tags (`qmatrix_tags.json`), the compiled artifact (`lessons_structured.json`), the authoring spec (`AUTHORING.md`), and the read-only review viewer (`viewer.html`).

## Does NOT own
- Parsing/validation/compilation tooling (`scripts/` at repo root), the runtime gate UI (`../practice/lessons.js`), backend exposure state (`backend/app/lessons.py`), or the drill bank (`../questions_structured.json`).

## Key Files
- `AUTHORING.md`: the format contract — read it before editing any KP.
- `kc_registry.json`: 64 KCs across 9 lessons (np-1..4, es-1..2, eo-1..3); prereq edges must stay acyclic.
- `qmatrix_tags.json`: per-question target/supporting KC tags; drives the gate's "unexposed KC" check in local mode.
- `lessons_structured.json`: compiled output — NEVER hand-edit; regen via `scripts/compile_lessons.py`. Feeds both the frontend player and backend guard maps.
- `viewer.html`: static review viewer (serve this folder with `python3 -m http.server`).

## Data & External Dependencies
- Faded/guided/independent exercises reference drill-bank question ids; faded solutions must pass those questions' test_cases.
- Docker image COPYs this folder (root `.dockerignore` whitelists `!Local_Deployed_Shared/lessons/`).

## How It Works (Flow)
1. Author/edit a KP md as single-concept segments: explain → ONE worked example → ONE faded exercise.
2. `python3 scripts/validate_lessons.py --coverage` → `python3 scripts/compile_lessons.py`.
3. LessonGate renders one SEGMENT per page as a notebook: prose with every plain ` ```python ` fence — concept body and worked example alike — turned into a runnable cell sharing state top to bottom. Continue advances; finishing KP records KC exposure and resumes normal questions. Faded content is not rendered inside lesson.

## Invariants & Constraints
- **This directory is the SOURCE OF TRUTH for what the tutor may teach.** As of 2026-07-27 the ITS serves only questions carrying a `target_kcs` entry in `qmatrix_tags.json`; the 75 questions without one (CNN, PyTorch Fundamentals, Autograd, Optimizers) are parked until their chapter's KCs are authored and validated. So a question's presence here is what makes it reachable — adding `target_kcs` unparks it, removing them hides it. The older ARENA concept graph is demoted, not deleted; it is retained as the diff target for blind-authoring later chapters. See `docs/decision-kc-only-serving.md` and `docs/method-anchoring-controls-for-kc-authoring.md`.
- **Author later chapters blind.** The ARENA concept graph must not be in the authoring context — anchoring cannot be prompted away (Lou & Sun 2024: CoT, Thoughts-of-Principles, Ignore-Anchor-Hint and Reflection all failed, and authored structure is an "expert opinion" anchor, the strongest kind). Author fresh, then diff against ARENA as a check.
- One KP file per KC; one concept per segment; exactly ONE worked example per segment, and ONE or TWO faded exercises (Seth's format rules — see AUTHORING.md; the second faded item is a 2026-07-30 pilot, 3 segments so far, awaiting his review).
- Watch out renders inside lesson teaching content. Lesson screen has no popup, faded prompt, Check, or grading.
- A faded qid appears in at most one segment; frontmatter id lists mirror the sections.
- Never hand-edit `lessons_structured.json`; always validate before compiling.
- **A fence gets a Run button iff CI runs it.** Plain ` ```python ` = executed by `validate_lessons.py` AND rendered as a runnable notebook cell; ` ```python no-run ` = illustrative only, never executed anywhere. There is no third state, so a block a learner can run is a block that is known to work.
- **Each segment must stand alone.** The namespace is fresh PER SEGMENT (validator and player agree), because the lesson screen shows one segment per page — a fence leaning on a name defined in an earlier segment would hand the learner a NameError. Every segment opens with its own imports.
- Cells within one segment DO share state, top to bottom, like a notebook. Later fences may build on earlier ones on the same page; the runtime re-executes the whole prefix on each Run.
- Prefer fences that print or end in a bare expression, and add `assert` checks where a claim is worth proving. A cell whose output is empty falls back to listing the names it bound, so it never reads as "nothing happened".
- Bank einops fixtures hardcode `/delta_numbers.npy` — validator rewrites the path; don't "fix" it in content.

## Extension Points
- New concept → add KC to `kc_registry.json`, write `kp-<slug>.md`, retag affected questions (scripts/build_qmatrix.py), validate with `--coverage`.

## Known Issues, Recurring Bugs, and Pain Points (and How to Prevent Them)

- **Multi-concept lesson pages** — `RESOLVED`
  - When it happens: a KP's Concept section introduced several APIs before any practice.
  - Symptom: beginner overwhelmed; can't practice each idea separately (Seth's 2026-07-19 feedback).
  - Root cause: original Pass-1 format allowed one big Concept per KP.
  - Prevention/fix: segment format + validator enforcement; player pages one segment at a time.
  - Status: `RESOLVED` for np-1 openers + 19 restructured KPs; remaining KPs render as single-segment legacy pages until converted.

## Recent Changes
- 2026-07-30: The last 8 KPs with `guided: []` got a guided rung — **every KP now has all three rungs**. These eight had an exhausted pool (every numpy question in the bank was already spoken for by some KP), so the only honest options were reusing a question or authoring new ones; eight new questions were authored, q523–q530, one per KP, each teaching something its KP already covers but no item in the ladder ever asked for: `ndarray-model` q523 (transpose shares the block, `.contiguous()` copies it), `ranges` q524 (the exclusive stop pointed downwards, negative step), `argsort-ranking` q525 (sort-by, descending — argsort then flip), `topk-selection` q526 (`largest=False`, and reading `.values`/`.indices` off one result), `inplace-out` q527 (`clamp_`, with the grader checking the caller's own buffer changed), `memory-model` q528 (storage pointer vs `data_ptr` on a slice — the distinction the page names and nothing drilled), `geometry-transforms` q529 (polar → cartesian, columns assembled with `stack`), `linalg-applied` q530 (batched solve over leading axes). Authoring route: append to `csv files of problems/curated_additions.csv` (LAST source, so ids are minted above every existing one and nothing renumbers) plus a matching record in `chatgpt/curated_overrides.jsonl` for function mode and test cases — then `export_questions_json.py`, `build_qmatrix.py`, atom tags, `export_kc_atom_crosswalk.py`. All eight were graded through the real harness before shipping: canonical solution 4/4, placeholder starter 0/4, and expected values vary per case so a constant-return cheat cannot pass. Side effects: per-node-measurable KCs 23/63 (was 20), and three symbol declarations moved to their true first-showing page — `Tensor.flip` from kp-memory-model to kp-argsort-ranking, `torch.topk#largest`/`#dim` onto kp-topk-selection (whose `new_syntax` was the free-text `topk`, which declared nothing), `torch.cos`/`torch.sin` onto kp-geometry-transforms, which now shows the polar→cartesian direction it had only described. "Shown before it is taught" 27 → 25.
- 2026-07-30: `new_syntax:` completed — **149 undeclared symbols → 0**, and the bank's syntax violations fell 945 → 333 as a side effect (the bank was being blamed for symbols no lesson had claimed). Four collector defects were making the list partly fictional and had to go first: einops calls were filed as `Tensor.rearrange` (a method that does not exist), `t.nn.functional.pad` split into a phantom `Tensor.pad` plus `torch.nn` and `torch.nn.functional`, `type(x).__name__` became `Tensor.__name__`, and a demo's own helper function counted as an unexplained builtin. einops keyword args are no longer collected either — they are the pattern's axis names, invented per call (`h=2`, `g1=3`), so demanding a lesson for each would teach nothing. Ten Python-level entries (`assert`, `type`, `isinstance`, `repr`, `round`, `abs`, comprehensions, `for`, f-strings, `AssertionError`) moved into ASSUMED with the reasoning written down; `getattr` deliberately stayed out because kp-dtype-astype teaches it as a real move. The remaining 138 were declared on the page that shows them first, except where a later page is the unambiguous owner (`Tensor.item` → kp-aggregations' 0-d segment, `torch.einsum` → einsum's opener), which keeps the earlier use visible rather than papering over it. Five legacy free-text tags (`topk`, `slice-notation`, `matmul-operator`…) declared nothing and were dropped.
- 2026-07-30: Lesson ordering now uses the registry sequence, not lattice depth. `lesson_order()` ranked KCs by prerequisite depth, which is not a total order — nine of np-1's twelve KCs sit at depth 0 or 1, so "was this taught before that page?" was unanswerable across most of a chapter and same-depth uses silently passed. Registry order is verified to be a linear extension of the lattice (asserted at load; it holds today, 0 violating edges), so it says strictly more without contradicting the graph. Effect: "shown before it is taught" 17 → 27, all real. Biggest cluster is np-1 verification helpers — `Tensor.item`, `torch.equal`, `Tensor.mean/max` used in asserts and prints before kp-aggregations teaches them — plus `torch.einsum` in np-4's kp-linalg-applied, `torch.stack` in kp-tile-repeat-meshgrid, `Tensor.flip` in two np-2 pages. **Open backlog: fix by moving the use, moving the lesson, or accepting it — a curriculum-order call, not a mechanical one.**
- 2026-07-30: Second faded item piloted on 3 segments — the copy-completion backlog. `audit_ladder_pairing.py` flags 61 faded items whose solution computes nothing the worked example does not, i.e. the blank is filled by transcription and the ladder promotes on that evidence. Its own docstring says the fix is not editable into the 61 items: a first completion adjacent to the example is correct fading, and what is missing is the SECOND item that grows the distance. So the format now allows one or two faded items per segment (was exactly one — validator and AUTHORING.md updated), and three segments got a second: `numpy.rescaling` +q97 (norm to a target length, not to 1), `einsum.matvec-matmul` +q286 (`'d,nd->n'` — vector first, shared axis moved), `einops.merge-axes` +q347 (`'c h w -> h (c w)'` — non-adjacent merge). Each was MOVED from that KP's independent list, not copied, so no question is served twice. New per-segment metric: "series never reaching distance" 61 → 58. The raw per-item count stays 61 by design — the near-copy first items are not defects. **Pilot: awaiting Seth's review before the remaining 58.** Of those, 27 have an independent item that already clears both coverage and distance; 31 would need a new bank question authored.
- 2026-07-30: Silent worked examples eliminated corpus-wide — **0 remain**. The other 85 blocks (np-2 through eo-3, 48 files) now print what they compute, on the same principle used in np-1: print the contrast the block exists to teach, not a transcript of every line. Concretely that means printing both readings of an ambiguous pattern side by side (`(c g)` vs `(g c)` channel groups, `(h r)` vs `(r h)` repeats, `(h w)` vs `(w h)` merges), printing the shape arithmetic a merge or split performs, and turning bare `except SomeError: pass` blocks into `except SomeError as err: print(...)` so the learner sees the actual message for the mistake the KP warns about. Three assert-only *check* cells in np-1 (`kp-aggregations`, `kp-dtype-astype`, `kp-elementwise-ufuncs`) were also silent and now report what they verified. All 63 KP files execute under `validate_lessons.py --coverage`; the compiled artifact has zero silent runnable fences.
- 2026-07-30: Silent worked examples fixed in np-1. `scripts/audit_lesson_syntax.py` counts worked examples that "show the learner nothing" — assert-only blocks that run clean and print a blank result pane. **112 of the corpus's 122 worked-example cells were silent**; all 27 in np-1 now print what they compute. This is the content half of the "it always says no printed output" complaint: the player-side fix landed 2026-07-30, but a cell with no `print` genuinely has no output to show. Remaining backlog: 85 silent worked examples in np-2..eo-3.
- 2026-07-30: Chapter np-1 converted to the notebook format for real. All 12 of its KPs had exactly one runnable cell (the worked example); they now carry 4–12 each, and every one is executed by `validate_lessons.py` before it ships — the fence-iff-CI invariant is what keeps the Run buttons honest. The cells are demonstrations, not decoration: contrasts printed side by side (`repeat` vs `repeat_interleave`, `floor` vs `trunc`, `*` vs `@`, xy vs ij indexing), `try/except` blocks that let the learner see the actual error text for the mistakes the KP exists to prevent, and asserts on the claims worth proving. This is the pilot Seth asked to review before np-2..eo-3 get the same treatment; the other 51 KPs still have one cell each on purpose.
- 2026-07-30: Practice inventory audited and filled out. 78 bank questions were tagged to a KC but referenced by no KP — pool nothing served. 77 are now wired in: 12 became new guided items with authored hints (filling KPs that had `guided: []`), 65 became independent items. Guided items are also SERVED now (`practice/kc-practice.js`), where before they compiled into the artifact and only `viewer.html` read them. Counts: guided 52 → 64, independent 164 → 229, distinct bank questions reached 338 → 415 of 416 tagged. The single exclusion is **q387, a true duplicate of q322**; a solution-level scan of all 491 bank questions found exactly that one duplicate pair, so recycling is not a systemic problem here — and no question is practice for two different KCs. Still open: 8 KPs (`argsort-ranking`, `geometry-transforms`, `inplace-out`, `linalg-applied`, `memory-model`, `ndarray-model`, `ranges`, `topk-selection`) have `guided: []` AND an exhausted pool, so giving them a guided item means either authoring new bank questions or reusing one, and reuse is the thing we are avoiding.
- 2026-07-30: Lesson pages became notebooks. Every plain ` ```python ` fence in a segment's Concept prose — not just the one in Worked example — is now a runnable cell, and cells on a page share state top to bottom. `validate_lessons.py` switched from a KP-wide namespace to a fresh namespace PER SEGMENT so CI mirrors what the learner can actually reach (all 122 existing segments already passed in isolation). Pilot content: `numpy/kp-ndarray-model.md` rewritten to 3/2/2 fences with asserts and printed output. Pilot only — see Seth's standing rule about not mass-converting before review.
- 2026-07-27: `lessons/` promoted to **source of truth** for the tutor. The ITS now serves only KC-tagged questions (380 servable, 75 parked); the ARENA concept graph is demoted but retained. Validation runs chapter by chapter with the learner in the loop — walk the 64-KC chapter 1, fix/add what's missing, then blind-author the next chapter. See the two new invariants above.
- 2026-07-20: Lesson changed to inline teaching + optional runnable worked code; faded practice removed from lesson UI.
- 2026-07-19: Segmented format introduced; ~20 numpy KPs restructured; compiled JSON gains per-KP `segments`.
- 2026-07-15: Pass 1 content created (64 KPs).
