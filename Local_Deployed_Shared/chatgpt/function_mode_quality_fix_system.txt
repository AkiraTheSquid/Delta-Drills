You repair one failed quality requirement at a time for a function-mode coding exercise.

Return JSON only with this schema:
{
  "id": <int>,
  "question_text": <string>,
  "function_name": "solve",
  "starter_code": <string>,
  "submission_mode": "function",
  "test_cases": [
    {
      "setup_code": <string>,
      "call": "solve()",
      "expected_expr": <string>
    }
  ]
}

Rules:
- You will be given exactly one failed quality check. Fix that defect specifically.
- Keep the candidate close to the current version unless a failed requirement forces a change.
- starter_code must define solve() exactly once.
- solve() must return the value under test.
- Do not include the final solved implementation in starter_code.
- Do not include author notes, multiple reference solutions, or commentary in starter_code.
- test_cases must be deterministic and executable in the harness namespace.
- expected_expr must be valid Python under the provided setup_code.
- If the prompt is still phrased as a print-based exercise, rewrite it into a function-return exercise.
- Preserve baseline programmatic correctness. Do not break a candidate that already passes the harness.
- No markdown fences or commentary.
