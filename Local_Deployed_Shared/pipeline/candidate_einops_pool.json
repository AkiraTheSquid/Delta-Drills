{
  "95": "import torch as t\nimport einops\n\ndef solve(img):\n    return einops.einsum(img, t.tensor([0.299, 0.587, 0.114]), '... c, c -> ...')\n",
  "144": "import torch as t\nimport einops\n\ndef solve(z, v):\n    return einops.einsum(z, v, 'n m, m -> n')\n",
  "151": "import torch as t\nimport einops\n\ndef solve(a, b):\n    return a * einops.rearrange(b, 'h w -> h w 1')\n",
  "23": "import torch as t\nimport einops\n\ndef solve(z):\n    return einops.rearrange(z, 'h w -> (w h)')\n",
  "490": "import torch as t\nimport einops\n\ndef solve(x):\n    \"\"\"Return x collapsed to a single axis.\"\"\"\n    return einops.rearrange(x, '... -> (...)')\n",
  "510": "import torch as t\nimport einops\n\ndef solve(a):\n    \"\"\"Return the transpose of a 2-D tensor, and its shape.\"\"\"\n    tr = einops.rearrange(a, 'i j -> j i')\n    return (tr.tolist(), tuple(tr.shape))\n",
  "558": "import torch as t\nimport einops\n\ndef solve(rows):\n    \"\"\"Return a.T's contents as a plain nested list.\"\"\"\n    a = t.tensor(rows)\n    return einops.rearrange(a, 'i j -> j i').tolist()\n",
  "84": "import torch as t\nimport einops\n\ndef solve(a, b):\n    return einops.reduce([a, b], 'two ... -> ...', 'mean')\n",
  "135": "import torch as t\nimport einops\n\ndef solve(x):\n    return einops.reduce(x, '... h w -> ...', 'sum')\n",
  "220": "import torch as t\nimport einops\n\ndef solve(x):\n    return einops.reduce(x, 'h w -> w', 'sum')\n",
  "418": "import torch\nimport einops\n\ndef solve(x):\n    return einops.reduce(x, 'b c h w -> b c', 'mean')\n",
  "503": "import torch as t\nimport einops\n\ndef solve(x):\n    \"\"\"Return the sum of each ROW.\"\"\"\n    return einops.reduce(x, 'h w -> h', 'sum')\n",
  "504": "import torch as t\nimport einops\n\ndef solve(x):\n    \"\"\"Return the row sums with the reduced axis KEPT.\"\"\"\n    return einops.reduce(x, 'h w -> h 1', 'sum')\n",
  "505": "import torch as t\nimport einops\n\ndef solve(x):\n    \"\"\"Make every row of a float tensor sum to 1.\"\"\"\n    return x / einops.reduce(x, 'h w -> h 1', 'sum')\n",
  "515": "import torch as t\nimport einops\n\ndef solve(x):\n    \"\"\"Scale every row to unit length.\"\"\"\n    return x / einops.reduce(x.pow(2), 'h w -> h 1', 'sum').sqrt()\n",
  "1122": "import torch as t\nimport einops\n\ndef solve(ny, nz, yl, zl):\n    y = t.linspace(-yl, yl, ny)\n    z = t.linspace(-zl, zl, nz)\n    yy = einops.repeat(y, 'y -> (y z)', z=nz)\n    zz = einops.repeat(z, 'z -> (y z)', y=ny)\n    return einops.rearrange([yy, zz], 'two n -> n two')\n",
  "1123": "import torch as t\nimport einops\n\ndef solve(ny, nz, yl, zl):\n    y = t.linspace(-yl, yl, ny)\n    z = t.linspace(-zl, zl, nz)\n    yy = einops.repeat(y, 'y -> (y z)', z=nz)\n    zz = einops.repeat(z, 'z -> (y z)', y=ny)\n    r = t.zeros(ny * nz, 2, 3)\n    r[:, 1, 0] = 1\n    r[:, 1, 1] = yy\n    r[:, 1, 2] = zz\n    return r\n",
  "1124": "import torch as t\nimport einops\n\ndef solve(ny, nz, yl, zl):\n    y = t.linspace(-yl, yl, ny)\n    z = t.linspace(-zl, zl, nz)\n    yy = einops.repeat(y, 'y -> (y z)', z=nz)\n    zz = einops.repeat(z, 'z -> (y z)', y=ny)\n    r = t.zeros(ny * nz, 2, 3)\n    r[:, 1, 0] = 1\n    r[:, 1, 1] = yy\n    r[:, 1, 2] = zz\n    return r[:, 1]\n",
  "1127": "import torch as t\nimport einops\n\ndef solve(ny, nz, yl, zl):\n    y = t.linspace(-yl, yl, ny)\n    z = t.linspace(-zl, zl, nz)\n    yy = einops.repeat(y, 'y -> (y z)', z=nz)\n    zz = einops.repeat(z, 'z -> (y z)', y=ny)\n    r = t.zeros(ny * nz, 2, 3)\n    r[:, 1, 0] = 1\n    r[:, 1, 1] = yy\n    r[:, 1, 2] = zz\n    return r[:, 0] + 2 * r[:, 1]\n",
  "1128": "import torch as t\nimport einops\n\ndef solve(ny, nz, yl, zl):\n    y = t.linspace(-yl, yl, ny)\n    z = t.linspace(-zl, zl, nz)\n    yy = einops.repeat(y, 'y -> (y z)', z=nz)\n    zz = einops.repeat(z, 'z -> (y z)', y=ny)\n    r = t.zeros(ny * nz, 2, 3)\n    r[:, 1, 0] = 1\n    r[:, 1, 1] = yy\n    r[:, 1, 2] = zz\n    return einops.rearrange(r, '(ny nz) pts d -> ny nz pts d', ny=ny, nz=nz)[:, 0]\n",
  "1129": "import torch as t\nimport einops\n\ndef solve(ny, nz, yl, zl):\n    y = t.linspace(-yl, yl, ny)\n    z = t.linspace(-zl, zl, nz)\n    yy = einops.repeat(y, 'y -> (y z)', z=nz)\n    zz = einops.repeat(z, 'z -> (y z)', y=ny)\n    r = t.zeros(ny * nz, 2, 3)\n    r[:, 1, 0] = 1\n    r[:, 1, 1] = yy\n    r[:, 1, 2] = zz\n    r[:, 0, 1] = 1\n    r[:, 1, 1] = r[:, 1, 1] - 1\n    return r\n",
  "1130": "import torch as t\nimport einops\n\ndef solve(ny, nz, yl, zl):\n    y = t.linspace(-yl, yl, ny)\n    z = t.linspace(-zl, zl, nz)\n    yy = einops.repeat(y, 'y -> (y z)', z=nz)\n    zz = einops.repeat(z, 'z -> (y z)', y=ny)\n    r = t.zeros(ny * nz, 2, 3)\n    r[:, 1, 0] = 1\n    r[:, 1, 1] = yy\n    r[:, 1, 2] = zz\n    r[:, 1, 0] = 2\n    return r[:, 1]\n",
  "1192": "import torch as t\nimport einops\n\ndef solve(x, w, b):\n    y = x @ w.T + b\n    return einops.rearrange(y, '... out_f -> (...) out_f')\n",
  "1193": "import torch as t\nimport einops\n\ndef solve(x, w, b):\n    y = x @ w.T + b\n    return einops.reduce(y, '... out_f -> out_f', 'mean')\n",
  "1201": "import torch as t\nimport einops\n\ndef solve(x):\n    return einops.rearrange(x, 'b ... -> b (...)')\n",
  "1202": "import torch as t\nimport einops\n\ndef solve(x, w1, b1):\n    flat = einops.rearrange(x, 'b ... -> b (...)')\n    return flat @ w1.T + b1\n",
  "1203": "import torch as t\nimport einops\n\ndef solve(x, w1, b1):\n    flat = einops.rearrange(x, 'b ... -> b (...)')\n    h = (flat @ w1.T + b1).clamp(min=0)\n    return h\n",
  "1204": "import torch as t\nimport einops\n\ndef solve(x, w1, b1, w2, b2):\n    flat = einops.rearrange(x, 'b ... -> b (...)')\n    h = (flat @ w1.T + b1).clamp(min=0)\n    y = h @ w2.T + b2\n    return y\n",
  "1205": "import torch as t\nimport einops\n\ndef solve(x, w1, b1, w2, b2):\n    flat = einops.rearrange(x, 'b ... -> b (...)')\n    h = (flat @ w1.T + b1).clamp(min=0)\n    return einops.reduce(h @ w2.T + b2, 'b c -> c', 'mean')\n",
  "1206": "import torch as t\nimport einops\n\ndef solve(x, w1, b1, w2, b2):\n    flat = einops.rearrange(x, 'b ... -> b (...)')\n    h = (flat @ w1.T + b1).clamp(min=0)\n    y = h @ w2.T + b2\n    return y.argmax(dim=1)\n",
  "1207": "import torch as t\nimport einops\n\ndef solve(x, w1, b1):\n    flat = einops.rearrange(x, 'b ... -> b (...)')\n    h = (flat @ w1.T + b1).clamp(min=0)\n    return einops.reduce(h, 'b hidden -> hidden', 'mean')\n",
  "1208": "import torch as t\nimport einops\n\ndef solve(x, w1, b1):\n    flat = einops.rearrange(x, 'b ... -> b (...)')\n    h = (flat @ w1.T + b1).clamp(min=0)\n    return (h > 0).sum(dim=1)\n",
  "1210": "import torch as t\nimport einops\n\ndef solve(x, w1, b1, w2, b2):\n    flat = einops.rearrange(x, 'b ... -> b (...)')\n    h = (flat @ w1.T + b1).clamp(min=0)\n    y = h @ w2.T + b2\n    return y.softmax(dim=-1)\n",
  "1212": "import torch as t\nimport einops\n\ndef solve(x, w1, b1, w2):\n    flat = einops.rearrange(x, 'b ... -> b (...)')\n    h0 = (flat @ w1.T + b1)[:, :1].clamp(min=0)\n    return einops.einsum(h0, w2[:, :1], 'b one, classes one -> b classes')\n",
  "1219": "import torch as t\nimport einops\n\ndef solve(x, v):\n    return einops.repeat(v, 'n -> m n', m=x.shape[0])\n",
  "1222": "import torch as t\nimport einops\n\ndef solve(v):\n    return einops.einsum(v, v, 'n, m -> n m')\n",
  "1224": "import torch as t\nimport einops\n\ndef solve(x):\n    return einops.repeat(x, 'm n -> m n 2')\n",
  "821": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(ray, us):\n    \"\"\"Many points on one ray, by broadcasting u down a column.\"\"\"\n    ray = t.tensor(ray)\n    u = t.tensor(us)\n    u_col = einops.rearrange(u, \"k -> k 1\")\n    return (ray[0] + u_col * ray[1]).tolist()\n",
  "826": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(ray, n):\n    \"\"\"n integer steps along one ray.\"\"\"\n    ray = t.tensor(ray)\n    u = einops.rearrange(t.arange(n), \"n -> n 1\")\n    return (ray[0] + u * ray[1]).tolist()\n",
  "827": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(rays, u):\n    \"\"\"One u, many rays.\"\"\"\n    rays = t.tensor(rays)\n    origins = einops.rearrange(rays[:, 0], \"n d -> n d\")\n    dirs = einops.rearrange(rays[:, 1], \"n d -> n d\")\n    return (origins + u * dirs).tolist()\n",
  "828": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(rays, x):\n    \"\"\"Per-ray u from x, then per-ray y, vectorised.\"\"\"\n    rays = t.tensor(rays)\n    origins = rays[:, 0]\n    dirs = rays[:, 1]\n    u = (x - origins[:, 0]) / dirs[:, 0]\n    u_col = einops.rearrange(u, \"n -> n 1\")\n    return (origins[:, 1] + u * dirs[:, 1]).tolist()\n",
  "829": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(ray, xs):\n    \"\"\"Many x's -> many u's -> many points, one broadcast.\"\"\"\n    ray = t.tensor(ray)\n    u = (t.tensor(xs) - ray[0, 0]) / ray[1, 0]\n    u_col = einops.rearrange(u, \"k -> k 1\")\n    return (ray[0] + u_col * ray[1]).tolist()\n",
  "839": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(ny, nz, y_limit, z_limit):\n    \"\"\"make_rays_2d: a grid of directions in one canvas.\"\"\"\n    rays = t.zeros((ny, nz, 2, 3))\n    rays[:, :, 1, 0] = 1\n    y_vals = t.linspace(-y_limit, y_limit, ny)\n    z_vals = t.linspace(-z_limit, z_limit, nz)\n    rays[:, :, 1, 1] = einops.repeat(y_vals, \"ny -> ny nz\", nz=nz)\n    rays[:, :, 1, 2] = einops.repeat(z_vals, \"nz -> ny nz\", ny=ny)\n    return einops.rearrange(rays, \"ny nz r d -> (ny nz) r d\").tolist()\n",
  "451": "# Solution created by Antigravity\nimport einops\nimport torch\n\ndef solve(X, Y, dLdM):\n    \"\"\"Matmul backward rules via einsum: dL/dX = (dL/dM) Y^T, dL/dY = X^T (dL/dM).\"\"\"\n    dLdX = einops.einsum(dLdM, Y, \"n m, k m -> n k\")\n    dLdY = einops.einsum(X, dLdM, \"n k, n m -> k m\")\n    return (dLdX, dLdY)\n",
  "454": "# Solution created by Antigravity\nimport einops\nimport torch\n\ndef solve(x):\n    \"\"\"Per-channel statistics over batch and spatial dims via einops.\"\"\"\n    mean = einops.reduce(x, \"b c h w -> c\", \"mean\")\n    diff = x - einops.rearrange(mean, \"c -> 1 c 1 1\")\n    var = einops.reduce(diff ** 2, \"b c h w -> c\", \"mean\")\n    return (mean, var)\n",
  "459": "# Solution created by Antigravity\nimport einops\nimport torch\n\ndef solve(x, weight, bias, mean, var):\n    \"\"\"BatchNorm per-channel broadcasting via rearrange.\"\"\"\n    w = einops.rearrange(weight, \"c -> 1 c 1 1\")\n    b = einops.rearrange(bias, \"c -> 1 c 1 1\")\n    m = einops.rearrange(mean, \"c -> 1 c 1 1\")\n    v = einops.rearrange(var, \"c -> 1 c 1 1\")\n    return (x - m) / torch.sqrt(v) * w + b\n",
  "460": "# Solution created by Antigravity\nimport einops\nimport torch\n\ndef solve(weight):\n    \"\"\"Align length-C vector to dim 1 of a 4D tensor.\"\"\"\n    return einops.rearrange(weight, \"c -> 1 c 1 1\")\n",
  "461": "# Solution created by Antigravity\nimport einops\nimport torch\n\ndef solve(x, bias):\n    \"\"\"Bias broadcasting alignment via rearrange.\"\"\"\n    C = x.shape[1]\n    b = einops.rearrange(bias, \"c -> 1 c 1 1\")\n    try:\n        bad = x + bias\n        return (\"no error\", x + b)\n    except RuntimeError:\n        return (\"raised\", x + b)\n",
  "37": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(a, b):\n    return float(einops.einsum(a, b, \"i, i -> \"))\n",
  "62": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(z):\n    return int(einops.reduce(z, \"i -> \", \"sum\"))\n",
  "89": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(a, b):\n    return einops.rearrange(t.stack((a, b), dim=1), \"n two -> (n two)\")\n",
  "121": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(a, b):\n    return einops.einsum(a, b, \"i j, i j -> i\")\n",
  "141": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(a, b):\n    return einops.einsum(a, b, \"i j, j i -> i\")\n",
  "501": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, scale):\n    \"\"\"Scale every row by its own factor.\"\"\"\n    return x * einops.rearrange(scale, \"r -> r 1\")\n",
  "502": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(rows, cols):\n    \"\"\"Build multiplication table i * j via einsum outer product.\"\"\"\n    return einops.einsum(t.arange(rows), t.arange(cols), \"r, c -> r c\")\n",
  "509": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(a, b):\n    \"\"\"Elementwise product and matrix product.\"\"\"\n    return (a * b, einops.einsum(a, b, \"i k, k j -> i j\"))\n",
  "511": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(a, x):\n    \"\"\"Return a @ x for a matrix and a vector, plus the result shape.\"\"\"\n    r = einops.einsum(a, x, \"m n, n -> m\")\n    return (r.tolist(), tuple(r.shape))\n",
  "513": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(mats, x):\n    \"\"\"Apply a BATCH of matrices to one vector.\"\"\"\n    r = einops.einsum(mats, x, \"b m n, n -> b m\")\n    return (r.tolist(), tuple(r.shape))\n",
  "514": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(a, b):\n    \"\"\"Dot product via einsum contraction.\"\"\"\n    return float(einops.einsum(a, b, \"i, i -> \"))\n",
  "950": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, s):\n    \"\"\"Scale each sample in batch by s.\"\"\"\n    return x * einops.rearrange(s, \"b -> b 1 1\")\n",
  "952": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(a, b):\n    \"\"\"(m, n) table where entry [i, j] = a[i] * b[j].\"\"\"\n    return einops.einsum(a, b, \"m, n -> m n\")\n",
  "953": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(imgs, mean):\n    \"\"\"Each channel of every image centered by channel mean.\"\"\"\n    return imgs - einops.rearrange(mean, \"c -> 1 1 1 c\")\n",
  "954": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, row_scale, col_bias):\n    \"\"\"Rows scaled by row_scale, then cols shifted by col_bias.\"\"\"\n    return x * einops.rearrange(row_scale, \"r -> r 1\") + einops.rearrange(col_bias, \"c -> 1 c\")\n",
  "958": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(origins, directions, ts):\n    \"\"\"(b, k, 3) points along b rays at k parameters.\"\"\"\n    o = einops.rearrange(origins, \"b d -> b 1 d\")\n    d = einops.rearrange(directions, \"b d -> b 1 d\")\n    t_expanded = einops.rearrange(ts, \"k -> 1 k 1\")\n    return o + t_expanded * d\n",
  "974": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(a, b):\n    \"\"\"Matrices joined top to bottom.\"\"\"\n    out, _ = einops.pack([a, b], \"* c\")\n    return out\n",
  "975": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(a, b):\n    \"\"\"Matrices joined side by side.\"\"\"\n    out, _ = einops.pack([a, b], \"r *\")\n    return out\n",
  "976": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(a, b):\n    \"\"\"Two vectors alternated.\"\"\"\n    return einops.rearrange(t.stack((a, b), dim=1), \"n two -> (n two)\")\n",
  "979": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(a, b, c):\n    \"\"\"Three vectors interleaved.\"\"\"\n    return einops.rearrange(t.stack((a, b, c), dim=1), \"n three -> (n three)\")\n",
  "983": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(mats):\n    \"\"\"The pile, its elementwise mean, and how many were piled.\"\"\"\n    piled = t.stack(mats, dim=0)\n    mean = einops.reduce(piled, \"k ... -> ...\", \"mean\")\n    return piled, mean, int(piled.shape[0])\n",
  "1363": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    \"\"\"Row totals for every one of the n matrices.\"\"\"\n    return einops.reduce(x, \"n r c -> n r\", \"sum\")\n",
  "1364": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    \"\"\"Average the n matrices down to one.\"\"\"\n    return einops.reduce(x, \"n r c -> r c\", \"mean\")\n",
  "1365": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    \"\"\"Column means that still have two axes.\"\"\"\n    return einops.reduce(x, \"r c -> 1 c\", \"mean\")\n",
  "1366": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    \"\"\"One mean per slice, shaped to broadcast back over it.\"\"\"\n    return einops.reduce(x, \"n h w -> n 1 1\", \"mean\")\n",
  "1367": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    \"\"\"Each column's mean, relative to the grand mean.\"\"\"\n    col_mean = einops.reduce(x, \"r c -> c\", \"mean\")\n    grand_mean = einops.reduce(x, \"r c -> \", \"mean\")\n    return col_mean - grand_mean\n",
  "1368": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(imgs):\n    \"\"\"One number per image: its average brightness.\"\"\"\n    return einops.reduce(imgs, \"b h w -> b\", \"mean\")\n",
  "1369": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    \"\"\"Centre each row on its own mean.\"\"\"\n    return x - einops.reduce(x, \"r c -> r 1\", \"mean\")\n",
  "1370": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    \"\"\"The heaviest column's total.\"\"\"\n    return float(einops.reduce(x, \"r c -> c\", \"sum\").max())\n",
  "1371": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(counts):\n    \"\"\"Average share of each column, after every row is made to sum to 1.\"\"\"\n    shares = counts / einops.reduce(counts, \"r c -> r 1\", \"sum\")\n    return einops.reduce(shares, \"r c -> 1 c\", \"mean\")\n",
  "1372": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(imgs):\n    \"\"\"Turn each image into a distribution over its own pixels.\"\"\"\n    return imgs / einops.reduce(imgs, \"b h w -> b 1 1\", \"sum\")\n",
  "1373": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(imgs):\n    \"\"\"Three summaries of one batch, each over a different set of axes.\"\"\"\n    per_image = einops.reduce(imgs, \"b h w -> b\", \"sum\")\n    per_pixel = einops.reduce(imgs, \"b h w -> h w\", \"mean\")\n    overall = float(einops.reduce(imgs, \"b h w -> \", \"mean\"))\n    return per_image, per_pixel, overall\n",
  "1303": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    \"\"\"Population variance of each input channel.\"\"\"\n    mean = einops.reduce(x, \"b c h w -> c\", \"mean\")\n    diff = x - einops.rearrange(mean, \"c -> 1 c 1 1\")\n    return einops.reduce(diff ** 2, \"b c h w -> c\", \"mean\")\n",
  "1304": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, rv, momentum):\n    \"\"\"Updated running variance after this batch.\"\"\"\n    mean = einops.reduce(x, \"b c h w -> c\", \"mean\")\n    diff = x - einops.rearrange(mean, \"c -> 1 c 1 1\")\n    var = einops.reduce(diff ** 2, \"b c h w -> c\", \"mean\")\n    return (1 - momentum) * rv + momentum * var\n",
  "1309": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, g, b, eps):\n    \"\"\"Training-mode channel means and population variances after affine transform.\"\"\"\n    mean = einops.reduce(x, \"b c h w -> c\", \"mean\")\n    diff = x - einops.rearrange(mean, \"c -> 1 c 1 1\")\n    var = einops.reduce(diff ** 2, \"b c h w -> c\", \"mean\")\n    z = diff / t.sqrt(einops.rearrange(var, \"c -> 1 c 1 1\") + eps)\n    y = z * einops.rearrange(g, \"c -> 1 c 1 1\") + einops.rearrange(b, \"c -> 1 c 1 1\")\n    ym = einops.reduce(y, \"b c h w -> c\", \"mean\")\n    y_diff = y - einops.rearrange(ym, \"c -> 1 c 1 1\")\n    yv = einops.reduce(y_diff ** 2, \"b c h w -> c\", \"mean\")\n    return t.stack((ym, yv))\n",
  "1312": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(g, b, rm, rv, eps, momentum):\n    class BatchNorm(t.nn.Module):\n        def __init__(self):\n            super().__init__()\n            self.weight = t.nn.Parameter(g.clone())\n            self.bias = t.nn.Parameter(b.clone())\n            self.register_buffer(\"running_mean\", rm.clone())\n            self.register_buffer(\"running_var\", rv.clone())\n            self.register_buffer(\"num_batches_tracked\", t.tensor(0))\n        def forward(self, x):\n            if self.training:\n                mean = einops.reduce(x, \"b c h w -> c\", \"mean\")\n                diff = x - einops.rearrange(mean, \"c -> 1 c 1 1\")\n                var = einops.reduce(diff ** 2, \"b c h w -> c\", \"mean\")\n                with t.no_grad():\n                    self.running_mean.copy_((1 - momentum) * self.running_mean + momentum * mean)\n                    self.running_var.copy_((1 - momentum) * self.running_var + momentum * var)\n                    self.num_batches_tracked += 1\n            else:\n                mean = self.running_mean\n                var = self.running_var\n                diff = x - einops.rearrange(mean, \"c -> 1 c 1 1\")\n            z = diff / t.sqrt(einops.rearrange(var, \"c -> 1 c 1 1\") + eps)\n            out = z * einops.rearrange(self.weight, \"c -> 1 c 1 1\") + einops.rearrange(self.bias, \"c -> 1 c 1 1\")\n            return einops.reduce(out, \"b c h w -> b c\", \"mean\")\n    return BatchNorm()\n",
  "1416": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(c, n_classes):\n    class Head(t.nn.Module):\n        def __init__(self):\n            super().__init__()\n            self.lin = t.nn.Linear(c, n_classes)\n        def forward(self, x):\n            return self.lin(einops.reduce(x, \"b c h w -> b c\", \"mean\"))\n    return Head()\n",
  "993": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    \"\"\"Return row lengths via einops reduction.\"\"\"\n    return t.sqrt(einops.reduce(x * x, \"m n -> m\", \"sum\"))\n",
  "994": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    \"\"\"Return squared row lengths, shape (m, 1).\"\"\"\n    return einops.reduce(x * x, \"m n -> m 1\", \"sum\")\n",
  "995": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    \"\"\"Return unit rows, shape (m, n).\"\"\"\n    row_norms = t.sqrt(einops.reduce(x * x, \"m n -> m 1\", \"sum\"))\n    return x / row_norms\n",
  "996": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    \"\"\"Return each row rescaled to length three.\"\"\"\n    row_norms = t.sqrt(einops.reduce(x * x, \"m n -> m 1\", \"sum\"))\n    return 3 * x / row_norms\n",
  "997": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    \"\"\"Return distance of each row from origin.\"\"\"\n    return t.sqrt(einops.reduce(x * x, \"m n -> m\", \"sum\"))\n",
  "998": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    \"\"\"Return one unit direction per row, shape (m, n).\"\"\"\n    n = t.sqrt(einops.reduce(x * x, \"m n -> m 1\", \"sum\"))\n    return x / t.clamp(n, min=1e-6)\n",
  "1001": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    v = x[:, 1:]\n    v_norm = t.sqrt(einops.reduce(v * v, \"m n -> m 1\", \"sum\"))\n    return v / v_norm\n",
  "1005": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    row_norm = t.sqrt(einops.reduce(x * x, \"m n -> m 1\", \"sum\"))\n    u = x / row_norm\n    u_mean = einops.reduce(u, \"m n -> n\", \"mean\")\n    return t.sqrt(einops.reduce(u_mean * u_mean, \"n -> \", \"sum\"))\n",
  "1009": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, y):\n    a = x / t.sqrt(einops.reduce(x * x, \"m d -> m 1\", \"sum\"))\n    b = y / t.sqrt(einops.reduce(y * y, \"n d -> n 1\", \"sum\"))\n    s = einops.einsum(a, b, \"m d, n d -> m n\")\n    return s.max(dim=1)[0]\n",
  "1010": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, y):\n    a = x / t.sqrt(einops.reduce(x * x, \"m d -> m 1\", \"sum\"))\n    b = y[0] / t.sqrt(einops.reduce(y[0] * y[0], \"d -> \", \"sum\"))\n    return einops.einsum(a, b, \"m d, d -> m\")\n",
  "1011": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, y):\n    \"\"\"Similarities for every candidate against every query, shape (n, m).\"\"\"\n    a = x / t.sqrt(einops.reduce(x * x, \"m d -> m 1\", \"sum\"))\n    b = y / t.sqrt(einops.reduce(y * y, \"n d -> n 1\", \"sum\"))\n    return einops.einsum(b, a, \"n d, m d -> n m\")\n",
  "1013": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, y):\n    \"\"\"Each query's mean similarity to candidates.\"\"\"\n    a = x / t.sqrt(einops.reduce(x * x, \"m d -> m 1\", \"sum\"))\n    b = y / t.sqrt(einops.reduce(y * y, \"n d -> n 1\", \"sum\"))\n    s = einops.einsum(a, b, \"m d, n d -> m n\")\n    return einops.reduce(s, \"m n -> m\", \"mean\")\n",
  "1017": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, y):\n    \"\"\"Euclidean distances between normalized query and candidate directions.\"\"\"\n    a = x / t.sqrt(einops.reduce(x * x, \"m d -> m 1\", \"sum\"))\n    b = y / t.sqrt(einops.reduce(y * y, \"n d -> n 1\", \"sum\"))\n    diff = einops.rearrange(a, \"m d -> m 1 d\") - einops.rearrange(b, \"n d -> 1 n d\")\n    return t.sqrt(einops.reduce(diff * diff, \"m n d -> m n\", \"sum\"))\n",
  "1018": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, y):\n    \"\"\"Similarity of queries to unit centroid direction of candidates.\"\"\"\n    a = x / t.sqrt(einops.reduce(x * x, \"m d -> m 1\", \"sum\"))\n    c = einops.reduce(y, \"n d -> d\", \"mean\")\n    c_norm = c / t.sqrt(einops.reduce(c * c, \"d -> \", \"sum\"))\n    return einops.einsum(a, c_norm, \"m d, d -> m\")\n",
  "1034": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    \"\"\"Normalized class probabilities (Softmax).\"\"\"\n    z = x - x.max(dim=1, keepdim=True)[0]\n    e = z.exp()\n    return e / einops.reduce(e, \"b c -> b 1\", \"sum\")\n",
  "1037": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    \"\"\"Log probabilities without built-in functions.\"\"\"\n    z = x - x.max(dim=1, keepdim=True)[0]\n    return z - einops.reduce(z.exp(), \"b c -> b 1\", \"sum\").log()\n",
  "1040": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    \"\"\"Entropy of each row's distribution.\"\"\"\n    z = x - x.max(dim=1, keepdim=True)[0]\n    l = z - einops.reduce(z.exp(), \"b c -> b 1\", \"sum\").log()\n    return -einops.reduce(l.exp() * l, \"b c -> b\", \"sum\")\n",
  "1044": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    z = x - x.max(dim=1, keepdim=True)[0]\n    e = z.exp()\n    p = e / einops.reduce(e, \"b c -> b 1\", \"sum\")\n    mean_row = einops.reduce(x, \"b c -> b 1\", \"mean\")\n    return einops.reduce(p * (x < mean_row), \"b c -> b\", \"sum\")\n",
  "1049": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, y):\n    return einops.reduce((x.argmax(dim=1) == y).long(), \"b -> \", \"sum\")\n",
  "1050": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, y):\n    z = x - x.max(dim=1, keepdim=True)[0]\n    l = z - einops.reduce(z.exp(), \"b c -> b 1\", \"sum\").log()\n    loss = -l[t.arange(len(y)), y]\n    return einops.reduce(loss, \"b -> \", \"mean\")\n",
  "1315": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, threshold):\n    frac = einops.reduce((x > threshold).float(), \"... -> \", \"mean\")\n    return float(frac)\n",
  "1325": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, weights):\n    prod_sum = einops.reduce(x * weights, \"... -> \", \"sum\")\n    w_sum = einops.reduce(weights, \"... -> \", \"sum\")\n    return float(prod_sum / w_sum)\n",
  "1329": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, target):\n    num = einops.reduce(x * target, \"... -> \", \"sum\")\n    den = einops.reduce(x * x, \"... -> \", \"sum\")\n    return float(num / den)\n",
  "1331": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, weights):\n    total = einops.reduce(weights, \"... -> \", \"sum\")\n    mean = einops.reduce(x * weights, \"... -> \", \"sum\") / total\n    return float(einops.reduce(weights * (x - mean) ** 2, \"... -> \", \"sum\") / total)\n",
  "1471": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    return einops.reduce(x, \"r c -> r\", \"sum\")\n",
  "1472": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    return einops.reduce(x, \"r c -> c\", \"sum\")\n",
  "1473": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    return einops.reduce(x, \"r c -> r\", \"mean\")\n",
  "1474": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    return einops.reduce(x, \"b h w -> h w\", \"mean\")\n",
  "1476": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    return einops.reduce(x, \"b h w -> b\", \"mean\")\n",
  "1477": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    return einops.reduce(x, \"r c -> r 1\", \"sum\")\n",
  "1479": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    return einops.reduce(x, \"a b c -> c\", \"sum\")\n",
  "1480": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    return einops.reduce(x, \"r c -> 1 c\", \"mean\")\n",
  "1481": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    return float(einops.reduce(x, \"... -> 1\", \"mean\").item())\n",
  "1482": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    return einops.reduce(x, \"b c h w -> b 1 1 1\", \"sum\")\n",
  "948": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(a, b):\n    return einops.rearrange(a, \"m -> m 1\") - einops.rearrange(b, \"n -> 1 n\")\n",
  "951": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(rows, cols):\n    return einops.rearrange(t.arange(rows * cols), \"(r c) -> r c\", c=cols)\n",
  "955": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(a, b, c):\n    return einops.einsum(a, b, c, \"i, j, k -> i j k\")\n",
  "956": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(imgs, mean, gain):\n    centered = imgs - einops.rearrange(mean, \"c -> 1 1 1 c\")\n    return centered * einops.rearrange(gain, \"b -> b 1 1 1\")\n",
  "978": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(mats):\n    return einops.reduce(t.stack(mats), \"k ... -> ...\", \"max\")\n",
  "981": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(a, b):\n    v, _ = einops.pack([a, b], \"* c\")\n    h, _ = einops.pack([a, b], \"r *\")\n    return ((v, tuple(v.shape)), (h, tuple(h.shape)))\n",
  "46": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(n):\n    return einops.rearrange(t.arange(n * n), \"(h w) -> h w\", h=n, w=n)\n",
  "491": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, cols):\n    flat = einops.rearrange(x, \"... -> (...)\")\n    return einops.rearrange(flat, \"(r c) -> r c\", c=cols)\n",
  "492": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(n, rows):\n    return einops.rearrange(t.arange(n), \"(r c) -> r c\", r=rows)\n",
  "495": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    return einops.reduce(x, \"... -> 1\", \"sum\").item()\n",
  "496": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    mean_val = einops.reduce(x, \"... -> 1\", \"mean\").item()\n    return (mean_val, x.numel())\n",
  "610": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(rows):\n    a = t.tensor(rows)\n    return tuple(einops.rearrange(a, \"i j -> j i\").shape)\n",
  "611": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(rows):\n    a = t.tensor(rows)\n    return einops.rearrange(a, \"i j -> j i\").tolist()[0]\n",
  "612": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(rows):\n    a = t.tensor(rows)\n    back = einops.rearrange(einops.rearrange(a, \"i j -> j i\"), \"j i -> i j\")\n    return (t.equal(back, a), tuple(back.shape))\n",
  "613": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(rows):\n    a = t.tensor(rows)\n    view = einops.rearrange(a, \"i j -> j i\")\n    return (view.tolist(), tuple(view.shape))\n",
  "614": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(rows):\n    a = t.tensor(rows)\n    view = einops.rearrange(a, \"i j -> j i\")\n    return (tuple(a.shape), tuple(view.shape), view.tolist(), a.ndim)\n",
  "415": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, w):\n    unfolded = x.unfold(2, w.shape[2], 1)\n    return einops.einsum(unfolded, w, \"b i l k, o i k -> b o l\")\n",
  "419": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    return einops.rearrange(x, \"b ... -> b (...)\")\n",
  "1000": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    v = einops.reduce(x, \"m n -> n\", \"sum\")\n    return v / v.norm()\n",
  "1003": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    d = einops.rearrange(x, \"m d -> m 1 d\") - einops.rearrange(x, \"m d -> 1 m d\")\n    return d.norm(dim=-1)\n",
  "1006": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, y):\n    return einops.einsum(x, y, \"m d, n d -> m n\")\n",
  "1007": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(y):\n    return einops.rearrange(y.norm(dim=1), \"n -> n 1\")\n",
  "1008": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, y):\n    a = x / einops.rearrange(x.norm(dim=1), \"m -> m 1\")\n    b = y / einops.rearrange(y.norm(dim=1), \"n -> n 1\")\n    return einops.einsum(a, b, \"m d, n d -> m n\")\n",
  "1012": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, y):\n    a = x / einops.rearrange(x.norm(dim=1), \"m -> m 1\")\n    b = y / einops.rearrange(y.norm(dim=1), \"n -> n 1\")\n    sim = einops.einsum(a, b, \"m d, n d -> m n\")\n    return sim.argmax(dim=1)\n",
  "1014": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, y):\n    a = x / einops.rearrange(x.norm(dim=1), \"m -> m 1\")\n    b = y / einops.rearrange(y.norm(dim=1), \"n -> n 1\")\n    sim = einops.einsum(a, b, \"m d, n d -> m n\")\n    return sim < 0\n",
  "1015": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, y):\n    a = x / einops.rearrange(x.norm(dim=1), \"m -> m 1\")\n    b = y / einops.rearrange(y.norm(dim=1), \"n -> n 1\")\n    sim = einops.einsum(a, b, \"m d, n d -> m n\")\n    return 1 - sim\n",
  "1016": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, y):\n    a = x / einops.rearrange(x.norm(dim=1), \"m -> m 1\")\n    b = y / einops.rearrange(y.norm(dim=1), \"n -> n 1\")\n    sim = einops.einsum(a, b, \"m d, n d -> m n\")\n    return y[sim.argmax(dim=1)]\n",
  "1032": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    max_val = einops.reduce(x, \"b c -> b 1\", \"max\")\n    return x - max_val\n",
  "1033": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    z = x - einops.reduce(x, \"b c -> b 1\", \"max\")\n    return z.exp()\n",
  "1035": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    m = einops.reduce(x, \"b c -> b\", \"max\")\n    m_col = einops.rearrange(m, \"b -> b 1\")\n    s = einops.reduce((x - m_col).exp(), \"b c -> b\", \"sum\")\n    return m + s.log()\n",
  "1036": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    z = x - einops.reduce(x, \"b c -> b 1\", \"max\")\n    e = z.exp()\n    p = e / einops.reduce(e, \"b c -> b 1\", \"sum\")\n    return einops.reduce(p, \"b c -> b\", \"max\") / einops.reduce(p, \"b c -> b\", \"min\")\n",
  "1038": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    z = x - einops.reduce(x, \"b c -> b 1\", \"max\")\n    e = z.exp()\n    p = e / einops.reduce(e, \"b c -> b 1\", \"sum\")\n    return einops.reduce(p, \"b c -> b\", \"max\")\n",
  "1039": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    z = x - einops.reduce(x, \"b c -> b 1\", \"max\")\n    e = z.exp()\n    p = e / einops.reduce(e, \"b c -> b 1\", \"sum\")\n    return p[:, 0]\n",
  "1041": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    m = einops.reduce(x, \"b c -> b\", \"max\")\n    m_col = einops.rearrange(m, \"b -> b 1\")\n    mean_exp = einops.reduce((x - m_col).exp(), \"b c -> b\", \"mean\")\n    return m + mean_exp.log()\n",
  "1042": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    z = (x - einops.reduce(x, \"b c -> b 1\", \"max\")) / 2\n    e = z.exp()\n    return e / einops.reduce(e, \"b c -> b 1\", \"sum\")\n",
  "1043": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    z = x - einops.reduce(x, \"b c -> b 1\", \"max\")\n    e = z.exp()\n    p = e / einops.reduce(e, \"b c -> b 1\", \"sum\")\n    return einops.reduce(p, \"b c -> c\", \"mean\")\n",
  "1101": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(r, s):\n    mid = einops.reduce(s[:, :2], \"pts d -> d\", \"mean\")\n    return mid - r[0, :2]\n",
  "1137": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(tr):\n    diffs = t.stack((tr[1] - tr[0], tr[2] - tr[0]))\n    return einops.rearrange(diffs, \"two d -> d two\")\n",
  "1138": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(r, tr):\n    o, d = r\n    a, b, c = tr\n    cols = t.stack((-d, b - a, c - a))\n    return einops.rearrange(cols, \"three d -> d three\")\n",
  "1149": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(r, tr):\n    centroid = einops.reduce(tr, \"pts d -> d\", \"mean\")\n    return centroid - r[0]\n",
  "1133": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(ny, nz, yl, zl):\n    y = t.linspace(-yl, yl, ny)\n    z = t.linspace(-zl, zl, nz)\n    yy = einops.repeat(y, \"ny -> ny nz\", nz=nz)\n    zz = einops.repeat(z, \"nz -> ny nz\", ny=ny)\n    r = t.zeros(ny * nz, 2, 3)\n    r[:, 1, 0] = 1\n    r[:, 1, 1] = einops.rearrange(yy, \"ny nz -> (ny nz)\")\n    r[:, 1, 2] = einops.rearrange(zz, \"ny nz -> (ny nz)\")\n    return r[:, 1] - t.tensor([0.0, 0.0, 1.0])\n",
  "1134": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(ny, nz, yl, zl):\n    y = t.linspace(-yl, yl, ny)\n    z = t.linspace(-zl, zl, nz)\n    yy = einops.repeat(y, \"ny -> ny nz\", nz=nz)\n    zz = einops.repeat(z, \"nz -> ny nz\", ny=ny)\n    r = t.zeros(ny * nz, 2, 3)\n    r[:, 1, 0] = 1\n    r[:, 1, 1] = einops.rearrange(yy, \"ny nz -> (ny nz)\")\n    r[:, 1, 2] = einops.rearrange(zz, \"ny nz -> (ny nz)\")\n    norm = einops.rearrange(r[:, 1].norm(dim=1), \"b -> b 1\")\n    r[:, 1] = r[:, 1] / norm\n    return r\n",
  "1135": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(ny, nz, yl, zl):\n    y = t.linspace(-yl, yl, ny)\n    z = t.linspace(-zl, zl, nz)\n    yy = einops.repeat(y, \"ny -> ny nz\", nz=nz)\n    zz = einops.repeat(z, \"nz -> ny nz\", ny=ny)\n    r = t.zeros(ny * nz, 2, 3)\n    r[:, 1, 0] = 1\n    r[:, 1, 1] = einops.rearrange(yy, \"ny nz -> (ny nz)\")\n    r[:, 1, 2] = einops.rearrange(zz, \"ny nz -> (ny nz)\")\n    image = einops.rearrange(r, \"(ny nz) p d -> ny nz p d\", ny=ny, nz=nz)\n    rev = image[:, t.arange(nz - 1, -1, -1)]\n    return einops.rearrange(rev, \"ny nz p d -> (ny nz) p d\")\n",
  "1136": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(ny, nz, yl, zl):\n    y = t.linspace(-yl, yl, ny)\n    z = t.linspace(-zl, zl, nz)\n    yy = einops.repeat(y, \"ny -> ny nz\", nz=nz)\n    zz = einops.repeat(z, \"nz -> ny nz\", ny=ny)\n    r = t.zeros(ny * nz, 2, 3)\n    r[:, 1, 0] = 1\n    r[:, 1, 1] = einops.rearrange(yy, \"ny nz -> (ny nz)\")\n    r[:, 1, 2] = einops.rearrange(zz, \"ny nz -> (ny nz)\")\n    o = t.tensor([-1.0, 1.0, 0.0])\n    r[:, 0] = o\n    d = r[:, 1] - o\n    norm = einops.rearrange(d.norm(dim=1), \"b -> b 1\")\n    r[:, 1] = d / norm\n    return r\n",
  "1153": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(r, tr):\n    nr, nt = r.shape[0], tr.shape[0]\n    o = einops.repeat(r[:, 0, :], \"nr d -> nr nt d\", nt=nt)\n    d = einops.repeat(r[:, 1, :], \"nr d -> nr nt d\", nt=nt)\n    a = einops.repeat(tr[:, 0, :], \"nt d -> nr nt d\", nr=nr)\n    b = einops.repeat(tr[:, 1, :], \"nt d -> nr nt d\", nr=nr)\n    c = einops.repeat(tr[:, 2, :], \"nt d -> nr nt d\", nr=nr)\n    cols = t.stack((-d, b - a, c - a))\n    m = einops.rearrange(cols, \"three nr nt d -> nr nt d three\")\n    valid = t.linalg.det(m).abs() >= 1e-8\n    safe = t.where(valid[..., None, None], m, t.eye(3))\n    diff = einops.rearrange(o - a, \"nr nt d -> nr nt d 1\")\n    suv = t.linalg.solve(safe, diff)[..., 0]\n    s, u, v = suv[..., 0], suv[..., 1], suv[..., 2]\n    return valid & (s >= 0) & (u >= 0) & (v >= 0) & (u + v <= 1)\n",
  "1154": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(r, tr):\n    nr, nt = r.shape[0], tr.shape[0]\n    o = einops.repeat(r[:, 0, :], \"nr d -> nr nt d\", nt=nt)\n    d = einops.repeat(r[:, 1, :], \"nr d -> nr nt d\", nt=nt)\n    a = einops.repeat(tr[:, 0, :], \"nt d -> nr nt d\", nr=nr)\n    b = einops.repeat(tr[:, 1, :], \"nt d -> nr nt d\", nr=nr)\n    c = einops.repeat(tr[:, 2, :], \"nt d -> nr nt d\", nr=nr)\n    cols = t.stack((-d, b - a, c - a))\n    m = einops.rearrange(cols, \"three nr nt d -> nr nt d three\")\n    valid = t.linalg.det(m).abs() >= 1e-8\n    safe = t.where(valid[..., None, None], m, t.eye(3))\n    diff = einops.rearrange(o - a, \"nr nt d -> nr nt d 1\")\n    suv = t.linalg.solve(safe, diff)[..., 0]\n    s, u, v = suv[..., 0], suv[..., 1], suv[..., 2]\n    hit = valid & (s >= 0) & (u >= 0) & (v >= 0) & (u + v <= 1)\n    return t.where(hit, s, t.tensor(float(\"inf\")))\n",
  "1155": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(r, tr):\n    nr, nt = r.shape[0], tr.shape[0]\n    o = einops.repeat(r[:, 0, :], \"nr d -> nr nt d\", nt=nt)\n    d = einops.repeat(r[:, 1, :], \"nr d -> nr nt d\", nt=nt)\n    a = einops.repeat(tr[:, 0, :], \"nt d -> nr nt d\", nr=nr)\n    b = einops.repeat(tr[:, 1, :], \"nt d -> nr nt d\", nr=nr)\n    c = einops.repeat(tr[:, 2, :], \"nt d -> nr nt d\", nr=nr)\n    cols = t.stack((-d, b - a, c - a))\n    m = einops.rearrange(cols, \"three nr nt d -> nr nt d three\")\n    valid = t.linalg.det(m).abs() >= 1e-8\n    safe = t.where(valid[..., None, None], m, t.eye(3))\n    diff = einops.rearrange(o - a, \"nr nt d -> nr nt d 1\")\n    suv = t.linalg.solve(safe, diff)[..., 0]\n    s, u, v = suv[..., 0], suv[..., 1], suv[..., 2]\n    hit = valid & (s >= 0) & (u >= 0) & (v >= 0) & (u + v <= 1)\n    depth = t.where(hit, s, t.tensor(float(\"inf\")))\n    return einops.reduce(depth, \"nr nt -> nr\", \"min\")\n",
  "1156": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(r, tr):\n    nr, nt = r.shape[0], tr.shape[0]\n    o = einops.repeat(r[:, 0, :], \"nr d -> nr nt d\", nt=nt)\n    d = einops.repeat(r[:, 1, :], \"nr d -> nr nt d\", nt=nt)\n    a = einops.repeat(tr[:, 0, :], \"nt d -> nr nt d\", nr=nr)\n    b = einops.repeat(tr[:, 1, :], \"nt d -> nr nt d\", nr=nr)\n    c = einops.repeat(tr[:, 2, :], \"nt d -> nr nt d\", nr=nr)\n    cols = t.stack((-d, b - a, c - a))\n    m = einops.rearrange(cols, \"three nr nt d -> nr nt d three\")\n    valid = t.linalg.det(m).abs() >= 1e-8\n    safe = t.where(valid[..., None, None], m, t.eye(3))\n    diff = einops.rearrange(o - a, \"nr nt d -> nr nt d 1\")\n    suv = t.linalg.solve(safe, diff)[..., 0]\n    s, u, v = suv[..., 0], suv[..., 1], suv[..., 2]\n    hit = valid & (s >= 0) & (u >= 0) & (v >= 0) & (u + v <= 1)\n    return hit.any(dim=1)\n",
  "1157": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(r, tr):\n    nr, nt = r.shape[0], tr.shape[0]\n    o = einops.repeat(r[:, 0, :], \"nr d -> nr nt d\", nt=nt)\n    d = einops.repeat(r[:, 1, :], \"nr d -> nr nt d\", nt=nt)\n    a = einops.repeat(tr[:, 0, :], \"nt d -> nr nt d\", nr=nr)\n    b = einops.repeat(tr[:, 1, :], \"nt d -> nr nt d\", nr=nr)\n    c = einops.repeat(tr[:, 2, :], \"nt d -> nr nt d\", nr=nr)\n    cols = t.stack((-d, b - a, c - a))\n    m = einops.rearrange(cols, \"three nr nt d -> nr nt d three\")\n    valid = t.linalg.det(m).abs() >= 1e-8\n    safe = t.where(valid[..., None, None], m, t.eye(3))\n    diff = einops.rearrange(o - a, \"nr nt d -> nr nt d 1\")\n    suv = t.linalg.solve(safe, diff)[..., 0]\n    s, u, v = suv[..., 0], suv[..., 1], suv[..., 2]\n    hit = valid & (s >= 0) & (u >= 0) & (v >= 0) & (u + v <= 1)\n    return einops.reduce(hit.int(), \"nr nt -> nr\", \"sum\")\n",
  "1158": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(r, tr):\n    nr, nt = r.shape[0], tr.shape[0]\n    o = einops.repeat(r[:, 0, :], \"nr d -> nr nt d\", nt=nt)\n    d = einops.repeat(r[:, 1, :], \"nr d -> nr nt d\", nt=nt)\n    a = einops.repeat(tr[:, 0, :], \"nt d -> nr nt d\", nr=nr)\n    b = einops.repeat(tr[:, 1, :], \"nt d -> nr nt d\", nr=nr)\n    c = einops.repeat(tr[:, 2, :], \"nt d -> nr nt d\", nr=nr)\n    cols = t.stack((-d, b - a, c - a))\n    m = einops.rearrange(cols, \"three nr nt d -> nr nt d three\")\n    valid = t.linalg.det(m).abs() >= 1e-8\n    safe = t.where(valid[..., None, None], m, t.eye(3))\n    diff = einops.rearrange(o - a, \"nr nt d -> nr nt d 1\")\n    suv = t.linalg.solve(safe, diff)[..., 0]\n    s, u, v = suv[..., 0], suv[..., 1], suv[..., 2]\n    hit = valid & (s >= 0) & (u >= 0) & (v >= 0) & (u + v <= 1)\n    depth = t.where(hit, s, t.tensor(float(\"inf\")))\n    nearest = einops.reduce(depth, \"nr nt -> nr\", \"min\")\n    seen = nearest < float(\"inf\")\n    return t.where(seen.any(), nearest[seen].mean(), t.tensor(0.0))\n",
  "1159": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(r, tr):\n    nr, nt = r.shape[0], tr.shape[0]\n    o = einops.repeat(r[:, 0, :], \"nr d -> nr nt d\", nt=nt)\n    d = einops.repeat(r[:, 1, :], \"nr d -> nr nt d\", nt=nt)\n    a = einops.repeat(tr[:, 0, :], \"nt d -> nr nt d\", nr=nr)\n    b = einops.repeat(tr[:, 1, :], \"nt d -> nr nt d\", nr=nr)\n    c = einops.repeat(tr[:, 2, :], \"nt d -> nr nt d\", nr=nr)\n    cols = t.stack((-d, b - a, c - a))\n    m = einops.rearrange(cols, \"three nr nt d -> nr nt d three\")\n    valid = t.linalg.det(m).abs() >= 1e-8\n    safe = t.where(valid[..., None, None], m, t.eye(3))\n    diff = einops.rearrange(o - a, \"nr nt d -> nr nt d 1\")\n    suv = t.linalg.solve(safe, diff)[..., 0]\n    s, u, v = suv[..., 0], suv[..., 1], suv[..., 2]\n    hit = valid & (s >= 0) & (u >= 0) & (v >= 0) & (u + v <= 1)\n    depth = t.where(hit, s, t.tensor(float(\"inf\")))\n    value, index = depth.min(dim=1)\n    return t.where(value < float(\"inf\"), index, -1)\n",
  "1160": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(r, tr):\n    nr, nt = r.shape[0], tr.shape[0]\n    o = einops.repeat(r[:, 0, :], \"nr d -> nr nt d\", nt=nt)\n    d = einops.repeat(r[:, 1, :], \"nr d -> nr nt d\", nt=nt)\n    a = einops.repeat(tr[:, 0, :], \"nt d -> nr nt d\", nr=nr)\n    b = einops.repeat(tr[:, 1, :], \"nt d -> nr nt d\", nr=nr)\n    c = einops.repeat(tr[:, 2, :], \"nt d -> nr nt d\", nr=nr)\n    cols = t.stack((-d, b - a, c - a))\n    m = einops.rearrange(cols, \"three nr nt d -> nr nt d three\")\n    valid = t.linalg.det(m).abs() >= 1e-8\n    safe = t.where(valid[..., None, None], m, t.eye(3))\n    diff = einops.rearrange(o - a, \"nr nt d -> nr nt d 1\")\n    suv = t.linalg.solve(safe, diff)[..., 0]\n    s, u, v = suv[..., 0], suv[..., 1], suv[..., 2]\n    hit = valid & (s >= 0) & (u >= 0) & (v >= 0) & (u + v <= 1)\n    counts = einops.reduce(hit.int(), \"nr nt -> nt\", \"sum\")\n    return counts.argmax()\n",
  "1164": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(r, tr):\n    nr, nt = r.shape[0], tr.shape[0]\n    o = einops.repeat(r[:, 0, :], \"nr d -> nr nt d\", nt=nt)\n    d = einops.repeat(r[:, 1, :], \"nr d -> nr nt d\", nt=nt)\n    a = einops.repeat(tr[:, 0, :], \"nt d -> nr nt d\", nr=nr)\n    b = einops.repeat(tr[:, 1, :], \"nt d -> nr nt d\", nr=nr)\n    c = einops.repeat(tr[:, 2, :], \"nt d -> nr nt d\", nr=nr)\n    cols = t.stack((-d, b - a, c - a))\n    m = einops.rearrange(cols, \"three nr nt d -> nr nt d three\")\n    valid = t.linalg.det(m).abs() >= 1e-8\n    safe = t.where(valid[..., None, None], m, t.eye(3))\n    diff = einops.rearrange(o - a, \"nr nt d -> nr nt d 1\")\n    suv = t.linalg.solve(safe, diff)[..., 0]\n    s, u, v = suv[..., 0], suv[..., 1], suv[..., 2]\n    hit = valid & (s >= 0) & (u >= 0) & (v >= 0) & (u + v <= 1)\n    counts = einops.reduce(hit.int(), \"nr nt -> nr\", \"sum\")\n    return counts > 1\n",
  "1483": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    return einops.rearrange(x, \"b h w -> b (h w)\")\n",
  "1484": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, h, w):\n    return einops.rearrange(x, \"(h w h2 w2) -> h w h2 w2\", h=h, w=w, h2=2, w2=2)\n",
  "1485": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    return einops.rearrange(x, \"b s c -> (b s) c\")\n",
  "1486": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, c, h, w):\n    return einops.rearrange(x, \"b (c h w) -> b c h w\", c=c, h=h, w=w)\n",
  "1487": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(losses, batch_sizes):\n    l = t.tensor(losses, dtype=t.float32)\n    b = t.tensor(batch_sizes, dtype=t.float32)\n    num = float(einops.einsum(l, b, \"n, n -> \"))\n    den = float(einops.reduce(b, \"n -> \", \"sum\"))\n    return num / den\n",
  "1488": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(logits, labels):\n    preds = logits.argmax(dim=-1)\n    matches = (preds == labels).float()\n    return float(einops.reduce(matches, \"b -> \", \"mean\").item())\n",
  "1489": "# Solution created by Antigravity\nimport torch as t\n\ndef solve(val_losses, patience):\n    if len(val_losses) <= patience:\n        return False\n    prev_losses = t.tensor(val_losses[:-patience])\n    recent = t.tensor(val_losses[-patience:])\n    min_prev = prev_losses.min()\n    return bool((recent >= min_prev).all().item())\n",
  "1490": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    return einops.rearrange(x, \"b (t two) c h w -> b t two c h w\", two=2)\n",
  "1491": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    return einops.rearrange(x, \"b h t d -> b t h d\")\n",
  "1492": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    return einops.rearrange(x, \"b t c h w -> b (t c) h w\")\n",
  "1493": "# Solution created by Antigravity\nimport torch as t\nimport torch.nn.functional as F\n\ndef solve(x, w):\n    return F.conv2d(x, w, stride=2)\n",
  "1494": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, f_x):\n    downsampled = einops.rearrange(x, \"b c (h h2) (w w2) -> b c h w (h2 w2)\", h2=2, w2=2)[..., 0]\n    return f_x + downsampled\n",
  "1495": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(a, b):\n    diff = einops.rearrange(a, \"m d -> m 1 d\") - einops.rearrange(b, \"n d -> 1 n d\")\n    return einops.reduce(diff ** 2, \"m n d -> m n\", \"sum\").sqrt()\n",
  "1496": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, y):\n    return einops.rearrange(x, \"m -> m 1\") - einops.rearrange(y, \"n -> 1 n\")\n",
  "1497": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, groups):\n    return einops.rearrange(x, \"b h w (g c) -> b g c h w\", g=groups)\n",
  "1498": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(a):\n    orig = a[0, 0].item()\n    tr = einops.rearrange(a, \"i j -> j i\")\n    tr[0, 0] += 10.0\n    return bool(a[0, 0].item() != orig)\n",
  "1499": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x, k):\n    return einops.rearrange(x, \"(n k) -> n k\", k=k)\n",
  "1500": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(img):\n    return einops.rearrange(img, \"(h h2) (w w2) -> (h w) (h2 w2)\", h2=2, w2=2)\n",
  "1501": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(imgs, p):\n    return einops.rearrange(imgs, \"b c (h p1) (w p2) -> b (h w) (c p1 p2)\", p1=p, p2=p)\n",
  "1502": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(imgs):\n    return einops.rearrange(imgs, \"(r c) h w -> (r h) (c w)\", r=2, c=2)\n",
  "1503": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(tiles, r, c):\n    return einops.rearrange(tiles, \"(r c) h w ch -> (r h) (c w) ch\", r=r, c=c)\n",
  "1504": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(montage, h, w):\n    return einops.rearrange(montage, \"(r h) (c w) -> (r c) h w\", h=h, w=w)\n",
  "1505": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    return einops.reduce(x, \"(n two) -> n\", \"mean\", two=2)\n",
  "1506": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    return einops.reduce(x, \"b c (h h2) (w w2) -> b c h w\", \"max\", h2=2, w2=2)\n",
  "1507": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    return einops.reduce(x, \"b (c two) h w -> b c h w\", \"mean\", two=2)\n",
  "1508": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(n):\n    r = einops.rearrange(t.arange(n), \"i -> i 1\")\n    c = einops.rearrange(t.arange(n), \"j -> 1 j\")\n    return (r + c) % 2\n",
  "1509": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(a, b, c, d):\n    top, _ = einops.pack([a, b], \"r *\")\n    bottom, _ = einops.pack([c, d], \"r *\")\n    res, _ = einops.pack([top, bottom], \"* c\")\n    return res\n",
  "1510": "# Solution created by Antigravity\nimport torch as t\n\ndef solve(n):\n    return t.eye(n, dtype=t.int64)\n",
  "1511": "# Solution created by Antigravity\nimport torch as t\n\ndef solve(a, b):\n    return a.to(t.float32) / b.to(t.float32)\n",
  "1512": "# Solution created by Antigravity\nimport einops\nimport torch as t\n\ndef solve(x):\n    q = (x * 255.0).round().to(t.uint8)\n    deq = q.to(t.float32) / 255.0\n    diff = (x - deq).abs()\n    return float(einops.reduce(diff, \"... -> \", \"max\").item())\n",
  "1513": "# Solution created by Antigravity\nimport torch as t\n\ndef solve(n, seed):\n    t.manual_seed(seed)\n    return t.randperm(n)\n",
  "1514": "# Solution created by Antigravity\nimport torch as t\n\ndef solve(n, seed):\n    t.manual_seed(seed)\n    perm = t.randperm(n)\n    return t.eye(n, dtype=t.int64)[perm]\n"
}
