/* ================================================================
   PRACTICE ENTRY — start after modules load
   ================================================================ */

if (typeof initPractice === "function") {
  initPractice();
}
