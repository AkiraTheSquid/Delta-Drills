# Fable review — q847–940 + einops KP pages (rubric: scripts/CONTENT_RUBRIC.md)

## Drills

### Systemic (major)
- STMT_SELF_CONTAINED — 52 prompts open with a cross-reference: "Variant of exercise (1)…(8)" (q889–920), "Variant of (A1)/(A2)/(B1)/(B2)/(B3)" (q921–940), "Variant of `einsum_trace`/`einsum_mv`/`einsum_mm`/`einsum_inner`/`einsum_outer`" (q865–888). q864/869/874/879/884 open with a bare label "`einsum_trace`:". Drill is served alone → the reference is noise at best, and for the B-family it is the ONLY place the input is described.
- STMT_INPUT — the B-family (q929–940) never says what `temps` is except by reference: q930 "readings come in blocks of k days instead of 7", q931 "return the TOTAL of each week's readings", q933, q935, q938, q939 have no input description at all. q866 "a batch of square matrices (b, n, n)" never says nested list. q910 "same grid" — no input at all.
- GIVE_PROMPT — q921–924 "build `t.arange(n)` and reshape it with `einops.rearrange`", q925 "`einops.rearrange` on `t.tensor(vals)`", q933/q936 "reduce, repeat back, subtract", q937 "Use `einops.reduce` for the min and max and `einops.repeat` to lay them back out", q938–940 "pass `t.std` as the reduction to `einops.reduce`". These spell the call sequence. (`einops.einsum` named in q847–888 is acceptable: the KP is einsum and the pattern string is the decision.)
- GIVE_RUNG / DIFF_RUNG — 80 of 94 sit on Integrated ("never an example, needs more than one idea"). Single-idea items filed Integrated: q913 (transpose), q914/915 (move c), q916 (swap b,c), q928 (flatten), q864/869/874/879/884 (base einsum ops), q893, q896, q901, q921, q925, q929, q931. These belong on Solo; Faded/Solo rungs are starved (8/6).
- DIFF_DUP — same move on same KP: q849≡q870 (v@A), q850≡q875 (A@B.T), q852≡q883 (matrix inner product), q854≡q886 (batched outer), q859≡q866 (batch diagonals vs traces, 1 char apart), q853≡q873. q921≡q925 (arange vs list, same pattern). q889≡q346, q890≡q342, q891≡q357(guided!), q913≈q345.
- GIVE_EXAMPLE / EXPL_TRANSFER — page examples reproduce drill outputs on the drill's own literals: q847 CASE0 `[[0,1,2],[3,4,5]]→[3,5,7]` is the kp-einsum concept table + example verbatim; q903 CASE0 `[[1,2],[3,4]]→[[1,1,2,2],…]` is kp-repeat-model's `up` verbatim; q852 pattern `"i j, i j ->"` is named "matrix inner product" in the concept bullet; q851 `"i i -> i"` named "the diagonal itself" in the bullet.
- EXPL_PREREQ — q938–940 use a callable reduction (`t.std`) that no einops page teaches. q929–931 are factored-axis pooling `(h 7) -> h` but are owned by reduce-model, whose page defers factors to "the next KP".

### Per-item (major unless marked)
- q897 STMT_TASK: prompt contains authoring residue — "images side by side (batch into width) and each stretched? No: each image repeated twice vertically as a block". Rewrite: "A batch `(b, c, h, w)` as a nested list. Lay the images side by side (batch into width) and stack the whole strip on top of itself once, shape `(c, 2·h, b·w)`. Return the nested list."
- q908 STMT_TERMS (minor): formula `[x, c·H + y] = img[c][y][x]` uses undefined `H`.
- q910 STMT_INPUT: "same grid" — no shape; must restate `(b, c, h, w)` and that `cols` is given.
- q851 GIVE_STARTER: docstring "The diagonal via a repeated index" names the move. Also q871 "transpose-then-multiply" (minor).
- q928 DIFF_LABEL (note): only "easy" among siblings; also a MERGE filed under split-axes.
- q930 STMT_CASES (minor): CASE0 k=7 duplicates q929's data; wrong_example output shows float noise `6.5714287757873535`.
- q937–940 CORR_TYPES (minor): expected lists compare rounded float32 values by exact equality (0.1667, 0.4629…) — passes today because expected was generated by the answer, but any dtype change (float64 input) breaks it; grade with tolerance or state "float32".
- q918 CORR_TYPES (note): mean-pool expects exact floats `[[[4.0]]]` — fine, values are exact in float32.
- q932/q936/q940 STMT_INPUT (minor): "(weeks, 7)" said; OK. But q936 answer uses `d=x.shape[1]` while q940 hardcodes `d=7` — inconsistent, not a defect.

### Pass (rubric-clean apart from the systemic items above)
q848, q853–q863, q867–q868, q872, q876–q878, q880–q882, q885, q887–q888, q889–q896, q898–q907, q909, q911–q920, q926–q927, q929, q932, q934.

## Lesson pages

Mechanical (audit_ladder_pairing --strict, run 2026-09-07): INTERLEAVE fails on every einops page (one fence of 20–35 lines carries the whole example, max 16); INTRO fails on all 4 kp-einsum segments (example opens with code); pairing audit says the faded drill on 7 pages (q345, q391, q361, q389, q325, q324, q356) is transcription of the example.

- kp-einsum: EXPL_GENERAL_FIRST pass (rule before instance, 4 segments, Rocktäschel refs). EXPL_INTRO major ×4, EXPL_INTERLEAVE major ×4. EXPL_TRANSFER major: q847, q851, q852 are the concept bullets restated (see above). Integrated section is 28 bare docstrings ("einsum_trace", "trace of A squared") — the page gives the learner nothing; either drop the section or keep it to ids. Misconceptions block good.
- kp-repeat-model: EXPL_GENERAL_FIRST pass; example strong (both orders on the same data). EXPL_INTERLEAVE major (35-line fence). q903 ≡ example `up` (GIVE_EXAMPLE). Owns q933–940 whose reduce+repeat+`t.std` moves are not on the page (EXPL_PREREQ).
- kp-merge-axes: pass on GENERAL_FIRST/WHY; INTERLEAVE major. Concept bullets are literally q889/q890/q891's answers (side by side, stack vertically, channel unroll) — GIVE_EXAMPLE for those integrated drills.
- kp-split-axes: good; INTERLEAVE major. q928 is a merge, misfiled.
- kp-reduce-model: good; INTERLEAVE major; q929–931 need the pooling factor the page defers.
- kp-grids-montage: good (constant-image fixture is the best example in the set); INTERLEAVE major. q909 ≡ the channels-first bullet.
- kp-pattern-language: good; INTERLEAVE major; q913–916 are the concept's three bullets.
- kp-pooling: good; INTERLEAVE major; q917/918 ≡ example (`'b c (h h2) (w w2)'` mean/max), one axis fewer.
