# Delta Drills content review

Standard: [CONTENT_RUBRIC.md](/home/stellar-thread/Applications/Delta-Drills-Local/scripts/CONTENT_RUBRIC.md). Scope: drills **1374–1470**, seven specified lesson pages. **No edits or file writes.**

**Drills: 75 major, 4 minor, 18 pass. Lessons: 7 major.** Verdict reflects highest finding severity.

Validation: **353/353 cases passed using the backend’s actual comparison harness, executed in memory.** Remaining 28 Adam cases reviewed statically: importing Adam’s runtime dependencies attempted temporary-file creation, blocked by the write guard. No runtime failure attributed to those blocked checks. Backend seeds fixtures and converts tensors before comparison; neither seeded q1469 results nor tensor-valued expectations warrant correctness findings.

## 1. Drills — per-id findings

**Pass:** 1383, 1385, 1387, 1393, 1395, 1396, 1398–1401, 1404–1405, 1422, 1432, 1435–1436, 1463, 1470.

**Minor only:** 1394, 1417, 1418, 1424.

**All remaining IDs: major**, with findings below. Repeated defects grouped; an ID can appear under multiple findings.

### D1. Faded items have no scaffold

**`GIVE_RUNG` — major**

**IDs:** 1374–1379, 1389–1392, 1402–1403, 1413–1416, 1426–1431, 1441–1448, 1458–1461.

Exact starter body in every listed item:

> `pass`

Frontmatter assigns these 34 items to `faded`, but every starter requires implementation from scratch.

**Replacement sentence:** “Complete the blanks in the supplied scaffold to produce the behavior described below.”

**Required accompanying fix:** Supply class, registration, iteration, or measurement scaffolding appropriate to each item; blank every relevant symbol declared in that KP’s `new_syntax`. Changing the sentence alone does not repair this defect.

### D2. First sentence supplies context instead of the function’s goal

**`STMT_TASK` — major**

Repeated exact openings:

| IDs | Exact quote |
|---|---|
| 1402, 1406, 1407, 1409–1412 | “A class `ResidualBlock` taking (in channels, out channels, stride) is provided.” |
| 1415 | “Classes `ResidualBlock` and `BlockGroup` (arguments: block count, in channels, out channels, first stride) are provided.” |
| 1419, 1423, 1425 | “Classes `ResidualBlock` and `BlockGroup` (block count, in channels, out channels, first stride) are provided.” |
| 1430 | “The lesson's `ResNet` class (arguments: stem width, block counts, output widths, first strides, class count) is provided.” |
| 1434 | “Two modules `m` and `src` have identical architecture AND identical state names.” |
| 1438 | “The lesson's `ResNet` class is provided, and `src` is a pretrained stand-in: a module with the same tensor sequence as a ResNet built from `c0`, `n_blocks`, `out_feats`, `strides` with `1000` classes, but possibly different child names.” |
| 1439 | “`m` is a network from the lesson's `ResNet` class and `src` is a flat Sequential chain holding the same layers in the same order under different names.” |
| 1448 | “Write the training function.” |
| 1455 | “Feature-extraction training.” |

Replace each opening with its concrete goal; move supplied-helper declarations below it.

| ID | Replacement first sentence |
|---|---|
| 1402 | “Return a group of `n` residual blocks that changes channel count and resolution only in its first block.” |
| 1406 | “Return a group that changes channels in its first block and downsamples in its last block.” |
| 1407 | “Return a group that applies one shared residual-block object `n` times.” |
| 1409 | “Return a group that doubles channels at every block and downsamples only at its first block.” |
| 1410 | “Return a module that applies two configured residual-block groups consecutively.” |
| 1411 | “Return a chain of residual-block groups matching the supplied block counts, widths, and strides.” |
| 1412 | “Return a residual-block group whose forward method reports the output after every block.” |
| 1415 | “Return an image classifier with the specified stem, residual groups, and classification head.” |
| 1419 | “Return a network that produces one spatially averaged feature vector per image.” |
| 1423 | “Return a classifier that produces both each image’s feature vector and its class logits.” |
| 1425 | “Return a classifier whose forward method reports its input, stem, group, and logits shapes.” |
| 1430 | “Return a ResNet subclass instance whose original parameters are frozen and whose replacement classification head is trainable.” |
| 1434 | “Copy source state entries whose names begin with `prefix`, preserving every other destination entry.” |
| 1438 | “Return a classifier with the supplied pretrained body and a fresh, trainable head for `n_classes` classes.” |
| 1439 | “Copy positionally paired source state into `m` and report how many paired names differ.” |
| 1448 | “Train `m` for `epochs` epochs and return its batch losses and epoch accuracies.” |
| 1455 | “Train only the final child’s parameters and return batch losses and epoch accuracies.” |

### D3. Standalone contract depends on lesson context or an unspecified component

**`STMT_SELF_CONTAINED` — major**

| ID | Exact quote | Replacement sentence |
|---|---|---|
| 1397 | “The child `left` is the usual five-step branch” | “The left branch contains a bias-free padded `3×3` convolution from `i` to `o` with stride `s`, batch normalization over `o`, a rectifier, a bias-free padded `3×3` convolution from `o` to `o` with stride one, and batch normalization over `o`.” |
| 1408 | “the number of trainable parameters in a group of `n` residual blocks” | “Each block has two bias-free `3×3` convolutions, each followed by affine batch normalization; only the first block can change channels or resolution and therefore require an additional projection shortcut.” |
| 1425 | “children `in_layers`, `residual_layers`, `out_layers` exactly as in the lesson” | “Use the stem, group, and head specifications supplied in this statement, registering them as `in_layers`, `residual_layers`, and `out_layers`.” The complete replacement specification appears in fix 10 below. |
| 1437 | “Given a network `m` built by the lesson's `ResNet` class” | “The supplied module `m` has a Sequential child `residual_layers` containing its groups and a child `out_layers` containing its head; `k` is an integer from zero through the number of groups.” |
| 1440 | “prepare it for feature extraction with `n` classes” | “Freeze existing parameters and replace the final Linear child of `m.out_layers` with a fresh, biased Linear layer having the same input width and `n` outputs.” |
| 1455 | “train for `epochs` epochs exactly as in the lesson” | “Record mean cross-entropy before each Adam update and, after each epoch, record the fraction of correctly classified examples across all evaluation batches.” |
| 1457 | “Train exactly as in the lesson” | “Train for `epochs` epochs using Adam and mean cross-entropy, measuring accuracy across all evaluation examples after each epoch.” |

Supplied helper classes genuinely exist in test setup. Their existence alone is **not** a missing-runtime-dependency finding.

### D4. Prompt names the operation or supplies transcribable implementation

**`GIVE_PROMPT` — major**

| IDs | Exact quote | Replacement sentence |
|---|---|---|
| 1384 | “using index assignment” | “Replace child `i` with `mod`, preserving the other children and their order, then return the modified chain’s output for `x`.” |
| 1402 | “whose first block is `ResidualBlock(i, o, s)` and whose remaining `n - 1` blocks are `ResidualBlock(o, o, 1)`” | “The first block performs the group’s channel conversion and downsampling; later blocks preserve the resulting shape.” |
| 1403 | “the first triple is `(i, o, s)` and each of the other `n - 1` triples is `(o, o, 1)`” | “Return the input-channel count, output-channel count, and stride needed by each block in a group that changes shape only at its first block.” |
| 1420 | “has `c0` times `3` times `49` weights plus `c0` biases” and “has `c` times `n_classes` weights plus `n_classes` biases” | “Return the total number of trainable scalar values in the specified stem and classification head.” Supply layer dimensions and bias/normalization properties, without their expanded counting formula. |
| 1460 | “the number of modules `apply` visits” | “Return the number of modules in the hierarchy, including `m` itself.” |
| 1461, 1465, 1468, 1469 | “use `apply` to attach” | “Instrument every module in the hierarchy, including the root, for the measurement described below.” |
| 1467 | “using `apply`” | “Return the number of descendant modules at every depth, excluding `m` itself.” |

### D5. Tensor and training contracts leave essential assumptions unstated

**`STMT_INPUT` — major**

**IDs:** 1379, 1441–1457.

| IDs | Exact quote | Replacement contract sentence |
|---|---|---|
| 1379 | “Given `w1` of shape `(hidden, in)` and `w2` of shape `(out, hidden)`” | “`w1` and `w2` are CPU float32 tensors shaped `(hidden, in)` and `(out, hidden)`, with positive dimensions.” |
| 1441–1443 | “an input batch `x`” | “`x` and `target` are floating-point tensors accepted by `m`, with `target.shape == m(x).shape`; the output is nonempty, all parameters participate in the loss, and their gradients initially contain no accumulated values.” |
| 1444 | “Given `m`, `x`, a `target` of the output's shape” | “`m` is a differentiable module, `x` is its floating-point input batch, and `target` is a floating-point tensor with exactly the same nonempty shape as `m(x)`.” |
| 1449 | “the output of `m` on `x` and `target`” | “`x` and `target` are compatible floating-point tensors, `target.shape == m(x).shape`, every parameter participates in the loss, `lr` is positive, and `k` is a nonnegative integer.” |
| 1445–1446, 1451, 1454 | “a batch `x` and integer labels `y`” — q1445 uses “a batch `x`, integer labels `y`” | “`x` is a floating-point batch accepted by `m`, `m(x)` has shape `(B, C)`, and `y` is an integer tensor of shape `(B,)` containing class indices from zero through `C-1`.” |
| 1447–1448, 1452 | “a list of `(x, y)` pairs” / “lists of `(x, y)` pairs” | “Each pair contains a floating-point input batch accepted by `m` and labels shaped `(B,)`; the corresponding logits have shape `(B, C)`, and evaluation batches contain at least one example in total.” |
| 1450 | “for each `(x, y)` in `train_batches`” | “`train_batches` is a list of nonempty tensor batches with logits shaped `(B, C)` and integer labels shaped `(B,)`; `lr` is the positive learning rate for a fresh Adam optimizer.” |
| 1453 | “a module `m` containing a batch norm and a batch `x`” | “`x` is a floating-point tensor accepted by `m` in both modes, with enough values per channel for training-mode batch normalization; both outputs retain the shape returned by `m`.” |
| 1455–1457 | “the test batches” | “Training and evaluation inputs are lists of nonempty tensor batches with logits shaped `(B, C)` and integer labels shaped `(B,)`; `lr` is positive, `epochs` is a positive integer, and initial parameter gradients are empty.” |

Place these contracts in short constraint lists, preserving the one-to-three-sentence task statement.

### D6. Input promises conflict with cases or indexing behavior

**`STMT_INPUT` — major**

| ID | Exact quote | Finding and replacement |
|---|---|---|
| 1386 | “integer indexing with negative indices allowed”; answer: `i=i%len(self._modules)` | Unrestricted wrapping differs from ordinary sequence indexing; empty indexing divides by zero. **Replacement:** “Integer indexing follows Python sequence bounds: indices outside `-len(m) <= i < len(m)` raise `IndexError`.” Add out-of-range and empty-chain checks, then repair reference indexing. |
| 1427 | “with the same architecture” | Cases 2 and 4 change kernel size or output width and expect shape mismatches. **Replacement:** “The modules have equally many state entries, but paired entries may have different shapes.” |
| 1439 | “`m` is a network from the lesson's `ResNet` class and `src` is a flat Sequential chain” | Cases 2–4 include non-flat sources, non-ResNet destinations, and matching names. **Replacement:** “`m` and `src` are modules with equally many state entries and compatible tensors at corresponding positions; their entry names may match or differ.” |
| 1454 | “a classifier `m` in evaluation mode” | Cases 1 and 3 construct models without calling evaluation mode. **Replacement fixture sentence:** “Construct every supplied classifier in evaluation mode before calling `solve`.” |

### D7. Object identity, exact type, and buffer requirements are ungraded

**`CORR_DECISIVE` — major**

Each substitute below passed every existing case.

| IDs | Exact quote | Accepted wrong behavior | Replacement sentence and decisive check |
|---|---|---|---|
| 1380–1381 | 1380: “containing the same child modules”; 1381: “the same module object registered `k` times” | Deep-copying modules passes. | “Every returned slot must contain the original supplied module object, not an equivalent copy.” Check object identity for every slot; include a parameterized module. |
| 1382 | “whose type is exactly the library ReLU module” | `isinstance` passes despite accepting subclasses. | “Count direct children whose concrete class is ReLU; exclude subclasses and nested descendants.” Add both a ReLU subclass and a nested chain containing ReLU. |
| 1388 | “The count is a buffer, not a parameter.” | An integer `Parameter` with gradients disabled passes. | “`calls` must be persistent integer buffer state and must be excluded from parameter enumeration.” Check buffer membership, parameter exclusion, initial value, and subsequent increments. |

### D8. State-copy cases do not distinguish copying parameters from copying all state

**`CORR_DECISIVE` — major**

**IDs:** 1428, 1438, 1439.

Exact quotes:

> 1428: “replace each of its values by the value of `src`'s state mapping at the same position”

> 1438–1439: “Copy `src`'s state into `m` by position”

Skipping every running mean, running variance, and batch counter passed all cases in each drill. Source and destination buffers start with matching defaults.

**Replacement sentence:** “Copy every paired state tensor, including normalization statistics and batch counters, while preserving the destination module object.”

**Decisive cases:** Give source and destination different running means, positive running variances, and batch counters; compare every resulting destination state entry against its paired source entry.

### D9. “Unchanged” is tested as “different from source”

**`CORR_DECISIVE` — major**

| ID | Exact quote | Accepted wrong behavior | Replacement sentence |
|---|---|---|---|
| 1433 | “leaving every other entry of `m` unchanged” | Zeroing shape-mismatched entries passes all four cases. | “Every shape-mismatched destination entry must retain exactly its original value.” |
| 1434 | “leave the rest of `m` untouched” | Zeroing all unselected entries passes all four cases. | “Every destination entry outside the prefix selection must retain exactly its original value.” |

Clone destination state before execution and compare preserved entries against those clones. Comparisons against source values cannot establish preservation.

### D10. Cases accept incorrect prediction and aggregation rules

**`CORR_DECISIVE` — major**

| ID | Exact quote | Verified gap | Replacement sentence |
|---|---|---|---|
| 1421 | “the position of the largest logit along the class axis” | Every expected prediction is the last class. Returning the last class for every image passes all three cases. | “Return each image’s highest-scoring class, choosing the smallest class index on ties.” Add a batch whose winners are `[0, 2, 1]`. |
| 1447 | “accuracy `correct / total`” | Unweighted mean of batch accuracies passes all four cases because batch sizes are equal within each case. | “Divide correct predictions across all batches by the total number of examples, allowing unequal batch sizes.” Add one correct example followed by three incorrect examples: accuracy `0.25`, not `0.5`. |

### D11. Hook removal is required but not verified

**`CORR_DECISIVE` — major**

**IDs:** 1459, 1462, 1464, 1466, 1469.

Exact quotes:

> 1459: “detach the hook”

> 1462, 1466: “detached before returning”

> 1464: “detach the hook”

> 1469: “detach every hook”

Removing `h.remove()` from each reference implementation still passes all its cases. Additionally, q1464 accepts `return m(x) * c`, which uses no hook.

**Replacement sentence:** “Remove every hook created by this function before returning, while preserving hooks that were already registered.”

**Decisive checks:** Observe registrations and returned handles, then verify removal after `solve`. For q1464, verify that the required output replacement happened through an instrumented module call.

### D12. NaN detector has contradictory return types and no clean-input behavior

**ID 1468**

- **`STMT_OUTPUT` — major.** Exact quote: “a tuple of two Python ints … as a boolean.”  
  **Replacement:** “Return `(record_count, first_is_source)`, where `record_count` is a Python integer and `first_is_source` is a Python boolean.”

- **`STMT_CASES` — major.** Exact reference expression: `flagged[0][1] is False`.  
  No NaN-free case exists. `solve(Identity(), ones(1))` raises `IndexError`.  
  **Replacement:** “If no module emits NaN, return `(0, False)`.” Add that case and guard the reference’s empty record list.

### D13. Near-miss explanation does not accurately identify its demonstrated mistake

**`STMT_NEAR_MISS` — minor**

| ID | Exact quote | Replacement explanation |
|---|---|---|
| 1378 | “swapping them builds a layer that maps the wrong direction” | “The wrong result `[5, -3]` uses the supplied weight without transposing it; swapping constructor dimensions cannot explain this square-weight case.” |
| 1402 | “a stride-2 group of three blocks halves the image three times” | “This input has two blocks; applying stride two in both shrinks the `4×4` image to `1×1`, instead of the required `2×2`.” |
| 1413 | “a 16×16 image leaves the stem as 7×7, not 8×8” | “For a `16×16` image, the convolution produces `8×8`; omitting pool padding then produces `3×3` instead of `4×4`.” |
| 1417 | “13 at stride 2 gives 7, not 6” | “This case uses stride three: spatial sizes 13 and 7 round up to 5 and 3, while floor division incorrectly gives 4 and 2.” |
| 1434 | “Editing the mapping changes nothing until it is loaded back into the module.” | “Replacing mapping entries does not update the module until loading; modifying the referenced tensors in place would update the module directly.” |

The distinction in q1434 follows the documented shallow-reference behavior of [`state_dict`](https://docs.pytorch.org/docs/2.12/generated/torch.nn.Module.html#torch.nn.Module.state_dict).

### D14. Statements exceed three sentences

**`STMT_LENGTH` — minor**

| IDs | Exact quote | Finding and replacement |
|---|---|---|
| 1406, 1410 | “A class `ResidualBlock` taking (in channels, out channels, stride) is provided.” | Four sentences each. Use their D2 goal sentences as openings; move helper availability and special cases into constraints. |
| 1424 | “Without building any module, return the total parameter count of the ResNet from the lesson (stem, groups, head) for the given configuration, as a Python int.” | Five sentences, with architecture and counting instructions embedded in prose. **Replacement:** “Return the total number of trainable scalar values in the classifier specified below.” Put stem, block, projection, and head properties in a compact table. |

### D15. Solo items make fewer decisions than their faded siblings

**`DIFF_RUNG` — minor**

| ID | Exact quote | Replacement for a scaffolded faded version |
|---|---|---|
| 1384 | “replace the child … using index assignment” | “Complete the missing operation that replaces the selected child before the provided forward call.” |
| 1394 | “the ReLU of the sum of the two branch outputs” | “Complete the expression that combines matching branch outputs into a nonnegative residual result.” |
| 1421 | “the position of the largest logit along the class axis” | “Complete the expression that returns one class prediction per image.” |
| 1454 | “return the cross-entropy loss … as a Python float” | “Complete the missing expression that measures mean classification loss.” |
| 1467 | “but not `m` itself” | “Complete the expression that counts descendants without counting the root.” |

Move these to faded practice with actual blanks, or add independent decisions before retaining solo placement.

### D16. “Survive” is undefined and gives the wrong motivation

**`STMT_TERMS` — minor**

**ID:** 1418.

Exact quote:

> “Small images do not survive the stem's two halvings”

Padded halvings retain positive spatial dimensions; the useful concern is lost spatial detail.

**Replacement:** “Return a stem that preserves spatial detail in small images by keeping their height and width unchanged.”

## 2. Lesson pages — per page, per rubric code

All seven pages have general concept prose before examples and prose introductions to their worked examples. Those aspects pass `EXPL_GENERAL_FIRST` and `EXPL_INTRO`.

Every page inherits **`GIVE_RUNG` — major** from its faded starters, quoted and repaired in D1. Lesson-specific findings follow, keyed by segment.

### [kp-sequential.md](/home/stellar-thread/Applications/Delta-Drills-Local/Local_Deployed_Shared/lessons/pytorch/kp-sequential.md) — major

**Owned faded IDs:** 1374–1379.

| Segment | Code / severity | Exact quote | Replacement |
|---|---|---|---|
| Registered children run in registration order | `EXPL_INTERLEAVE` — major | `class Chain(t.nn.Module):` | The containing fence has **21 lines**. Split helper definitions from `Chain`, introducing the second fence with: “The chain registers its children so parameter discovery and execution order describe the same network.” |
| Same segment | `EXPL_TRANSFER` — major | q1374: “applies each module in the list `mods` in order” | The worked `Chain` already implements that complete behavior. Replacement drill sentence: “Build a registered chain that applies every other supplied module, preserving its original position in each child’s name.” |
| A layer is an object with its weight inside | `EXPL_TRANSFER` — major | q1378: “return a library Linear layer with no bias whose weight parameter equals `w`” | The preceding code already constructs and fills that layer. Replacement drill sentence: “Given weights laid out as `(in_features, out_features)`, return a bias-free layer representing that map.” |

### [kp-residual-block.md](/home/stellar-thread/Applications/Delta-Drills-Local/Local_Deployed_Shared/lessons/pytorch/kp-residual-block.md) — major

**Owned faded IDs:** 1389–1392.

| Segment | Code / severity | Exact quote | Replacement |
|---|---|---|---|
| The left branch is conv, norm, ReLU, conv, norm | `EXPL_CORRECT` — major | “The left branch of a block is exactly this pattern twice over.” | “Only the first convolution–normalization pair is followed by ReLU; the second pair preserves signed values until the shortcut is added.” |
| Same segment; Misconceptions | `EXPL_CORRECT` — major | “which would cancel any bias exactly” | “During training, centering cancels a convolution’s channelwise bias; evaluation instead uses stored statistics, which must account for that bias.” |
| The shortcut must match the left branch, then the two add | `EXPL_CORRECT` — major | “so the block starts out close to the identity” | “The shortcut supplies an identity contribution when shapes match; the whole block approximates identity only when the residual branch is small and the final ReLU leaves the input unchanged.” |
| Same segment, q1391 | `EXPL_TRANSFER` — major | “return the SHORTCUT (right branch)” | `right_branch` already supplies the complete implementation. Replacement drill sentence: “Given compatible input and target feature-map shapes, construct the shortcut that makes the two branches addable.” Specify allowed strides and unambiguous shape pairs. |
| Same segment, q1392 | `EXPL_TRANSFER` — major | “three registered children named `left`, `right` and `relu`” | The worked `Block` already has that exact interface and behavior. Replacement drill sentence: “Return a block that reports both its branch sum before rectification and its final output.” |

### [kp-block-group.md](/home/stellar-thread/Applications/Delta-Drills-Local/Local_Deployed_Shared/lessons/pytorch/kp-block-group.md) — major

**Owned faded IDs:** 1402–1403.

| Segment | Code / severity | Exact quote | Replacement |
|---|---|---|---|
| Only the first block of a group resizes | `EXPL_CORRECT` — major | “so it owns the projection shortcut” | “The first block needs a projection only when its stride differs from one or its input and output channel counts differ.” |
| Same segment | `EXPL_CORRECT` — major | “doing the channel change in every block would leave nothing at the group's declared width” | “After the first block produces `o` channels, later blocks must accept `o`; reusing input width `i` causes a channel mismatch when `i != o`.” |
| Same segment, q1402 | `EXPL_TRANSFER` — major | `self.blocks.add_module("0",Block(i,o,s))` | The example and drill build the same group. Replacement drill sentence: “Complete a group whose channel conversion occurs in its first block and whose spatial downsampling occurs in its last block.” |

### [kp-resnet34.md](/home/stellar-thread/Applications/Delta-Drills-Local/Local_Deployed_Shared/lessons/pytorch/kp-resnet34.md) — major

**Owned faded IDs:** 1413–1416.

| Segment | Code / severity | Exact quote | Replacement |
|---|---|---|---|
| The stem shrinks the image four-fold before any block | `EXPL_CORRECT` — major | “everything after it is residual blocks” | “The stem is followed by residual groups, spatial averaging, and a linear classification head.” |
| Solo practice, q1418 | `EXPL_CORRECT` — major | “Small images do not survive the stem's two halvings” | “Repeated downsampling can discard useful detail from small images, so this variant preserves their spatial resolution.” |
| Stem, four stages, then average-pool and a linear head | `EXPL_CORRECT` — major | “accepts images of any size that survives the strides” | “Spatial averaging supports different positive image sizes; training-mode batch normalization additionally requires more than one value per channel at each normalization layer.” |
| Same segment | `EXPL_INTERLEAVE` — major | `class ResidualBlock(t.nn.Module):`; `class AveragePool(t.nn.Module):` | The two fences contain **22 and 19 lines**, with no prose between them. Split block, group, pooling, and network definitions. Insert: “Each group receives the previous group’s output width; the head receives one spatial average per final channel.” |
| Same segment | `EXPL_PRINTS` — minor | `assert BlockGroup(2,4,8,2)(t.zeros(1,4,8,8)).shape==(1,8,4,4)` | Neither long fence prints. Replacement explanatory sentence: “Print each constructed module’s output shape before checking it, so the result being asserted is visible.” Add prints for both group and network checks. |
| Stem segment, q1413 | `EXPL_TRANSFER` — major | “Return the ResNet stem for `c0` output channels” | This repeats the displayed stem constructor. Replacement drill sentence: “Complete a stem that selects resolution-preserving or downsampling behavior from the input image size.” |
| Stages/head segment, q1415 | `EXPL_TRANSFER` — major | “The forward applies the three in order.” | The example already supplies the full `ResNet` class. Replacement drill sentence: “Assemble the configured model so its forward method returns both spatially averaged features and class logits.” |

### [kp-feature-extraction.md](/home/stellar-thread/Applications/Delta-Drills-Local/Local_Deployed_Shared/lessons/pytorch/kp-feature-extraction.md) — major

**Owned faded IDs:** 1426–1431.

| Segment | Code / severity | Exact quote | Replacement |
|---|---|---|---|
| A module’s state is an ordered mapping of named tensors | `EXPL_CORRECT` — major | “one entry per parameter **and** per buffer” | “The state mapping contains registered parameters and persistent buffers, excluding entries registered as `None`.” |
| Same segment | `EXPL_CORRECT` — major | “A Sequential chain of a convolution and a batch norm therefore has six entries” | “A bias-free convolution followed by default affine batch normalization has six state entries.” |
| Same segment | `EXPL_CORRECT` — major | “Two models with the same architecture have state dicts of the same length whose entries line up position by position, whatever the names.” | “Positional copying requires a verified correspondence between state entries; matching architectures or tensor shapes alone does not establish that correspondence.” |
| Copying weights by position, and what a parameter is | `EXPL_CORRECT` — major | “The shapes must agree entry by entry — that is the check that the two architectures really match.” | “Matching shapes establish load compatibility, but you must also verify that paired entries represent the same layer and state role.” |
| Freeze everything, then replace the head | `EXPL_CORRECT` — major | “keeps a pretrained network's body fixed” | “Freezing parameters prevents their gradient-based updates; keep the frozen body in evaluation mode as well when its normalization statistics and features must remain fixed.” |
| Copying segment, q1428 | `EXPL_TRANSFER` — major | “take `m`'s state mapping, replace each of its values … load the result into `m`” | The preceding example already implements that exact procedure. Replacement drill sentence: “Copy source state using supplied destination-to-source name pairs, preserving unpaired destination entries.” |
| Copying segment, q1429 | `EXPL_TRANSFER` — major | “how many parameter tensors … how many entries … total number of values” | The preceding cells already perform these three counts. Replacement drill sentence: “Return separate scalar counts for parameters and persistent buffers in a module containing both kinds of state.” |
| Freeze segment, q1431 | `EXPL_TRANSFER` — major | “the number of values across its trainable parameters … and across its frozen parameters” | The worked example already computes both totals. Replacement drill sentence: “Return trainable and frozen scalar counts separately for each direct child.” |

Verified counterexamples:

- Two models with identical forward architecture produced `13` before positional copying; reversing registration order made the copied destination produce `15`.
- Freezing a batch-normalization module’s parameters did not freeze its running mean: one training forward changed it from `0` to approximately `0.1`.

Persistent-buffer and shallow-reference distinctions are documented in [`Module.state_dict`](https://docs.pytorch.org/docs/2.12/generated/torch.nn.Module.html#torch.nn.Module.state_dict).

### [kp-training-loop.md](/home/stellar-thread/Applications/Delta-Drills-Local/Local_Deployed_Shared/lessons/pytorch/kp-training-loop.md) — major

**Owned faded IDs:** 1441–1448.

| Segment | Code / severity | Exact quote | Replacement |
|---|---|---|---|
| An optimizer owns the parameters and applies the step; Misconceptions | `EXPL_CORRECT` — major | “on the very first step every value moves by exactly the learning rate” | “With default Adam and no weight decay, the first update is `-lr*g/(abs(g)+eps)`; its magnitude approaches `lr` for sufficiently large nonzero gradients, while a zero gradient produces no update.” |
| Same segment | `EXPL_CORRECT` — major | “in the direction that lowers the loss” | “Adam moves against its normalized gradient estimate, but a finite step is not guaranteed to lower the loss.” |
| Clear the gradients each step | `EXPL_CORRECT` — major | “Two backward passes without clearing leave twice the gradient” | “Two backward passes add their gradients; the result doubles only when both passes produce the same gradient and the initial gradient is zero.” |
| Backward / optimizer segments, q1441–1444 | `GIVE_EXAMPLE` — major | `lin.weight.copy_(t.tensor([[1.,2.]]))`; `x=t.tensor([[1.,1.]])` | These same literals appear in q1441 case 4, q1442 case 3, q1443 case 4, and q1444 case 4, reproducing the illustrated updates, gradient, or loss pair. Replacement example sentence: “For the demonstration, use one weight equal to `2`, input `3`, zero target, and learning rate `0.01`.” Recompute all accompanying values and keep the demonstration off faded practice screens. |
| Train mode for the update, eval mode for the measurement | `EXPL_INTERLEAVE` — major | `def train(m,train_batches,test_batches,lr,epochs):` | Worked fence has **18 lines**. Keep the function definition separate from its demonstration. Insert: “Each training batch contributes one loss, while evaluation contributes one accuracy after the entire epoch.” |
| Same segment | `EXPL_LENGTH` — minor | “A module has a mode flag.” | Concept prose contains **76 words**, below 80. Add: “Accuracy weights every example equally, even when batches differ in size.” |
| Same segment, q1447–1448 | `EXPL_TRANSFER` — major | q1447: “Return the accuracy `correct / total`”; q1448: “Return the tuple `(loss_list, acc_list)`.” | The page already supplies `evaluate` and `train` with these behaviors. Replacement sentences: q1447, “Measure accuracy separately for each class across batches of unequal sizes”; q1448, “Return per-epoch mean training loss weighted by batch size, together with evaluation accuracy.” |

Adam correction follows its documented update equation and epsilon term. [PyTorch Adam](https://docs.pytorch.org/docs/2.12/generated/torch.optim.Adam.html)

### [kp-forward-hooks.md](/home/stellar-thread/Applications/Delta-Drills-Local/Local_Deployed_Shared/lessons/pytorch/kp-forward-hooks.md) — major

**Owned faded IDs:** 1458–1461.

| Segment | Code / severity | Exact quote | Replacement |
|---|---|---|---|
| `apply` visits every module in the tree | `EXPL_CORRECT` — major | “deepest first” | “Traversal is depth-first post-order: finish a child’s descendants before visiting that child, continue through siblings, and visit the root last.” |
| Same segment; Misconceptions | `EXPL_CORRECT` — major | “every module downstream of the culprit is flagged too; the first record is the source” | “NaNs often propagate, but downstream modules can remove them; the first flagged module is the earliest observed NaN output, and inspecting its input distinguishes inherited NaNs from newly produced ones.” |
| First segment, q1458 | `EXPL_TRANSFER` — major | `shapes.append(tuple(output.shape))` | The concept cell already performs the complete collection/removal workflow. Replacement drill sentence: “Record each direct child’s input and output shapes, then report which children changed shape.” |
| Recursive segment, q1460 | `EXPL_TRANSFER` — major | `count[0]+=1`; `net.apply(tally)` | The concept cell already counts visits. Replacement drill sentence: “Return separate counts for leaf modules and modules containing children.” |
| Recursive segment, q1461 | `EXPL_TRANSFER` — major | `if t.isnan(output).any():` | The worked detector already supplies the complete solution. Replacement drill sentence: “Count module calls whose output introduces NaN when the input contains none.” |

q1468’s existing `(3, False)` case directly disproves the page’s unconditional claim that the first flagged module is the source.

## 3. Systemic patterns

| Pattern | Rubric codes | Scope | Concrete repair |
|---|---|---|---|
| Faded labels describe solo work | `GIVE_RUNG` — major | **34 drills; all seven pages** | Replace `pass` with meaningful scaffolds and blank relevant new syntax. |
| Demonstration becomes recall exercise | `EXPL_TRANSFER`, `GIVE_EXAMPLE` — major | All seven pages; exact reused numerical fixtures in q1441–1444 | Change the task’s input representation, output contract, or decision—not merely its numbers. |
| Context and recipes displace task contracts | `STMT_TASK`, `STMT_SELF_CONTAINED`, `GIVE_PROMPT`, `STMT_INPUT` — major | D2–D6 | Lead with the result; provide types, shapes, interfaces, and constraints on the same page; remove implementation instructions. |
| Tests verify a plausible result but miss a required property | `CORR_DECISIVE` — major | Sharing, exact type, buffer registration, copied buffers, preserved state, weighted accuracy, hook removal | Add cases targeting the named property; the verified wrong substitutes above must fail. |
| Examples support narrower claims than the prose makes | `EXPL_CORRECT` — major | Residual, block-group, ResNet, feature-extraction, training, hooks pages | State the condition under which each claim holds. |
| Long code and prose reduce inspectability | `EXPL_INTERLEAVE` — major; `EXPL_PRINTS`, `EXPL_LENGTH`, `STMT_LENGTH` — minor | D14 and lesson findings | Split code at conceptual boundaries; use intervening explanations and visible results. |

No `CORR_TYPES` defect established: the grader converts tensors and applies floating-point tolerances. No same-KP, same-rung exact-AST duplicate found. Cross-rung repetition was assessed as aid/transfer quality, not mislabeled `DIFF_DUP`.

## 4. Top 10 fixes with rewrites

Ranked across drills and lessons. Lesson entries include corrected teaching text and a corresponding standalone practice prompt. Behavioral I/O pairs below show results, not solution code.

### 1. q1468 — define a total NaN-detector contract

**`STMT_OUTPUT`, `STMT_CASES` — major**

> Instrument every module in `m`, including the root, and record module calls whose output contains NaN during one forward pass. Remove the hooks you created, run the model again, and return `(record_count, first_is_source)`, where the second value indicates that the first recorded call received no NaN input. Return `(0, False)` when no call is recorded.

Constraints:

- Each module receives one positional floating-point tensor and returns one tensor.
- `x` is a valid input; existing hooks must remain intact.
- Result types: `(int, bool)`.

I/O: identity module with `[1.0]` → `(0, False)`; identity module with `[NaN]` → `(1, False)`.

### 2. Feature-extraction lesson — remove the unsafe positional-copy guarantee

**`EXPL_CORRECT` — major**

**Teaching replacement:**

> State entries can be copied by position only when their semantic correspondence has been verified. Equal lengths and shapes do not prove that paired entries belong to the same layer or represent the same kind of state. The examples below use models whose parameter and buffer correspondence is known in advance.

**Corresponding q1427 prompt:**

> Pair the state entries of `m` and `src` in their respective mapping orders. Return one `(destination_name, source_name, same_shape)` tuple per pair, where `same_shape` is a Python boolean.

Constraint: equally many entries; shapes and names may differ.

I/O: destination shapes `a:(2,)`, `b:(1,)`; source shapes `x:(2,)`, `y:(3,)` → `[("a","x",True), ("b","y",False)]`.

### 3. Training-loop lesson — correct Adam’s first-step explanation

**`EXPL_CORRECT` — major**

**Teaching replacement:**

> With default Adam and no weight decay, the first update is `-lr*g/(abs(g)+eps)`. Nonzero gradients large relative to epsilon produce changes close to the learning rate; zero gradients produce no change. Neither Adam nor ordinary gradient descent guarantees that every finite update lowers the loss.

**Corresponding q1444 prompt:**

> Take one fresh Adam step on the mean squared error between `m(x)` and `target`. Return the loss immediately before and after that update as a tuple of Python floats.

Constraints:

- Floating-point tensors; `target.shape == m(x).shape`.
- Positive learning rate; initial gradients empty.
- Preserve the model’s supplied mode.

I/O: scalar model `m(x)=w*x`, `w=1`, `x=1`, target `0`, learning rate `0.1` → approximately `(1.0, 0.81)`.

### 4. Forward-hooks lesson — distinguish detection from causation

**`EXPL_CORRECT` — major**

**Teaching replacement:**

> A hook identifies a module call whose output contains NaN. That module may have received NaN already, and a later module may remove it. Compare the input and output of the recorded call before calling it a NaN source.

**Corresponding q1466 prompt:**

> Observe one forward pass through Sequential chain `m` and return one Python boolean per direct child indicating whether that child emitted any NaN. Remove all hooks created by the function before returning.

Constraints: compatible floating-point tensor input; tensor-valued child outputs; no shared child instances.

I/O: two identity children receiving `[NaN]` → `[True, True]`; neither flag alone establishes a new NaN source.

### 5. q1428 — make copying buffers observable

**`CORR_DECISIVE` — major**

> Copy every source state entry into its positionally paired destination entry, including persistent buffers, and return the original module `m`. Preserve source state and the destination’s training mode.

Constraints:

- Equal entry counts.
- Verified positional correspondence and matching shapes.
- Destination and source values may initially differ.

I/O: destination running mean `[0,0]` paired with source running mean `[2,-3]` → destination running mean `[2,-3]`.

**Grading change required:** Use distinct buffer values and check every copied entry.

### 6. q1447 — distinguish example-weighted accuracy from batch-weighted accuracy

**`CORR_DECISIVE`, `STMT_INPUT` — major**

> Evaluate `m` without recording gradients and return the fraction of correctly classified examples across all supplied batches. Count each example equally, regardless of batch size, and leave `m` in evaluation mode.

Constraints:

- Logits `(B,C)`; integer labels `(B,)`.
- Batch sizes may differ; at least one example overall.
- On tied logits, choose the smallest class index.

I/O: batch A has `1/1` correct; batch B has `0/3` correct → `0.25`.

### 7. q1433 — verify preservation against original destination state

**`CORR_DECISIVE` — major**

> Pair the state entries of `m` and `src` by position and copy only pairs with equal tensor shapes. Preserve every other destination entry exactly and return the number of entries copied as a Python integer.

Constraints: equal entry counts; names may differ; copying includes persistent buffers.

I/O: destination entries have values `[7,8]` and `[9]`; paired source entries have values `[1,2]` and `[3,4,5]` → return `1`, destination becomes `[1,2]` and `[9]`.

### 8. q1388 — distinguish persistent buffer state from parameters

**`CORR_DECISIVE` — major**

> Return a module that applies `mods` in order and tracks how many times its forward method has been called. Register the supplied modules under consecutive numeric names and maintain a persistent scalar integer buffer named `calls`, initially zero.

Constraints:

- Preserve supplied module objects.
- `calls` must not appear among parameters.
- An empty chain returns its input and still increments the counter.

I/O: an empty chain called three times → unchanged outputs and `calls == 3`.

### 9. q1421 — reject constant-class prediction

**`CORR_DECISIVE` — major**

> Return the predicted class index for each image in `x`, choosing the greatest logit and breaking ties by smallest class index. Return an integer tensor of shape `(B,)`.

Constraints:

- `x` has shape `(B,3,H,W)`.
- The supplied evaluation-mode model returns logits `(B,C)`.
- `B >= 1`, `C >= 1`.

I/O: logits `[[4,1,0], [2,3,6], [0,5,2]]` → `[0,2,1]`.

### 10. q1425 — put the complete architecture on the drill page

**`STMT_SELF_CONTAINED`, `STMT_TASK` — major**

> Build the configured classifier described below. Its forward method must return a list containing the input shape, the shape after the stem, the shape after each residual group, and the final logits shape, each as a tuple of Python integers.

| Component | Contract |
|---|---|
| `in_layers` | Biased `7×7` convolution, 3 input channels, `c0` output channels, stride 2, padding 3; batch normalization; rectification; `3×3` maximum pooling, stride 2, padding 1. |
| `residual_layers` | One supplied `BlockGroup(n, i, o, s)` per configuration entry. First input width is `c0`; later input widths equal the preceding group’s output width. |
| Supplied `BlockGroup` | Contains `n` residual blocks; only its first block changes channels or resolution. Each spatial size becomes `ceil(size/s)`. |
| `out_layers` | Spatial averaging to one value per channel, followed by a biased Linear classifier with `n_classes` outputs. |

Constraints: configuration lists have equal positive length; block counts, widths, strides, and class count are positive integers; forward inputs are compatible floating-point tensors `(B,3,H,W)`.

I/O: `c0=2`, block counts `[1,1]`, widths `[2,4]`, strides `[1,2]`, three classes, input `(2,3,16,16)` → `[(2,3,16,16), (2,2,4,4), (2,2,4,4), (2,4,2,2), (2,3)]`.

---

Git: MODERATE — repo=Delta-Drills-Local branch=main; staged=0, unstaged=29, untracked=11, conflicts=0. Concurrent changes added `CLAUDE.md` and the PyTorch lesson README to tracked dirtiness; review wrote nothing.