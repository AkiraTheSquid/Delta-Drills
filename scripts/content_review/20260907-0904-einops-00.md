# Content review — einops-00

Reviewers: fable, astra · rubric: `scripts/CONTENT_RUBRIC.md` · 2026-09-07

## einops.einsum — 42 drills

- ⚠️ fable: unparseable reply (exit 1)
- ⚠️ astra: unparseable reply (exit 1)

### Findings


## einops.merge-axes — 8 drills

- ⚠️ fable: unparseable reply (exit 1)
- ⚠️ astra: unparseable reply (exit 1)

### Mechanical rules

- q889 `STMT_SELF_CONTAINED` (major): Variant of exercise (1) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q890 `STMT_SELF_CONTAINED` (major): Variant of exercise (1) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q891 `STMT_SELF_CONTAINED` (major): Variant of exercise (1) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q892 `STMT_SELF_CONTAINED` (major): Variant of exercise (1) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q905 `STMT_SELF_CONTAINED` (major): Variant of exercise (5) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q906 `STMT_SELF_CONTAINED` (major): Variant of exercise (5) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q907 `STMT_SELF_CONTAINED` (major): Variant of exercise (5) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q908 `STMT_SELF_CONTAINED` (major): Variant of exercise (5) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.

### Findings


## einops.repeat-model — 20 drills

- ⚠️ fable: unparseable reply (exit 1)
- ⚠️ astra: unparseable reply (exit 1)

### Mechanical rules

- q893 `STMT_SELF_CONTAINED` (major): Variant of exercise (2) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q894 `STMT_SELF_CONTAINED` (major): Variant of exercise (2) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q895 `STMT_SELF_CONTAINED` (major): Variant of exercise (2) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q896 `STMT_SELF_CONTAINED` (major): Variant of exercise (2) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q897 `STMT_SELF_CONTAINED` (major): Variant of exercise (3) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q898 `STMT_SELF_CONTAINED` (major): Variant of exercise (3) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q899 `STMT_SELF_CONTAINED` (major): Variant of exercise (3) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q900 `STMT_SELF_CONTAINED` (major): Variant of exercise (3) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q901 `STMT_SELF_CONTAINED` (major): Variant of exercise (4) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q902 `STMT_SELF_CONTAINED` (major): Variant of exercise (4) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q903 `STMT_SELF_CONTAINED` (major): Variant of exercise (4) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q904 `STMT_SELF_CONTAINED` (major): Variant of exercise (4) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q933 `STMT_SELF_CONTAINED` (major): Variant of (B2) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q934 `STMT_SELF_CONTAINED` (major): Variant of (B2) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q935 `STMT_SELF_CONTAINED` (major): Variant of (B2) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q936 `STMT_SELF_CONTAINED` (major): Variant of (B2) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q937 `STMT_SELF_CONTAINED` (major): Variant of (B3) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q938 `STMT_SELF_CONTAINED` (major): Variant of (B3) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q938 `GIVE_PROMPT` (major): torch.std, which is what the learner is supposed to choose. Describe the goal instead. → Describe the goal, not the call.
- q939 `STMT_SELF_CONTAINED` (major): Variant of (B3) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q939 `GIVE_PROMPT` (major): torch.std, which is what the learner is supposed to choose. Describe the goal instead. → Describe the goal, not the call.
- q940 `STMT_SELF_CONTAINED` (major): Variant of (B3) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q940 `GIVE_PROMPT` (major): torch.std, which is what the learner is supposed to choose. Describe the goal instead. → Describe the goal, not the call.

### Findings


## einops.grids-montage — 4 drills

- ⚠️ fable: unparseable reply (exit 1)
- ⚠️ astra: unparseable reply (exit 1)

### Mechanical rules

- q909 `STMT_SELF_CONTAINED` (major): Variant of exercise (6) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q910 `STMT_SELF_CONTAINED` (major): Variant of exercise (6) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q911 `STMT_SELF_CONTAINED` (major): Variant of exercise (6) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q912 `STMT_SELF_CONTAINED` (major): Variant of exercise (6) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.

### Findings


## einops.pattern-language — 4 drills

- ⚠️ fable: unparseable reply (exit 1)
- ⚠️ astra: unparseable reply (exit 1)

### Mechanical rules

- q913 `STMT_SELF_CONTAINED` (major): Variant of exercise (7) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q914 `STMT_SELF_CONTAINED` (major): Variant of exercise (7) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q915 `STMT_SELF_CONTAINED` (major): Variant of exercise (7) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q916 `STMT_SELF_CONTAINED` (major): Variant of exercise (7) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.

### Findings


## einops.pooling — 4 drills

- ⚠️ fable: unparseable reply (exit 1)
- ⚠️ astra: unparseable reply (exit 1)

### Mechanical rules

- q917 `STMT_SELF_CONTAINED` (major): Variant of exercise (8) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q918 `STMT_SELF_CONTAINED` (major): Variant of exercise (8) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q919 `STMT_SELF_CONTAINED` (major): Variant of exercise (8) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q920 `STMT_SELF_CONTAINED` (major): Variant of exercise (8) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.

### Findings


## einops.split-axes — 8 drills

- ⚠️ fable: unparseable reply (exit 1)
- ⚠️ astra: unparseable reply (exit 1)

### Mechanical rules

- q921 `STMT_SELF_CONTAINED` (major): Variant of (A1) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q921 `GIVE_PROMPT` (major): torch.arange, which is what the learner is supposed to choose. Describe the goal instead. → Describe the goal, not the call.
- q922 `STMT_SELF_CONTAINED` (major): Variant of (A1) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q922 `GIVE_PROMPT` (major): torch.arange, which is what the learner is supposed to choose. Describe the goal instead. → Describe the goal, not the call.
- q923 `STMT_SELF_CONTAINED` (major): Variant of (A1) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q923 `GIVE_PROMPT` (major): torch.arange, which is what the learner is supposed to choose. Describe the goal instead. → Describe the goal, not the call.
- q924 `STMT_SELF_CONTAINED` (major): Variant of (A1) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q924 `GIVE_PROMPT` (major): torch.arange, which is what the learner is supposed to choose. Describe the goal instead. → Describe the goal, not the call.
- q925 `STMT_SELF_CONTAINED` (major): Variant of (A2) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q925 `GIVE_PROMPT` (major): torch.tensor, which is what the learner is supposed to choose. Describe the goal instead. → Describe the goal, not the call.
- q926 `STMT_SELF_CONTAINED` (major): Variant of (A2) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q927 `STMT_SELF_CONTAINED` (major): Variant of (A2) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q928 `STMT_SELF_CONTAINED` (major): Variant of (A2) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.

### Findings


## einops.reduce-model — 4 drills

- ⚠️ fable: unparseable reply (exit 1)
- ⚠️ astra: unparseable reply (exit 1)

### Mechanical rules

- q929 `STMT_SELF_CONTAINED` (major): Variant of (B1) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q930 `STMT_SELF_CONTAINED` (major): Variant of (B1) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q931 `STMT_SELF_CONTAINED` (major): Variant of (B1) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.
- q932 `STMT_SELF_CONTAINED` (major): Variant of (B1) → The drill is served alone, in any order. Say what the exercise did instead of pointing at it.

### Findings

