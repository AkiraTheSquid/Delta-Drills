# app

## Purpose
- The FastAPI backend for Delta Drills: PDF chapter-splitting jobs and the adaptive coding-practice API.
- This folder is the deployable backend service. `main.py` is the ASGI entry point (`uvicorn app.main:app`).

## Owns
- Application wiring: the `FastAPI` instance, CORS, and router mounting (`main.py`). `main.py` is intentionally thin — endpoint logic lives in `*_router.py` modules.
- Authentication endpoints: `auth_router.py` (`/auth/signup`, `/auth/login`).
- Job endpoints + background worker: `jobs_router.py` (`/jobs` POST/GET, `/jobs/{id}/artifacts`, `process_job`).
- Chapter endpoints: `chapters_router.py` (`/jobs/{id}/chapters`, `/jobs/{id}/chapters/{cid}/download`).
- Authentication primitives: password hashing, JWT issuance, current-user dependency (`auth.py`).
- Persistence: the SQLAlchemy session/engine (`db.py`), ORM models (`models.py`), and Pydantic schemas (`schemas.py`).
- Background processing: PDF auto-TOC and chapter splitting (`processing.py`), artifact capture (`job_artifacts.py`), filesystem layout (`storage.py`).
- Practice domain primitives that aren't router code: adaptive difficulty math (`adaptive.py`), subtopic prioritization (`prioritization.py`), the question catalog (`questions.py`), the sandboxed Python runner (`code_runner.py`), and the practice request/response schemas (`practice_schemas.py`).
- Graph-backed curriculum primitives: explicit concept nodes, prerequisite edges, lesson/problem mappings, and mastery-gated unlocking (`concept_graph.py` + `data/concept_graphs/*.json`).
- The aggregated practice router lives in the `practice/` subpackage.
- Persistent notebook kernels: one live Python session per signed-in learner, kept alive between cells. On Modal (`modal_kernel.py`, chosen by `kernel_backend.py` when `MODAL_TOKEN_ID`/`MODAL_TOKEN_SECRET` are set) that session is a real IPython in its own sandbox; without credentials it is a fork of this process (`kernel_runner.py`).
- Startup/shutdown handlers: schema creation, the torch preload, and stopping every live kernel on the way down (`lifecycle.py`).

## Does NOT own
- Practice route handlers, prompt strings, OpenAI call logic, grading dispatch — see `practice/` and its README.
- Frontend code, CSS, or static assets — see `Local_Deployed_Shared/` at the repo root.
- Question CSVs and other data files — sourced from `Local_Deployed_Shared/` at runtime via `questions.py`.
- Deployment scripts and Fly.io config — see `This-Directory-Only/backend/scripts/` and `fly.toml`.

## Key Files
- `attempt_log.py` (2026-09-01): rows now carry `feature_sources` — the per-KC prereq and per-atom encompassed mastery behind the features — and a third kind, `bkt_update`, records the BKT channel's moved posteriors (FIRe credits included) beside each attempt. Additive; old rows read fine; `bkt_update` is never evidence.
- `kc_stats.py`: the first READER of the attempt log — global per-KC aggregates (accuracy, Brier, per-stage counts) plus rung-stall and served-while-predicting-failure flags, over every learner's `*.attempts.jsonl`. Served by `practice/stats_router.py` at `/kc-stats`; no user ids in the payload.
- `main.py`: thin assembly layer. Builds the `FastAPI` app, configures CORS, mounts every `*_router.py` module + the `practice/` package, and exposes `/health`. No endpoint logic lives here.
- `auth_router.py`: `/auth/signup`, `/auth/login`. Owns the IntegrityError-on-duplicate flow.
- `jobs_router.py`: `/jobs` (POST create), `/jobs/{job_id}` (GET), `/jobs/{job_id}/artifacts` (GET). Owns `process_job` — the BackgroundTask worker that runs auto-TOC + `split_chapters`.
- `chapters_router.py`: `/jobs/{job_id}/chapters` and `/jobs/{job_id}/chapters/{chapter_id}/download`.
- `auth.py`: bcrypt password hashing, JWT encode/decode, `get_current_user` FastAPI dependency.
- `db.py`: `SessionLocal`, `engine`, `get_db` dependency. Reads `DATABASE_URL` from `config.py`.
- `models.py`: SQLAlchemy ORM — `User`, `Job`, `Chapter`, `JobArtifact`, and the Groups tables `StudyGroup` / `StudyGroupMember` / `StudyGroupDay`.
- `schemas.py`: Pydantic shapes for the non-practice endpoints (jobs, chapters, auth tokens).
- `practice_schemas.py`: Pydantic shapes for the practice endpoints. Kept here (not under `practice/`) so any non-practice consumer can import them without a circular dep through the router package.
- `concept_graph.py`: Pydantic schema + JSON loader for the first explicit curriculum graph layer. This is the bridge between hand-seeded concept structure and later graph-aware scheduling.
- `../scripts/export_concept_graph_yed.py`: exports a concept-graph JSON file to GraphML so you can inspect the graph visually in yEd before wiring it into runtime sequencing.
- `adaptive.py`: per-user state, target-difficulty curve, attempt recording, feedback application.
- `content_gaps.py`: **where the course ran out** — one JSON object on the volume (`DATA_DIR/content-gaps.json`), keyed `user|kc|stage`, counting how often a learner hit a rung with nothing unseen left. Written from `practice/questions_router.py`; read by the `/drill-gaps` skill, which turns each row into drills to author. 🔴 Every failure is swallowed: a queue that 500s because it could not write a to-do list is strictly worse than one that quietly forgets to.
- `prioritization.py`: subtopic-selection weights and gradient-based prioritization.
- `example_schedule.py`: **when a worked example pops up in front of a drill, and which answers count as the learner's own** — one table (`SCHEDULE`) per stored rung: Faded only after an unaided miss, Solo at widening positions (0, 2, 5, 9), Integrated once on entry. `plan(attempts, stage)` is pure and is called at serve time (`prioritization.ladder_fields` → `ladder_example`) and again at record time so the stored attempt carries `example: bool`; `unaided_finish` is the third clause of `kc_graph.kc_evidence_exhausted`, and `aided`/`unaided`/`unaided_run` are what the ladder's two promotion routes read (see the asymmetry below). An experiment (2026-08-30) — retune the table, nothing else. Spec: `../../SPEC_WORKED_EXAMPLE_SCHEDULE.md` (repo `This-Directory-Only/`).
- `questions.py`: loads the question catalog (CSV-backed, then a stack of JSONL override layers) and exposes `get_question_by_id`, `get_questions_by_subtopic`, and `reload_questions()` for re-reading the bank after a layer is written at runtime.
- `feedback_ai_layer.py`: storage for the ONE override layer written at runtime rather than by a batch script — the question repairs driven by per-problem feedback (`practice/feedback_ai_improver.py`). Owns `ai_feedback_overrides.jsonl` (live, rewritten whole) and `ai_feedback_revisions.jsonl` (append-only audit log), both under `DELTA_FEEDBACK_AI_DIR`. Stdlib-only and imports nothing from `app`, so `questions.py` can read the layer during startup without a cycle.
- `feedback_repair_queue.py`: the jobs those repairs come from. A flagged question becomes a `pending` record in `ai_feedback_queue.jsonl` (same directory, same stdlib-only rule) and the request ends there; the model call happens on Seth's machine via `ops/question_repair/run_repairs.py`. **No model credential exists server-side** — that is the design, and `practice/watch.py` fails if an API-key path reappears.
- `question_derivation.py`: the pure text/code inference helpers `questions.py` uses while parsing CSVs — difficulty classification, primary-library and task-type inference, fixture/test-case derivation. Stateless: no module globals, no filesystem, no question store. Re-imported by `questions.py`, so callers keep importing these names from `app.questions`.
- `code_runner.py`: subprocess sandbox with a 5s timeout used by the practice grading flow.
- `kernel_runner.py`: **the notebook's live session.** `code_runner` forks a hardened child that answers one block and exits — right for grading, wrong for a notebook, where cell 8 is entitled to the `a` that cell 6 bound. A kernel is that same child with the exit removed: a globals dict and a duplex pipe, serving one cell at a time until it is interrupted, evicted or idles out. Every fence the grading child raises per run is raised here ONCE, at kernel start (`os.setsid()`, environment scrubbed to `PATH`, the backend dir dropped from `sys.path`, `app.*` poisoned out of `sys.modules`) — persistence changes how LONG the child lives, not what it can reach. Caps: `DD_KERNEL_MAX` (4), `DD_KERNEL_IDLE_SECONDS` (1200), `DD_KERNEL_RSS_MB` (512). A timeout sends SIGINT first so the cell dies and the session survives, the way Colab's stop button behaves; `os.killpg(SIGKILL)` is the fallback and it costs the session.
- `kernel_backend.py`: **which kernel implementation this process uses.** `modal` when Modal credentials exist (env, `.env`, or `~/.modal.toml`), else `fork`; `KERNEL_BACKEND=auto|modal|fork` forces it. Both modules share one surface (`run_cell`, `reset_kernel`, `kernel_status`, `shutdown_all`), and `kernel_router.py` imports from here so it never learns which it got.
- `modal_kernel.py` + `modal_kernel_shim.py`: **one Modal sandbox per learner, a real IPython inside.** The shim runs IN the sandbox (stdlib + `jupyter_client`, never importable here), drives ipykernel over the sandbox's stdin/stdout one JSON line per cell, forwards display_data mimebundles (plotly / HTML / PNG) as `outputs[]`, and owns the per-cell timeout (`interrupt_kernel`, then a 5 s grace before the session is declared dead). Because the executor is IPython and not `exec()`, `%pip` and `!wget` mean what they mean in Colab — provided the client harness runs `get_ipython().transform_cell()` before `ast.parse` (it does; `practice/notebook.js`). The image is Colab-shaped: a sparse checkout of ARENA at `/root/<chapter>/exercises`, pinned to `ARENA_SHA` (🔴 the FULL sha — GitHub refuses to fetch a short one; bump it together with the compiled notebooks), so ARENA's own setup cell finds `/root/chapter0_fundamentals` and skips its download branch unedited. Lifetimes: Modal `idle_timeout` (`DD_KERNEL_IDLE_SECONDS`, 1800) reaps a silent sandbox; `DD_KERNEL_LIFETIME_SECONDS` (4 h) is the hard billing ceiling; startup `sweep_orphans()` terminates every sandbox under the app that this process does not hold, and it is AWAITED before serving so a first-request sandbox cannot be mistaken for an orphan. Every failure after `Sandbox.create` terminates the sandbox before raising. `DD_KERNEL_MAX` (20) is a spend guard, `DD_KERNEL_CPU`/`DD_KERNEL_MEMORY_MB` (2 / 4096) size the box. The fork runner's fences have nothing to fence here: the sandbox has no `DATABASE_URL` and no `app` package.
- `lifecycle.py`: `register_lifecycle(app)` — the startup/shutdown handlers `main.py` used to hold inline. Extracted because `main.py` is capped at 60 LOC, and shutdown now has real work to do (`kernel_runner.shutdown_all`).
- `job_artifacts.py` + `storage.py` + `processing.py`: the PDF-job pipeline.
- `study_groups.py` (2026-09-02): the roster store behind the Groups tab — create / join-by-token / join-out-of-the-directory, all through ONE `_add_member` gate, plus the invite token (a 128-bit capability), the initials-only public directory, and the ownership hand-over when the owner leaves. A port of Delta Note's accountability groups. Reads nobody's practice state: the mastery numbers on that page are `groups_router`'s job.
- `study_group_days.py` (2026-09-02): the OTHER half of that page — one three-state checklist per person per day, stored as the opaque Tiptap `{v, doc}` string Delta Note's sub-goals use. 🔴 Keyed by `(user_id, day)` and never by group: a checklist is something a learner wrote, so it follows them between groups. 🔴 `day` is the LEARNER'S LOCAL DATE, parsed from what the browser sent and never derived here — a day written under the server's date reads back empty rather than failing.
- `practice/`: see `practice/README.md` — every `/api/practice/*` endpoint lives there.
- `config.py`: settings object (env-driven), incl. `openai_api_key`.

## Data & External Dependencies
- Postgres via SQLAlchemy (`db.py`).
- OpenAI API (used only inside `practice/chatgpt_helpers.py`).
- Local filesystem job tree under `user_data/` (see `storage.py`).
- `Local_Deployed_Shared/` for question CSVs and shared assets.

## How It Works (Flow)
1. `uvicorn app.main:app` boots `main.py`, which constructs `FastAPI`, configures CORS, and mounts four routers: `practice_router`, `auth_router`, `jobs_router`, `chapters_router`.
2. Auth requests hit `/auth/*` → `auth_router.py` issues JWTs via `auth.create_access_token`.
3. Job creation hits `POST /jobs` → `jobs_router.create_job` writes the `Job` row, copies the upload, and queues `jobs_router.process_job` as a `BackgroundTask`. The worker runs `processing.run_auto_toc` / `split_chapters`, captures `JobArtifact` rows via `job_artifacts.capture_auto_toc_artifacts`, and writes `Chapter` rows.
4. Chapter listing/download hits `/jobs/{id}/chapters/*` → `chapters_router.py` reads `Chapter` rows and streams files via `FileResponse`.
5. Practice requests hit `/api/practice/*` → forwarded into the `practice/` package (see its README for the full flow).

## Invariants & Constraints
- `main.py` is an assembly layer, not an endpoint module. It must stay under ~60 LOC and contain no endpoint bodies. `watch.py::check_invariants` enforces the LOC bound. Add new endpoints to the matching `*_router.py` (or create one).
- Each top-level router file MUST declare `router = APIRouter(...)`. `main.py` MUST mount every one via `app.include_router(<module>)`. `watch.py` text-greps both.
- The practice router import path is `from app.practice import router as practice_router`. Do not reintroduce a top-level `practice_router.py` — it was split for a reason (the file went RED at 495 LOC, score 675).
- `practice_schemas.py` lives at this level on purpose; do not move it under `practice/`. Other modules (e.g. tests, scripts) may import its types without paying for the practice routers.
- `code_runner.run_code` enforces a 5-second timeout. Do not call user code in-process from any endpoint.
- **A kernel's hardening runs once and then holds for twenty minutes.** The grading child re-hardens on every run and exits; a kernel does not. So `os.setsid()`, `os.environ.clear()` and `sys.modules["app"] = None` are load-bearing for the whole window — a learner who can `import app.config` from a lesson cell reads the box's secrets. `watch.py::check_a_notebook_kernel_cannot_read_the_server` pins each one by name.
- **The registry's RSS check cannot stop a running cell, so the kernel watches itself.** `_reap_locked` deliberately skips a session that holds its lock (killing it mid-cell would turn a healthy run into "Kernel died mid-cell"), which leaves one `t.ones(10**10)` free to reach the OOM killer and take the API down with it. A thread inside each kernel polls its own `/proc/self/statm`: over budget it asks the cell to stop (`interrupt_main`, which a Python allocation loop unwinds from with the session intact), and if that is ignored it exits the process. Do not delete one half — the interrupt alone cannot stop a C-level allocation, and the kill alone costs a session that would have recovered.
- **The kernel is authenticated and its session id is derived from the user, never sent by the client.** A client-chosen id would let one learner attach to another learner's live namespace. `/kernel/status` is scoped to the caller for the same reason.
- API keys come from `practice/chatgpt_helpers.load_chatgpt_api_key`. Do not read `OPENAI_API_KEY` from `os.environ` elsewhere.
- **The ITS serves only questions tagged to a lesson-graph KC** (`lessons.kc_only_serving()`, env `DELTA_KC_ONLY`, **default ON**). The lesson graph is being validated chapter by chapter; a question whose KC does not exist has no prerequisites, difficulty ordering or mastery target to be scheduled against. Today: 380 servable (Numpy/Einops/Einsum), 75 parked (CNN/PyTorch/Autograd/Optimizers). The filter lives in `questions.load_questions()` so every consumer inherits it — **do not add a second gate at a call site**, and do not "fix" a missing `DELTA_KC_ONLY` by defaulting it off. Full rationale: `docs/decision-kc-only-serving.md`.
- **`_questions_by_id` is complete; the serving pools are not.** `get_question_by_id` must keep resolving all 455 questions so history, `served_question_ids` and in-flight client questions still work. Only `_questions` / `_questions_by_subtopic` / `_subtopics` are narrowed. Do not "unify" them.
- **A placement run must carry a `plan`, and `get_diag` reopens one that does not.** Before 2026-09-07 a run was a count of probes, so `diag["plan"]` did not exist; left `active`, such a run has no budget for `remaining_secs` to spend AND the placement page hides the 1h/3h/6h picker for as long as a run is active — nine of sixty production learners sat on "In progress · 0 of at most 15" with no picker and no way to finish. `get_diag` treats an active plan-less run as the abandoned old run it is and clears `active`; it never touches the probe log (`start()` clears that on the retake) and it is idempotent. Do not "fix" this by backfilling a default plan instead: that would silently make every trapped learner's run a 1-hour test they never chose. Pinned by `scripts/test_placement.py`.
- **An unanswered probe is resumed, never re-rolled.** `select_probe` returns `diag["pending"]`'s question when one is outstanding. Re-rolling cost two things: the served question was already in `served_question_ids` so it could never be served again, and its serve-to-answer time was never charged — `spent_secs` only grows in `record_probe` — which made the plan's hard cap dodgeable by reloading before answering. A cap can only be hard if abandoning a problem costs time.
- `prioritization.select_next_subtopic` is the single source of truth for which subtopic to serve next. Do not introduce override hooks (the previous staleness-review override was deleted on 2026-04-27 because it caused localhost behavior to diverge from the Statistics page).

## Extension Points
- New non-practice endpoint, existing area: add it to the matching `*_router.py` (`auth_router`, `jobs_router`, `chapters_router`). Then update `EXPECTED_PATHS` in `watch.py::check_public_api`.
- New non-practice endpoint area: create `something_router.py` exporting `router = APIRouter(prefix="/something", tags=["something"])`, import + `app.include_router(...)` it in `main.py`, and add `'something_router'` to the iteration in `watch.py::check_invariants` + the import list in `check_imports`.
- New practice endpoint: extend the `practice/` package — see `practice/README.md`.
- New ORM model: define in `models.py`, add a Pydantic shape in `schemas.py`, run a migration via the project's existing scheme (see `schema.sql`).
- New background-job stage: extend `processing.py` and use `job_artifacts.capture_*` helpers to surface results. The dispatcher `process_job` lives in `jobs_router.py`.

## Known Issues, Recurring Bugs, and Pain Points (and How to Prevent Them)

- **Practice router monolith** — `RESOLVED`
  - When it happens: many practice endpoints live in one file, prompt strings inline, helpers sprinkled around.
  - Symptom: `practice_router.py` reached 495 LOC / 9 DEPS / RED, with judge prompts duplicated between `submit` and `ai-judge`.
  - Root cause: organic growth — every new endpoint was appended to the same module.
  - Prevention/fix: split into `app/practice/` with one router per feature group, plus shared `chatgpt_helpers`, `prompts`, and `grading` modules. New endpoints go in the matching sub-router or get a new sub-router file.
  - Status: `RESOLVED` (2026-04-27).

- **`main.py` wiring sprawl** — `RESOLVED`
  - When it happens: every new non-practice endpoint gets appended to `main.py` instead of a dedicated router.
  - Symptom: `main.py` reached 243 LOC / 8 DEPS / ORANGE (score 403) — auth + jobs + chapters + the background worker all sharing the same file with the FastAPI assembly.
  - Root cause: the entry-point file is the path of least resistance for "just add an endpoint".
  - Prevention/fix: split into `auth_router.py`, `jobs_router.py`, `chapters_router.py`. `main.py` is now a thin assembler (≤60 LOC enforced by `watch.py::check_invariants`). New endpoints must go in a `*_router.py` module.
  - Status: `RESOLVED` (2026-04-27).

- **Cycle via `TEMP_staleness_review_REMOVE_LATER/`** — `RESOLVED`
  - When it happens: previously, every `mod query` flagged `[CYCLE] prioritization.py ↔ TEMP_staleness_review_REMOVE_LATER/staleness.py`.
  - Root cause: leftover scaffolding from the disabled staleness-review feature.
  - Prevention/fix: folder deleted along with the `ENABLE_STALENESS_REVIEW` flag and conditional import in `prioritization.py`. If the feature returns, build it inside `prioritization.py` rather than as an external override module.
  - Status: `RESOLVED` (2026-04-27).

## Recent Changes
- 2026-09-11 (**"Significantly harder" did nothing on the Solo rung; a miss made Integrated unreachable**): `solo_progress.py`, `prioritization.py`, new `attempt_history.py`, `practice/question_pick.py`, `practice/grading.py`, `scripts/test_solo_progress.py`, `scripts/test_kc_ladder_report.py`. Seth on numpy.aggregations: three "Significantly harder" ratings moved the aim 60 → 79 (`difficulty_offset` at the 20 cap) and the next drill was 54 after 52. Two causes. (1) `solo_progress.next_band` walked the rung's difficulty bands bottom-up and ignored the aim; with one drill per band the picker then had nothing to choose between, so the rating changed a bar on screen and nothing served. `next_band(…, target=)` now treats bands more than `REACH` (15, the picker's own half-width) below the aim as cleared and walks from the first within reach; an aim above every band walks DOWN from the hardest ("harder than anything here" = the hardest thing here). `REACH` lives in `solo_progress` and `grading` imports it, so the two halves of the pick cannot drift (codex). `narrow_to_next_kc` passes `target_difficulty(state, subtopic, kc)` — the same aim the picker is handed — and takes `last_served`: a band that IS the drill on screen is stepped past (a one-drill band has nothing to rotate to, and question_pick's on-screen guard read the repeat as a dry concept); every other band stands with its two-success gate — a rung-wide unshown-first filter was codex's MEDIUM (it let a skipped drill's band be left before its two successes). (2) Promotion off Solo needs six DISTINCT correct answers on the latest attempt per drill, and a miss spent the drill for good (`answered_question_ids` counts misses) — a six-drill bank with one miss topped out at 5/6 and Integrated (q990–992, 62–70) could never be served. New `attempt_history.py` (split out for size: `prioritization` re-exports `answered_question_ids` and the new `missed_question_ids`, both off one `_latest_outcomes` history pass); once the rung has nothing unseen, the missed drills come back with NO gap (`narrow_to_next_kc`, before the walk-down — a walk-down is review, not a retake; never straight after the miss). The frontier's eligibility counts a missed drill as work left, so a concept whose only remaining work is retries is not skipped. 🔴 The Solo ceiling is content: the harder drills live on Integrated and are gated by the six-correct rule, so a rating can only reorder the Solo bank, never reach past it — the strip's aim bar can show a number the rung cannot serve.
- 2026-09-11 (**the faded rung is retired: lesson → Solo → Integrated**): `kc_ladder_math.py`, `kc_graph.py`, `example_schedule.py`, `prioritization.py` (comment), `watch.py`, `scripts/test_kc_ladder_report.py`, `scripts/test_lesson_gate.py`; frontend `practice/stage-ladder.js`, `infotips-registry.js`, both `watch.py`s. Seth: "we shouldn't have the faded rung anymore. it just go straight to solo, but it still has the rate of the lessons that show different examples". New `DRILL_FLOOR = "partial"` and `LIVE_STAGES = (worked, partial, solo)`; `_floored(stage)` lifts a row filed at `faded` (or with no stage) onto the floor. `_stage_from` floors every demotion and the confidently-struggling branch at `DRILL_FLOOR`; `_earned` is `solo` above `PROMOTE_LO["partial"]` (0.51) else the floor; `PROMOTE_LO` lost its `faded` key. `_STAGE_TO_RANKS` lost `faded`, so `questions_at_stage(…, "faded")` is `[]` and the fill-in-the-blank drills (ranks 0/1) are never served by the ladder — NOT folded into `partial`: a blanked starter with the blanks removed is not the drill its author cut. Every registry KC has a rank-2/3/unranked drill, so no concept lost its floor. `example_schedule.SCHEDULE` lost `faded` and `partial` gained `after_miss: True` — "miss one, see the support again" used to be a demotion onto the faded rung, and with nothing below `partial` the support that comes back is the example. 🔴 `LADDER_STAGES` still has four names: every attempt ever filed at `faded` is read back through it (`_streak_stage`, `_lowest_rung`, `_streak_toward`), and a run made there promotes to the floor, never past it. `ladder_starter` is now unreachable for a served drill (kept: the ARENA-page and concept-node flows still hand out the fade). `test_kc_ladder_report.py` rewritten for three rungs — and its three checks broken since the solo-progress gate (167b921f) now build rows on real drills (`_solo_rows`) so promotion to `solo` is reachable and tested. `test_lesson_gate.py`: a paused faded drill resumes on the floor with its own starter.
- 2026-09-07 (**the placement clock is per concept**): `diagnostic.py` reads `lessons/placement_time_caps.json` (`kc_cap_secs`, values clamped to `PER_PROBLEM_SECS` = the 20:00 ceiling). `problem_secs_allowed(user_state, kc=None)` takes the PENDING probe's concept — `select_probe` sets `pending` before `questions_router` builds the payload, so the new `NextQuestionResponse.diagnostic_secs_allowed` is that problem's own clock; `_elapsed_for` charges against the answered concept's cap (`record_probe` now resolves `kc` first). `cap_range` feeds `plan.per_problem_min_secs/max_secs` for the picker. Why: Seth, 6h run, 5 probes in 58 minutes under a flat 20:00; einops rearrange/reduce/repeat concepts and broadcasting get ARENA's ~10 min, einsum 8, `make_rays_1d` 15, one-call py-0/np-1 drills 5–6. Time is not a feature of the estimate (only the result is), so the change cannot skew scoring — it changes which problems time out, which is exactly the intent. Codex review (two passes) then closed three holes the flat clock also had: a CORRECT answer landing past the clock + `LATE_GRACE_SECS` (180 — judged after grading, so slow grading must fit inside it) is recorded `incorrect` with `late: true` (the charge is capped at the clock, so an uncapped answer was free time and free evidence); a resumed probe advertises `pending_secs_left` (cap minus time on screen, ceil) rather than a fresh clock, and the client treats 0 as a real answer; coming back after a real break (past the clock + grace) records that probe as a timed-out `incorrect` (`timed_out: true`, charged its whole clock) and serves a NEW problem — restamping it whole would have let a learner solve it offline and paste the answer; the cap is snapshotted on `pending.cap_secs` at serve so a table change mid-probe cannot move the clock; a pending probe WITHOUT a snapshot (served before this deploy — Seth's q889 was open for hours at the cut-over) is judged on the flat 20:00 it was served with and restamped whole once, uncharged, instead of becoming a timed-out miss. and `record_probe` records ONLY the pending probe — any other id (never served, or already recorded) is a no-op, which closes the /submit + /local-eval hole where any question id counted as placement evidence and a re-submit of q1 after q2 was served replaced q1's record and refunded its time. Records are final; only `override_probe` changes one. `test_placement.py`: 62 checks.
- 2026-09-07 (**the 6-hour placement was unreachable, and the clock was dodgeable**): three fixes to the day's own rewrite. (1) `get_diag` migrates a legacy ACTIVE run with no `plan` to "not started", which is what put the 1h/3h/6h picker back — measured on the Fly volume, 9 of 60 learners (Seth included) were stuck on the pre-rewrite shape `{active: true, probes: [], no plan}` from 2026-08-23, and `active` is exactly the flag `placement-plan.js::_paintPicker` hides the picker on. Non-destructive: their probe logs are kept, and those probes carry no `kc`, so `_model_probes` already ignored them. (2) `select_probe` resumes an outstanding `pending` probe instead of picking a new one, so a reload gets its problem back rather than burning it and resetting the uncharged clock. (3) `practice/diagnostic_router.diagnostic_answer` no longer lets an EMPTY `pending` through — it used to, so any question id at all could be posted as "dont_know" and recorded as evidence about a concept the learner was never asked about. A retry of the probe just answered still works (`record_probe` replaces the record and refunds its time). New `scripts/test_placement.py`, 29 checks, replaces `test_diagnostic_history.py`, which has been failing at import since the topic-θ model it tests (`p_correct`) was deleted. Verified end to end against a local backend + the deployed frontend: a 6-hour run reaches all 44 concepts and all 8 ARENA-linked ones with ARENA's own problem, and the planted production state renders "Not started" with the picker.
- 2026-09-07 (**graph-wide placement**): `diagnostic.py` rewritten around new `placement_model.py`. The placement now holds one P(known) per registry KC (44; disabled ones via `kc_prefs` are excluded) instead of one θ per bank topic. ALEKS-shaped updates (Cosyn et al. 2021): correct ≫ incorrect, "I don't know" strongest; a pass propagates UP to prerequisites (×0.5/hop), a miss DOWN to dependents (×0.3/hop, half again for a plain incorrect). 🔴 Invariant (`practice/watch.py::check_placement_cannot_classify_without_a_direct_probe`): prior + propagation is clipped to ±1.2 log-odds, so no concept is classified known/unknown until probed directly. Selection = expected value-weighted variance reduction over the whole graph (value = coreness × learner weight), ARENA-linked concepts (`lessons/arena_exercise_kcs.json`) probed with ARENA's own problem first (`ARENA_BONUS`). Budget is TIME: `start(hours=1|3|6)`, 20:00 cap per problem, spent time measured server-side serve→answer (`diag.pending`) or from the client's `elapsed_secs`; stop = time out, or no probe would move any concept (`STOP_GAIN`), never before every ARENA-linked concept had its ARENA problem. `finish()` seeds atoms through the crosswalk — atoms with evidence inside the BKT half-life only rise, stale/never-practised ones take the placement's number either way — and records `kcs`, `fast_track` (uncertain KCs) and `edge_violations` (dependent known, prerequisite unknown: the only edge-fitting n=1 allows). New `GET /diagnostic/plan` (1h/3h/6h with point estimates from a difficulty→seconds heuristic; no learner timing existed) and `POST /diagnostic/start {hours}`. Public names unchanged so `questions_router.py` did not move; `areas` still folds to PyTorch/Einops/Einsum for the old card. Simulated on Seth's real state: 3h ≈ 40 probes, 0 misclassified, every concept probed directly.
- 2026-09-06 (**`kc_graph.py` split, Modulario 700-LOC gate**): the ladder arithmetic (`LADDER_STAGES`, `PROMOTE_LO`, `DEMOTE_HI`, `_LADDER_WINDOW`, `_PROMOTE_STREAK`, `_wilson`, `_streak_toward`, `_step_down`, `_step_up`, `_streak_stage`, `_correct_run`, `_lowest_rung`) moved to new `kc_ladder_math.py`, pure functions over an attempt list. `kc_graph` re-exports every name, so `kc_graph._wilson` / `kc_graph.LADDER_STAGES` in engine_bridge, prioritization and the tests are unchanged. 745 → 644 LOC.
- 2026-09-06 (**per-learner concept preferences**): new `kc_prefs.py` + `practice/kc_prefs_router.py`; `UserPracticeState.kc_prefs` persisted (additive). One invariant: off is the `enabled` flag, weight is clamped to [0.25, 4] and never 0. `kc_graph.frontier` drops disabled concepts and sorts by `(descendants+1) × weight`; `kc_is_unlocked` treats a disabled prerequisite as satisfied; `question_kc_gate` refuses drills on a disabled KC (which is what keeps `prioritization`'s weakest-first fallback from serving them); `kc_report` rows carry `state: "disabled"` and `pref`. Test: `scripts/test_kc_prefs.py`.
- 2026-09-05 (**notebook kernels run on Modal, and magics run**): new `modal_kernel.py` / `modal_kernel_shim.py` / `kernel_backend.py`. Why: the ARENA notebooks' first cell is `%pip` + `!wget` and the fork kernel `exec()`s source, so every ARENA notebook opened on a read-only setup cell and then `StopIteration`'d on cell 2 because the repo it downloads was never on the 2 GB box. Now one Modal sandbox per learner (2 CPU / 4 GB, ~1.3 s cold, kernel ready ~3.6 s) runs real ipykernel with ARENA baked at `/root`, and `fig.show()` comes back as a plotly mimebundle in `outputs[]` (`ExecutionResult.outputs`, `KernelExecResponse.outputs`) instead of vanishing. Measured on the live page: 0.0 setup cell → ok, `section_dir` resolves, both `display_array_as_img` figures drawn. Codex review fixed before landing: a failed `exec`/ready-read leaked a billing sandbox (every post-create path now terminates it); the orphan sweep raced the first request (now awaited in startup); and the frontend's rich-HTML insert is sanitised. `modal>=1.5,<2` added to requirements; Fly needs `MODAL_TOKEN_ID` + `MODAL_TOKEN_SECRET` secrets before the deploy — without them the fork backend keeps serving as before.
- 2026-08-31 (**a concept off the frontier is still narrowed to its rung**):
  four checks in `scripts/test_kc_ladder_report.py` had been failing, and they
  were right. `narrow_to_next_kc` looks up the frontier KC twice and, when both
  miss, used to return the candidate pool UNNARROWED — skipping every rung
  check below it, so the difficulty picker chose on difficulty alone and a
  `solo` drill could reach a learner whose ladder said `faded`. That is
  promotion by exhaustion arriving through the one door the rest of the
  function is built to hold shut. The miss is ordinary, not exotic: `frontier`
  drops a KC that is `kc_is_learned`, and `kc_evidence_exhausted` makes that
  true of every concept whose drills have all been served. New `_resident_kc`
  narrows by MEMBERSHIP as a last resort — frontier-blind on purpose, because
  it is not answering "what next" (nothing can, or the frontier would have
  said) but "which concept are these questions from, so their rungs can be
  compared". A concept the learner has ladder history with wins over a bare
  count. The pool now comes back unnarrowed only when no question in it carries
  a KC at all, which is the one case with no rung to honour.
  🔴 A fully-served concept now returns `[]` WITH a gap, so the router answers
  409 "author more drills" instead of the silent 404 the unnarrowed pool used
  to produce. That is the 2026-08-28 rule; the test's old "must still serve
  something" check was a leftover from before it and has been restated.
- 2026-08-31 (**the mastery models know an example was on the screen**): the
  other half of the unaided rule — the ladder decides who advances, these two
  decide how good the learner is believed to be, and both read a drill served
  behind a worked-example popup exactly like one answered cold.
  `logistic_engine` gains an **`example` feature** (`DEFAULT_EXAMPLE_OFFSET =
  0.7` logits, added to the rung's own offset; `MODEL_VERSION` → `logistic-v0.2`
  because the design matrix changed) — deliberately NOT folded into `stage`,
  because since 2026-08-30 the popup is scheduled per ATTEMPT while the rung is
  not, so two drills on the same rung differ in whether one was shown. The
  offset is derived, not invented: an example used to sit inline beside the
  drill and was part of what `STAGE_FADED = 0.7` covered, and the popup replaced
  exactly that. `EngineConfig.validate` now requires `0 < example_offset <=
  faded`. `bkt_mastery` gains **`P_GUESS_AIDED = 0.50`** — raising G is the
  standard way to say an observation is less diagnostic, and it cuts both ways:
  a correct answer raises the posterior less AND a wrong one lowers it less.
  The learn-transit is untouched, because studying a solved instance and then
  writing the same shape IS a learning event; what it is not is a demonstration.
  🔴 That also bounds the size of this knob — a run of aided correct answers
  still walks an atom toward the unlock bar, just more slowly; what limits the
  exposure is the schedule showing a finite number of examples per rung.
  `engine_bridge.served_example` / `answer_was_aided` read the flag off the
  ladder row this answer already wrote, under the same question-id match
  `served_stage` uses — one screen, one flag, two models. Log rows written
  before the feature existed carry no `example` key, `predict` treats an absent
  feature as zero, and those attempts were in fact unaided, so replay is
  unchanged. Guarded by `watch.check_an_example_is_priced_into_the_model`.
  `BKTParams` is `kw_only` from this commit: `p_guess_aided` was inserted
  between `p_guess` and `p_slip`, and a positional construction would have
  silently read the slip rate as the aided guess rate (codex, 2026-08-31 —
  raised against `engine_bridge.record`, where it does not apply because
  those parameters are keyword-only, but it does apply here).
- 2026-08-31 (**assistance holds a rung, it does not buy one**): the `example`
  flag was stored per attempt and then read by nothing except the small-pool
  finish, so both promotion routes counted an answer given behind a
  worked-example popup as evidence of unassisted competence — and the Solo
  schedule shows one on the first and third drills of the rung against a
  three-answer streak, so a learner could leave the rung having read two of the
  three answers. `kc_graph._streak_stage` now counts only unaided answers in the
  run (an aided one is neutral: it neither counts nor breaks the run) while an
  unbroken aided run still HOLDS the rung it was made at; the Wilson route is
  capped by `_capped_by_unaided`, floored at the rung the last attempt was
  served at so that arriving on a rung behind its entry example cannot demote
  anyone. Demotion is untouched and still reads the full record. `kc_estimate`
  gained `promote_lo` and `unaided`, and `practice/stage-ladder.js` draws
  `promote_lo` so the bar cannot fill on answers that will not promote.
  Guarded by `watch.check_only_unaided_answers_promote`.
- 2026-08-30 (**worked-example popups on the drill rungs**): new `example_schedule.py`; `kc_graph.record_kc_outcome` stores `example` per attempt and `kc_evidence_exhausted` requires the last two Integrated answers unaided; `prioritization.ladder_fields` adds `ladder_example` (frontend `practice/example-gate.js` draws it). `watch.py` checks the table's shape, not its numbers.
- 2026-08-30: Two atoms joined `NON_GATING_ATOMS` and two joined the graph's `intentional_root_atoms`, for the 2026-07-08 reason in a new form. The ARENA content cut retired every question that trained `as-strided-windowing` and `inf-masking`, and a gating prerequisite with no trainer locks its dependents for every learner forever — `audit_question_bank.py --gate` caught it as `atom_prereq_untrainable` on 16 questions. `conv-output-shape` and `stride-zero-broadcast` had `as-strided-windowing` as their ONLY prerequisite, so demoting it left them ungated; both are now declared intentional roots rather than left silently rootless. Where the concept SURVIVED the cut the better fix was available and taken instead: q103 and q198 stayed in the bank so `softmax-from-logits` and `boolean-mask-combine` keep a trainer. Gate is green.
- 2026-08-28 (**never re-serve a question; say so when the rung runs dry**): new `content_gaps.py`, plus `kc_graph.py`, `prioritization.py`, `lessons.py`, `questions.py`, `practice_schemas.py`, `practice/questions_router.py`, `watch.py`.
  - 🔴 **`narrow_to_next_kc` used to end `return fresh or [q for q in narrowed if q.id in supported]`.** With three authored faded drills on a KC, that `or` branch was the steady state within minutes — the repeats Seth reported ("I basically memorized all the problems"). It now returns a 3-tuple `(questions, kc, gap)` and never falls back to a served question.
  - 🔴 **A spent rung falls back DOWNWARD, it does not 409.** A hard stop on the top rung would deadlock the learner: nothing servable means no evidence, and no evidence means they can never move off that rung. So an exhausted rung serves UNSEEN work from a LOWER rung (`gap["served_from"]`), still records the gap and still shows the banner. 409 `content_exhausted` only when nothing anywhere on the concept is unseen.
  - `content_gaps.py` writes `DATA_DIR/content-gaps.json`, keyed `user|kc|stage` with a `hits` counter, so a learner hitting the same wall twenty times is one row. Every failure is swallowed — a queue that 500s because it could not write a to-do list is worse than one that forgets to. Read it with the `/drill-gaps` skill.
  - **`rung_support` is now `faded → bool(scaffold)` and True everywhere else**; `_STAGE_NEEDS_EXAMPLE` is gone. Only the faded rung promises anything on screen. `_applied_with_example` is filled from `applied_items + solo_items`, and `has_worked_example` is ORDERING (serve the example-bearing solo drills first) not rung-assignment.
  - `Question.wrong_examples` is a new override-only field (`[{call, output, why}]`) shipped on `NextQuestionResponse`. 🔴 `pipeline/watch.py` asserts the override whitelist here and in `pipeline/export_questions_json.py` MATCH — add a field to both or it applies in one place and is silently dropped in the other.
- 2026-08-20 (**the run of correct answers leaked across the rung it bought**): `kc_graph.py`, `watch.py`. `kc_estimate` reports `streak` so the practice strip can fill the rung along the route the learner can actually watch move. It counted EVERY trailing correct answer, whatever rung it was given at — but `_streak_stage` promotes to one rung above the LOWEST answer in the run, so the moment three correct answers at `faded` bought `partial`, those same three were still trailing and the strip drew the newly reached rung already full: a promised promotion the ladder will not make, because from `partial` that run now aims at `partial`. The run is now scoped by `_streak_toward(run, stage)` — it counts only while every answer in it was given at or above the rung the learner is on, and reads zero otherwise, which is exactly when the streak route is spent until a miss resets it. `kc_estimate` also sends `stage`, the rung the estimate describes, because the strip's `setProgress` deliberately keeps showing the OLD rung until the next question arrives: without it the promoting answer would empty a run the section on screen had earned and the bar would run backwards. New behavioural check `check_a_new_rung_starts_empty`; the frontend's source-string checks cannot see a number leak across a rung boundary. **Found by codex on the ladder diff**, including the mirror-image bug the first fix introduced. The promotion rules themselves are untouched — this is what is REPORTED, not what promotes.
- 2026-08-19: **The default edition's lesson notebook is stateful.** New `kernel_runner.py` keeps one forked Python session per signed-in learner alive between cells, so `a` bound in cell 1 is still bound in cell 3 without re-running the cells in between — which is what the prefix-replay design could never scale to (the compiled chapter-1 notebook is 656 cells). New `practice/kernel_router.py` exposes `/kernel/exec`, `/kernel/reset`, `/kernel/status`. The lifecycle handlers moved out of `main.py` into `lifecycle.py` to stay under the 60-LOC cap; shutdown now stops every live kernel. Codex review caught four defects, all fixed and pinned by checks before this landed: a running cell could allocate past `MAX_RSS_MB` because the reaper skips a busy session (→ the in-kernel watchdog above); `fresh` was reported AFTER the clicked cell had already run, so the client's replay ran it a second time (→ `skip_on_fresh`, which installs the harness and runs nothing else); `/kernel/status` handed any signed-in learner every other learner's session id and lesson; and the registry lock was released before the session lock, so an eviction in the gap answered a healthy request with "Kernel is not running" (→ the handoff is revalidated and the choice remade). Spec: `docs/spec-stateful-notebook-kernel.md` — local only, `docs/` is gitignored in this repo, so it is not in a clone.
- 2026-08-18 (**the repair loop left the server**): new `feedback_repair_queue.py`. The layer that writes itself is unchanged, but nothing in `app/` calls a model any more. A flag becomes a job; `ops/question_repair/run_repairs.py` picks it up on Seth's machine, drives the local `claude` CLI under his own login in a read-only sandbox, and posts the rewrite back through `practice/problem_feedback_router.py`. Two things this buys beyond dropping the `anthropic` dependency and the `ANTHROPIC_API_KEY` Fly secret. The bank can no longer be rewritten by an unattended process nobody is watching — a repair now requires a machine with a person's login on it. And the runner can do what a request handler cannot: it re-runs a rewritten `answer_code` against the question's real `test_cases` through `code_runner` before the change is offered, which is the gate that actually stops a broken reference answer. The endpoint still re-runs every server-side gate on its own live copy of the question, because it is reachable with an allowlisted token and no runner involved.
- 2026-08-13 (**the bank gained a layer that writes itself**): new `feedback_ai_layer.py`; `questions.py` gained the layer read and `reload_questions()`. Every override layer before this one was produced offline by a batch script and shipped inside the image, so `_load_function_overrides` could assume the whole stack was static and read once at startup. The per-problem feedback loop (`practice/feedback_ai_improver.py`) breaks that assumption: it writes a repair mid-session and the fix has to be live on the next draw. Two consequences worth knowing before touching this. First, the new layer is read from `DELTA_FEEDBACK_AI_DIR` and NOT from the `chatgpt/` runtime dir with the others — that tree is rebuilt by every deploy, so a runtime write there would be silently reverted; production points it at the Fly `/data` volume. Second, `reload_questions()` re-parses every CSV under the same lock as the initial load, so it is safe against concurrent readers but far too expensive to call per request — it exists for one-off writes. Keep the layer list in sync with `Local_Deployed_Shared/pipeline/export_questions_json.py`, which now reads the same file so a local export can fold production's repairs into the shipped bank.
- 2026-08-06 (**the lattice was answering with priors — the crosswalk was never in the image**): new `kc_data_health.py`, `main.py`, `../../Dockerfile`, `../../../.dockerignore`. `kc_graph` reads `Local_Deployed_Shared/concept-graph/kc_atom_crosswalk.json`, that folder is gitignored, and the Dockerfile copied `lessons/` but never it — so in production `_crosswalk()` was `{}` and `kc_mastery()` returned `params.p_init` with `tier: "unmapped"`, `covered_w: 0`, for **all 63 KCs**. Nothing failed: `_read_json` logs a warning and returns `{}`, and every caller carries on. Consequences were not confined to the graph — `kc_is_learned` compares that constant against `LEARNED_THRESHOLD`, so **no concept could ever be learned and no child could ever unlock**; the frontier never moved off the roots. Measured on the real account: 95 atom posteriors, 41 graded attempts in one subtopic, every concept reporting 0.02. After the COPY: `numpy.ndarray-model` 0.894, 23 measured-tier, 6 frontier, 1 learned.
  - `/health` now returns `lattice: {kc_registry, kc_crosswalk, qmatrix}`. The failure mode is a **quiet zero**, so the count is published where anyone can see it without auth. `kc_data_health.py` counts loaded rows rather than checking `path.exists()` — a file present but truncated to `{}` fails identically and must read identically.
  - `main.py` is capped at 60 lines by `watch.py`; the endpoint stays three lines and the logic lives in the new module.
- 2026-08-06 (**one concept per visit**): `lessons.py`, `practice/lessons_router.py`, `practice/questions_router.py`, `prioritization.py`, `practice_schemas.py`, `watch.py`. `_kc_segments` reads the KP's concept list, `_segment_step` says which concept the learner is owed next, and `unexposed_target_kcs` reports that rather than the whole KP. Exposure keys gained a second form: `<kc>` still means "the whole KP is done" and is what every gate checks, `<kc>#<concept_id>` means one concept of it — so the gate can fire again for concept 2 without ever having claimed concept 1 taught the lot. `/exposure` validates the new form through `exposure_key_exists`, which checks against the CURRENT segment list so a concept renamed out of the markdown stops being storable.
  - `segment_drill` swaps the queue's pick for the concept's own faded item. The two used to agree by accident often enough to look intentional and cannot agree now — the gate teaches concept 2 of 3 while the queue aims at the KP's difficulty as a whole. The swap declines whenever the drill is served, missing, or in another subtopic: filing an attempt under the wrong subtopic's evidence is worse than a slightly-off question.
  - `ladder_integrated` (`is_integrated`) marks a problem that needs the whole KP with every concept of it read. Read at serve time, stored nowhere — a fifth entry in `LADDER_STAGES` would either rewrite stored history or invent a rung nothing can be promoted out of.

- 2026-08-06 (**the `faded` rung now actually fades something**): `lessons.py`, `practice/questions_router.py`, `practice_schemas.py`. `_STAGE_TO_RANKS` puts guided drills (rank 1) on the `faded` rung, but nothing ever built them a scaffold: guided items carry hints, `ladder_starter` finds no authored starter for them, and `ladder_fade.fade` returns None on a one-statement body. So the rung whose whole promise is "most of the solution is written — supply the rest" served a bare `def solve(x)`. `compile_lessons._derived_faded` now writes one for 57 of the 72 guided items, by blanking the calls in the canonical answer's LAST step (backward fading, with blanks instead of deletion — a deleted step is a blank page, which is the thing the rung exists not to be), and `lessons._load` registers those alongside the authored ones: 125 authored + 57 derived = 182.
  - **Only when something was actually blanked.** An unblanked "starter" is the answer, and a rung that hands over the answer manufactures a promotion. The 15 that produce nothing (`return x * 2` has no call to hide) are reported instead: the new `ladder_support` field says whether the rung's promise is on the page at all, and the display tells the truth rather than relabelling the rung — the rung is what the mastery record holds and what the next promotion is measured against.

- 2026-08-05: **The logistic engine is wired in.** `logistic_engine.py` (Logistic Knowledge Tracing, Pavlik et al. — the additive-logistic model that generalises AFM/PFA/R-PFA/IRT and Elo/Glicko) had been written, tested and imported by nothing but its own test since it landed; `attempt_log.py` was never written to. Every one of its tests passed the whole time, because an unwired model is silent rather than wrong. New **`engine_bridge.py`** is the entire connection and deliberately the only file that knows both vocabularies — the engine stays a pure model with no state loading, no question bank and no graph, and the serving path never learns what a Posterior is. It materialises the design matrix (`ability`=1.0, `difficulty` via `difficulty_to_logits`, `stage` via the ladder's recorded rung, `prereq` and `encompassing` as centred means over the graph and the atom crosswalk, `recency` from the posterior's own `last_seen`), folds each outcome in through `E.step`, and writes the attempt-log row that pairs the prediction made BEFORE the outcome with the outcome — the pairing that makes the model falsifiable instead of merely opinionated, and the thing that turns those v0 weights into something with a Brier score.
  - **Scored against EVERY concept the question targets**, not just the primary one — `record_attempt_across_kcs` over `question_kcs`, one posterior and one design row each. A problem that exercises three ideas was teaching the model about one. This is not double-counting: each concept moves by its own residual against its own prediction, so a concept already strong barely moves while a weak one moves a lot. That is attribution.
  - 🔴 **The rung is read from the ladder, never recomputed.** `record_ladder_outcome` runs before the scoring tail and stores the rung the learner was sitting on; by the time `finalize_attempt` runs, `kc_stage` may have promoted — because answering correctly is what promotes it. Recomputing would charge a scaffolded answer at the unscaffolded rung it just earned, and the model would learn that assistance does not help. `served_stage` exists for exactly this, and an unrecognised rung is refused rather than guessed.
  - The engine update runs **after** the BKT update, because `encompassing` is a mean over the concept's atoms and reading it first would feed the model pre-answer posteriors. Pinned by `practice/watch.py`.
  - **Both models still run.** BKT keeps feeding the atom posteriors that `kc_mastery`, the unlock lattice and the Statistics panel read; the engine supplies the ability estimate `_aim_mastery` prefers once a concept has `MIN_ATTEMPTS_TO_SERVE = 3` graded attempts. Two reasons not to cut over: the engine's weights are v0 defaults, sane but unfitted, so it is a better *estimator* whose *calibration* is unproven until the log has rows; and the unlock lattice gates content, where a miscalibrated engine could lock a learner out of the course rather than merely serve them something too hard.
  - 🔑 **An existing learner is not demoted by the switch.** The engine's cold prior is deliberately pessimistic (mean −1.0, P≈0.27); consulting it fresh would have dropped a learner sitting at 20/20 from an aim near 87 to about 60. `attempt_log.backfill_from_state` is right that old records cannot reconstruct estimator state — no stored features, no stored predictions — so instead the ladder's Wilson lower bound seeds the ability **mean** while the prior keeps its full **variance**. A belief plus an admission of how loosely it is held, which is what a prior is for; the engine's own evidence dominates within a handful of attempts either way. Verified on the live account: the concept moves 58.5 → 70.5 over four correct answers and back to 65.1 on a miss. New `scripts/test_engine_bridge.py` (21 checks) drives all of it through the real scoring tail, because "the model updates when you call the updater" is not the property in doubt.
- 2026-08-05: **The difficulty aim is measured on the concept, not on the subtopic.** `prioritization` gains `_aim_mastery` and an optional `kc=` on `target_difficulty`; `narrow_to_next_kc` now returns `(candidates, kc)` so the caller can aim at the concept it settled on; `question_target_difficulty(user_state, subtopic, qid)` is the finalize-side equivalent, keyed to `question_kcs(qid)[0]` — the same primary KC `ladder_fields` reports, so the bar the strip redraws after a submit is measured on the concept the strip names. **Why:** `subtopic_mastery` is the unweighted mean over every atom a subtopic exercises, and a subtopic is not a concept. "Numpy: Core array literacy" carries thirty atoms; on a real account twenty-five of them sat untouched at the beginner prior (0.02), so the aim was pinned at **24.5/100 while the concept in front of the learner measured 0.918** and its own ladder read 14/20. Worse, the aim finished that session 0.15 points LOWER than it started — BKT decay across the untouched atoms outran the evidence on the practised ones, so answering correctly moved the number the wrong way. The learner was being shown a per-concept estimate and served a per-subtopic aim, and the two had no reason to agree. The concept number is `kc_estimate`'s Wilson LOWER bound (already drawn as the left edge of the estimate range), which behaves on a thin record without special-casing: 0 at n=0, 0.44 at 3/3, 0.43 at 13/20, 0.84 at 20/20. With no attempts on the concept, a `measured` crosswalk row supplies the prior instead, and a `topic-proxy` row — the topic's atoms wearing the concept's name — falls through to the subtopic. Verified on the live account: the screenshot's concept moves 24.5 → **58.5**, and a brand-new concept stays at 21.6 rather than exploding. The bare `target_difficulty(user_state, subtopic)` is unchanged and still what the subtopic scorer uses, which is correct — it ranks every subtopic there is, and across a whole subtopic there is no one concept to measure.
- 2026-08-04: **The felt-difficulty rating moves something again.** The three-option self-report ("About right / A little hard / Way too hard", and the easy-side converse after a correct answer) has been writing `attempt.alpha` into history and being read by nothing since BKT replaced the EWMA that used to consume it — the buttons were decoration. `adaptive.py` gains `DIFFICULTY_NUDGE` / `DIFFICULTY_OFFSET_DECAY` / `DIFFICULTY_OFFSET_LIMIT`, `SubtopicState.difficulty_offset` and `nudge_difficulty_offset()`; `prioritization.target_difficulty` adds that offset to its BKT-derived aim. Signed by the OUTCOME and sized by the RATING: "way too easy" after a correct answer is +6, "a little" is +3, the same two after a miss are −6/−3, "about right" decays the accumulated offset by ×0.75, and an UNRATED attempt (a Skip, an ended session, a route with no rating step) moves nothing at all — nobody was asked, so eroding a real correction there would make the rating quietly worthless. Capped at ±20 of the 20–100 span. It is deliberately NOT fed into mastery: mastery is a claim about what the learner can do and only evidence may move it; this is a claim about where to aim, which is the one thing the learner judges better than we do. `target_difficulty` now reads the row with `.get` rather than `get_subtopic_state`, which CREATES it — the subtopic scorer asks about every subtopic there is, including ones never practised. `difficulty_offset` is persisted explicitly in `_save_user_state` (the writer builds its dict by hand, so a new dataclass field does NOT round-trip by itself) and defaults to 0.0 on older saves. New `practice/watch.py::check_felt_difficulty_reaches_the_next_question` drives it end to end.
- 2026-08-04: **A streak can promote.** `kc_graph` gains `_PROMOTE_STREAK = 3` and `_streak_stage`: a trailing run of correct answers is worth exactly one rung above the one the Wilson lower bound has earned. Without it a learner whose window still carries misses from when the drills were broken (11 misses then 5 correct: 7/20, Wilson lower 0.181 against a 0.34 bar) could not promote at all, while a single miss demoted instantly — the asymmetry that produced "it keeps increasing in difficulty, but it's still too easy for me". The Wilson path is untouched and still reaches `solo` on its own.
- 2026-08-04: **`prioritization.narrow_to_next_kc` served unaided drills to learners still on the scaffold.** When every question of the chosen KC was already served, `select_next_kc(eligible=…not in served)` returned None and the function returned the whole unfiltered pool — before any rung filter ran. That is how question 30 / ID 485 (a `solo` drill) reached a learner sitting at the displayed "Worked" rung. It now re-asks on membership alone (the picker recycles by staleness), and the rung filter runs BEFORE the unserved check so a supported stage can never fall through to an unaided question. Verified over all 63 KCs against real state: 0 leaks, 0 stalls.
- 2026-07-30: **The ladder no longer demotes anyone back onto the lesson page.** `kc_graph._stage_from` could return `worked` from two demotion paths — stepping down from a miss recorded at `faded`, and the confidently-struggling branch (Wilson upper under `DEMOTE_HI`). `worked` is not a drill: it is the rung at which `LessonGate` takes the whole screen, so either path replayed a lesson the learner had already read, and because the stage is re-derived from `attempts[-1]` on every request it repeated before every subsequent question on that KC until they answered one correctly. Both paths now floor at `faded`, where the worked example stays on screen beside the problem. `worked` is therefore reachable only from `worked_seen == 0` and now means first contact and nothing else; `_step_down` takes a required `floor` so no future caller can land someone on the lesson page by omission. `scripts/test_kc_ladder_report.py` covers both paths plus a property check over all four stages (32 checks).
- 2026-07-27: **KC-only serving.** `lessons.py` gained `has_target_kcs()` and `kc_only_serving()` (env `DELTA_KC_ONLY`, default ON); `questions.load_questions()` now splits the store into a complete by-id map (455) and servable-only pools (380 across 12 subtopics), parking the 75 CNN/PyTorch/Autograd/Optimizer questions until their chapter of the lesson graph is authored and validated. `lessons/` is now the source of truth; the ARENA concept graph is demoted but retained (it is the diff target for blind-authoring later chapters). Filtering at load means `select_next_subtopic`, the `questions_router` candidate pool and `diagnostic.py`'s probe selection all inherit it; `questions_router.py` (ORANGE) was deliberately not touched. See `docs/decision-kc-only-serving.md`.
- 2026-07-24: Split `questions.py` (740 LOC, over the 700 ceiling) — moved the stateless derivation helpers into `question_derivation.py`, leaving 570 LOC. `compose_full_solution` / `wrap_answer_as_function` deliberately STAYED in `questions.py` because `watch.py::check_public_api` text-greps for those two defs there. Verified behaviour-neutral: all 455 questions produce an identical fingerprint over `(subtopic, difficulty_label, primary_library, task_type, submission_mode, starter_code, test_cases)` before and after.
- 2026-07-24: `GET /api/practice/next-question` gained an optional `focus_subtopic` query param (single-KC practice from the concept graph). See `practice/README.md`.
- 2026-04-27: Split `main.py` (243 LOC, ORANGE) into `auth_router.py`, `jobs_router.py`, `chapters_router.py`. `main.py` is now a 24-LOC assembler (LIME). `watch.py` extended to assert each router exists, declares `router = APIRouter(...)`, is mounted by `main.py`, and that `main.py` stays ≤60 LOC.
- 2026-04-27: Split monolithic `practice_router.py` into the `app/practice/` package; updated `main.py` to import from `app.practice`.
- 2026-04-27: Deleted `TEMP_staleness_review_REMOVE_LATER/` and its hooks in `prioritization.py`, resolving the long-standing import cycle.
