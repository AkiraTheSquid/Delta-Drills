{"id": 1, "hint": "Reductions that report a position (not a value) also take an axis argument \u2014 pick the axis that runs across each row."}
{"id": 2, "hint": "A cell's color depends only on whether its row index plus its column index is even or odd."}
{"id": 3, "hint": "np.dtype accepts a list of (name, type) pairs; numpy's one-byte unsigned integer type goes in the second slot."}
{"id": 4, "hint": "The function that builds a matrix from a diagonal vector takes an offset argument that shifts which diagonal the vector lands on."}
{"id": 5, "hint": "numpy's linear-algebra module has a one-call norm, or build it yourself from squaring, summing, and a square root."}
{"id": 6, "hint": "Divide the vector by its own Euclidean length \u2014 and return a new array rather than scaling in place."}
{"id": 7, "hint": "Compute the mean along the row axis and keep its dimensions so broadcasting lines each mean up with its own row."}
{"id": 8, "hint": "Generator objects have a method that returns uniform [0,1) floats and accepts a size argument."}
{"id": 10, "hint": "Center first by subtracting the overall mean, then divide by the overall standard deviation."}
{"id": 11, "hint": "Broadcast a column of row numbers across the width \u2014 or use the numpy helper that returns the index grids of a shape."}
{"id": 12, "hint": "Combine two comparisons with the elementwise AND operator to build a boolean mask, then assign through it on a copy."}
{"id": 14, "hint": "The deduplication function has an optional flag that makes it also report how often each distinct value appeared."}
{"id": 15, "hint": "A no-axis mean collapses the whole array to one number, and subtracting a scalar broadcasts to every entry."}
{"id": 16, "hint": "numpy's lower-triangle helper takes an offset that can exclude the main diagonal itself."}
{"id": 17, "hint": "numpy has a padding function whose width argument adds the same margin on every side \u2014 or allocate a bigger zero array and drop z into its interior slice."}
{"id": 18, "hint": "Either use numpy's NaN-replacement helper, or build a boolean NaN mask and select between 0.0 and the original values."}
{"id": 19, "hint": "Complex arrays expose their two components either as array attributes or through a matching pair of numpy functions."}
{"id": 22, "hint": "numpy has a discrete-difference function that takes an axis argument \u2014 or subtract two shifted column slices."}
{"id": 23, "hint": "Flattening accepts an order argument \u2014 the Fortran convention walks down columns first. Transposing before a default flatten works too."}
{"id": 24, "hint": "Find the POSITION of the maximum rather than its value, then assign into a copy at that single index."}
{"id": 25, "hint": "Indexing a numpy array with a list of row positions selects (or assigns) several whole rows at once, and negative positions count from the end."}
{"id": 26, "hint": "numpy arrays have reduction methods that, when called with no axis argument, collapse the entire array down to one scalar."}
{"id": 27, "hint": "Start from a uniform matrix and overwrite its interior block with one 2-D slice assignment \u2014 remember that negative slice bounds count from the end of an axis."}
{"id": 28, "hint": "numpy arrays have a built-in reduction that collapses the whole vector to one number \u2014 the average of its entries."}
{"id": 29, "hint": "numpy has one function that turns a pair of coordinate vectors into exactly this pair of grids \u2014 the same one plotting code calls right before evaluating f(X, Y) over a surface."}
{"id": 30, "hint": "Indexing with a list of column positions lets you read or write several columns at once \u2014 just do it on a copy so the caller's array survives."}
{"id": 31, "hint": "Start from a matrix of all ones, then use numpy's upper-triangle helper to zero out everything strictly below the main diagonal."}
{"id": 32, "hint": "NaN never compares equal to anything \u2014 even itself \u2014 so use numpy's elementwise NaN test and then collapse the resulting boolean array to a single yes/no answer."}
{"id": 34, "hint": "numpy has a built-in that lays whole copies of an array end-to-end a given number of times \u2014 a different function from the one that repeats each element in place."}
{"id": 35, "hint": "numpy has a built-in that duplicates each individual element a given number of times consecutively \u2014 it is not the one that stacks whole copies of the array end to end."}
{"id": 36, "hint": "numpy can reinterpret an array's existing data under a new set of dimensions in a single call, as long as the total element count stays the same."}
{"id": 37, "hint": "Think of the dot product as an elementwise product followed by a sum \u2014 numpy offers a single built-in reduction that does both at once for a pair of vectors."}
{"id": 38, "hint": "numpy has a reduction that reports the position of an extreme value rather than the value itself."}
{"id": 40, "hint": "numpy has a single function that reports where entries are nonzero, packaging the result as one index array per dimension."}
{"id": 41, "hint": "numpy has a one-call constructor that builds a zero-filled array by borrowing another array's shape and dtype \u2014 you never have to read .shape or .dtype yourself."}
{"id": 42, "hint": "numpy's random module has functions that take a shape (or its unpacked dimensions) and return that many independent draws from the unit interval."}
{"id": 43, "hint": "numpy has a vectorized function that pairs two arrays and keeps the larger entry position by position \u2014 Python's built-in max and the reducing np.max both do something different."}
{"id": 46, "hint": "First build the full run of consecutive integers as a flat array, then change its shape into the square grid you need."}
{"id": 47, "hint": "numpy has a single function that, given a 1-D vector, builds the square matrix with that vector on the main diagonal."}
{"id": 48, "hint": "A strided slice can appear on the left-hand side of an assignment, and a scalar broadcasts into every position that slice selects."}
{"id": 49, "hint": "numpy has an elementwise rounding family \u2014 pick the member that always rounds toward negative infinity, not toward zero."}
{"id": 50, "hint": "Scale the identity matrix, or hand a constant diagonal vector to the diagonal-matrix builder."}
{"id": 51, "hint": "Arrays carry their buffer size as an attribute \u2014 or multiply the element count by the per-element item size."}
{"id": 52, "hint": "True counts as nonzero (and as 1 in a sum) \u2014 just remember to convert the numpy result to a plain int."}
{"id": 53, "hint": "numpy has a constructor that takes both endpoints and a COUNT of points \u2014 unlike the one that takes a step size."}
{"id": 55, "hint": "A month-precision datetime64 plus 1 is the NEXT month \u2014 and np.arange can walk between two months in steps of days."}
{"id": 58, "hint": "numpy has two sorts: the top-level FUNCTION returns a sorted copy, while the array METHOD sorts in place."}
{"id": 59, "hint": "Every numpy ufunc accepts out= to write its result into an existing array \u2014 sequence the four operations so nothing you still need gets overwritten."}
{"id": 60, "hint": "One row is just the 0..cols-1 range \u2014 then either tile it down the rows or let broadcasting add it to a zero matrix."}
{"id": 61, "hint": "Turn 'closest' into 'smallest absolute difference', then ask numpy for the POSITION of that smallest value."}
{"id": 62, "hint": "Any of numpy's summing routes works \u2014 the catch is converting the numpy scalar into Python's own integer type."}
{"id": 63, "hint": "For positive numbers, flooring, truncating, integer-dividing by 1, and subtracting the remainder mod 1 all agree."}
{"id": 64, "hint": "numpy has one comparison that forgives tiny float error and another that demands bit-for-bit agreement \u2014 this drill wants both."}
{"id": 65, "hint": "Arrays expose a writeable flag in .flags that can be turned off."}
{"id": 66, "hint": "Slice the two coordinate columns apart, then use the two-argument arctangent \u2014 it gets the quadrant right where a plain arctan of y/x cannot."}
{"id": 67, "hint": "numpy has a clamping method that takes a lower bound \u2014 or take the elementwise maximum against zero."}
{"id": 68, "hint": "Consecutive differences tell the whole story: the sequence is non-decreasing exactly when none of them is negative."}
{"id": 69, "hint": "The element-repeating function accepts not just one count but a whole array of per-element counts."}
{"id": 70, "hint": "Indexing a 2-D array with an integer array along the first axis selects whole rows in exactly the order you list them."}
{"id": 71, "hint": "Index the TABLE with the label array \u2014 fancy indexing gathers one table entry per label in a single operation."}
{"id": 72, "hint": "A checkerboard cell's value is decided by the parity of row+column \u2014 the corner value just flips which parity gets the 1s."}
{"id": 73, "hint": "numpy ships an N-dimensional enumerate that yields a position tuple alongside each value."}
{"id": 74, "hint": "A strided slice can be the TARGET of an assignment \u2014 do it on a copy so the caller's array survives."}
{"id": 75, "hint": "numpy has a quarter-turn rotation helper \u2014 or compose a transpose with a flip and check the direction on a non-square array."}
{"id": 76, "hint": "Reversing a slice along one axis mirrors the array across the other \u2014 pick the axis carefully for each of the two mirrors."}
{"id": 77, "hint": "The same numpy function that BUILDS a matrix from a diagonal also EXTRACTS diagonals when handed a matrix \u2014 and it takes an offset."}
{"id": 78, "hint": "Take the upper triangle of an all-True array \u2014 or compare the row and column index grids directly."}
{"id": 79, "hint": "The deduplication function takes an axis argument \u2014 along axis 0 it treats whole rows as the things to compare."}
{"id": 80, "hint": "Shift so the minimum sits at zero, then divide by the full spread between the extremes."}
{"id": 81, "hint": "Tile the vector with a (reps, 1) repetition pattern, then average down the rows (axis 0)."}
{"id": 82, "hint": "The cumulative-sum function takes an axis \u2014 choose the one that walks across each row."}
{"id": 83, "hint": "Allocate a zeroed structured array with two float fields, then fill both fields at once from a coordinate-grid pair."}
{"id": 84, "hint": "Stacking along a NEW leading axis turns the pair into one array whose first axis you can average away."}
{"id": 85, "hint": "Build a boolean per-row test \u2014 'is this whole row zero?' \u2014 then invert it and use it as a row mask."}
{"id": 86, "hint": "One numpy function returns nonzero positions already grouped as one (row, col) pair per hit \u2014 a cousin of np.nonzero with a different packaging."}
{"id": 87, "hint": "numpy's histogram function does the binning and counting in one call \u2014 give it the bin count and the (0, 1) range explicitly."}
{"id": 89, "hint": "Pair the arrays up as columns of one matrix, then read that matrix in row-major order."}
{"id": 90, "hint": "The padding function's constant mode takes the border value as an argument \u2014 it isn't limited to zeros."}
{"id": 91, "hint": "Choose p distinct FLAT indices out of n*n without replacement, then scatter 1s into them \u2014 np.put writes into an array as if it were flat."}
{"id": 92, "hint": "Get the ORDER that would sort just the key column, then apply that order to the whole array's rows with fancy indexing."}
{"id": 93, "hint": "The rows of the k x k identity matrix ARE the k one-hot vectors \u2014 gather the right ones by fancy indexing with the label array."}
{"id": 94, "hint": "A boolean mask built from ONE array can index a DIFFERENT array of the same shape."}
{"id": 95, "hint": "A dot product between the trailing channel axis and a length-3 weight vector collapses (h, w, 3) to (h, w) in one operation."}
{"id": 96, "hint": "Structured dtypes nest: a field's type can itself be a list of (name, type) pairs."}
{"id": 97, "hint": "Dividing by the current norm gives a unit vector \u2014 then scale that up to whatever length you were asked for."}
{"id": 98, "hint": "Distance-to-target is an array too \u2014 you want the position of its minimum, converted to a plain int."}
{"id": 99, "hint": "A moving average is a convolution with a uniform kernel \u2014 and the 'valid' mode keeps only fully overlapping windows."}
{"id": 100, "hint": "ufuncs accept a where= mask plus an out= array of defaults \u2014 division simply never happens at the masked-off positions."}
{"id": 101, "hint": "Sample row INDICES without replacement, then gather those rows with fancy indexing."}
{"id": 102, "hint": "Get values with their counts first, then compute the ordering that sorts the COUNTS and apply it to both arrays \u2014 reversed."}
{"id": 103, "hint": "Softmax is unchanged by subtracting any constant from all logits \u2014 subtract the maximum so the largest exponent is exp(0)."}
{"id": 104, "hint": "Shuffling the ROWS of the identity matrix keeps exactly one 1 in every row and column."}
{"id": 105, "hint": "meshgrid over two linspaces gives every grid point's coordinates at once; the rest is elementwise arithmetic on those grids."}
{"id": 106, "hint": "Reduce along axis 0 for both statistics \u2014 the resulting per-column vectors broadcast back across the rows on their own."}
{"id": 107, "hint": "numpy's linear-algebra module has a direct system solver \u2014 faster and more accurate than inverting and multiplying."}
{"id": 108, "hint": "The quantile function accepts a LIST of fractions and an axis at the same time \u2014 one call produces the whole table."}
{"id": 109, "hint": "Averaging along axis 0 collapses the rows, leaving one mean per column \u2014 and that vector broadcasts back over the rows without any reshaping."}
{"id": 110, "hint": "Collapse each axis with any() to learn which rows and which columns contain something, then take the first and last True positions."}
{"id": 111, "hint": "np.nditer with a None operand allocates the broadcast output for you and iterates all three in lockstep \u2014 assigning through z[...] fills it."}
{"id": 112, "hint": "Build a mask from the absolute difference of row and column indices and keep only the band within one step."}
{"id": 113, "hint": "Use __new__ to set the attribute on construction and __array_finalize__ to propagate it through views."}
{"id": 114, "hint": "Apply argsort twice; the second call inverts the sorting permutation into ranks."}
{"id": 115, "hint": "Build a NaN mask and ask whether it holds for every row within each column."}
{"id": 116, "hint": "Use open grid coordinates and add the absolute row and column distances from the center."}
{"id": 117, "hint": "Reach for the stride-trick that produces overlapping windows of a fixed size."}
{"id": 118, "hint": "Form the pairwise-difference matrix with an outer subtraction, invert elementwise, then take the determinant."}
{"id": 119, "hint": "Partition out the top-k indices, then sort only that small subset by value in descending order."}
{"id": 120, "hint": "Compute NaN-ignoring column means, find the NaN coordinates, and scatter the matching column mean into each hole."}
{"id": 121, "hint": "Write an einsum that shares both indices on the inputs but keeps only the row index in the output."}
{"id": 122, "hint": "Use the built-in correlation routine and tell it that each variable is a column, not a row."}
{"id": 123, "hint": "Reinterpret the buffer with a same-width int view, then assign the floats back into it to trigger the cast."}
{"id": 124, "hint": "Find each row's argmax column, then use paired row/column fancy indexing to drop a 1 at those spots."}
{"id": 125, "hint": "Parse with the missing-value-tolerant text reader and tell it what to substitute for blank fields."}
{"id": 126, "hint": "Reshape so each spatial axis splits into a block index and an in-block index, then sum the in-block axes."}
{"id": 127, "hint": "Turn each coordinate column into a row vector and subtract it from its own transpose so broadcasting builds every pairwise difference at once."}
{"id": 128, "hint": "Compute all pairwise dot products with X @ X.T, then divide by the outer product of the per-row Euclidean norms."}
{"id": 129, "hint": "Form the full squared-distance matrix by broadcasting, then use a partial sort to grab the smallest distances per row while skipping the point's own zero-distance entry."}
{"id": 130, "hint": "Build a boolean mask of nonzeros; argmax on it finds the first True per row, and any() tells you which rows should fall back to -1."}
{"id": 131, "hint": "Count occurrences with bincount and add them, then apply an unbuffered np.add.at pass so repeated indices are not dropped."}
{"id": 132, "hint": "bincount takes a weights argument that accumulates each value into the bin named by the matching index."}
{"id": 133, "hint": "Reshape the pixels into an (N,3) list of colors and let np.unique with axis=0 deduplicate entire rows."}
{"id": 134, "hint": "Build an array of occurrence counts indexed by value, then find the position of the largest count \u2014 first-match behavior on ties is exactly what you need."}
{"id": 135, "hint": "numpy reductions accept a tuple of axes, so you can collapse several dimensions in one call."}
{"id": 136, "hint": "Assign A[[1,0]] into A[[0,1]] \u2014 fancy indexing copies the source rows first, so the swap is atomic."}
{"id": 137, "hint": "There is a numpy function that tallies how many times each integer label occurs \u2014 and it can also take per-element weights, letting you get per-label sums the same way."}
{"id": 138, "hint": "Index the rows with the permutation, then write the result back through a full slice (Z[:] = ...) to mutate in place."}
{"id": 139, "hint": "An integer array used as an index picks out whole rows at once, in exactly the order the integers are listed."}
{"id": 140, "hint": "np.diag takes a k offset \u2014 negative values pull diagonals below the main diagonal."}
{"id": 141, "hint": "Write the diagonal entry as sum_j A[i,j]*B[j,i] and translate it directly into an einsum subscript string."}
{"id": 142, "hint": "An elementwise finiteness test gives a boolean array the same shape as z \u2014 reduce it along the right axis, then turn the surviving booleans into index positions."}
{"id": 143, "hint": "Compare each element to its predecessor to find run boundaries, keeping the first element unconditionally."}
{"id": 144, "hint": "A single matrix-vector product (Z @ v) already gives the dot of every row with v."}
{"id": 145, "hint": "Compare the interior slice against its left and right shifts, then offset the resulting indices by one to account for the dropped first element."}
{"id": 146, "hint": "Make an empty length-2n array and assign A and B into its even and odd strided slices."}
{"id": 147, "hint": "Get the two percentile cut points, then clamp the array between them."}
{"id": 148, "hint": "np.unique can return first-occurrence indices; then sort the indices to restore original discovery order."}
{"id": 149, "hint": "np.diff takes an order argument n that repeats the differencing."}
{"id": 150, "hint": "Index the rows of an identity matrix of size C by the label vector."}
{"id": 151, "hint": "Add a trailing singleton axis to the (5,5) array so it broadcasts over the last dimension."}
{"id": 152, "hint": "The maximum ufunc has an accumulate method; run it along the row axis."}
{"id": 153, "hint": "Take per-row norms with keepdims, then divide using the where/out guard to skip zero-norm rows."}
{"id": 154, "hint": "Subtract column means, divide by column stds (guard against zero), then clip to the range."}
{"id": 155, "hint": "np.repeat can take a per-element count array."}
{"id": 156, "hint": "Cumsum along one axis, then cumsum the result along the other."}
{"id": 157, "hint": "Transpose so each column is one field, then feed the columns to a record-array constructor with names and formats."}
{"id": 158, "hint": "Apply a per-row function that returns the size of the unique values along the row axis."}
{"id": 159, "hint": "Allocate the right-sized zero array and scatter the values onto every (nz+1)-th slot."}
{"id": 160, "hint": "Tile the vector into n rows, then keep the lower triangle."}
{"id": 162, "hint": "Divide by per-row sums (keepdims), using the where/out guard to skip zero-sum rows."}
{"id": 163, "hint": "Check that every row-wise consecutive difference is positive, then mask-select those rows."}
{"id": 164, "hint": "Build the |i-j| index matrix via broadcasting, then use it to gather from c."}
{"id": 165, "hint": "Use the edge endpoint columns as paired row/column indices to set 1s both ways."}
{"id": 166, "hint": "numpy.lib.stride_tricks.sliding_window_view turns the 1D array into a 2D array of overlapping windows; reduce along the window axis."}
{"id": 167, "hint": "Pass a tuple window_shape=(3, 3) to sliding_window_view to collect every 3x3 block at once."}
{"id": 168, "hint": "argmax picks the first max; reverse each row so the last max becomes first, then convert the index back."}
{"id": 169, "hint": "Normalize each row to unit length first, then a single matrix product with the transpose gives all pairwise cosines."}
{"id": 170, "hint": "Use np.nanmean per column, locate the NaNs with np.where(np.isnan(...)), then scatter the matching column means back in."}
{"id": 171, "hint": "np.meshgrid(*arrays, indexing='ij') gives the broadcasted coordinate grids; stack on the last axis and reshape to (-1, n)."}
{"id": 172, "hint": "Count value occurrences per row with np.bincount; argmax over the counts naturally breaks ties toward the smaller value."}
{"id": 173, "hint": "np.linalg.solve accepts a multi-column b and returns one solution column per right-hand side."}
{"id": 174, "hint": "The median of an odd-length row is the middle order statistic; np.partition places exactly that element at index cols//2."}
{"id": 175, "hint": "A length-3 sliding window over the 1D array yields each shifted triple as a row."}
{"id": 176, "hint": "Build the 2x4 sliding windows, then reduce over the two trailing window axes with .sum(axis=(-2, -1))."}
{"id": 177, "hint": "Make a lookup array large enough to cover the maximum key, fill in the mappings, then index it with a."}
{"id": 180, "hint": "np.fill_diagonal on shifted slices M[:-1, 1:] and M[1:, :-1] targets the upper and lower diagonals."}
{"id": 181, "hint": "Insert a new axis so the two copies broadcast into an (n, n, d) difference tensor, then reduce over the feature axis."}
{"id": 182, "hint": "Augment the points with a ones column for homogeneous coordinates, multiply by T.T, then keep the first two columns."}
{"id": 183, "hint": "argsort each row, then scatter 0..cols-1 into those sorted positions to invert the permutation into ranks."}
{"id": 184, "hint": "Mirror the write: set both Z[i, j] and Z[j, i] to v."}
{"id": 185, "hint": "Find where neighbors differ to get run starts, sample values there, and diff the start positions for the counts."}
{"id": 186, "hint": "Compute the centered window's start/stop in Z, clip them to the array bounds, and copy into the aligned slice of a fill-initialized output."}
{"id": 187, "hint": "argpartition gives you the top-k column indices per row cheaply; scatter 1s into a zeros_like array using broadcasted row indices against those columns."}
{"id": 189, "hint": "One CCW quarter-turn equals transposing the array and then flipping it along the first axis."}
{"id": 190, "hint": "The rank equals the number of non-negligible singular values; numpy has a one-call helper that does the SVD and thresholding for you."}
{"id": 191, "hint": "Generate an (N, len(X)) matrix of random integer indices to fancy-index X all at once, take row means, then read off the 2.5 and 97.5 percentiles."}
{"id": 192, "hint": "Apply np.power with exponent 3 directly to the whole array; no loop or einsum needed."}
{"id": 193, "hint": "Build a pairwise squared-distance matrix by broadcasting, mask the diagonal with infinity, then argpartition each row to grab the k nearest indices."}
{"id": 194, "hint": "argpartition gets the top-k indices cheaply; then argsort the corresponding values per row and reverse to reorder those indices descending."}
{"id": 195, "hint": "Reshape the matrix into (blocks, within, blocks, within) and sum over the two within-block axes."}
{"id": 196, "hint": "Use sliding_window_view to expose every valid window, then contract each window against the kernel with tensordot over both kernel axes."}
{"id": 197, "hint": "Allocate a zero matrix sized to the total rows and columns, then walk a running (row, col) offset and paste each block into its slice."}
{"id": 198, "hint": "Broadcast the binary rows pairwise and use bitwise AND/OR summed over columns for intersection and union sizes; guard a zero union before dividing."}
{"id": 199, "hint": "Broadcast A and B with inserted singleton row axes so the column axis aligns, multiply, then collapse the two row axes into one."}
{"id": 200, "hint": "Take the per-row cumulative sum, draw one uniform per row, and find the first cumulative bin that exceeds it with argmax."}
{"id": 201, "hint": "Write each line in implicit ax+by+c form from its endpoints, then broadcast point coordinates against the per-line coefficients and divide by the line length."}
{"id": 202, "hint": "A row is all-equal iff its row max equals its row min; keep the rows where they differ via a boolean mask."}
{"id": 204, "hint": "Cast the vector to uint8, give it a trailing axis, and let np.unpackbits along that axis produce the MSB-first 8-bit rows."}
{"id": 205, "hint": "np.unique can deduplicate whole rows directly when you tell it which axis to operate over."}
{"id": 206, "hint": "Partition on the negated array so the largest values' indices land first, then take the first n and index back into the array."}
{"id": 207, "hint": "Duplicate each vertex and roll to form consecutive (a,b) edge pairs, sort each pair so direction doesn't matter, then take unique rows via a structured-dtype view."}
{"id": 208, "hint": "A matrix-vector product is a contraction over the matrix columns and the vector rows; the batch sum is a contraction over the leading axis of each. np.tensordot can take both pairs of axes in one call."}
{"id": 209, "hint": "Count neighbours with eight offset slices of the board summed together, then derive birth and survival masks from that count and the current cell state. Write into a fresh zero board each iteration so reads aren't corrupted by writes."}
{"id": 210, "hint": "Test integrality with the fractional part (modulo 1) and require it across the whole row, then intersect with a row-sum-equals-n condition. Use the resulting boolean mask to index the rows."}
{"id": 211, "hint": "Add two trailing length-1 axes to A so it broadcasts against B, producing a 4D match tensor. Reduce with any over the right axes to ask 'does this B element appear in the A row', then all to require every B row be covered."}
{"id": 212, "hint": "The ones constructor accepts a dtype argument; pick the boolean dtype so the fill value is True."}
{"id": 213, "hint": "Make a zero vector of the right length, then assign into the single index you need (remember positions are counted from 0)."}
{"id": 214, "hint": "arange's stop is exclusive; push the stop slightly beyond your desired last value so the endpoint is included."}
{"id": 215, "hint": "arange takes a dtype keyword; specify the 32-bit signed integer type explicitly."}
{"id": 216, "hint": "The values to emit are just the indices 0..len(C)-1; repeat each index by its corresponding count using np.repeat."}
{"id": 217, "hint": "Find the smallest 2x2 block that already alternates correctly, then replicate it across the grid with np.tile."}
{"id": 218, "hint": "Compute column means and stds along axis 0 (keepdims for broadcasting), subtract and divide, but replace any zero std with 1 before dividing to handle constant columns safely."}
{"id": 219, "hint": "numpy has a reduction that gives you the position (index) of the largest value rather than the value itself \u2014 use that index to assign into a copy."}
{"id": 220, "hint": "Numpy reductions take an axis argument \u2014 pick the axis that runs down the rows so it collapses, leaving one value per column."}
{"id": 224, "hint": "NumPy has one constructor that accepts nested Python sequences directly and infers a row for each inner list."}
{"id": 225, "hint": "numpy ships ready-made constructors that build a fresh array of a given shape prefilled with a fixed value \u2014 pick the one whose fill value is one."}
{"id": 227, "hint": "numpy has a one-call constructor that allocates an all-zero array of a given size, and its default dtype is already floating point."}
{"id": 228, "hint": "numpy's array-creation functions include a one-call constructor for a square matrix with ones on the main diagonal and zeros everywhere else."}
{"id": 229, "hint": "numpy's range-building function treats its stop argument as exclusive, so pick a stop that pushes one past the last value you want."}
{"id": 230, "hint": "numpy arrays have a method that returns a copy of the array converted to whatever dtype you ask for."}
{"id": 231, "hint": "numpy lets you assign a single scalar to a whole contiguous run of an array in one statement \u2014 no loop needed."}
{"id": 232, "hint": "Elementwise arithmetic on a numpy array gives each element's remainder at once, and comparing that whole result to a scalar yields a boolean array."}
{"id": 233, "hint": "Slicing accepts a negative step, which walks the array from the end back to the start."}
{"id": 234, "hint": "numpy has a built-in that computes a running (cumulative) total along an array in one call."}
{"id": 235, "hint": "An ndarray has a method that reorders its own contents, unlike the top-level numpy function that hands back a sorted copy."}
{"id": 236, "hint": "Comparing a numpy array to a scalar with a comparison operator broadcasts elementwise and yields a boolean mask of the same shape."}
{"id": 237, "hint": "The entries z[i, i] form the main diagonal \u2014 numpy can extract that diagonal (or sum it directly) in one vectorized step."}
{"id": 238, "hint": "numpy has a pair of stacking helpers that join a sequence of arrays along the row axis or along the column axis."}
{"id": 239, "hint": "Elementwise multiplication is not what you want here \u2014 look for the operation that combines each row of the first array with each column of the second."}
{"id": 240, "hint": "Think of each value as a magnitude and a sign: the magnitude always rounds in the same direction, and the sign can be reattached elementwise afterwards."}
{"id": 241, "hint": "numpy has set-style operations that work directly on arrays \u2014 look for the one that keeps only the values common to both inputs."}
{"id": 242, "hint": "If you generate the evenly spaced points including both endpoints, you need two extra points \u2014 then drop the first and last."}
{"id": 243, "hint": "Share the batch index on both inputs and give the two vector indices distinct names in the output so neither is contracted."}
{"id": 244, "hint": "Reorder the two axis names on the output side to swap them."}
{"id": 245, "hint": "Repeat the index on both operands and leave the output side empty to contract it to a scalar."}
{"id": 246, "hint": "Pick index labels so the output keeps only the row/column index that is shared by both factors at the diagonal, and contract the inner index."}
{"id": 247, "hint": "Omit the row index in the output pattern so it gets summed away, leaving only the column axis."}
{"id": 248, "hint": "Give the two matrices independent row labels but a shared column/feature label, and contract that shared one."}
{"id": 249, "hint": "Contract X against itself over the channel and spatial axes while keeping only the batch axis in the output."}
{"id": 250, "hint": "Both operands are the same array; share the hidden axis d and keep separate row indices i and j so every pair within a batch item gets dotted."}
{"id": 251, "hint": "Match the hidden axis d on both X and v, and keep only b and t in the output."}
{"id": 252, "hint": "Use all three operands in one einsum, sharing i with the matrix rows and j with the matrix columns, and leave the output side empty."}
{"id": 253, "hint": "Contract only the channel axis c; keep h and w as matched free axes in both inputs and the output."}
{"id": 254, "hint": "Treat b and h as batch axes, contract the shared keys axis k, and leave q and d as the resulting matrix shape."}
{"id": 255, "hint": "Contract the row axis n between w and A to get a weighted row-sum, then divide by the total weight."}
{"id": 256, "hint": "Give the two vectors distinct index names and keep both in the output so nothing is summed."}
{"id": 257, "hint": "Use the same letter for both axes of A and keep that letter in the output."}
{"id": 258, "hint": "Contract the row axis n on both copies of X, keeping the two column axes as the separate output dimensions d and e."}
{"id": 259, "hint": "Mean-center first, then contract the sample axis n of the centered data with itself and divide by n-1."}
{"id": 260, "hint": "Name the shared inner dimension k on both matrices and drop it from the output, keeping i and j."}
{"id": 261, "hint": "Single operand, name the one axis, and leave the output side empty to sum everything."}
{"id": 262, "hint": "Share the matrix's column axis j with the vector and keep only the row axis i."}
{"id": 263, "hint": "Keep b and h as batch axes, contract the head dim d, and let the distinct query index q and key index k form the output matrix."}
{"id": 264, "hint": "Use all three operands and let consecutive matrices share an inner index (k then m); keep only the first and last free indices."}
{"id": 265, "hint": "Carry the batch axis a unchanged on both inputs and contract only the inner axis j."}
{"id": 266, "hint": "Keep the batch axis b, repeat one letter on the two matrix axes, and sum it out by leaving it off the output."}
{"id": 267, "hint": "Match both axes between A and B and output nothing so all elementwise products are summed."}
{"id": 268, "hint": "Match the batch axis b on both inputs, contract the matrix column axis j with the vector, and keep b and i."}
{"id": 269, "hint": "Name the channel axis in both the image and the weight vector, then leave it out of the output pattern so it gets contracted away."}
{"id": 270, "hint": "Use identical index labels for both tensors and leave the output side of the arrow empty to collapse everything to one number."}
{"id": 271, "hint": "A single-tensor pattern with the same axes on both sides is just a transpose; reorder the output labels to place the channel last."}
{"id": 272, "hint": "Match the input feature axis to the matrix's first axis and let the matrix's second axis become the new feature dimension."}
{"id": 273, "hint": "Reuse the same letter for both inner matrix axes to select the diagonal, and keep the batch axis on the output."}
{"id": 274, "hint": "Keep only one axis label on the output side; the omitted axis is the one summed over."}
{"id": 275, "hint": "Use four completely different labels so no axis is summed, then order them on the output to interleave A's and B's dimensions."}
{"id": 276, "hint": "Keep the batch label on both inputs and the output, and share the inner contraction axis between the two operands."}
{"id": 277, "hint": "Use one repeated label for both axes to pick the diagonal, then leave the output empty to sum it to a scalar."}
{"id": 278, "hint": "Pass the vector twice with different index labels and keep both labels in the output to build the outer product."}
{"id": 279, "hint": "Share the input feature axis with the matrix's first axis and emit the matrix's output axis alongside the batch."}
{"id": 280, "hint": "List every axis except the one you want gone on the output side; the missing label is summed."}
{"id": 282, "hint": "First contract the t axis with einsum to get sums, then divide by the size of that dimension."}
{"id": 283, "hint": "Pass all three tensors and chain the contraction labels so each weight's output axis feeds the next weight's input axis."}
{"id": 284, "hint": "Multiply the batch of vectors by itself, contract the feature axis, and keep the batch axis to get per-vector squared norms."}
{"id": 285, "hint": "Keep all four labels but exchange the head and seq labels on the output side."}
{"id": 286, "hint": "Share the query's only axis with M's feature axis and keep the row axis on the output."}
{"id": 287, "hint": "Pass x twice with different feature labels but the same batch label, and keep all three on the output."}
{"id": 288, "hint": "Contract the shared feature axis and let the leading weight axis n broadcast, placing it first on the output."}
{"id": 289, "hint": "Name all three input axes but omit the batch axis from the output side; einsum reduces over any axis you don't keep."}
{"id": 290, "hint": "Name each axis on the input and leave the output empty so every axis is summed away into one number."}
{"id": 291, "hint": "Pair the per-channel scalar's single axis with the image's channel axis and keep that axis in the output instead of reducing it."}
{"id": 292, "hint": "Use two different axis letters that both survive into the output to build the outer product, then add the scalar afterward."}
{"id": 293, "hint": "Pass the tensor through einsum unchanged, then index the (0, 0) corner across the batch axis."}
{"id": 294, "hint": "Label the weight's leading axis as the surviving output and let every other axis be shared between W and X so it gets summed away."}
{"id": 295, "hint": "Name all four axes but keep just the batch axis in the output so the rest are summed."}
{"id": 296, "hint": "Give the two copies different sequence-position labels but the same feature label, and keep the feature axis in the output rather than reducing it."}
{"id": 297, "hint": "Share the inner axis j between B and M, keep the batch axis, and let the output expose i and k."}
{"id": 298, "hint": "Cross the two index letters between A and B and reduce everything; the trace of a product is sum_ij A_ij B_ji."}
{"id": 299, "hint": "Align the batch axis, contract the shared feature axis d, and let the distinct query and key position axes form the output matrix."}
{"id": 300, "hint": "Use rearrange and group the two spatial axes in parentheses on the right to merge them into one."}
{"id": 301, "hint": "Keep b and h aligned, give the two sequence axes distinct labels (n and m), and contract the shared last dimension d."}
{"id": 302, "hint": "Reduce to (b, c) with einsum to get the sum, then divide by the number of averaged positions to turn it into a mean."}
{"id": 303, "hint": "Get the transpose by reading the input axes in swapped order into the same output layout, then subtract from the original."}
{"id": 304, "hint": "Match the weight vector's single axis to x's last axis and omit it from the output so it gets contracted."}
{"id": 305, "hint": "Share the hidden axis between h and W, keep batch and time, and let the weight's second axis become the new last dimension."}
{"id": 306, "hint": "Feed all three operands into one einsum, chaining the index letters so both vectors meet the matrix, and reduce to a scalar."}
{"id": 308, "hint": "Slice the sequence into all-but-last and all-but-first, then contract the feature axis while keeping the position axis."}
{"id": 309, "hint": "Name the head axis in both operands and simply omit it from the output pattern so einsum contracts (weights then sums) over it."}
{"id": 310, "hint": "Give the two copies of v different free indices for the matrix dimensions and a shared batch index that you leave out of the output so it gets summed."}
{"id": 311, "hint": "Treat the diagonal as a 1D operand sharing the contracted index j with both matrices; the output keeps only the outer indices i and k."}
{"id": 312, "hint": "This is a standard matrix-times-vector contraction: share the column index j between P and v and keep only the row index i."}
{"id": 313, "hint": "Split both spatial axes with named factors of size 2, then merge those factors into the channel axis in the output pattern."}
{"id": 314, "hint": "Fold batch into width with (b w), then undo it by splitting the width axis back into (b w) while supplying b so einops knows the split sizes."}
{"id": 315, "hint": "Factor the sequence-length axis into (n p) and fix the inner factor p to the segment length so einops infers the number of segments."}
{"id": 316, "hint": "Split the 4*bbox channel axis into a leading coord=4 factor, hoist it to the front, then unpack the result into the four coordinate tensors."}
{"id": 317, "hint": "Use repeat (not rearrange) and place the literal tiling factor next to the height axis inside a grouped (h 3) on the output side."}
{"id": 318, "hint": "Factor the batch axis into grid rows and columns, then fuse the row factor with height and the column factor with width in the output."}
{"id": 319, "hint": "Just relist the axes in the target order on the right-hand side; no factors or merges are needed for a layout swap."}
{"id": 320, "hint": "Factor the height axis into a leading half-selector of size 2, then merge that selector into the batch axis to produce twice as many shorter images."}
{"id": 321, "hint": "Split the channel axis into named height/width block factors plus a remaining channel, then fuse those block factors into the spatial axes."}
{"id": 322, "hint": "Fold the spatial subgrid factors and batch into one leading axis, then run the mirror-image pattern to undo it, keeping hs and ws fixed."}
{"id": 323, "hint": "Split the flattened patch axis back into grid rows and columns, then merge each grid index with its corresponding intra-tile pixel axis."}
{"id": 324, "hint": "Factor both spatial axes into output-size and window-size parts, drop the window parts from the output, and use the 'mean' reduction."}
{"id": 325, "hint": "List only the batch axis on the output side so every other axis is reduced, and choose the 'mean' operation."}
{"id": 326, "hint": "Reduce with 'max' and omit only the channel axis from the output pattern so the maximum is taken across channels."}
{"id": 327, "hint": "Just reorder the axis names on the output side to put channels between height and width; no splitting or merging is involved."}
{"id": 328, "hint": "Use einops.reduce with a pattern that keeps width and channels while omitting height, and pick the averaging reduction."}
{"id": 329, "hint": "Split the batch axis into two grid factors with the row count fixed, then merge one factor into height and the other into width."}
{"id": 330, "hint": "A single rearrange with the same axis names reordered so batch lands last does the whole job."}
{"id": 331, "hint": "Factor the batch into a fixed number of rows and an inferred number of columns, then fuse rows into height and columns into width."}
{"id": 332, "hint": "Reduce over width with max while keeping a singleton width axis, then rely on broadcasting to subtract."}
{"id": 333, "hint": "Permute the four named axes so the channel name moves to the end, leaving batch first."}
{"id": 334, "hint": "Treat the list as a batch axis, then merge height with batch in that order so rows alternate between the two images."}
{"id": 335, "hint": "One rearrange that lists the channel name first and keeps height then width after it."}
{"id": 336, "hint": "In one reduce, decompose batch into grid factors and each spatial axis into output-size times a 2-wide pool factor, reduce with max, and fuse grid factors into the spatial output."}
{"id": 337, "hint": "Compose batch into width, then invert with the same merged pattern split apart, supplying the batch size so einops can factor it."}
{"id": 338, "hint": "Use einops.repeat to insert a new time axis between batch and dim, giving its size as a keyword."}
{"id": 339, "hint": "einops.repeat with a new trailing axis whose size is the feature count broadcasts each weight across features."}
{"id": 340, "hint": "Reduce the spatial axes to size-1 with mean, keeping batch and channel, then subtract via broadcasting."}
{"id": 341, "hint": "einops.repeat with a literal multiplier fused into the height axis stacks two vertical copies."}
{"id": 342, "hint": "Merge batch with height, then invert by splitting the combined axis with both sizes provided."}
{"id": 343, "hint": "Split channels into the true channel count times two size-2 factors, then merge those factors into height and width."}
{"id": 344, "hint": "One rearrange that exchanges the height and width names, keeping channels first."}
{"id": 345, "hint": "Permute the named axes so channels sit immediately after batch and ahead of the spatial axes."}
{"id": 346, "hint": "Fold the batch axis into width so the two images sit horizontally adjacent, keeping height and channels."}
{"id": 347, "hint": "Use rearrange and merge the channel dimension into the width by writing it as a composite output axis, keeping height first."}
{"id": 348, "hint": "Use repeat with a new factor axis folded into the height group, leaving the other axes identical on both sides."}
{"id": 349, "hint": "Use reduce with 'sum' and an empty right-hand side so no axes survive."}
{"id": 350, "hint": "Factor each spatial axis into an outer and a size-2 inner index, then swap the two inner indices on the output."}
{"id": 351, "hint": "Use repeat with a literal 2 merged into both the height and width output groups."}
{"id": 352, "hint": "Use repeat on the channels-first image with a literal 2 merged into the height axis only."}
{"id": 353, "hint": "Use rearrange and combine the two middle axes into one composite, leaving batch and depth alone."}
{"id": 354, "hint": "Reduce with 'min' over the two spatial axes but keep them as size-1 dimensions, then subtract."}
{"id": 355, "hint": "Use repeat: stack the batch into the height composite and fold a literal 2 into the width, putting channels first."}
{"id": 356, "hint": "Use rearrange to combine the three non-batch axes into one composite dimension."}
{"id": 357, "hint": "Use rearrange and combine channels with height into a single leading composite axis."}
{"id": 358, "hint": "Use the () token in the rearrange pattern to add a singleton axis, then a second rearrange that matches and drops it."}
{"id": 359, "hint": "Split the channel axis into a leading factor of 2 and the remaining channels, then bring that factor to the front."}
{"id": 360, "hint": "Add a size-1 leading axis with rearrange, then a second rearrange that consumes that axis to drop it."}
{"id": 361, "hint": "The input and output patterns are identical here; rearrange acts as an identity over the batched HWC axes."}
{"id": 362, "hint": "Split the channel axis so the size-2 factor is the inner (fastest) index, then promote that factor to the front."}
{"id": 363, "hint": "Factor height and width into outer indices and size-3 inner blocks, then reduce those blocks with 'mean'."}
{"id": 364, "hint": "Split the batch axis into row and column factors, then merge the row factor into height and the column factor into width."}
{"id": 365, "hint": "Let rearrange stack the list as the batch axis, then permute so spatial dims lead and the batch axis goes last."}
{"id": 367, "hint": "Keep the axes you want to retain on the right of the arrow and let reduce average over the ones you drop."}
{"id": 368, "hint": "Split each spatial axis into an output-size axis times a literal window of 2, then reduce with 'max'."}
{"id": 369, "hint": "Factor the channel axis into named groups, then transpose just the two group axes in the output pattern."}
{"id": 370, "hint": "Reduce to a shape that keeps the channel axis but has singletons elsewhere, so it broadcasts back over x on subtraction."}
{"id": 371, "hint": "Read off the forward pattern and write its mirror image: split the packed batch axis and fold the factors back into height and width."}
{"id": 372, "hint": "Decompose channels into (group, in-group) and swap the order of those two factors in the output."}
{"id": 373, "hint": "Fold the channel axis into height by listing it first inside the merged group, and use () to add the trailing size-1 axis."}
{"id": 374, "hint": "Merge the batch axis into width in the output pattern so images sit side by side."}
{"id": 375, "hint": "Split the time axis into output-length times a window of 2, then mean-reduce the window."}
{"id": 376, "hint": "Wrap the two images in a list to create a batch axis, then merge that axis into width."}
{"id": 377, "hint": "Combine the pooling factorization with a merged trailing output axis so reduce both maxes and flattens at once."}
{"id": 378, "hint": "Factor time into output-length times a window of 4 and average over the window."}
{"id": 379, "hint": "Just reorder the height and width axis names on the right side of the arrow."}
{"id": 380, "hint": "Combine batch and height into one merged axis and move channels last in the output pattern."}
{"id": 381, "hint": "Make the four images a batch, split it into rows and columns, then fold rows into height and columns into width."}
{"id": 382, "hint": "Split the batch into r=3 rows (columns are inferred), then merge rows into height and columns into width."}
{"id": 383, "hint": "Use repeat with a new factor r=2 merged in front of the channel axis to double the channels."}
{"id": 384, "hint": "Merge the heads axis into the embedding axis in the output, keeping tokens as a separate dimension."}
{"id": 385, "hint": "Use repeat and expand the width axis with a factor of 4 placed after w in the merged group."}
{"id": 386, "hint": "Factor the width into an outer index and an inner group of 2, then let reduce collapse the inner group with a mean."}
{"id": 387, "hint": "Decompose height and width into (stripe, size) pairs, then compose the stripe indices together with batch as the new leading dimension."}
{"id": 388, "hint": "Just reorder the named axes in the output pattern so time leads batch; no splitting or merging is needed."}
{"id": 389, "hint": "Split the batch into row and column grid indices, then fold the row index into height and the column index into width."}
{"id": 390, "hint": "Split the single flattened axis into (h w) and pass the known height and width sizes so einops can unflatten it."}
{"id": 391, "hint": "Keep the channel axis as-is and merge height and width into one parenthesized axis in the output."}
{"id": 392, "hint": "Bring channels to the front and compose the batch axis together with height so the images stack along the height dimension."}
{"id": 393, "hint": "Factor channels into (pair_index, within_pair) with the inner factor 2, then move the within-pair index to the front."}
{"id": 394, "hint": "Use () to insert and later remove a singleton batch axis, and a single composed (c h w) axis to flatten the features in between."}
{"id": 395, "hint": "Split each spatial axis into an outer index and a block factor, then compose those block factors together with channels into the new channel dimension."}
{"id": 396, "hint": "Factor the last axis into heads and per-head dim, then reorder so heads sits right after batch."}
{"id": 397, "hint": "Put height and width first, then merge batch and channels into one parenthesized trailing axis."}
{"id": 398, "hint": "Split the patch axis into (h w) using the given grid sizes, then compose each grid index with its corresponding patch axis to rebuild the spatial dims."}
{"id": 399, "hint": "Reduce across channels with a mean, but write a () in the output so the channel axis collapses to size 1 rather than disappearing."}
{"id": 400, "hint": "Keep channels and height up front, then merge batch into the width axis so images sit side by side."}
{"id": 401, "hint": "Decompose height and width into (grid, patch) pairs with patch size 3, then compose the two grid indices into a single patch-count axis."}
{"id": 402, "hint": "Reduce over the height axis with max but retain a singleton there so the result broadcasts back against the original image when subtracted."}
{"id": 403, "hint": "Split width into a remaining-width and a factor of 4, then compose that factor as the outer part of a new height axis."}
{"id": 404, "hint": "Factor height and width into (grid, patch) pairs, then compose the two grid indices into one leading axis while keeping channel and patch axes separate."}
{"id": 405, "hint": "Plug the four values straight into floor((H + 2P - K)/S + 1) using integer division for the floor."}
{"id": 406, "hint": "Apply floor((H + 2P - K)/S + 1); the stride of 2 is what roughly halves the side length."}
{"id": 407, "hint": "Build the actual nn.Conv2d, push a zero tensor through it, and read tuple(out.shape)."}
{"id": 408, "hint": "Count taps per output position as channels times the kernel area."}
{"id": 409, "hint": "The padding that preserves size for an odd kernel is half of K-1."}
{"id": 410, "hint": "Allocate a zero buffer two longer than x, then copy x into the middle slice."}
{"id": 411, "hint": "Read conv.weight.shape and recall the order is out, in, then the kernel height and width."}
{"id": 412, "hint": "Construct the conv with bias disabled and format whether conv.bias is None into the string."}
{"id": 413, "hint": "Sum p.numel() over the Sequential; remember the conv has no bias and BN contributes two vectors of size num_features."}
{"id": 414, "hint": "Call F.conv2d(x, k) directly; with all-ones input the averaging kernel returns ones at every valid position."}
{"id": 415, "hint": "Identify which axes are shared (summed) between patches and weight and which survive in the (B, C_out, L_out) output."}
{"id": 416, "hint": "Multiply out-channels by per-filter weights (in_ch*K*K) and add one bias per out-channel."}
{"id": 417, "hint": "Pass the tensor through the pool and read the shape; a stride-2 2x2 window halves H and W."}
{"id": 418, "hint": "Reduce the two spatial axes with mean(dim=(2,3)) and inspect the remaining shape."}
{"id": 419, "hint": "Flatten everything except the batch dimension; multiply the non-batch dims together."}
{"id": 420, "hint": "Each learnable BN tensor has num_features entries and there are two of them."}
{"id": 421, "hint": "Enumerate named_buffers and collect their keys; expect the two running stats plus a counter."}
{"id": 422, "hint": "Switch the module to eval mode and read the boolean training attribute."}
{"id": 423, "hint": "Register the Linear inside __init__ and sum p.numel() over model.parameters(); count weight plus bias."}
{"id": 424, "hint": "nn.Parameter assigned as a Module attribute is auto-registered; count what .parameters() yields."}
{"id": 425, "hint": "Order is zero_grad, forward+loss, backward, step \u2014 emit them as one semicolon-joined string."}
{"id": 426, "hint": "Tensors created inside the inference_mode context never track gradients."}
{"id": 427, "hint": "After freezing every parameter, count how many still have requires_grad True."}
{"id": 428, "hint": "model.eval() sets the training flag \u2014 just read it back."}
{"id": 429, "hint": "Reduce over the column axis to collapse each row to its largest entry."}
{"id": 430, "hint": "amax gives just values; inspect the class name of what max(dim=...) returns to see the difference."}
{"id": 431, "hint": "Unfold both spatial axes into 2x2 patches, then take the max over the patch dimensions."}
{"id": 432, "hint": "An in-place constant fill overwrites whatever torch.empty allocated."}
{"id": 433, "hint": "Zero standard deviation makes every normal sample equal the mean."}
{"id": 434, "hint": "Initialize weights normally and the bias to a constant, then summarize the weight stats and dump the bias."}
{"id": 435, "hint": "Differentiate x cubed elementwise (3x^2) and scale by the incoming gradient."}
{"id": 436, "hint": "Use the sigmoid's own output to form its derivative y*(1-y), then multiply by the upstream gradient."}
{"id": 437, "hint": "Gate the upstream gradient by where z>0, then pull it back through the linear layer with W transposed."}
{"id": 438, "hint": "Pass raw logits and integer targets straight into F.cross_entropy \u2014 no manual softmax."}
{"id": 439, "hint": "Index the negative log-softmax at the target and compare to the batched F.cross_entropy."}
{"id": 440, "hint": "Subtract the max logit before exponentiating, then take loss = logsumexp - target logit."}
{"id": 441, "hint": "Subtract lr times the gradient from the parameters \u2014 one elementwise update."}
{"id": 442, "hint": "Loop three times: backward for the gradient, then update x in-place under no_grad and clear x.grad."}
{"id": 443, "hint": "Compute the residual X@theta - y first, project it back through X.T, scale by 2/n, then take a single gradient-descent step."}
{"id": 444, "hint": "Iterating an iterator exhausts it; iterating a list does not. Watch what the second pass over each sees."}
{"id": 445, "hint": "The grad is all ones and never recomputed, so each step subtracts the same constant from every weight. Count how many steps run."}
{"id": 446, "hint": "Convert the generator to a list immediately, then both the count and the two sum passes operate on stored elements."}
{"id": 447, "hint": "Branch on whether the input is an int; duplicate it if so, otherwise unpack its two elements as ints."}
{"id": 448, "hint": "Normalize each hyperparameter to a pair, then apply the conv output formula separately for the height and width axes."}
{"id": 449, "hint": "Apply the int-or-sequence normalizer to each list element with a comprehension."}
{"id": 450, "hint": "Call backward on M with the upstream gradient tensor; let autograd populate X.grad and Y.grad for you."}
{"id": 451, "hint": "Post-multiply the upstream gradient by Y transposed for dL/dX; pre-multiply by X transposed for dL/dY."}
{"id": 452, "hint": "The derivative of 0.5*sum(M*M) w.r.t. M is just M; plug that in as dL/dM and compare the manual products against autograd."}
{"id": 453, "hint": "Call .var twice, toggling the unbiased flag; the only difference is dividing by N versus N-1."}
{"id": 454, "hint": "Reduce over every axis except the channel axis (dim 1), and use the biased variance flag."}
{"id": 455, "hint": "Recreate BatchNorm's normalization by hand using the biased per-channel variance with keepdim, then compare against the layer's output with allclose."}
{"id": 456, "hint": "Loop through the values in order, each time mixing (1-mu) of the old running value with mu of the new value."}
{"id": 457, "hint": "Compute per-feature mean and population variance with axis=0, then EWMA-update the two running buffers once per batch in order."}
{"id": 458, "hint": "Use the running-stat update with mu = 1 - beta, applied component-wise to each gradient in order; skip any bias correction."}
{"id": 459, "hint": "Reshape every length-C vector to (1, C, 1, 1) before the elementwise normalize, scale, and shift so each lines up with dim 1."}
{"id": 460, "hint": "Put the channel count on dim 1 and make the batch and spatial dims singletons."}
{"id": 461, "hint": "The raw add fails because C != W on the trailing axis; in the except branch reshape the bias to (1, C, 1, 1) so it broadcasts per channel."}
{"id": 462, "hint": "Think about what value, when placed in a window, is guaranteed to lose a max comparison against any real number."}
{"id": 463, "hint": "Remember which fill value PyTorch's pooling layers use at the boundary, and slide non-overlapping windows of size 2."}
{"id": 464, "hint": "Build each padded array, slice consecutive length-3 windows by index, and reduce along the window axis."}
{"id": 465, "hint": "Reverse broadcasting by summing the upstream gradient over exactly the axes that were size 1 in the original shape, keeping the dimension."}
{"id": 466, "hint": "The size-1 axis here is the row dimension; sum the upstream rows together while keeping that dimension."}
{"id": 467, "hint": "Each operand's gradient equals the number of times broadcasting replicated it along its size-1 axis."}
{"id": 468, "hint": "Store the constructor arguments as attributes and have extra_repr format them into the requested f-string."}
{"id": 469, "hint": "With no submodules, the repr collapses to the class name wrapping your extra_repr string in parentheses."}
{"id": 470, "hint": "With a submodule present, the repr expands to multiple indented lines: your extra_repr first, then each child module."}
{"id": 471, "hint": "The global flag wins over a tensor's requires_grad for graph construction, but it does not change the computed numbers."}
{"id": 472, "hint": "Capture the default flag first, then observe how each result's requires_grad mirrors the flag state at compute time."}
{"id": 473, "hint": "Wrap the in-place update in the context manager that turns off tracking, then check that the leaf's requires_grad survives outside it."}
{"id": 474, "hint": "Differentiate a sum of products with respect to one factor and the other factor is what remains."}
{"id": 475, "hint": "The gradient of each weight is 2 times the corresponding pre-activation output times the input feature that multiplied it."}
{"id": 476, "hint": "Differentiate the mean-squared term, then apply a single gradient-descent step inside a no-grad block."}
{"id": 477, "hint": "The call freezes every parameter recursively; count only those whose requires_grad survives."}
{"id": 478, "hint": "If no input to an operation requires grad, the result carries neither a requires_grad flag nor a grad_fn."}
{"id": 479, "hint": "Freeze everything, then selectively unfreeze one submodule; the parameter order follows the module order."}
