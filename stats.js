/* ================================================================
   STATS ENTRY — start after modules load
   ================================================================ */

if (typeof initStats === "function") {
  initStats();
}
